# Plan 001: Establish a CI pipeline and a one-command verification gate

> **Executor instructions**: Follow this plan step by step. Run every
> verification command and confirm the expected result before moving to the
> next step. If anything in the "STOP conditions" section occurs, stop and
> report — do not improvise. When done, update the status row for this plan
> in `plans/README.md` — unless a reviewer dispatched you and told you they
> maintain the index.
>
> **Drift check (run first)**: `git diff --stat f88800a..HEAD -- package.json lefthook.yml turbo.json apps/server/package.json apps/web/package.json apps/desktop/package.json packages`
> If any in-scope file changed since this plan was written, compare the
> "Current state" excerpts against the live code before proceeding; on a
> mismatch, treat it as a STOP condition.

## Status

- **Priority**: P1
- **Effort**: M
- **Risk**: LOW
- **Depends on**: none
- **Category**: dx
- **Planned at**: commit `f88800a`, 2026-06-12

## Why this matters

The repo has no CI at all — no `.github/workflows/`, no other CI config. Quality
gates exist only as local pre-commit hooks (`lefthook.yml`), which are bypassable
with `--no-verify` and do not run tests. `lefthook.yml` even contains a TODO
comment saying a benchmark gate should "move to CI". Three dev tools are pinned
to `"latest"`, so the toolchain can silently change between installs, which makes
any CI (and any two developer machines) non-reproducible. After this plan: one
command (`bun run verify`) checks the whole repo, the toolchain is pinned, and a
GitHub Actions workflow runs that command on every push/PR.

## Current state

- Remote: `https://github.com/ShaulLavo/platform.git` — GitHub Actions is the right CI target.
- Root `package.json` — has scripts `typecheck`, `test`, `lint`, `format:check` (all `turbo …`), but **no** `verify`/`check`/`ci` script. Its `devDependencies` include three unpinned entries:

  ```json
  "@typescript/native-preview": "latest",
  "oxfmt": "latest",
  "oxlint": "latest",
  ```

  The same `"latest"` pattern may repeat in workspace `package.json` files (`apps/*`, `packages/*`). Find every instance in step 1.

- `packageManager` field in root `package.json`: `"bun@1.3.10"`.
- `turbo.json` defines tasks `lint`, `format:check`, `typecheck`, `test` (test `dependsOn: ["^build"]`), plus `globalPassThroughEnv` for FS*\*/OBSERVABILITY*\* vars — no env file is required for typecheck/lint/test.
- Per-workspace test scripts (already exclude Playwright browser tests from the default run):
  - `apps/web/package.json:23` — `"test": "bun --bun vitest run --project node --project dom"`
  - `apps/server/package.json:25` — `"test": "bun --bun vitest run"`
- Typecheck uses `tsgo` (from `@typescript/native-preview`), e.g. `apps/web/package.json:26` — `"typecheck": "tsgo --build"`.
- `lefthook.yml` pre-commit runs oxfmt/oxlint on staged files and `bun run typecheck`; tests are not run anywhere automatically.
- Workspace caveat: root `package.json` `workspaces.packages` includes `"../Editor/packages/*"` (a sibling repo used for local editor development). `README.md:23-25` states a sibling checkout is **not** required for `bun install` — the editor runtime comes from npm `@singapor/*` packages. CI will not have the sibling checkout; this is expected to work.

## Commands you will need

| Purpose      | Command                | Expected on success |
| ------------ | ---------------------- | ------------------- |
| Install      | `bun install`          | exit 0              |
| Typecheck    | `bun run typecheck`    | exit 0              |
| Lint         | `bun run lint`         | exit 0              |
| Format check | `bun run format:check` | exit 0              |
| Tests        | `bun run test`         | exit 0, all pass    |

Run all from the repo root: `/Users/shaul/Desktop/D/platform`.

## Scope

**In scope** (the only files you should modify/create):

- Root `package.json` (add `verify` script; pin the three `"latest"` deps)
- Any workspace `package.json` under `apps/*` or `packages/*` that contains a `"latest"` version for `@typescript/native-preview`, `oxfmt`, or `oxlint`
- `bun.lock` (regenerated by `bun install` after pinning — commit the result)
- `.github/workflows/ci.yml` (create)

**Out of scope** (do NOT touch, even though they look related):

- `lefthook.yml` — pre-commit behavior is deliberate (fast hooks, no tests); CI is the enforcement layer.
- Per-workspace `test`/`typecheck`/`lint` script contents — the `--bun` flag and project selection are load-bearing (see AGENTS.md "Bun/Vitest Gotchas").
- The Playwright `browser` test project — it spawns real servers and is excluded from default test runs on purpose. Do not add it to CI in this plan.
- Pinning or upgrading any dependency other than the three named ones.
- `references/` and `packages/editor-*` symlinks.

## Git workflow

- Branch: `advisor/001-ci-pipeline-and-verify`
- Commit style: conventional commits, lowercase type prefix, e.g. `feat: add root validation, clean up code & fixes` (from `git log`). Suggested: one commit for pinning, one for the verify script, one for the workflow.
- Do NOT push or open a PR unless the operator instructed it.

## Steps

### Step 1: Capture the pre-existing baseline

From the repo root run each of: `bun run typecheck`, `bun run lint`, `bun run format:check`, `bun run test`. Record which (if any) fail **before any change**.

**Verify**: all four exit 0. If any fails, that failure is pre-existing — STOP and report it (the repo needs a green baseline before a CI gate is useful; do not fix unrelated failures inside this plan).

### Step 2: Pin the three `"latest"` dev tools

1. Find every declaration: `grep -rn '"latest"' --include=package.json apps packages package.json | grep -v node_modules`
2. For each of `@typescript/native-preview`, `oxfmt`, `oxlint`, find the version actually installed: look in `bun.lock` for the package's resolution entry, or run `bun pm ls | grep -E 'native-preview|oxfmt|oxlint'`.
3. Replace each `"latest"` with that exact version (exact pin, no `^`/`~` — these are toolchain, not library, deps).
4. Run `bun install` to sync `bun.lock`.

**Verify**: `grep -rn '"latest"' --include=package.json apps packages package.json | grep -v node_modules` → no matches. Then `bun run typecheck && bun run lint && bun run format:check` → all exit 0 (same versions, so behavior must not change).

### Step 3: Add the `verify` script

In root `package.json` `scripts`, add:

```json
"verify": "turbo typecheck lint format:check test"
```

**Verify**: `bun run verify` → exit 0, turbo reports the four tasks across workspaces.

### Step 4: Create `.github/workflows/ci.yml`

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.10
      - uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('bun.lock') }}
          restore-keys: bun-${{ runner.os }}-
      - run: bun install --frozen-lockfile
      - run: bun run verify
```

Keep `bun-version` in sync with the `packageManager` field in root `package.json` (currently `1.3.10` — re-check it).

**Verify**: the YAML parses — `bun -e "const f=await Bun.file('.github/workflows/ci.yml').text(); require('js-yaml')"` is NOT available; instead use: `bunx yaml-lint .github/workflows/ci.yml` → exit 0 (or, if `bunx yaml-lint` cannot run offline, validate with `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"` → exit 0).

### Step 5: Local end-to-end check of what CI will run

Simulate the workflow body locally: `bun install --frozen-lockfile && bun run verify`.

**Verify**: exit 0.

## Test plan

No new test files. The deliverable IS the verification infrastructure. The end-to-end check is step 5; the live GitHub Actions run can only be validated after the operator pushes the branch — note that in your report.

## Done criteria

Machine-checkable. ALL must hold:

- [ ] `grep -rn '"latest"' --include=package.json apps packages package.json | grep -v node_modules` → no matches
- [ ] `bun run verify` exits 0
- [ ] `.github/workflows/ci.yml` exists and parses as YAML
- [ ] `git status` shows no modified files outside the in-scope list
- [ ] `plans/README.md` status row updated

## STOP conditions

Stop and report back (do not improvise) if:

- Step 1 baseline fails — the repo is not green before your change.
- `bun install --frozen-lockfile` fails because of the `"../Editor/packages/*"` workspace glob (i.e. bun errors on the missing sibling directory). That falsifies the README claim and needs a maintainer decision (likely making the glob optional or removing it) — report it; do not edit the workspaces field yourself.
- Pinning a tool version changes lint/format/typecheck output (step 2 verify fails) — the lockfile and the manifest disagreed; report the versions involved.
- `bun run verify` is differently green/red from running the four scripts individually (a turbo task-graph problem) — report rather than tweaking `turbo.json`.

## Maintenance notes

- When bumping `packageManager` bun version, update `bun-version` in `ci.yml` in the same commit.
- The three pinned tools (`oxlint`, `oxfmt`, `@typescript/native-preview`) now need deliberate upgrades; lint/format rule changes will show up as a diff in the upgrade PR — that is the point.
- Follow-ups deferred out of this plan: adding the Playwright `browser` test project to CI (needs Playwright install + server spawn in CI), the editor scroll benchmark gate referenced by the TODO in `lefthook.yml`, and branch-protection rules (operator action on GitHub, not a code change).

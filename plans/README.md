# Implementation Plans

Generated by the improve skill. First pass on 2026-06-12 (@ `f88800a`);
**second pass on 2026-06-18 (@ `445a97d`)** added plans 005–008 and refreshed
001 against current `HEAD`. Execute in the order below unless dependencies say
otherwise. Each executor: read the plan fully before starting, honor its STOP
conditions, and update your row when done.

**Operator rule for this repo: all work happens on `main` — no new branches,
worktrees, commits, pushes, or PRs unless the operator explicitly asks.** Every
plan's "Git workflow" section reflects this.

Selection note (2nd pass): the operator reviewed the vetted findings table
interactively and selected F1, F2, F3, F4, and direction finding D1 for plans.
The full table and the rejected findings are below.

## Execution order & status

| Plan | Title                                                                                          | Priority | Effort | Depends on                         | Status                                                                                                                                                    |
| ---- | ---------------------------------------------------------------------------------------------- | -------- | ------ | ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 001  | [CI pipeline + one-command verify + pinned toolchain](001-ci-pipeline-and-verify.md)           | P1       | M      | —                                  | PARTIAL (2026-06-19): toolchain pinned + `verify` landed green; `ci.yml` BLOCKED — `link:@singapor/*` breaks clean-CI install (TODO in root package.json) |
| 002  | [GitService mutation-path characterization tests](002-git-service-mutation-tests.md)           | P1       | M      | —                                  | DONE (20 tests; characterized a real parseBranches field-shift bug)                                                                                       |
| 004  | [.env.example + truthful README setup docs](004-onboarding-env-and-readme.md)                  | P3       | S      | —                                  | DONE                                                                                                                                                      |
| 005  | [Patch vulnerable dependencies (bun audit)](005-patch-vulnerable-dependencies.md)              | P2       | S–M    | — (cleaner after 001)              | TODO                                                                                                                                                      |
| 006  | [Workbench panel-state characterization tests](006-workbench-panels-characterization-tests.md) | P2       | M      | —                                  | DONE (22 tests; characterized active-tab-after-close, path close, resize, setter, and normalize behavior)                                                 |
| 007  | [Projection latestTurnJson read-modify-write dedup](007-projection-latest-turn-json-dedup.md)  | P3       | S–M    | — (Step 1 tests gate the refactor) | DONE (3 latestTurn characterization tests; server typecheck/lint/test green)                                                                              |
| 008  | [SPIKE: wire the chat panel into the workbench](008-wire-up-chat-panel-spike.md)               | P3       | M      | —                                  | DONE (2026-06-19): sidebar mounts chat/logs; browser verified both panels; web typecheck/lint/test green; notes in `docs/chat-and-logs-wiring-notes.md`   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

> Numbering note: there is no plan 003. The first pass referenced a "003 (tab
> lifecycle UX)" in its dependency notes but the file was never written, and HEAD
> `445a97d` since rewrote the editor tab system, so that premise is moot.
> Numbering is kept monotonic; 003 is intentionally skipped.

## Recommended order & dependency notes

- **001 first.** It is the verification baseline — once CI runs `verify` (and, per 005's maintenance note, `bun audit`), every later plan is guarded. 001 now carries a MED risk: the `link:@singapor/*` overrides may block a clean CI `bun install` (see its STOP conditions). It is not a hard blocker for the others, which all verify locally.
- **005, 006, 007, 008 are independent** of each other and can run in any order.
- **007 is self-gating**: its Step 1 (characterization tests) must be green against the unmodified pipeline before its Steps 2–3 refactor; the tests are the contract.
- **005 ↔ 008 interaction**: 008 mounts the chat feature, which uses `lexical`/`@lexical/react`/`@singapor/diff`. 005 must **not** remove those deps. If both run, treat those three as load-bearing.
- Characterization-before-change still holds for `apps/server/src/git/` (plan 002) and now for `orchestration/projection-pipeline.ts` (plan 007 Step 1).

## Vetted findings table (2nd pass, 2026-06-18 @ `445a97d`)

| #   | Finding                                                                                                                                       | Category       | Effort | Confidence | Planned as                                                      |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ------ | ---------- | --------------------------------------------------------------- |
| F1  | Vulnerable deps on reachable paths (DOMPurify XSS via streamdown markdown render of model output; undici _high_ via cheerio in fonts service) | security/deps  | S–M    | HIGH       | 005                                                             |
| F2  | CI baseline still missing (no `.github/`, no `verify`, toolchain still `"latest"` in app manifests)                                           | dx             | M      | HIGH       | 001 (refreshed)                                                 |
| F3  | `workbench-panels.ts` pure layout/tab model has no dedicated test (active-tab-after-close + clamp logic)                                      | tests          | M      | HIGH       | 006                                                             |
| F4  | Projection `latestTurnJson` read-parse-merge-stringify in two handlers; canonical helper already exists                                       | perf/tech-debt | S–M    | MED        | 007                                                             |
| F5  | Search streaming re-groups/re-sorts per batch; `sortedSearchGroups` sorts in place                                                            | perf           | S–M    | MED        | not planned (investigate via existing `bench:workspace-search`) |
| F6  | `searchResultFileExcerptOffset` clamps excerpt index before computing scroll offset                                                           | correctness    | S      | LOW        | not planned (investigate)                                       |

Direction (maintainer's call, not ranked against bugs):

- **D1 — chat feature built but not mounted** → planned as spike 008.
- **D2 — command-palette provider registry** (`docs/command-palette-vscode-parity-backlog.md` specs multi-prefix Quick Access; code is a single command picker) → not planned; design-plan shaped, available on request.

## Findings considered and rejected

Recorded so the next audit doesn't re-litigate them:

### First pass (2026-06-12)

- **CORRECTNESS: "TOCTOU in tree query updater" (`use-workspace-events.ts`)** — rejected; `if (!current) return current` inside `setQueryData` is the correct TanStack Query pattern.
- **CORRECTNESS: stale dirty-set race in `applyOpenFileOperations`** — superseded by the `PLAN.md` FileSyncService roadmap, not a tactical patch.
- **SECURITY (1st pass)** — clean at standard depth: origin allowlist + auth guard enforced and tested; path containment before fs ops; array-based git spawns; no committed secrets.
- **DEPS: `vite: rolldown-vite` alias and `preact@11 beta`** — deliberate technology choices.
- **TEST: `packages/ui` (0 tests), `packages/tree` (2 tests)** — real but lower leverage.
- **DX: pre-commit hooks don't run tests** — deliberate (fast hooks); CI is the enforcement layer.

### Second pass (2026-06-18)

- **SECURITY: "wallpaper arbitrary file read via symlink" (`apps/server/src/wallpaper/service.ts`)** — rejected. The `/wallpaper/{info,still,media}` routes take **no client-supplied path**; they serve the host's own OS-configured wallpaper (`currentWallpaperPath()` via `osascript`) on localhost. No privilege boundary is crossed (the user setting the wallpaper is the user receiving it). By-design for a local dev tool.
- **PERF: search per-file stat cache "misses"** — rejected; `safeEntryStats` already caches per query; micro-opt.
- **PERF: projection "multiple DB round-trips per event"** — folded into 007 (same root cause as F4).
- **TECHDEBT: `codex.ts` is 2843 LOC** — rejected as a finding; large but isolated, low external fan-in (the auditing agent agreed it is not a blocker).
- **TECHDEBT: god modules `chat-projection-writers.ts` (1311), `search-buffer-state.tsx` (1502), `FileTreeView.tsx` (3555, has a "split this up" TODO)** — real but L-effort, HIGH-risk refactors on critical paths; the workspace-state half is owned by `PLAN.md`. Not high-leverage now.
- **DIRECTION: consolidate "competing chat state models"** — already the remit of the `PLAN.md` 12-week roadmap; not a separate finding.
- **DIRECTION: terminal↔chat integration** — speculative, no code grounding; rejected.
- **DX: knip "40 unused files / 51 unused exports"** — **not** a delete-dead-code finding: ~25 are the chat feature, which is built-but-unmounted (see D1/plan 008), not abandoned. Re-evaluate the genuine dead subset _after_ 008 mounts chat (plan 008 Step 5 produces that list). A few editor-tab-paths exports orphaned by HEAD's tab refactor are low-leverage cleanup.

## Direction options (maintainer's call, not planned)

- **Continue `PLAN.md` weeks 4–5 (FileSyncService)** — closes the one credible correctness race from the first audit.
- **D2 (command-palette provider registry)** — grounded in `docs/command-palette-vscode-parity-backlog.md`; design-plan shaped if you want it written up.

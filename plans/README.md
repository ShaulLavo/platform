# Implementation Plans

Generated by the improve skill. First pass 2026-06-12 (@ `f88800a`); second pass
2026-06-18 (@ `445a97d`); **third pass 2026-08-16 (@ `ace313f`)** — a nine-area
multi-agent audit with adversarial verification of every finding (58 raised, 57
survived, 1 rejected). Execute in the order below unless dependencies say
otherwise. Each executor: read the plan fully before starting, honor its STOP
conditions, and update your row when done.

**Operator rule for this repo: all work happens on `main` — no new branches,
worktrees, commits, pushes, or PRs unless the operator explicitly asks.** Every
plan's "Git workflow" section reflects this.

---

## The third pass in one paragraph

This codebase does not have a quality problem — it has a **redundancy** problem.
Discipline is unusually high (0.23% duplication, 16 TODOs in 222K lines, exactly
one file violating the nesting rule, argv-only subprocess invocation with byte
budgets and deadlines, no committed secrets, comment density that explains
_rejected_ alternatives). What is wrong is almost entirely one shape: **the same
truth is written down two-to-five times by hand, and nothing checks the copies
against each other.** Nine independent auditors found that under nine different
names. Explicitly healthy and needing no work: `git/service.ts` (1,264 lines but
coherent), the settings registry's scope rule, focus-visible coverage,
popover/menu motion, `packages/contracts/src/settings/`.

### 🔴 Authoring defect: absolute counts are forbidden as done criteria

**Four of the six Phase 3 plans came back BLOCKED, and not one was blocked by the
code.** All four were blocked by their own Done criteria. This is a defect in how
the plans were written, not in the repo.

Three distinct causes, all the same root:

1. **An unattainable gate** — 037 required a bare `bun run verify` exits 0.
   `verify` is whole-monorepo and short-circuits, so one unrelated failure
   anywhere makes it unreachable _and_ proves nothing about the change.
2. **Baselines that drifted** — 038 demanded `244 passed (244)`; the tree reports
   250 files / 1796 tests. 040 asserted a watcher failure that plan 047 had
   already fixed. Both were authored at `ace313f`, then plans 013–035 landed and
   invalidated every fixture they assert on.
3. **A criterion contradicted by its own step** — 039 required "no new lint
   warnings" while separately forbidding the only change that avoids them.

**The durable rule, now applied to all four plans:**

> **Never assert an absolute test count.** Capture a baseline at Step 0 and
> compare deltas:
>
> ```bash
> cd <workspace> && bun run test 2>&1 | tail -5 > /tmp/<plan>-test-before.txt
> ```
>
> The gate is _no previously-passing test may fail_ — never a number. A failure
> already present in the Step 0 snapshot is not the executor's problem and must
> not block the plan. **Never gate on `bun run verify`**; use the per-workspace
> `typecheck` / `lint` / `test` scripts.

A plan authored against one commit cannot assert a count that a sibling plan will
change. That is not a detail — it cost four of six executions.

### 🟡 One known web-test failure, and where it came from

`apps/web/src/features/settings/tests/page.test.tsx > refuses an
application-scoped key from the workspace tab, and says why` fails.
`getByText(/can only be set in User settings/)` now matches **two** elements —
"application settings…" and "machine settings…" — because a second
scope-restricted row became visible. One-line fix: make the query specific or use
`getAllByText`. **A test-query defect, not a product bug.**

Provenance, recorded because it matters for trust: it arrived red in commit
`8807fb8`, which committed in-progress settings work. The pre-commit hook runs
format/lint/typecheck but deliberately not tests, so nothing caught it, and the
commit was described as leaving a green baseline — true of `format:check`, not of
the suite. Verified red at `8807fb8` itself.

Web baseline is therefore **1 failed | 1795 passed (1796)**. It blocks nothing
once the plans use delta gates, but **fixing it is the single highest-leverage
action available** — it is one line and it clears the noise from every future
Step 0 snapshot.

### ⚠️ Sequencing correction (read this before picking up 009–012)

The four folder-reorganization plans (009, 010, 011, 012) were written **first**
but must be executed **last**. They are mechanical renames across the same files
that the structural items delete, split, and move. Running them earlier means
renaming files that are about to disappear and resolving every conflict twice.
They are Phase 4. This is a change from how they were originally sequenced.

---

## Execution order & status

### Phase 0 — Verification baseline (nothing risky starts before this)

| Plan | Title                                                                                                                                          | Priority | Effort | Depends on | Status                                                                                                                                                                                                                           |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 013  | [Test-baseline repairs: stop writing to the real SQLite; typecheck `scripts/`; give `packages/ui` a test script](013-test-baseline-repairs.md) | P1       | S–M    | —          | **DONE** (`58bf4d0`) — independently verified: `~/.platform/fs-metadata.sqlite` mtime unchanged across a full `apps/server` run. Also surfaced a real hidden error once `scripts/` got typechecked: `descriptorFor(id as never)` |
| 014  | [`packages/tree` path-store + `getVisibleRows` characterization tests](014-tree-path-store-characterization-tests.md)                          | P1       | M      | —          | **DONE** (`5774dc6`) — independently verified: `packages/tree` went 9 tests → **78 passing** across 5 files, oracle + fuzz in place. **The gate for 039 is now open**                                                            |

> ### ✅ The one failing server test was catching a REAL BUG — fixed by 047
>
> `apps/server/src/tests/app.test.ts > fs rpc events > reports external file
updates from the native watcher` used to fail deterministically (~15s
> timeout). It was never environmental flakiness: the test was right and the
> product was wrong. **Fixed by [047](047-watcher-event-classification.md)**
> (`f93dd1d`), which replaced the `knownNativePaths` cache with classification
> from stat data.
>
> The baseline for `cd apps/server && bun run test` is **0 failed**, and plans
> that state "all pass" are literal again. The suite total is now 779 rather
> than 773 because 047 added six watcher-classification tests.

### Phase 1 — Cheap, high-confidence, independent

| Plan | Title                                                                                                                                        | Priority | Effort | Depends on                  | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------ | --------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 047  | [Stop reporting a modified pre-existing file as `created` (F-WATCH)](047-watcher-event-classification.md)                                    | P1       | M      | —                           | **DONE** (`f93dd1d`, `1f8eb0d`) — Option B; red test green, suite 779/779, silent parcel→node fallback now logged                                                                                                                                                                                                                                                                                                                                                |
| 017  | [Security + correctness one-liners: argv option-injection hardening and the `..`-prefix containment bug](017-argv-and-path-containment.md)   | P1       | S      | —                           | **DONE** — `checkout`/`create-branch`/`file` refs now on `gitRefNameSchema`; `--` added to `git branch` and `fd` (not `git checkout`, where `--` means pathspec); one `isOutsideRoot`; server 787/787                                                                                                                                                                                                                                                            |
| 018  | [~~Stale-closure command bugs~~ → delete the inert memo](018-stale-closure-commands.md)                                                      | P3       | S      | —                           | **RESCOPED** — the executor hit Step 1's STOP and was **right; the plan's premise was wrong.** `use-open-file-at-ref.ts:21` returns a bare `async function`, so `openFileAtRef` — which is _in_ the dep array — changes identity every render, the memo never holds, and nothing freezes. Residual: an inert `useCallback` and an incomplete array that **arms a real bug** the day anyone stabilises `openFileAtRef`. See the correction at the top of the plan |
| 005  | [Patch vulnerable dependencies](005-patch-vulnerable-dependencies.md)                                                                        | P2       | S–M    | —                           | **DONE** — inventory rebuilt against the live 27 advisories; `bun audit` now reports **No vulnerabilities found**. All cleared in-major via `overrides`; no source change                                                                                                                                                                                                                                                                                        |
| 019  | [Delete `--sort path` from the ripgrep invocation](019-ripgrep-sort-path-removal.md)                                                         | P2       | S      | —                           | **DONE** — full-tree content search 158.06ms → 61.92ms (2.55x) on the real pipeline; truncation is now arrival-order, client still owns display order. First T6 instance closed                                                                                                                                                                                                                                                                                  |
| 020  | [One gated, indexed fold for the pending-request counters](020-pending-request-counter-single-fold.md)                                       | P2       | S      | —                           | **DONE** — SQL read filtered to 6 kinds + migration 9 index (`EXPLAIN` confirms the seek); in-memory fold gated; third fold deleted. Second T6 instance closed. `bun run verify` exits 0. **Note (036):** 020's second item — the in-memory projector refolding on every `thread.activity-appended` — no longer exists, because the in-memory projector no longer exists. Items 1 and 3 stand                                                                    |
| 021  | [Give fire-and-forget async a rejection boundary (server + client)](021-async-rejection-boundaries.md)                                       | P2       | S      | —                           | **DONE** — server half (Steps 1–3, `146dede`): crash closed, `runDetached` + `unhandledRejection` in place. Client half (Steps 4–5): `notifyChatCommandError` on all four dispatch sites, and the rename dialog now waits for the result. The deferred recovery half was never a blocker — split out as [048](048-settings-secret-refs-fail-closed.md)                                                                                                           |
| 048  | [Deliver the settings change when the secret store cannot be read, and fail closed while it cannot](048-settings-secret-refs-fail-closed.md) | P2       | S–M    | 021 server half (`146dede`) | **DONE** — `invalidate()` degrades instead of unwinding: a failed secret read sets `secretRefsStale`, records one wide warn event, and still notifies. While stale the one masking site emits `REDACTED` for every variable (round-trips as "leave it alone") rather than `''` ("delete it"). The constructor read still refuses to start, now as `settings.SECRETS_UNREADABLE` naming the file. Deferred: no UI indicator that secrets are unavailable          |
| 022  | [Delete the ~4,400 unreachable lines across tree, ui, contracts, provider, web](022-delete-unreachable-code.md)                              | P2       | M      | —                           | **DONE** — **5,382 lines deleted** across 55 files (tree store/scheduler/cleanup/SSR/instrumentation, 10 ui primitives + 2 deps, 190 barrel names, provider SPI). `bun run verify` exits 0                                                                                                                                                                                                                                                                       |
| 023  | [Stop rebuilding the chat projection once per streamed token delta](023-chat-per-delta-work.md)                                              | P2       | M      | —                           | **DONE** — 0.4361 → 0.0982 ms/delta (**4.4x**) on a full-cap transcript; the work log's contradicting `createdAt`-only re-sort deleted (a real latent bug, red before / green after)                                                                                                                                                                                                                                                                             |
| 024  | [Four design-engineering one-liners](024-design-one-liners.md)                                                                               | P3       | S      | —                           | **DONE** — toasts, editor tabs, diagnostics and the assistant copy button now resolve to theme tokens. T7 closed. Visual checks skipped: no dev server running                                                                                                                                                                                                                                                                                                   |
| 025  | [`WorkspaceIndex`: running counters, parent→children map, lazy snapshot](025-workspace-index-incremental.md)                                 | P3       | M      | —                           | **DONE** — 1.508 → 0.098 ms/event (**15x**) at 10,101 entries, and flat: 0.117 ms/event at 20,201. Snapshot clone deleted, not made lazy. Third T6 instance closed                                                                                                                                                                                                                                                                                               |

### Phase 2 — Typed contracts (each deletes a cast cluster)

| Plan | Title                                                                                                | Priority | Effort | Depends on | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---- | ---------------------------------------------------------------------------------------------------- | -------- | ------ | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 026  | [Bind `SettingWidget` to its schema type](026-setting-widget-typed.md)                               | P2       | M      | 022        | **DONE** — `WidgetFor` binds the widget tag to the schema on `defineSetting`'s parameter (all 34 entries compile unchanged); `settingControl` narrows once in contracts and `setting-row.tsx` dispatches with **zero** casts. Step 5 was already applied by 013. `docs/settings-reference.md` regenerated byte-identical                                                                                                                                   |
| 027  | [Type the orchestration WebSocket RPC responses](027-ws-rpc-response-typed.md)                       | P2       | M      | 022        | **DONE** — one `ORCHESTRATION_WS_RESULTS` map keyed by request method; the client's `sendRequest` and the server's handler record both read it, so a shape change on either side is now a build error. The client's hand-written `{deduped, sequence}` was a strict **subset** of the server's result, never a mismatch — the server also shipped a `receipt` object that no code on either side read, deleted here (`engine.ts` 18 → 15 `receipt` lines)  |
| 028  | [Derive the orchestration event catalog from one record](028-orchestration-event-catalog-derived.md) | P2       | M      | 022        | **DONE** — three hand-maintained copies (30-string array, 152-line variant, 30-string test literal) collapsed into one `ORCHESTRATION_EVENT_PAYLOADS` record; the variant and the `OrchestrationEventType` union are now derived from it. Parsed-shape digest byte-identical before/after (`e30ee8d0…`), pair diff `PAIRS-IDENTICAL`, all 51 `Extract<OrchestrationEvent, …>` narrowings compile untouched. First T1 instance closed; **036 is unblocked** |

### Phase 3 — Structural (characterization tests required first)

| Plan | Title                                                                                                              | Priority | Effort | Depends on                     | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ---- | ------------------------------------------------------------------------------------------------------------------ | -------- | ------ | ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 029  | [One command table — `defineCommand({id,title,icon,keys,when,run})`](029-one-command-table.md)                     | P2       | L      | 018; `keymap.test.ts` (exists) | **DONE** — six tables collapsed into one 133-entry table; 1,451 lines deleted from the projections. `keymap.test.ts` passes **unchanged**, `before-rows.json` is byte-identical, the bindings diff is only 39 added `meta` blocks on the 13 session bindings, and the 116 visible palette rows keep their exact order. Two plan corrections: the 13 new specs insert at index 40 rather than the tail (the plan's own `table.ts` design requires it, and ranking indexes the _filtered_ list, so it is inert — proven by comparing the visible order); and `commandIcon()` had to become an indexed record, because the React Compiler reads a call in an element position as `static-components` and errors. **Unblocks 042 + D3**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 030  | [One `dispatchChatCommand` helper](030-chat-dispatch-envelope.md)                                                  | P3       | M      | pairs with 021                 | **DONE** — ten envelopes in three dialects plus 021's four telemetry-less sites now go through one `dispatchChatCommand`; six `elapsedMs` copies and the three hand-written replay offsets collapsed. Step 9's live log check **skipped**: no dev server is running, so Step 0's calibration read 0                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 031  | [Type `runtimePayload`](031-runtime-payload-typed.md)                                                              | P3       | M      | —                              | **DONE** — Option A; the payload is parsed once by valibot in `rowToBinding`, so a mistyped key is now a `TS2561` compile error (proven with a deliberate `activeTurnIdd`, then reverted). Four validators, four local `isRecord` copies and both `createInternalError` throws on the turn-start path deleted                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 032  | [One session status enum](032-one-session-status-enum.md)                                                          | P2       | M      | reaper+service suites (exist)  | **DONE** — one 8-member `orchestrationSessionStatusSchema`; `isActiveBinding` inverted to the dead set, which is the bug fix (a `waiting` session was unreusable, unlisted, and never torn down — leaking a CLI child). Changed suite: **`provider-service.test.ts`** (added the reuse case it lacked); `provider-session-reaper.test.ts` untouched and green — its reading of `waiting` is the one kept                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 033  | [Search fallback: thread the `AbortSignal`, make it stream](033-search-fallback-abort-and-stream.md)               | P3       | M      | **must write test**            | **DONE** — the fallback walker is a real `async function*` and takes the signal as a parameter (not a `FindContext` field). New `search-fallback.test.ts`: 5 tests, and the three that pin the fix were confirmed **red** against the old walker. `truncated` is now accurate for this provider                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 034  | [`LspSessionPool` with an owner and `disposeAll()`](034-lsp-session-pool-ownership.md)                             | P3       | M      | 013 helps                      | **DONE** — the pool is an object `createApp` owns and `appCleanup` disposes, so language servers no longer outlive the server; two apps in one process stop sharing backends. LSP suite 19 → **26**, server suite **816/816**. The plan's known-red baseline is stale — 047 already fixed that watcher test, so the suite is green, not `1 failed`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 035  | [Move the six execution-affecting env knobs onto the settings registry](035-lsp-env-knobs-to-settings-registry.md) | P2       | M      | —                              | **DONE** — six env names → four `machine`-scope keys under a Language servers category; `docs/settings-reference.md` regenerated 34 → **38**. The `FS_EXPERIMENTAL_LSP_TY` module-load latch is gone, and the regression test proves it by exporting the old var and flipping the setting **both** ways                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 036  | [Collapse the dual projection — deletes `projector.ts` (625 lines)](036-collapse-dual-projection.md)               | P2       | L      | **028**; convergence suite     | **DONE** — the in-memory model is now a _cache_ of the SQL projection: `projector.ts` (647 lines) and three `read-model.ts` helpers are gone, and `projection-pipeline.ts` was not touched. Equivalence was proven before anything was deleted — the 26-case convergence suite was re-pointed at the new `refreshReadModel` and stayed green, and stubbing the refresh made 25 of 26 fail, so the gate has teeth. Refresh cost is the committed batch, not the thread: `read-model-bounds.test.ts`'s object-identity assertions still hold. Suite 210 → **211**; the one new test covers the cross-thread plan flag and was confirmed red with `touchedThreadIds`' `sourceProposedPlan` branch removed. Baseline was 210, not the plan's 207 (020/032 added three)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 037  | [Normalize the chat thread — 5 records + 4 types → 1](037-normalize-chat-thread.md)                                | P3       | L      | **023**; ~1,200 lines of tests | **BLOCKED** — its done criterion "`bun run verify` from the repo root exits 0" (line 1860) is **unattainable**, with no red-baseline fallback, so the plan cannot reach DONE as written: `apps/web` carries a pre-existing failure in `src/features/settings/tests/page.test.tsx > refuses an application-scoped key from the workspace tab` (`Found multiple elements with the text: /can only be set in User settings/`), confirmed by A/B with all plan work stashed. This is exactly the trap this README warns about — "no plan may use a bare `bun run verify` exits 0 as a done criterion". Gate 023 IS met and the premise re-verified (`Record<ThreadId` = 15, all five records present). **Step 2 nonetheless landed** — the three uncovered precedence rules (published `null` session outranking a detail snapshot, turn fallback by value, resnapshot preserving the arranged pin slot) are pinned **against unmodified source**; suite 116 files / 863 → **866**. Steps 3–7 (the 33-file collapse) not started. Unblock by fixing that settings test or restating the gate as a baseline comparison                                                                                                                                                                                                                                                                                                                                                                                    |
| 038  | [Collapse the editor document layer](038-collapse-editor-document-layer.md)                                        | P3       | L      | editor suites (exist)          | **BLOCKED** — Step 0 is an explicit STOP and it fires: it requires `Test Files 244 passed (244)` / `Tests 1764 passed (1764)` from `cd apps/web && bun --bun vitest run --project node --project dom`, and the real output is `1 failed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 249 passed (250)`/`1 failed       | 1795 passed (1796)`. The counts drifted (021/023/029/030/037 added tests) **and** the baseline is red: `src/features/settings/tests/page.test.tsx > refuses an application-scoped key from the workspace tab`fails with`Found multiple elements with the text: /can only be set in User settings/` — pre-existing, confirmed by A/B with all plan work stashed, and unrelated to this plan. Every "expected" count in 038 derives from those two numbers, so the plan cannot be executed as written. Unblock by fixing that settings test, then re-deriving Step 0's counts |
| 039  | [FileTreeView + FileTreeController splits](039-filetreeview-controller-split.md)                                   | P2       | L      | **014 (mandatory)**, then 022  | **BLOCKED** — gate **014 verified DONE** (5 files / 78 tests green) and 022 confirmed landed. **Steps 0–2 landed and green**: the `FILE_TREE_RENAME_VIEW` symbol alias is gone, and the three coupled sticky-keyboard refs are one union in `utils/render/stickyFocusMode.ts` with 7 new cases (`pendingStickyKeyboard` → 0, `pendingStickyFocusPathRef` → 5 unchanged, suite 5/78 → **6/85**, plan-014 suite green after every step). **Step 3 is where it blocks, and it was tested, not assumed**: bundling the seven element refs into `useFileTreeRowDom` necessarily adds **16** new `react-hooks(exhaustive-deps)` warnings, because oxlint exempts only refs declared by `useRef` _in the same scope_ and a returned bundle loses that exemption. Both spellings were tried (`dom.root.current` and `*Ref`-suffixed destructured aliases) — 16 warnings either way. The criterion "lint exits 0 with 0 errors **and no new warnings**" therefore cannot hold, and the only route to zero is adding those refs to dependency arrays, which Step 3 itself forbids ("Nothing may be added to or removed from any effect's dependency array"). Step 3 was reverted rather than ship 16 warnings; Steps 4–5 depend on it ("what keeps the drag and context-menu hooks at one parameter"). Unblock by deciding one of: relax the warning criterion, allow the dep-array additions (behaviour-neutral — the refs are stable by construction), or keep the refs unbundled and pass them individually |
| 040  | [`SerialWorker` + one `ReactorScheduler`](040-serial-worker-reactor-scheduler.md)                                  | P3       | L      | engine suites (exist)          | **BLOCKED** — its done criteria are unsatisfiable as written: they require `cd apps/server && bun --bun vitest run` to report `Test Files 1 failed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 81 passed (82)`and`Tests 1 failed | 778 passed (779)`with the one failure being`src/tests/app.test.ts > reports external file updates from the native watcher`. **Plan 047 fixed that test**, so the suite is now **88 files / 819 tests, 0 failures** — the criterion asserts a failure that no longer exists and can never be shown passing. No gate blocks the _work_; re-derive Step 0's counts against the green suite and the plan becomes executable                                                                                                                                                     |

### Phase 4 — Cohesion, vocabulary, and layout (last)

| Plan | Title                                                                                                                       | Priority | Effort | Depends on           | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---- | --------------------------------------------------------------------------------------------------------------------------- | -------- | ------ | -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 015  | [Give `@workspace/ui` a real motion system](015-motion-system.md)                                                           | P2       | M      | — (better after 019) | **DONE** — four motion tokens in `@theme inline`; every overlay now enters at 140ms and exits at 100ms on `ease-out-strong`, proven on a **real** dropdown in the running app (`0.14s cubic-bezier(0.23, 1, 0.32, 1) enter` / `0.1s … exit`, transform-origin still anchored to the trigger). The button's `transition-all` became an enumerated list **without `transform`**, so the 1px press lands on the pointer-down frame. Three plan corrections: `hover-card.tsx` no longer exists (022 deleted it), so it is six overlays not seven; `dropdown-menu`/`context-menu` **sub**-content override `data-slot`, so the reduced-motion block needs 9 selectors, not 8; and Step 4b's premise is moot for this app — `main.tsx` already passes `delay={600}`, so the primitive default only reaches other consumers. Base UI 1.5.0's `TooltipProvider` **does** group (it wraps `FloatingDelayGroup`, verified in its own source), so 400 stands. Reduced motion verified by mechanism rather than assertion: mid-animation transform is `matrix3d(0.95, …, -8, …)` normally and `matrix(1,0,0,1,0,0)` with the rule's six vars applied, opacity still animating in both. `bun run verify` exits **0**                    |
| 016  | [Move `features/logs/` onto the theme tokens](016-logs-theme-tokens.md)                                                     | P2       | S      | —                    | **DONE** — 15 palette hits → **0** repo-wide, all three `dark:` variants deleted rather than translated. Verified in the running app at `:3000` by reading the rendered `getComputedStyle` in **both** themes: badges, dots, the four timeline-bar tones and the Errors/Warn/Slow/Events metrics all resolve to distinct tokens (`destructive` h18 / `warning` h92 / `info` h230 / `success` h165). `bun run verify` exits **0** from the repo root — the README's red baseline is stale, the settings-page query defect is fixed in the operator's working tree and web is **250 files / 1796 tests, 0 failed**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 041  | [Row-state tokens + one empty/loading/error primitive](041-row-states-and-empty-primitives.md)                              | P3       | M      | land with 015/016    | **DONE** — two overlay tints (`--row-hover`, `--row-selected`) replace seven hand-rolled `/NN` row values; 17 token hits across 12 sites, exactly the count the plan predicts. Measured in the running app in **both** themes: the hovered element is now the **content-match row** — the majority row type, which had no hover class at all — at `oklch(… / 0.05)` light / `0.07` dark against a selected row at `0.12` / `0.14`. That replaces the plan's diagnosed 0.12% lightness collision. `EmptyState` + `LoadingState` land, and `Loading Git` is four pulsing bars rather than a sentence typographically identical to `No Git repository`. Four new tests, both files **red-checked** against the pre-plan shapes (drop the content row's hover class → 1 fails; render the git loading state as a `<section>` → `role="status"` not found). Two plan corrections: `skeleton.tsx` had been **deleted** by 022 as unreachable, so Step 5a recreates it — 041 is the plan that gives it its consumer; and the diagnostics panel had drifted under 024 (status tokens on the counts), which does not touch the two functions this plan replaces. web 250 → **252 files / 1800 tests**, `bun run verify` exits **0** |
| 042  | [Keybindings editor: wire it or delete it](042-keybindings-editor-wire-or-delete.md)                                        | P3       | M      | 029                  | TODO                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 043  | [State the `lib/` membership rule; pure moves only](043-lib-membership-rule.md)                                             | P3       | S      | land with 012        | TODO                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 044  | [`entryFromStat`/`pathBasename` into `fs/entry.ts`](044-fs-git-shared-primitives.md)                                        | P3       | S      | —                    | **DONE** — one `entryFromStat` in `fs/entry.ts`, **proven byte-identical** to the copies deleted from `watch.ts` and `service.ts` (`diff` against `ace313f` is empty); `pathBasename` moves with it and stays unexported. One `209_715_200`, in the new zero-import `fs/limits.ts`. `maxTextFileBytes` is required on `GitServiceOptions` and the constructor lost its `= {}` default, so the compiler found the call sites rather than a grep: **exactly 27 errors across 14 files**, every one the expected "missing property" / "Expected 2 arguments" shape — the plan's table was right to the line. The fs→git wiring gate holds: `skips snapshot refs for binary and large live diffs`, `skips untracked files above the text diff limit` and `omits blob text when the opened snapshot exceeds the text limit` all pass, so removing git's 200 MB fallback did not quietly restore it. Server suite **88 files / 819 tests, 0 failed**; typecheck, lint and format:check all exit 0; `git status -- apps/server` lists exactly the predicted 19 entries                                                                                                                                                            |
| 045  | [Origin guard: pick one model, delete the other](045-origin-guard-single-model.md)                                          | P3       | M      | —                    | TODO                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 046  | [Route `POST /settings/raw` through `write()`](046-settings-raw-write-guards.md)                                            | P3       | S      | —                    | TODO                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 009  | [Web code-layout spec + `features/search/` reorg](009-web-code-layout-spec-and-search-reorg.md)                             | P3       | M      | Phase 3 done         | TODO                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 010  | [Finish the feature-folder reorg (logs, git, editor, terminal, settings, workbench)](010-remaining-feature-folder-reorg.md) | P3       | M      | 009                  | TODO                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 011  | [Collapse `features/chat/lib/` into `utils/`](011-chat-lib-utils-collapse.md)                                               | P3       | M      | 009                  | TODO                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 012  | [Give the workspace domain a home in `features/`](012-features-workspace-consolidation.md)                                  | P3       | L      | 009, 010, 011        | TODO                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

### Completed in earlier passes

| Plan | Title                                                                                           | Status                                                                           |
| ---- | ----------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| 001  | [CI pipeline + one-command verify + pinned toolchain](001-ci-pipeline-and-verify.md)            | DONE — `.github/workflows/ci.yml` now exists and runs format/lint/typecheck/test |
| 002  | [GitService mutation-path characterization tests](002-git-service-mutation-tests.md)            | DONE (20 tests; caught a real `parseBranches` field-shift bug)                   |
| 004  | [.env.example + truthful README setup docs](004-onboarding-env-and-readme.md)                   | DONE                                                                             |
| 006  | [Workbench panel-state characterization tests](006-workbench-panels-characterization-tests.md)  | DONE (22 tests)                                                                  |
| 007  | [Projection `latestTurnJson` read-modify-write dedup](007-projection-latest-turn-json-dedup.md) | DONE                                                                             |
| 008  | [SPIKE: wire the chat panel into the workbench](008-wire-up-chat-panel-spike.md)                | DONE                                                                             |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) |
REJECTED (one-line rationale)

## ⚠️ Read before executing any plan: the baseline is currently red

`bun run verify` **does not exit 0 in this working tree today**, for a reason
unrelated to any plan here. Measured at `ace313f`:

- `cd apps/web && bun run format:check` **fails** on
  `src/features/settings/hooks/use-setting-inspection.ts` — uncommitted
  settings-feature WIP. `bun run format` fixes it, but it is the operator's
  work, so no plan touches it.
- `cd apps/server && bun run test` was reported by a plan reviewer to have one
  deterministically failing case: `src/tests/app.test.ts > fs rpc events >
reports external file updates from the native watcher` (15s watcher timeout).
  **Not independently confirmed** — the server suite writes to the real
  `~/.platform/fs-metadata.sqlite` (that is plan 013's subject), so it was not
  re-run here. Verify it yourself before trusting it, ideally after 013 lands.
- `cd apps/web && bun run typecheck` **does** exit 0.

Consequence: **no plan may use a bare "`bun run verify` exits 0" as a done
criterion**, because it is unattainable regardless of the plan's own
correctness. The fresh-context critics caught this and rewrote the affected
gates to (a) snapshot a baseline before the change and (b) compare against it
rather than against green. If you write a new plan, do the same. Fixing the
baseline itself is worth doing first — the format failure is one command.

> **Numbering note**: there is no plan 003 — the first pass referenced it in a
> dependency note but the file was never written and the premise is moot.
> Numbering is kept monotonic.

> **Plan 005 was stale and has been re-executed against a fresh inventory.**
> Written 2026-06-18 against 11 advisories; `bun audit` at `ace313f` reported
> **27** (1 critical, 10 high, 13 moderate, 3 low). The plan's _approach_ held
> exactly — every advisory cleared with a root `overrides` pin inside the same
> major, with no source change and no direct-dep major bump. `bun audit` now
> reports **No vulnerabilities found** (27 → 0). Three of the pins already
> existed and had simply been overtaken (`brace-expansion` 5.0.6 → 5.0.9,
> `dompurify` 3.4.11 → 3.4.13, `undici` 7.28.0 → 7.29.0), which is the argument
> for the maintenance note's CI gate: a pin is a snapshot, not a fix.

---

## The six cross-cutting themes

Every finding above is an instance of one of these. Fixing the theme is worth
more than fixing the instances.

**T1 — Parallel hand-maintained representations of one truth** _(6 areas, ~12
findings — the dominant theme)._ The event log is folded twice by hand (1,238 +
625 lines, policed by a 551-line convergence test). The event catalog is written
three times. Session status is modelled four ways by eight lossy mappers that
_actively contradict_ each other — `waiting` means "running" to the UI,
"inactive" to session reuse, and "never reclaim" to the reaper. One chat thread
is five parallel records; one document is three representations; `entryFromStat`
exists character-for-character twice. In every case the copies are unenforced:
adding a field compiles, ships, and drifts. **One rule closes all of them: a
second representation must be _derived_, never _maintained_.**

**T2 — Typed contracts that stop being typed exactly where the consumer needs
them** _(3 areas)._ `SettingWidget` is an 11-member string union with no relation
to the schema, so `{schema: v.boolean(), widget: 'font'}` typechecks and the
settings page pays with eight casts including `(next: never) => void`.
`orchestration-ws.ts:247` is `data: v.unknown()`, so `dispatchCommand`'s result
shape is written twice and agrees by coincidence. `runtimePayload` is
`unknown | null`, so the value deciding whether a live child process is reused is
validated by five hand-written sniffers across three files.

**T3 — No shared home for a 10-line utility → N copies** _(4 areas)._ Four WS
context adapters, six `basename`s with four different behaviours, four
`omitKey`s, seven dispatch-with-telemetry envelopes in three incompatible
dialects, six `elapsedMs` helpers, seven empty-state treatments, ten
`hover:bg-*` values. Note the asymmetry: `lib/` has a real high-fan-in core
(`client` 65 importers, `file-system-types` 60) but no _membership rule_, so it
simultaneously hosts single-consumer feature modules and fails to attract the
primitives features actually need.

**T4 — Exported-but-unreachable surface** _(all package areas)._ Contracts
barrel: 43% of 467 names have zero external reference. `packages/ui`: 28% of
lines plus two locked dependencies (`ai`, `embla-carousel-react`) for code nobody
runs. `packages/tree`: 16%, exported through 8 wildcard patterns including
`internal/*`. Plus the provider SPI's dead members and a fully-built, unwired
keybindings editor. **Net ~4,400 deletable lines** — knip's 111 symbols are a
_subset_.

**T5 — Holes in the verification baseline** _(3 areas + root)._ `bun --bun
vitest run` in `apps/server` opens, migrates, and WAL-locks the developer's real
`~/.platform/fs-metadata.sqlite`. `packages/ui` has no `test` script, so root
`verify` proves nothing about it. `scripts/` — 520 LOC that starts the dev server
and produces production builds — is in no workspace and is never typechecked.
`packages/tree`'s 20K lines have 9 test cases and zero over the path store.
**Every risky item is gated on this.**

**T6 — Right gate, wrong data structure behind it** _(2 areas)._ A correct
predicate guards an operation that then does full-collection work: `--sort path`
makes ripgrep single-threaded to produce an ordering the client re-sorts two
layers later; the pending-request query has no kind predicate although the plan
query directly above it does; `WorkspaceIndex` does ~4 full map traversals plus N
cloned objects per filesystem event and the caller discards the clone.

**T7 — Design system stops at the token definition.** The oklch status ramp is
carefully tuned and then bypassed by sonner's `richColors` on the loudest surface
in the app; the diagnostics panel — whose entire job is severity — renders all
four counts in `text-foreground`; `Skeleton` has zero consumers while seven
features hand-roll empty states.

**Process observation.** Three surviving findings were already written down in
this repo's own docs (`environments-and-remote-plan.md:394` calls session-token
mode dead code; `settings-registry-inventory.md:173` independently diagnoses the
LSP `ty` module-load latch; `settings-architecture-plan.md:328` specifies the
raw-write route). **The docs record debt accurately and nothing burns it down.**
Whatever comes out of this audit needs a closing mechanism, not another
document — which is what this status table is for.

---

## Findings considered and rejected

Recorded so future audits don't re-litigate them.

### Reported during execution, investigated and CONFIRMED

- **F-WATCH — a modified pre-existing file is reported to clients as `created`.**
  Found by the plan-013 executor while investigating the failing watcher test;
  independently confirmed by reading the code.
  **RESOLVED by [047](047-watcher-event-classification.md)** (`f93dd1d`).

  Mechanism, all in `apps/server/src/fs/watch.ts`:
  1. `FileChangeHub` keeps `knownNativePaths` (`:41`) as its record of files it
     knows about.
  2. That set has exactly four references — declared `:41`, read `:210`, deleted
     `:217`, added `:221`. **Nothing populates it at startup.** The directory scan
     that runs when the watcher attaches adds nothing, so at watch time the set is
     empty even though the files are all there.
  3. `nativeEventType` (`:202-212`) returns `changed` immediately when Node's raw
     event is `change`. When the raw event is `rename` — what macOS reports for
     the temp-file-plus-rename atomic save nearly every editor performs — it looks
     the path up in the empty set, misses, and falls through to `created`.

  **Consequence:** the first modification of any pre-existing file reaches clients
  as a creation, and the `changed` event that would tell them to refresh never
  arrives. Self-heals after one event per path, which is why it survived this long.

  Live `/fs/events` probe on a file that existed before the watcher started:

  ```
  801ms:  write 'after'               → created   ← wrong, the file already existed
  2802ms: write 'much-longer-content' → changed   ← right, path is now in the set
  ```

  **Why it is not folded into an existing plan:** it is server production code, in
  no plan's in-scope list, it predates all of this work, and the fix changes
  client-visible event semantics — which is a design decision, not a patch. The
  open question a plan must answer: should the startup scan seed
  `knownNativePaths`, or should the classifier stop depending on a
  "have I seen this path" set at all? Seeding is the smaller change; dropping the
  set is the one that cannot silently regress. Either way the web client's
  handling of `created` vs `changed` needs checking before the semantics move.

### Reported during execution, investigated and CONFIRMED (021)

- **`runDetached` closes the crash and reveals a dropped settings change.** Plan
  021's Step 2a test reproduces exactly as written: an unreadable secrets file
  (a directory where `secrets.json` should be) throws `EISDIR` out of
  `invalidate()` into the detached reload, and before the fix Vitest reports it
  as an unhandled rejection. After the fix the rejection is gone and the process
  survives — but the plan's own assertion, that the _next_ settings change is
  still delivered, **fails**: the snapshot comes back with defaults.
  This is the consequence plan 021 predicted in its own Maintenance notes —
  `invalidate()` clears `cachedSnapshot` and then reads secrets _before_
  notifying listeners, so the throw drops the notification instead of crashing.
  Closing it means editing `settings/store.ts:311`, which 021 lists under both
  "Out of scope" and STOP conditions, because stale `secretRefs` decide what
  `maskProviderSecrets` redacts. **That is the next owner's decision, and it is a
  redaction question, not an error-handling one.** The test now asserts what is
  true (the process survives) and documents what is not.

### Reported during execution, investigated and rejected

- **Plan 018's stale-closure premise — the two toggles are NOT one-way switches.**
  The plan's mechanism is right up to the last step: `react/exhaustive-deps` is
  `"warn"`, `preserve-manual-memoization` keeps the array as authored, and the
  array does omit `diffViewMode`, `wallpaperEnabled`, and both setters. What the
  plan missed is that the array's **existing** entries do not hold either.
  Probed by rendering the real hook under the real compiler: `openFileAtRef`
  (`useOpenFileAtRef` returns a fresh `async function` per render), `setTheme`,
  and `setSetting` all change identity on a bare re-render — only `closeTab` is
  stable. Two of those three were already dependencies, so the `useCallback`
  rebuilds on every render and nothing is ever captured stale. Four tests
  driving the real settings write path — including "change the setting after
  mount, then dispatch" — pass **unchanged against the unfixed code**.
  The dep array was still fixed (it was the file's only lint warning, and the
  correctness was an accident of an unstable sibling: stabilising
  `openFileAtRef` as a perf cleanup would have made the plan's bug real).
  The **durable lesson**: an incomplete dep array is invisible here, and so is
  an inert one. Promoting `react/exhaustive-deps` to `"error"` is still the
  right follow-up.

- **"`flattenEmptyDirectories` defaults disagree between `path-store/options.ts:6`
  (`!== false`, on) and `path-store/store.ts:99` (`=== true`, off)"** — reported by
  the plan-014 executor, which correctly declined to touch production code and
  flagged it instead. **Investigated and rejected: there is no behavioral
  divergence.** `store.ts:99` reads `state.snapshot.options`, which is typed
  `ResolvedPathStoreOptions` (`internal-types.ts:104-107`) where
  `flattenEmptyDirectories` is a strict `boolean`. The default is applied exactly
  once, in `resolvePathStoreOptions`. Against an already-strict boolean,
  `=== true`, `!== true`, and a bare read are all equivalent — which is why all
  five read sites (`store.ts:99`, `static-store.ts:374`, `flatten.ts:13`,
  `projection.ts:662,764`) behave identically today.
  The residue is style, not correctness: three spellings for one boolean. It
  becomes a real bug only if someone changes `snapshot.options` to carry raw
  user options, at which point `store.ts:99` would silently flip the default.
  Worth a one-line comment on the type, not a plan.

### Third pass (2026-08-16)

- **`noOpBinding` swallows nine VS Code default keys** — rejected, and it was the
  only finding of 58 that adversarial verification killed outright. Every one is
  a browser-owned chord (Control+Tab, Mod+W, Mod+1/2/3, F12…). The
  `TODO(electron)` at `default-bindings.ts:146` states the rule, and two tests in
  `keymap/tests/keymap.test.ts` (lines 526, 583) assert the no-ops explicitly.
  Implementing the "fix" would fail both tests. By design.
- **Content-index rg exclude-glob layer** — a benchmark question, not a win.
  Deleting the globs trades a per-query index scan for per-file open-and-read on
  every known-binary file, and the scan exits early at 512 globs, so the two
  failure modes are mutually exclusive. Do not touch without a measurement.
- **`BoundedTtlCache` full-map sweep** — the 10k–20k figure is the capacity
  ceiling; the live map is keyed per (thread, turn) with a 2h TTL and holds tens
  of entries. Asymptotically wrong, practically free.
- **Minimap `transition-[width,height]`** — the `nav` is an absolutely-positioned
  sibling of the scroll container, not inside it; two 8px spans transition, only
  on active-turn change. Wrong idiom, zero measured cost.
- **Four WS context adapters → one helper** — the load-bearing claim ("terminal
  and lsp already disagree") was refuted; they are behaviourally identical, only
  spelled differently. Do it opportunistically, never as its own PR.
- **`packages/tree` entry-point restructure** — the "tree is the outlier" premise
  is false; `packages/ui` uses the same wildcard scheme and `AGENTS.md` permits
  rather than requires entry barrels. Only the deletions survive (folded into
  item 9).
- **Making `useFileTree` options reactive** — the constructor-only contract is
  _documented at the top of the hook_, not silent. Revisit only when a real
  stale-callback bug appears.
- **"Delete `ChatSidePanel`, it's a competing host"** — refuted as framed. It is
  one tab of the workbench sidebar (`sidebar-panel.tsx:130`), a deliberately
  narrow companion beside the editor.
- **Rewriting the chat timeline derivation to be incremental** — L effort, no
  profile, and the verifier found that _no chat component uses `React.memo`_, so
  the 108 lines of hand-rolled identity sharing currently buy nothing. Measure
  first; the likely answer is that the apparatus is **deletable**.
- **Consolidating the six `basename`s / four `omitKey`s** — four genuinely
  different behaviours (one lowercases for icon lookup, one normalises Windows
  separators, one falls back to the whole path). Swapping a call site is a silent
  behaviour change typecheck cannot catch.
- **Chasing the "third latent divergence" between the two projections** —
  speculative; no code path produces an out-of-order `updatedAt`.
- **Duplication (jscpd 0.23%), TODO/FIXME debt (16 in 222K lines), nesting
  (clean except `FileTreeView.tsx`), the provider driver SPI's design,
  `use-workspace-events.ts` at 904 lines** — all checked and healthy.

### Second pass (2026-06-18)

- **"Wallpaper arbitrary file read via symlink"** — rejected. The
  `/wallpaper/{info,still,media}` routes take **no client-supplied path**; they
  serve the host's own OS-configured wallpaper on localhost. No privilege
  boundary crossed. By design for a local dev tool.
- **Search per-file stat cache "misses"** — `safeEntryStats` already caches per
  query; micro-opt.
- **`codex.ts` is 2,843 LOC** — large but isolated, low external fan-in. Third
  pass agreed; its _internal_ structure was re-audited and only the dead SPI
  members surfaced (item 9).
- **God modules `chat-projection-writers.ts`, `search-buffer-state.tsx`,
  `FileTreeView.tsx`** — the third pass **overturns this rejection** for
  `FileTreeView.tsx` (now item 27, gated on plan 014) and for the chat writers
  (now items 11 and 25). `search-buffer-state.tsx` remains unplanned.
- **knip's "40 unused files / 51 unused exports"** — superseded. The third pass
  found the real number is ~4,400 lines across four packages (T4/item 9); knip's
  count is a subset. Re-run knip _after_ item 9 lands.

### First pass (2026-06-12)

- **"TOCTOU in tree query updater"** — `if (!current) return current` inside
  `setQueryData` is the correct TanStack Query pattern.
- **`vite` → `rolldown-vite` alias and `preact@11` beta** — deliberate
  technology choices.
- **Pre-commit hooks don't run tests** — deliberate; CI is the enforcement layer.

---

## Direction options (maintainer's call, not ranked against defects)

- **D1 — `packages/ui` primitives gallery.** 34 primitives, 0 tests, no surface
  to review them in. A dev route rendering every primitive in every variant,
  light and dark, is roughly one file — and it is what makes plan 015's motion
  work and item 29's row-state work reviewable rather than theoretical. The audit
  independently found `packages/ui` has no `test` script at all (item 3), so the
  gallery and the test script are the same gap.
- **D2 — motion in the settings registry.** `--surface-opacity`,
  `--surface-blur`, and `--surface-saturation` are already real settings. Motion
  is the obvious sibling, and plan 015 leaves `--duration-enter` /
  `--duration-exit` as tokens specifically so this is a one-entry change.
- **D3 — command-palette provider registry.** `docs/command-palette-vscode-parity-backlog.md`
  specs multi-prefix Quick Access; the code is a single command picker. Carried
  from pass 2 and now **much cheaper after item 17** (one command table), which
  supplies the titles, icons, and gating the palette needs.

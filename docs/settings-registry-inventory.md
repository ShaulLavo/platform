> [!NOTE]
> **Historical inventory, generated 2026-08-13 and verified against `fda1523`.**
> This was raw planning material, **not** the current shipping list. The source
> of truth is `packages/contracts/src/settings/keys.ts`; the generated user-facing
> catalogue is [settings-reference.md](settings-reference.md).
>
> `?` in the Verified column means the default could not be found in code — almost
> always a NEW FEATURE key with no current behaviour to read.
> 🔒 = sensitive (value never logged, never served). ⟳ = requires restart.

# Settings registry inventory

## chat._ + providers._ + models._ + keybindings._

| Key | Type | Default | Scope | Phase | Vis | OK | Wiring |
| ---------------------------------------------- | ------------------ | -------------------------- | ------------------ | --------------- | ----------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `chat.defaultRuntimeMode` | enum(full-access\\ | approval-required\\ | auto-accept-edits) | `'full-access'` | application | P6 | user | yes | SCOPE: application, NOT window. This value becomes the provider session's permission posture — apps/server/src/provider/adapters/codex.ts:333 and adapters/claude.ts:396+ carry it into the session config, and the reuse check at codex.ts:334 keys on it. A cloned repo's .platform/settings.json setting 'full-access' would silently grant the agent unrestricted exec on first open, so the plan's standing rule (plan lines 133-139, which names chat.defaultRuntimeMode explicitly) forbids window scope. PLUMBING + one gap: the constant already exists and is threaded everywhere; what is missing is a _user default_ source. Today apps/web/src/features/chat/components/chat-draft-view.tsx:159 hardcodes DEFAULT_RUNTIME_MODE as the draft composer's mode and apps/web/src/features/chat/lib/chat-command-builders.ts:169 uses it as the createDraftThreadSubmission parameter default. Rewire both to read the setting; leave the server-side `?? DEFAULT_RUNTIME_MODE` fallbacks (orchestration/decider.ts:336,:717; provider-runtime-ingestion.ts:534,:551; provider-command-reactor.ts:541) as the untrusted-input floor rather than routing them through settings. |
| `chat.defaultInteractionMode` | enum(default\\ | plan) | `'default'` | application | P6 | user | yes | SCOPE: application, same reasoning as chat.defaultRuntimeMode. Plan mode vs default mode decides whether the agent writes/execs before the user approves, and it reaches the adapter's session config (codex.ts:332 `input.interactionMode ?? DEFAULT_INTERACTION_MODE`, then reconfigureInteractionMode at codex.ts:339; claude.ts:396). It is on the same permission axis as runtimeMode, so it cannot be window. PLUMBING: chat-draft-view.tsx:157 hardcodes DEFAULT_INTERACTION_MODE and chat-command-builders.ts:166 defaults the parameter; both read the setting instead. The per-thread override path (ChatComposerModesProvider, providers/composer-modes-provider.tsx:60) is unchanged — the setting only supplies the seed. |
| `chat.attachments.maxPerMessage` | number | `8` | application | P6 | advanced ⟳ | yes | requiresRestart: true is not a nicety. The constant is consumed inside valibot schemas built at module import — chat-model.ts:81 and :86 call v.maxLength(MAX_CHAT_ATTACHMENTS) on chatAttachmentsSchema/chatAttachmentUploadsSchema — so a value changed at runtime does not change what the server accepts until the module is re-evaluated. Either mark requiresRestart or rebuild the schema from the resolved setting at request time; do not ship a knob that moves the browser check (apps/web/src/features/chat/lib/chat-input-attachment-limits.ts:92) and leaves the wire limit frozen. SCOPE: application — this is a server-enforced resource ceiling (apps/server/src/attachments/store.ts:73), so a workspace file must not be able to raise it. |
| `chat.attachments.maxBytes` | number | `10 * 1024 * 1024` | application | P6 | advanced ⟳ | yes | Same module-scope-schema trap as maxPerMessage: baked into chatAttachmentSchema at chat-model.ts:51 (v.maxValue) and into MAX_CHAT_ATTACHMENT_DATA_URL_LENGTH at :44, which is itself used at :69 (v.maxLength). It is also derived a second time on the client: MAX_CHAT_ATTACHMENT_ENCODED_BYTES (apps/web/src/features/chat/lib/chat-input-attachment-limits.ts:42) budgets backwards from it through base64, and that derivation is module-scope too. Any live-update design has to recompute both. Server enforcement at apps/server/src/attachments/store.ts:73. SCOPE: application — raising it from a cloned repo enlarges what the server will decode into memory. |
| `chat.attachments.maxSourceBytes` | number | `50 * BYTES_PER_MEGABYTE` | application | P6 | advanced | yes | PLUMBING. Read at classify time (chat-input-attachment-limits.ts:102) and again in the compressor (apps/web/src/features/chat/lib/image-compression.ts:66), both per-call, so a live value works without a restart. It is an OOM guard on createImageBitmap, not a wire limit — the comment at :25-29 says so. Note the rejection message at :106 divides by BYTES_PER_MEGABYTE, so a non-integer-MB setting renders a fractional sentence; the widget should be MB-denominated. SCOPE: application — browser-side resource guard, no reason for a repo to raise it. |
| `chat.attachments.maxImageDimension` | number | `2048` | application | P6 | advanced | yes | PLUMBING. Read per encode at image-compression.ts:122 (`Math.min(MAX_IMAGE_DIMENSION, ...)`), so passing the resolved value in as an argument is a one-line change. Note that scaledImageDimensions already takes maxDimension as a parameter (image-compression.ts:94), so the seam exists — only compressImageToByteLimit's caller (apps/web/src/features/chat/lib/chat-input-attachments.ts:95) needs to thread it. |
| `chat.promptStash.maxEntries` | number | `20` | application | P6 | advanced | yes | PLUMBING, with one caveat: the cap is applied at write time (prompt-stash-store.ts:120, `.slice(0, MAX_PROMPT_STASH_ENTRIES)`), so lowering it does not prune the already-persisted queue until the next stash. Either prune on hydrate or accept a queue that shrinks lazily and say so. The store also owns its own localStorage key 'platform:prompt-stash:v1' (prompt-stash-store.ts:10) — that is stash _content_, not a preference, so it stays in localStorage; only the cap becomes a setting. SCOPE: application — a personal queue depth; a workspace file has no business setting it. |
| `chat.diff.defaultScope` | enum(working-tree) | `{ kind: 'working-tree' }` | window | P6 | user | yes | PLUMBING. DEFAULT*THREAD_DIFF_SCOPE is read in exactly one place, apps/web/src/features/chat/hooks/use-thread-diff-scope.ts:32, as the `??` fallback when a thread has no remembered pick. Registry note: the scope schema is a variant (thread-diff-scope-storage.ts:19-26) whose only other arm, 'turn', requires a concrete turnId — so 'working-tree' is the only value expressible as a \_default*, and the descriptor's enum has exactly one member today. Register it as an enum anyway so a future branch-diff arm (explicitly rejected at :14-18) does not need a type change. SCOPE: window is safe here — it reaches only which panel the diff pane opens on; no spawn, exec, env, or keymap. |
| `chat.diff.rememberedScopeLimit` | number | `200` | application | P6 | internal | yes | PLUMBING, internal. LRU bound on the localStorage map of per-thread diff picks; nothing deletes an entry when a thread goes away (comment at :8-11). One of the ~65 engineering constants the plan wants registered-but-collapsed. SCOPE: application — a localStorage budget on the user's own machine. |
| `chat.changedFiles.autoExpandFileLimit` | number | `5` | window | P6 | advanced | yes | PLUMBING. shouldAutoExpandChangedFiles (chat-changed-files-presentation.ts:47-53) is a pure function called once per card render from apps/web/src/features/chat/components/assistant-changed-files-section.tsx:39, so a resolved setting can be passed in as an argument with no memoization concerns. Both limits must be applied together — the heuristic is an AND of file count and line count. |
| `chat.changedFiles.autoExpandLineLimit` | number | `200` | window | P6 | advanced | yes | PLUMBING. Second half of the auto-expand AND at chat-changed-files-presentation.ts:52 (`stat.additions + stat.deletions <= LIMIT`). Same call site as the file limit. |
| `chat.changedFiles.expandDirectoriesByDefault` | boolean | `true` | window | P6 | user | yes | PLUMBING. The default is an inline `?? true` in the component, not a named constant — extract it into the registry and read it there. Persisted per-card overrides stay in the expansion store (apps/web/src/features/chat/state/chat-changed-files-expansion-store.ts:40); note that store's null-means-unset contract (lib/chat-changed-files-expansion-storage.ts:14-17) is what makes a settings default meaningful at all, so do not collapse null to false while wiring this. |
| `chat.changedFiles.previewFileLimit` | number | `3` | window | P6 | internal | yes | PLUMBING, internal. Already a defaulted parameter of selectChangedFilePreview (chat-changed-files-presentation.ts:86), so the seam exists; the caller at assistant-changed-files-section.tsx:158 just passes nothing today. |
| `chat.changedFiles.previewScopeLimit` | number | `4` | window | P6 | internal | yes | PLUMBING, internal. Already a defaulted parameter of summarizeChangedFileScopes (chat-changed-files-presentation.ts:61); caller at assistant-changed-files-section.tsx:148 passes nothing. |
| `chat.changedFiles.expansionMemoryLimit` | number | `200` | application | P6 | internal | yes | PLUMBING, internal. LRU bound on the persisted per-card expansion map; already a defaulted parameter of pruneChatChangedFilesExpansion (chat-changed-files-expansion-storage.ts:81), called from the store at chat-changed-files-expansion-store.ts:81. SCOPE: application — localStorage budget, not a repo's business. |
| `chat.assistantDeliveryMode` | enum(streaming\\ | buffered) | `'streaming'` | application | P6 | advanced ⟳ | yes | IT EXISTS, BUT NOTHING CONFIGURES IT. The behaviour is fully implemented — the branch is at provider-runtime-ingestion.ts:273 inside bufferAssistantDelta, with 'buffered' spilling through ProviderRuntimeBuffers instead of dispatching per delta — and the type is exported at :34. But `rg 'assistantDeliveryMode' apps -g '!tests'` returns only the class itself: the sole production construction site, apps/server/src/orchestration/engine.ts:383, never passes the option, so 'buffered' is reachable only from apps/server/src/orchestration/tests/provider-runtime-ingestion.test.ts:42,:58. PLUMBING: thread the resolved setting into the engine.ts:383 options object. requiresRestart: true because the mode is captured into a readonly field in the constructor (:72) and the ingestion instance lives for the process — honour a live change by rebuilding the ingestion or by reading the setting per-delta, not by pretending the field updates. SCOPE: application — server-side orchestration behaviour, and it is a whole-process object, not a per-window one. |
| `providers.instances` | object[] | `[]` | machine | P7 | user 🔒 | yes | SCOPE: machine, NOT window — the strongest case in this whole group. Each entry carries `binaryPath` (settings.ts:57, default '') and `environment` (settings.ts:59, default []), and both reach process spawn: apps/server/src/provider/provider-adapter-registry.ts:310-320 resolves the env and passes binaryPath straight into driver.create, and apps/server/src/provider/drivers/codex.ts:49-53 turns binaryPath into the PLATFORM*CODEX_BINARY env var of the spawned process. A checked-in .platform/settings.json is arbitrary code execution on repo open. machine rather than application because binaryPath and environment are per-machine facts that must never sync (plan's own machine definition: 'paths, binaries'). sensitive: true — environment values are where API tokens land; per the plan's Secrets section the \_values* must move to ~/.platform/secrets.json (mode 0600) before the first settings file is written, leaving only the variable name and a presence marker in the document. Main's wire redaction (apps/server/src/settings/utils/redaction.ts) is adopted as-is: redactSettings masks every non-empty value as REDACTED_SETTINGS_VALUE (packages/contracts/src/settings.ts:25), restoreRedactedSecrets puts stored values back matched by instance id AND variable name, and both routes are covered (apps/server/src/settings/routes.ts:19-22). NOTE: the document key on disk today is `providerInstances`, not `providers.instances` — this is a rename, and greenfield rules say do it in one pass with no alias. Also: launchArgs (settings.ts:58) is stored, validated and round-tripped but read by NOTHING in production (only tests and the empty default in apps/web/src/features/settings/utils/provider-rows.ts:46) — wire it into driver.create or delete it, per the plan's Phase 7 note. Finally, the env editor is NOT built yet: apps/web/src/features/settings/components/provider-row.tsx renders only an enable Switch, so the editor itself is NEW FEATURE work in Phase 7. Rows now come from the running catalog joined against saved entries (apps/web/src/features/settings/utils/provider-rows.ts:16-30, landed at fda1523), so the section no longer shows 'none configured' while two providers run. |
| `models.hidden` | object[] | `[]` | application | P7 | user | yes | NEW FEATURE on the consumer side — the key is dead in BOTH directions today, exactly as the plan says. Producer: apps/web/src/features/settings/utils/model-rows.ts:14-25 derives rows only from the prefs themselves, so on a fresh install the Models section can never populate. Consumer: providerModelOptionGroups (apps/web/src/features/chat/lib/provider-model-options.ts:75-90) takes only the provider snapshots and never reads ModelPreferences — verified by grep, the only non-test references to models.hidden/order in apps/web are the settings feature itself and the log counter at apps/web/src/features/settings/api.ts:43. Two edits, per Phase 7: source rows from the /providers catalog snapshot joined against the sparse prefs, and apply hidden/order when building the picker list. SCOPE: application, not window — a hidden/ordered ref names a providerInstanceId (settings.ts:73-76), so a workspace file could steer which credentialed provider account a turn bills to by hiding the others. It does not reach spawn, so the hard rule does not force this, but the influence is real; call it a deliberate judgement, not an oversight. |
| `models.order` | object[] | `[]` | application | P7 | user | yes | NEW FEATURE on the consumer side, same as models.hidden — same two dead ends (model-rows.ts:17-19 for the producer, provider-model-options.ts:75 for the consumer). The schema is already correct: both lists are sparse and name only the models the user has an opinion about (settings.ts:85-88), with everything else visible after, in provider order. Uniqueness within a list is enforced by hasUniqueModelRefs (settings.ts:70) keyed on modelRefKey (settings.ts:148). SCOPE: application, for the same billing/account-steering reason as models.hidden — a workspace-authored `order` puts a model at the head of the picker, which is where a default selection comes from. |
| `keybindings.overrides` | record of command IDs to nullable strings | `{}` | application | P7 | user | yes | The flat on-disk key is `keybindings.overrides`. A value is one hotkey or two separated by one space, with a maximum length of 64 characters. `null` unbinds a command; an absent command keeps its defaults. `merge: 'record'` combines overrides by command. The contract validates shape; the Platform keymap validates stroke grammar and reports shadowed bindings. The settings recorder, menus, and command palette read the shared resolved table. |

### Notes

- SCOPE VERDICTS ASKED FOR, stated plainly. chat.defaultRuntimeMode = application: it selects the provider session's permission posture and is read into the adapter session config (codex.ts:333, claude.ts:396), so a cloned repo must never be able to set 'full-access'. providers.instances = machine: binaryPath and environment reach Bun.spawn via provider-adapter-registry.ts:310-320 and drivers/codex.ts:49-53, and both are per-machine facts (paths, binaries) that must never sync. Neither can be window under the plan's standing rule. I also pulled chat.defaultInteractionMode up to application on the same permission axis even though the plan does not name it — plan vs default decides whether the agent acts before approval, and it rides the same adapter session config.
- DEFAULT-VERIFICATION METHOD: every `verified: true` row above was read at fda1523 (git rev-parse confirmed) by opening the defining file, not by trusting any prior inventory. Nothing in this group came back missing, so there is no `verified: false` row.
- THE ONE TRAP AN IMPLEMENTER WILL HIT: chat.attachments.maxPerMessage and chat.attachments.maxBytes are consumed inside valibot schemas built at module import (packages/contracts/src/chat-model.ts:51, :69, :81, :86) and are re-derived a second time at module scope on the client (MAX_CHAT_ATTACHMENT_ENCODED_BYTES, apps/web/src/features/chat/lib/chat-input-attachment-limits.ts:42). A settings value that only moves the browser check while the wire schema stays frozen at 8/10MB is worse than no setting — it turns a clean client-side refusal into a mid-turn server rejection. Either mark both requiresRestart (what I did) or rebuild the schema per request; do not ship the half.
- chat.assistantDeliveryMode is the odd row: the behaviour is complete and tested but has no production configuration path at all. engine.ts:383 is the only non-test construction of ProviderRuntimeIngestion and it never passes the option, so 'buffered' is currently reachable only from tests. I registered it because the task asked whether it exists — it does — but treat it as plumbing an existing dead branch, not as a new feature, and note the mode is captured into a readonly constructor field so live updates need the instance rebuilt.
- models.hidden and models.order are dead in BOTH directions at fda1523, confirmed by reading both ends: model-rows.ts:14-25 derives rows only from the prefs (so the section can never populate on a fresh install) and provider-model-options.ts:75-90 takes only provider snapshots (so the picker ignores them). Phase 7 is real feature work on both ends, not wiring. Same for the provider env editor: provider-row.tsx renders an enable Switch and nothing else, so the env UI the redaction work protects does not exist yet.
- launchArgs (packages/contracts/src/settings.ts:58) is stored, validated, uniquely-defaulted and round-tripped — and read by nothing. `rg launchArgs apps packages -g '!tests'` returns the schema line and the empty default in provider-rows.ts:46, full stop. I did not give it its own registry id because it is a field of providers.instances, but the Phase 7 decision (wire it into driver.create or delete it) has to be made before the provider editor ships, or the UI will offer a field that does nothing.
- The original `providerInstances` and `keybindings` document sections have been replaced by the flat registry keys `providers.instances` and `keybindings.overrides`. The current schema is generated from `packages/contracts/src/settings/keys.ts`.
- DELIBERATELY EXCLUDED, with pointers so nothing is lost. (a) CHAT_IMAGE_MIME_ALLOWLIST (chat-input-attachment-limits.ts:16) — not a preference: the list is dictated by what the Claude SDK accepts and the file says 'Do not widen'. Making it settable creates a knob whose only effect is a hard-failed turn. (b) QUALITY_LADDER and FALLBACK_SCALE_STEPS (image-compression.ts:27, :30) — module-private encoder tuning with no seam; register only if someone wants to tune compression from JSON. (c) The chat cache/engineering constant block: apps/web/src/features/chat/state/chat-cache-constants.ts:1-21 (12 caps, e.g. CHAT_MESSAGE_CACHE_LIMIT 2000, CHAT_PROJECTION_CACHE_THREAD_LIMIT 200, CHAT_PROJECTION_CACHE_PERSIST_MS 500), plus timeline-scroll-anchoring.ts:12-32, stream-reconnect.ts:6, chat-proposed-plan.ts:5-7, user-message-collapse.ts:5-6, markdown-highlight-cache.ts:11-12, orchestration-rpc-client.ts:29-34. These are all chat.\* and all belong in the plan's ~65 `internal` constants bucket, but they are a coherent separate sub-batch and were not in this section's brief — flagging them here rather than silently dropping them.
- chat.diff.defaultScope has exactly one expressible value today. The scope schema is a variant (thread-diff-scope-storage.ts:19-26) whose 'turn' arm requires a concrete turnId, and the comment at :14-18 explains that the branch-diff arm was deliberately left out because nothing on the client renders it. So the enum has one member. Register it as an enum anyway so adding the branch arm later is a value change, not a type change.

## workbench

| Key | Type | Default | Scope | Phase | Vis | OK | Wiring |
| ------------------------------------------ | ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ------------- | ---------------------------------------------------------- | ---------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `workbench.colorTheme` | enum(dark\\ | light\\ | system) | `'system'` | window | P3 | user | yes | PLUMBING. `ThemeProvider` is mounted with no props at apps/web/src/main.tsx:57, so `defaultTheme = 'system'` (:75) and `storageKey = 'theme'` (:76) are the live values. Read synchronously from localStorage by `storedTheme()` (:66-71) inside the `useState` initializer (:80-83); `applyResolvedTheme` (:54-64) writes the `light`/`dark` class on documentElement. Delete the `theme` localStorage key and the cross-tab `storage` listener (:100-117, :136-142) — the SSE push channel replaces it, which is exactly why the plan says push is not optional. Rewire `workspace.setDarkTheme` / `setLightTheme` / `setSystemTheme` (apps/web/src/keymap/commands.ts:367-377) onto `setSetting`; the command context loses `setTheme`. BOOT-CRITICAL. |
| `workbench.editor.colorTheme.dark` | string (theme id; validated by editorThemeExists) | `'dark-plus'` | window | P3 | user | yes | PLUMBING. `DEFAULT_DEFINITION_BY_COLOR_MODE.dark = requireVscodeThemeDefinition('dark-plus')` (:46-49). Persisted under `platform.editor-color-theme.v1` (:30) as `{selection:{dark,light},version:1}` (:385-396); read synchronously and memoized by `readSelectionByColorMode()` (:345-353) via `persistedThemeIdForColorMode` (:355-362). Valid ids are open — `editorThemeExists` (apps/web/src/features/editor/utils/theme-catalog.ts:114-116) accepts built-ins plus every `VSCODE_THEMES` id, so the schema must be a validated string, not a closed enum. The `theme ` palette mode (apps/web/src/components/command-palette/command-palette-utils.ts) is the second writer and must route through `setSetting`. Keep the in-memory hover-preview (:67) OUT of the settings write path — only commit writes. BOOT-CRITICAL. |
| `workbench.editor.colorTheme.light` | string (theme id; validated by editorThemeExists) | `'light-plus'` | window | P3 | user | yes | PLUMBING. Same store as the dark key; `light: requireVscodeThemeDefinition('light-plus')` (:48). Two keys rather than one because the store keys selection by `EditorColorMode` and the active mode follows the app theme (`activeEditorColorMode`, :64). BOOT-CRITICAL. |
| `workbench.surface.opacity` | number (percent, 0-100) | `80% (`--surface-opacity: 80%`)` | window | P3 | user | yes | NEW PLUMBING — no JS reads or writes this today; `rg` for `--surface-opacity` outside CSS returns only two explanatory comments (apps/web/src/components/workspace/file-tree/components/tree-pane.tsx:460, apps/web/src/features/chat-mode/components/session-row.tsx:58). It is a bare `:root` declaration and is deliberately NOT redefined in `.dark` (see the comment at globals.css:261-264), so a single write on documentElement covers both themes. Every surface token derives from it via `color-mix` (:104,:107,:110,:117,:120). Needs a new applier: write `documentElement.style.setProperty('--surface-opacity', ...)` at module scope in main.tsx beside `applyNativeVibrancy` (main.tsx:24). BOOT-CRITICAL. |
| `workbench.surface.contentOpacity` | number (percent, 0-100) | `95% (`--content-opacity: 95%`)` | window | P3 | user | yes | NEW PLUMBING. NAME TRAP: the custom property is `--content-opacity`, NOT `--surface-content-opacity` — the plan's "four `--surface-*` custom properties" phrasing is wrong for this one. Second trap: the declaring comment claims content wells are "editor, terminal", but the only consumer in the whole stylesheet is `[data-workbench] --terminal-background` (globals.css:406-412). The editor does not read it. Either wire the editor well to it in the same pass or narrow the description to the terminal — do not ship a slider that claims to affect the editor. BOOT-CRITICAL (readTerminalTheme snapshots computed CSS vars at terminal construction). |
| `workbench.surface.blur` | number (px) | `12px (`--surface-blur: 12px`)` | window | P3 | user | yes | NEW PLUMBING. Consumed by the `backdrop-material` utility (:426-427), `surface-vibrancy` (:454-457, through the `--surface-vibrancy-blur` fallback) and the card material at :595-596. CLAMP THE SCHEMA (suggest 0-40px): at `window` scope a cloned repo's `.platform/settings.json` can set this, and an unbounded backdrop-filter blur is a real GPU cost, not just an ugly look. Clamping in the valibot schema is the honest fix; do not reach for `machine` scope, which the plan reserves for spawn/exec/env/keymap and machine paths. BOOT-CRITICAL. |
| `workbench.surface.saturation` | number (percent) | `160% (`--surface-saturation: 160%`)` | window | P3 | advanced | yes | NEW PLUMBING. Same appliers and same consumers as `workbench.surface.blur` (globals.css:426-427, :454-457, :595-596). `advanced` rather than `user`: it is the one of the four with no intuitive user-facing meaning. BOOT-CRITICAL. |
| `workbench.wallpaper.enabled` | boolean | `true (stored inverted today as `wallpaperHidden: false`)` | window | P3 | user | yes | PLUMBING, WITH A POLARITY FLIP. The cached field is `wallpaperHidden`, defaulting `false` at both apps/web/src/lib/workspace-cache.ts:386-390 and :559; the registry key is stated positively, so the adapter inverts once and `wallpaperHidden` disappears from `CachedWorkspaceState` (:258), from the store (apps/web/src/features/editor/state/editor-workspace-state.tsx:97,:114), from the persistence writer (apps/web/src/lib/workspace-cache.ts:279-281, apps/web/src/hooks/use-workspace-cache-persistence.ts:186-188) and from the localStorage key `platform.workspace-state.v16.wallpaperHidden` (:59). Rewire `workspace.toggleWallpaper` (apps/web/src/keymap/commands.ts:397-399) onto `setSetting`; the command context loses `setWallpaperHidden`. Consumer is apps/web/src/features/workbench/components/wallpaper.tsx:13-26, which both unmounts the media (unmounting is load-bearing — CSS hiding leaves the video decoding, :22) and toggles `data-wallpaper-hidden` on documentElement, which globals.css:447-449 uses to drop `--surface-wallpaper` from every popover. BOOT-CRITICAL. |
| `workbench.wallpaper.animated` | boolean | `true (proposed — no literal default exists in code)` | machine | P3 | user | **?** | NEW FEATURE — there is no animated/still setting anywhere at fda1523, so no default could be verified. Today the video layer is gated on exactly two things and neither is user-authored: `prefersReducedMotion()` (web-wallpaper.tsx:37, apps/web/src/features/workbench/utils/wallpaper.ts:21-25) and whether `GET /wallpaper/info` reports a `video/*` content type (web-wallpaper.tsx:89-98, apps/server/src/wallpaper/routes.ts `/wallpaper/info`). `true` is the value that preserves current behaviour. `machine` scope, not `window`: a full-screen wallpaper video is a permanent decode loop and the single largest source of idle GPU load in the workbench (apps/web/src/features/workbench/hooks/use-wallpaper-playback.ts:1-8), so it must never be settable by a cloned repo, and it is per-machine by nature. New wiring: pass the resolved value into `WebWallpaper` and skip `setVideoSrc` when false; reduced-motion stays an unconditional override. |
| `workbench.wallpaper.source` | string (path or '' = follow the host desktop wallpaper) | `'' (proposed — no such key exists; the only literal is the bundled fallback '/workbench/wallpaper.png')` | machine | P3 | advanced | **?** | NEW FEATURE — no wallpaper-source setting exists at fda1523, so no default could be verified. Today the source is fixed: `apps/server/src/wallpaper/service.ts` reads the host's current macOS desktop wallpaper (returns null off darwin), the routes 404 on failure, and the client falls back to the bundled `WALLPAPER_URL = '/workbench/wallpaper.png'` on `<img onError>` (apps/web/src/features/workbench/components/web-wallpaper.tsx:64). MUST BE `machine`, not `window` — this is a filesystem path the SERVER opens and then serves over HTTP (`readDesktopWallpaperMediaFromPath`), so a cloned repo's workspace file setting it would be an arbitrary-file-disclosure vector. This is the same class of risk as `providers.instances.binaryPath` even though it is not a spawn. Ships with a `/wallpaper/vibrancy` regeneration story or an explicit note that `--surface-wallpaper` (globals.css:101) keeps pointing at the pre-blurred bundled asset and will not match a custom source. |
| `workbench.uiMode` | enum(chat\\ | workbench) | `'workbench'` | window | P3 | user | yes | PLUMBING. `DEFAULT_WORKSPACE_UI_MODE: WorkspaceUiMode = 'workbench'` (ui-mode.ts:3); validated on read by `isWorkspaceUiMode` (:7-11) and persisted at `platform.workspace-state.v16.uiMode` (apps/web/src/lib/workspace-cache.ts:58, read at :381-385, written at :275-277). Delete that localStorage key and the store field (apps/web/src/features/editor/state/editor-workspace-state.tsx:96,:113). Rewire `workspace.toggleUiMode` (apps/web/src/keymap/commands.ts:391-394, which calls `toggledWorkspaceUiMode`) onto `setSetting`; the command context loses `setUiMode`. BOOT-CRITICAL — this key picks which whole layout renders, so a late value is a full-app layout flash, not a color flash. |
| `workbench.tree.density` | enum(compact\\ | default\\ | relaxed) | `'compact' (app); the package's own fallback is 'default'` | window | P3 | user | yes | PLUMBING PLUS A REAL PACKAGE GAP. Two different defaults exist and the registry must record the app one: `tree-pane.tsx:145` hardcodes `density: 'compact'`, while `resolveFileTreeDensity` falls back to `'default'` when the option is absent (packages/tree/src/utils/model/density.ts:31). Presets are `compact {24px, 0.8} \\                                                                                                                                                                                                                                                                                                                                                                                                                        | default {30px, 1} \\ | relaxed {36px, 1.2}`(density.ts:10-14);`FileTreeDensity`also admits a raw number (density.ts:3) — do NOT expose that in the registry, keep the key a closed keyword enum. GAP:`density`is CONSTRUCTOR-ONLY.`useFileTree`builds the model once via`useState(() => new FileTree(options))`and syncs only`icons`and`gitStatus`(packages/tree/src/hooks/useFileTree.ts:21-42);`#density`is resolved in the constructor (packages/tree/src/utils/render/FileTree.ts:231) and written to the host as`--trees-item-height`/`--trees-density-override`(:718-727). Changing this key live therefore needs either a new`FileTree.setDensity()`that re-resolves and re-applies those two host vars, or a`key={density}`remount of the tree. Not`requiresRestart`, but not a passthrough either — budget it. |
| `workbench.chatMode.sessionRailVisible` | boolean | `true (`sessionRailOpen: true`)` | window | P6 | advanced | yes | PLUMBING. `createDefaultChatModePanels()` (panels.ts:33-39) is the sole default source, consumed by apps/web/src/lib/workspace-cache.ts:369-373 and :554 and persisted at `platform.workspace-state.v16.chatModePanels` (:54). Writers: `setChatModeSessionRailOpen` (panels.ts:69-76) and the store's `setChatModePanels` (apps/web/src/features/editor/state/editor-workspace-state.tsx:105). This is a settings/session-state boundary call: the setting is the STARTUP visibility, and the live toggle must either write through to it or stay separate — pick one, because a rail the user collapses that silently rewrites settings.json on every toggle is the file-churn failure the plan already rejects for window bounds. |
| `workbench.chatMode.toolPaneVisible` | boolean | `true (`toolPaneOpen: true`)` | window | P6 | advanced | yes | PLUMBING, same store and same startup-vs-live-toggle question as `sessionRailVisible`. Note `toggleChatModeToolTab` (panels.ts:48-57) flips this flag as a side effect of clicking the already-active tab, so it changes far more often than a user would expect a setting to; strongly prefer keeping the live pane state in the workspace cache and letting this key seed it. |
| `workbench.chatMode.defaultToolTab` | enum(editor\\ | files\\ | git\\ | logs\\ | problems\\ | search\\ | terminal) | `'git'` | window | P6 | advanced | yes | PLUMBING. Default `activeToolTab: 'git'` (panels.ts:35); the value space is `CHAT_MODE_TOOL_TABS` (:16-24, rail order git,files,editor,search,terminal,problems,logs — note the rail order is NOT the type's declaration order at :1-8) and is guarded by `isChatModeToolTab` (:41-45). Seeds `createDefaultChatModePanels`; the live selection stays in the workspace cache. |
| `workbench.startup.restoreWorkspace` | boolean | `true (proposed — restore is unconditional; no default exists)` | application | P6 | user | **?** | NEW FEATURE — there is no restore-on-startup key at fda1523, so no default could be verified. Today restoration always happens, through two paths with no gate: the cached `rootFolder` (apps/web/src/lib/workspace-cache.ts:353-357, seeded into the store at apps/web/src/features/editor/state/editor-workspace-state.tsx:96), and, when that is missing, an automatic fall back to the file server's newest recent directory (use-restore-recent-workspace-root.ts:24-35). Both need the gate, or 'do not restore' still reopens your last project one tick later. `application` scope: it is app-wide startup behaviour decided before any workspace exists, so a workspace file could not meaningfully own it. |
| `workbench.startup.restoreEditors` | boolean | `true (proposed — restore is unconditional; no default exists)` | application | P6 | user | **?** | NEW FEATURE — no key exists at fda1523. `EditorStateProvider` calls `readWorkspaceCache()` in a `useState` initializer (editor-state-provider.tsx:22) and hands the whole slice to `createEditorWorkspaceStore` (:24), which restores `editorTabs`, `activeEditorTabId`, `editorHistory`, `recentlyClosedEditorPaths` and `scrollPositionByPath` with no gate (apps/web/src/lib/workspace-cache.ts:484-503, :218-223). Gating this must skip the slice restore only — the root folder, layout and panel sizes are separate keys. Separate from `restoreWorkspace` because 'reopen my project, but with no tabs' is a real preference. |
| `workbench.startup.rememberedProjectLimit` | number | `8 (`WORKSPACE_SLICE_LIMIT = 8`)` | application | P6 | internal | yes | PLUMBING, INTERNAL. `WORKSPACE_SLICE_LIMIT = 8` (workspace-cache.ts:51) bounds how many projects keep their tabs, and its own comment says the ceiling exists to bound localStorage growth, not to protect any write. Enforced in three places that all read the same constant: `workspaceOrderFromCache` (:407-412) and `writeWorkspaceIndexCache` (:336-350), which also DELETES the slice and search-buffer storage of anything that falls off the end. Registering it means lowering the number destroys cached tabs on the next write — surface that in the description, and keep it `internal` (JSON-only). Note the constant is exported and asserted in apps/web/src/lib/tests/workspace-cache.test.ts:182. |

### Notes

- BOOT-CRITICAL LIST (the exact set for the plan's `platform.settings-boot-mirror.v1` section), verified at fda1523. Unconditional: (1) `workbench.colorTheme` — localStorage read at theme-provider.tsx:66-71 inside the useState initializer at :80, applies the light/dark class via applyResolvedTheme (:54-64). (2)+(3) `workbench.editor.colorTheme.dark` / `.light` — synchronous localStorage read in readSelectionByColorMode/readPersistedSelection (editor-color-theme-store.ts:345-383), feeding the shiki plugin's SYNCHRONOUS registration lookup (registrationByIdSync, :56, read at :184 and :308) at worker-session creation. (4)-(7) the four material knobs `--surface-opacity` / `--content-opacity` / `--surface-blur` / `--surface-saturation` (globals.css:90,93,94,95) — no JS reads them today, but they become boot-critical the instant settings can override them, because readTerminalTheme snapshots computed CSS variables at terminal construction. (8) `workbench.uiMode` — readWorkspaceCache() in the useState initializer at editor-state-provider.tsx:22 picks chat vs workbench; a late value is a whole-layout flash. (9) `workbench.wallpaper.enabled` — same synchronous read, and it drives the `data-wallpaper-hidden` attribute that globals.css:447-449 uses to drop `--surface-wallpaper` from every popover.
- CONDITIONALLY BOOT-CRITICAL: the three `workbench.chatMode.*` keys ride the exact same synchronous `readWorkspaceCache()` call (workspace-cache.ts:369-373) as uiMode. If the mirror carries `workbench.uiMode` but not these, a cold boot into chat mode paints the rail and tool pane in the wrong state for one frame. Either mirror all four together or mirror none — mirroring uiMode alone is the worst of the three options.
- NAMING TRAP: the plan says 'the four `--surface-*` custom properties'. Only three are named that way. The content-well knob is `--content-opacity` (globals.css:93), with no `surface-` prefix. Code that writes `--surface-contentOpacity` or `--surface-content-opacity` will silently do nothing.
- CONSUMER TRAP: `--content-opacity` is declared with a comment claiming it serves 'content wells (editor, terminal)', but its only consumer in the entire stylesheet is `[data-workbench] --terminal-background` (globals.css:406-412). The editor never reads it. Ship the key with an honest description or wire the editor in the same pass.
- DARK-MODE TRAP (in our favour): none of the four material knobs is redefined in `.dark` — that is deliberate and documented at globals.css:261-264 ('Re-adding e.g. `--card: ...` here would override the formula and make dark mode ignore --surface-opacity'). One write on documentElement therefore covers both themes. Do not add per-theme variants of these keys.
- POLARITY: the wallpaper is stored INVERTED today (`wallpaperHidden: false`, workspace-cache.ts:389/:559). The registry key is stated positively as `workbench.wallpaper.enabled` (matching the plan's own naming at plan line 298), so exactly one inversion belongs in the adapter. Two inversions is the classic way this ships backwards.
- TWO DEFAULTS EXIST FOR TREE DENSITY and only one is the app's. tree-pane.tsx:145 hardcodes `'compact'`; packages/tree's `resolveFileTreeDensity` falls back to `'default'` (density.ts:31). Register `'compact'`. Registering `'default'` would visibly change every user's file tree on the day the key ships.
- SCOPE JUDGEMENT — `workbench.wallpaper.source` is `machine`, not `window`, and this is the one scope call in this group that is a security decision rather than taste: the value is a filesystem path the SERVER opens and then serves over HTTP (apps/server/src/wallpaper/service.ts + routes.ts). A cloned repo's `.platform/settings.json` setting it would be arbitrary-file disclosure. Same class as `providers.instances.binaryPath` even though nothing is spawned. `workbench.wallpaper.animated` is `machine` for a different reason: a permanent full-screen video decode is the largest source of idle GPU load in the workbench (use-wallpaper-playback.ts:1-8) and is per-machine by nature.
- SCOPE JUDGEMENT — everything else here is `window` (theme, editor themes, the four material knobs, uiMode, tree density, chat-mode panels). None of them reaches spawn, exec, env, or the keymap, so the plan's hard rule does not bite. The residual risk is a hostile workspace file setting `workbench.surface.blur` to something enormous; the honest fix is a clamped valibot schema (suggest 0-40px), not a scope escalation — escalating would also break the legitimate per-project theme case that VS Code has always allowed.
- SCOPE JUDGEMENT — the three `workbench.startup.*` keys are `application`: they are decided before any workspace exists, so a workspace-scoped value could never be read in time to matter.
- SETTINGS-VS-SESSION-STATE BOUNDARY. `workbench.chatMode.sessionRailVisible` / `toolPaneVisible` are toggled by ordinary UI clicks — `toggleChatModeToolTab` (panels.ts:48-57) flips `toolPaneOpen` as a side effect of clicking the already-active tab. If the live toggle writes through to settings.json, the file churns on every click, which is precisely the failure the plan rejects for restored window bounds ('it would churn the file on every window drag'). Recommendation: these keys seed startup state; the live state stays in the workspace cache. Decide this before Phase 6, not during.
- DELIBERATELY EXCLUDED — chat-mode pane SIZES: `SESSION_RAIL_DEFAULT_SIZE = 300` / `TOOL_PANE_DEFAULT_SIZE = 420` and their min/max (panels.ts:26-31). These are layout geometry the user drags, persisted through `platform.resizable-layout.chat-mode` (which the plan already names as a reset target). Same category as restored window bounds — not settings.
- DELIBERATELY EXCLUDED — `editor.diff.viewMode`: it lives in the same `workspace-cache.ts` blob and the same Phase 3 batch, but it is an `editor.*` key and belongs to that section. Flagging it only so the two sections do not both claim it.
- ADJACENT CANDIDATES NOT REGISTERED, for whoever owns the files/tree section: `flattenEmptyDirectories: true` and `initialExpansion: 'closed'` are hardcoded beside density at tree-pane.tsx:146,149 and are the two most obvious `workbench.tree.*` follow-ups. Left out because this section's brief scoped the tree to density.
- SECRETS: nothing in `workbench.*` is sensitive. No key here carries a credential, and `workbench.wallpaper.source` is the only one that carries a path — it belongs in settings.json, not secrets.json, since it is not a credential.
- LOCALSTORAGE KEYS TO DELETE IN PHASE 3, all verified present at fda1523: `theme` (theme-provider.tsx:76, plus the cross-tab `storage` listener at :100-117/:136-142 which the SSE channel replaces), `platform.editor-color-theme.v1` (editor-color-theme-store.ts:30), and three entries under the `platform.workspace-state.v16` prefix — `.uiMode`, `.wallpaperHidden`, `.chatModePanels` (workspace-cache.ts:54,58,59). The four material knobs have no storage today.
- COMMAND WRITERS IN THIS GROUP, verified line numbers at fda1523 (the plan's own table cites :358-369/:384/:388, which are now off by ~9 lines): `workspace.setDarkTheme` / `setLightTheme` / `setSystemTheme` at apps/web/src/keymap/commands.ts:367-377, `workspace.toggleUiMode` at :391-394, `workspace.toggleWallpaper` at :397-399. `workspace.selectColorMode` / `selectColorTheme` (:359-366) only open the palette and need no rewiring themselves — their palette modes are the writers.

## terminal.integrated

| Key | Type | Default | Scope | Phase | Vis | OK | Wiring |
| ------------------------------------------ | ------------ | ------------------------------------------------------------------------------------------------ | ------- | -------- | ------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `terminal.integrated.fontFamily` | string | `"JetBrains Mono Nerd Font", "JetBrains Mono Variable", ui-monospace, SFMono-Regular, monospace` | machine | P5 | user | yes | PLUMBING. Passed once into `new Terminal({ fontFamily: DEFAULT_MONO_FONT_STACK })` at terminal-panel.tsx:369. LIVE-SETTABLE: ghostty-web@0.4.0's options Proxy handles `fontFamily` in handleOptionChange -> renderer.setFontFamily() + handleFontChange() (re-measures metrics, resizes canvas, forces full re-render). So this key can be applied via `terminal.options.fontFamily = value` with no remount. Read it in the panel and add an effect that assigns terminal.options.fontFamily; do NOT add it to the mountTerminal effect deps or it becomes a scrollback-destroying remount. |
| `terminal.integrated.fontSize` | number | `12` | window | P5 | user | yes | PLUMBING. LIVE-SETTABLE: handleOptionChange handles `fontSize` -> renderer.setFontSize() + handleFontChange(). Assign `terminal.options.fontSize`; the FitAddon must re-fit afterwards (handleFontChange resizes the canvas to metrics\*cols/rows but does not recompute cols/rows from the host box) — call fitAddonRef.current?.fit() after the assignment or the terminal keeps the old grid at the new glyph size. |
| `terminal.integrated.scrollback` | number | `10000` | window | P5 | user | yes | PLUMBING but NOT live-settable. `scrollback` is absent from ghostty-web@0.4.0's handleOptionChange switch, so `terminal.options.scrollback = n` after open() silently does nothing (the Proxy stores the value, the WASM terminal keeps the construction-time scrollbackLimit). Applying this key requires a terminal remount — i.e. the same teardown path that loses buffered output — or an upstream/patch change to plumb scrollbackLimit into the live wasm terminal. This directly contradicts Phase 5's 'Terminal changes mutate terminal.options.\* rather than re-creating the Terminal' for this key. |
| `terminal.integrated.smoothScrollDuration` | number | `80` | window | P5 | advanced | yes | PLUMBING but NOT live-settable — absent from handleOptionChange, same remount problem as scrollback. NAMING: the group brief asks for a boolean `smoothScrolling`; the code has a millisecond duration, and ghostty-web's own fallback when unset is 100ms (bundle: `smoothScrollDuration: A.smoothScrollDuration ?? 100`). Registering a boolean would be a lossy reshape (true->80, false->0) and would throw away the only value the code actually carries. Registered as the number; add a boolean alias only if VS Code name parity is wanted, and never register both. |
| `terminal.integrated.cursorBlinking` | boolean | `true` | window | P5 | user | yes | PLUMBING with a trap. There is no single cursorBlink preference today: two frozen constant pairs exist, FOCUSED_TERMINAL_CURSOR (:44-47, blink true) and UNFOCUSED_TERMINAL_CURSOR (:49-52, blink false), and applyTerminalCursorOptions (:377-382) swaps between them on focus/blur/focus-area. The user-facing meaning of this key is 'blink while focused', so it must replace FOCUSED_TERMINAL_CURSOR.cursorBlink only; the unfocused terminal must stay non-blinking regardless. The Terminal is constructed with the UNFOCUSED value (:367), so the constructor arg is not the default a user would recognise. LIVE-SETTABLE: cursorBlink is in handleOptionChange -> renderer.setCursorBlink(). |
| `terminal.integrated.cursorStyle` | enum(block\\ | underline\\ | bar\\ | outline) | `block` | window | P5 | user | yes | PLUMBING. Replaces FOCUSED_TERMINAL_CURSOR.cursorStyle only. LIVE-SETTABLE via handleOptionChange -> renderer.setCursorStyle(). SCHEMA TRAP: 'outline' exists only because of the local dependency patch (patches/ghostty-web@0.4.0.patch adds the outline branch to renderCursor); published ghostty-web types are 'block'\\ | 'underline'\\ | 'bar', which is why patchedTerminalCursorStyle casts at :384-387. The valibot enum must include 'outline' and the descriptor comment must name the patch, or an upstream bump silently makes a stored value unrenderable. |
| `terminal.integrated.cursorStyleInactive` | enum(block\\ | underline\\ | bar\\ | outline) | `outline` | window | P5 | advanced | yes | PLUMBING. Replaces UNFOCUSED_TERMINAL_CURSOR.cursorStyle, which is also the value the Terminal is constructed with (:368) and the value restored on blur (:114) and on focus-area loss (:188-193). Same 'outline'-needs-the-patch schema note as cursorStyle. |
| `terminal.integrated.allowTransparency` | boolean | `true` | window | P5 | internal | yes | INTERNAL, register as searchable-but-hidden. Not live-settable (absent from handleOptionChange). This is load-bearing for the workbench material story: the panel paints `background: var(--terminal-background)` on the section (:200) and the canvas is expected to be transparent over it; patches/ghostty-web@0.4.0.patch adds isTransparentColor handling to the renderer specifically for this. Exposing it as a user toggle would let one click break the terminal's material. Register so it is discoverable and documented, keep visibility internal. |
| `terminal.integrated.defaultProfile` | string | ``                                                                                              | machine | P5       | user ⟳        | **?**  | NEW FEATURE (no default in code). MACHINE SCOPE IS MANDATORY, not a preference: this value is handed to `pty.spawn(command.shell, [], ...)` at service.ts:879 via trySpawnPty (:371-386), so it reaches process spawn and the plan's hard rule forbids `window`. A cloned repo's .platform/settings.json setting a shell would be arbitrary code execution on folder open. Today the shell is resolved by a candidate chain with no setting: posix `[env.SHELL, 'bash', 'sh']`, win32 `[env.SHELL, env.COMSPEC, 'powershell.exe', 'cmd.exe']` (:536-542), deduped by uniqueShells (:544-546), tried in order until one spawns (:353-369). `''` must mean 'keep that chain'; a non-empty value should be tried first and still fall back, or the user can lock themselves out of a terminal. Must also be added to SERVER_CONSUMED_SETTINGS so the registry scope test covers it.                                                                             |
| `terminal.integrated.shellArgs`            | string[]     | `[]`                                                                                             | machine | P5       | advanced ⟳    | yes    | PLUMBING, and the empty array is a real literal: the node-pty bridge calls `pty.spawn(command.shell, [], {...})` — args are hard-coded empty at :879. Threading a value needs a new `args` field on TerminalPtySpawnOptions (:32-38), on the bridge `start` command envelope (NodePtyBridge #sendCommand at :618), and through defaultTerminalPtyFactory (:556-570). MACHINE SCOPE MANDATORY — argv of a spawned process; `-c <payload>` is direct execution. SERVER_CONSUMED_SETTINGS.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `terminal.integrated.env`                  | object       | `{}`                                                                                             | machine | P5       | advanced 🔒 ⟳ | **?**  | NEW FEATURE (no overlay in code). Today terminalEnv returns `{...env, COLORTERM: env.COLORTERM ?? 'truecolor', TERM: 'xterm-256color'}` over the service's env (`env = process.env`, :76). A user overlay must be merged inside terminalEnv, and its precedence versus the forced TERM decided explicitly — as written TERM is assigned last and unconditionally, so an overlay merged before it can never set TERM, while an overlay merged after it can break the emulator. Note the pty name is ALSO hard-coded to 'xterm-256color' at :883 in the bridge script, a second independent source. MACHINE SCOPE MANDATORY (env reaches spawn; PATH/LD_PRELOAD/NODE_OPTIONS in a cloned repo's workspace file is code execution). SENSITIVE: same shape and same risk as providerInstances[].environment, so per the plan's Secrets section its values belong in ~/.platform/secrets.json — which makes this Phase 5 key depend on the Phase 2 secrets split. |
| `terminal.integrated.cwd`                  | string       |`` | machine | P5 | advanced ⟳ | **?** | NEW FEATURE (no setting; cwd is always the resolved workspace root). `''` means 'workspace root'. Reaches spawn — it is passed to TerminalSession (:143) then to ptyFactory (:376) then to pty.spawn (:881) — so MACHINE SCOPE MANDATORY per the hard rule. It also needs the same containment check the rest of the fs layer uses: an absolute or ../ value must resolve through WorkspacePaths (this.paths.resolve, :179-187) rather than being spawned raw, or this key becomes a workspace-escape primitive. SERVER_CONSUMED_SETTINGS. |
| `terminal.integrated.inheritEnv` | boolean | `true` | machine | P5 | advanced ⟳ | **?** | NEW FEATURE (no flag; inheritance is unconditional). The behaviour the `true` default describes is real and visible — the service defaults `env = process.env` (:76) and terminalEnv spreads it wholesale (:550) — but no boolean literal exists in code, hence verified:false. Turning it off means building a minimal env (PATH, HOME, SHELL, TERM, COLORTERM) instead of the spread. MACHINE SCOPE MANDATORY (decides the env of a spawned process). Register only if the maintainer wants it; it is the least-earning key in this group. |
| `terminal.integrated.persistentSessionTtl` | number | `600000` | machine | P5 | advanced ⟳ | yes | PLUMBING — already an injectable constructor option (`detachTtlMs` on TerminalServiceOptions, :48/:75/:80), so wiring is only 'read the setting where app.ts:62 builds TerminalService'. User-meaningful despite looking like a constant: it is how long a detached shell keeps running after the last socket closes before dispose({kill:true}) (:318-325). Machine scope because it governs how long a spawned process outlives its client, and because TerminalService is constructed once per server, not per window. |
| `terminal.integrated.replayBufferSize` | number | `262144` | machine | P5 | internal ⟳ | yes | NEW FEATURE (module const, not injectable — unlike detachTtlMs it is not on TerminalServiceOptions, so it needs threading to be settable at all). This is the byte cap on the output ring that appendOutput trims (:339-351) and attach() replays (:265-266). It is the real bound on how much history survives a client remount, so it is the server-side half of the scrollback story — see the notes on the theme-remount claim. Internal visibility: exposing raw bytes is worse than sizing it correctly once. |

### Notes

- THE PLAN'S THEME-REMOUNT CLAIM IS CONFIRMED AT fda1523. apps/web/src/features/terminal/terminal-panel.tsx:148-173 is the effect that calls mountTerminal (:152); its dependency array at :173 is literally `[resolvedTheme, rootPath, sessionId]`. `resolvedTheme` comes from useTheme() at :68. The cleanup (:160-172) runs unmountTerminal, which disposes the FitAddon and the Terminal and calls host.replaceChildren() (:286-294). The comment at :144-147 says this is deliberate: 'ghostty-web bakes the theme into its WASM terminal at construction and has no runtime theme API, so a live theme switch is applied by rebuilding the terminal'. So every light/dark switch does destroy the client-side terminal.
- BUT THE LOSS IS NARROWER THAN 'DESTROYS SCROLLBACK', AND THE PLAN SHOULD SAY WHICH. The pty is NOT killed: TerminalService keeps persistentSessions keyed by (rootPath, sessionId) and a reconnect hits the `existing` branch and re-attaches (apps/server/src/terminal/service.ts:135-140). On attach the server replays its buffered output (:265-266), but that buffer is capped at TERMINAL_REPLAY_BUFFER_BYTES = 256 KiB and older chunks are shifted off (:64, :339-351). Precise statement: a theme switch keeps the shell and its state, and truncates visible history from the client's 10,000-line scrollback down to the last ~256 KiB of output. Write that in Phase 5 instead of 'destroys scrollback', or the fix will be measured against the wrong claim.
- THE FIX THE PLAN PROPOSES DOES NOT WORK AT THE PINNED VERSION. Phase 5 says 'apply the palette through `terminal.options.theme` + `refresh()`'. Neither half exists in ghostty-web@0.4.0 as installed (patch applied). (a) The options object is a Proxy whose setter calls handleOptionChange, and the `theme` case is `console.warn('ghostty-web: theme changes after open() are not yet fully supported')` and nothing else — verified in node_modules/ghostty-web/dist/ghostty-web.js around the handleOptionChange switch (line ~2249). (b) There is no `refresh()` on Terminal anywhere in dist/index.d.ts. The only live theme path is the renderer-level `setTheme(theme: ITheme)` (dist/index.d.ts:70) reached through the public `terminal.renderer`, followed by a forced render — and even that is likely partial, because the WASM terminal is separately configured with palette colors parsed at construction (parseColorToHex feeds the wasm config), so a renderer-only setTheme can leave the wasm-side palette stale. Our own patch (patches/ghostty-web@0.4.0.patch) already edits the renderer for the outline cursor and transparent background but does not touch theme. Phase 5 must either (i) extend the patch to route theme into both renderer and wasm config, or (ii) drop the scrollback-preservation claim and keep the remount. Do not assert both, and do not budget (i) as a one-liner.
- PHASE 5's BROADER PREMISE IS ONLY HALF TRUE. 'Terminal changes mutate terminal.options.\* rather than re-creating the Terminal' holds for exactly the keys in handleOptionChange's switch: cursorBlink, cursorStyle, fontSize, fontFamily, cols, rows (and a no-op branch for disableStdin). It does NOT hold for scrollback or smoothScrollDuration — both are absent from the switch, so assigning them after open() stores a value that nothing reads. Those two keys need a remount or a patch, same as theme. fontSize additionally needs a fit() after assignment: handleFontChange resizes the canvas to the new metrics but never recomputes cols/rows from the host element.
- SCOPE, STATED EXPLICITLY AS ASKED: terminal.integrated.defaultProfile, .shellArgs, .env, .cwd and .inheritEnv are MACHINE, never window. Traced: TerminalSession.trySpawnPty (service.ts:371-386) -> ptyFactory -> NodePtyBridge (:606-619) -> `pty.spawn(command.shell, [], {cols, cwd, env, name, rows})` in the bridge script (:879-885). Shell is argv[0], shellArgs is argv, env is the process environment, cwd is the working directory — all four are process-spawn inputs, which is precisely the plan's hard rule ('any descriptor whose value reaches process spawn, exec, env, or the keymap is application or machine, never window'). A checked-in .platform/settings.json setting any of them is remote code execution on folder open. All five belong in SERVER_CONSUMED_SETTINGS so the registry test enforces it rather than a reviewer.
- REQUIRES-RESTART ORDERING PROBLEM. All five server keys are marked requiresRestart:true, but the plan puts the `requiresRestart` and readOnly-with-reason machinery in Phase 6 while the terminal batch is Phase 5. Either pull that machinery forward into Phase 5 or ship these keys in Phase 6. And 'restart' is the wrong word for what actually happens: because sessions are persistent server-side with a 10-minute detach TTL, a browser reload re-attaches the SAME pty (service.ts:135-140), so a changed shell/env/cwd survives a page refresh unchanged. The honest UI affordance is 'applies to new terminals' plus a one-click kill — the client already has the `dispose` message (:282-284) and the menu's Reset action (apps/web/src/features/terminal/utils/menu.ts:91).
- terminal.integrated.env AND THE SECRETS SPLIT. It is the same shape and the same risk as providerInstances[].environment, so it carries sensitive:true, which under the plan's Secrets resolution means its values live in ~/.platform/secrets.json and never in settings.json. That makes this Phase 5 key depend on Phase 2 work, and it means the redaction helpers in apps/server/src/settings/utils/redaction.ts must be generalised beyond provider instances — today they key on instance id + variable name.
- NOTHING TO MIGRATE OR DELETE FOR THIS GROUP. packages/contracts/src/settings.ts at fda1523 holds only providerInstances, models and keybindings — no terminal section. `grep -rn localStorage apps/web/src/features/terminal` is empty, and apps/web/src/features/settings has no terminal UI. Every key here is a fresh registration against a hard-coded constant, so unlike the Phase 3 appearance batch there is no localStorage key to delete in the same pass.
- DELIBERATELY EXCLUDED — terminal `theme`. terminal-panel.tsx:373 passes `theme: readTerminalTheme(root)`, which reads ~24 CSS custom properties off the document root (apps/web/src/features/terminal/terminal-theme.ts:3-26) with a hex fallback map (:28-34). The palette is derived from the app theme by construction; registering a terminal theme key would create a second source of truth for colors and duplicate the workbench.colorTheme story. It is also the exact value the remount above exists to apply.
- DELIBERATELY EXCLUDED — DEFAULT_COLS = 80 / DEFAULT_ROWS = 24 (service.ts:60-61). They are the pre-fit placeholder geometry for the spawn call; the client's FitAddon measures the host and sends a resize on socket open (terminal-panel.tsx:254, :314-319), so these are overwritten within the first frame and no user-visible behaviour depends on them. Registering them would be noise even at internal visibility.
- DELIBERATELY EXCLUDED AS KEYS, FLAGGED AS A WIRING TRAP — TERM and COLORTERM. terminalEnv (service.ts:548-554) forces TERM='xterm-256color' unconditionally and defaults COLORTERM to 'truecolor' only when unset, and the bridge separately hard-codes `name: "xterm-256color"` at :883. They are better handled as the precedence rule for terminal.integrated.env than as two more keys, but whoever implements the env overlay must decide explicitly where the merge lands relative to :552, and must not forget the second TERM source at :883.
- SCOPE JUDGEMENT CALL — fontFamily is machine, the other appearance keys are window. A font stack names fonts installed on this machine (the default stack leads with the bundled JetBrains Mono Nerd Font that loadDefaultNerdFont fetches at runtime), which puts it in the plan's machine bucket alongside paths and binaries; the rest are pure render numbers a workspace may plausibly set. This is the one arguable call in the group and it is safe to flip to window — it is not a security boundary, since none of the appearance keys reach spawn. If it is flipped, flip editor.fontFamily with it so the two batches stay consistent.
- MINOR — the constructor's cursor values are not the user-facing defaults. terminal-panel.tsx:367-368 constructs with the UNFOCUSED pair (blink false, style outline) because a freshly mounted terminal is not focused yet; the values a user would call 'the default' are the FOCUSED pair at :45-46. An implementer reading only createTerminal will register the wrong defaults for cursorBlinking and cursorStyle.

## files._ + search._

| Key | Type | Default | Scope | Phase | Vis | OK | Wiring |
| ----------------------------- | -------------- | --------------------------------------------------------------------------------------------------------------- | --------------- | --------------- | ---------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `files.autoSave` | enum(off\\ | afterDelay\\ | onFocusChange\\ | onWindowChange) | `'off'` | window | P6 | user | **?** | NEW FEATURE. Nothing in the codebase auto-saves. The editor writes only through explicit save paths into `fs.write` (apps/server/src/fs/service.ts:179). Building this needs: a dirty-document timer/blur observer in apps/web/src/features/editor/state/editor-document-state, coordination with the existing `recordAppSave`/`forgetAppSave` echo suppression (apps/server/src/fs/service.ts:197,203) so autosaved writes do not bounce back through the watcher, and a decision about autosave vs. the `baseVersion`/`expectedMtimeMs` conflict check (apps/server/src/fs/write.ts:33-40). Estimate as a feature, not as a key. |
| `files.autoSaveDelay` | number | `1000` | window | P6 | advanced | **?** | NEW FEATURE. Only meaningful once files.autoSave === 'afterDelay' exists. Also needs a readOnly-with-reason state (plan Phase 6 adds that machinery) so the row greys out when autoSave is not 'afterDelay'. 1000ms is a proposal, not an observed value. |
| `files.watcherEnabled` | boolean | `true` | machine | P6 | advanced ⟳ | yes | PLUMBING, but boot-critical. `enabled: options.watch ?? true` is read once in the FileSystemService constructor and stored on FileChangeHub as `private readonly watchEnabled` (apps/server/src/fs/watch.ts:52); `retainWatcher` short-circuits to a noop when false (watch.ts:94-96). Today it is set only from `FS_WATCH !== 'false'` (apps/server/src/index.ts:18) into `AppOptions.watch`. The registry must resolve this before `createApp`, i.e. it is one of the plan's boot-critical keys — it cannot be delivered over the settings SSE because nothing re-reads it. Machine scope, not window: it is a per-box capability knob and a workspace file must never be able to blind the file watcher. |
| `files.maxTextFileBytes` | number | `209_715_200` | machine | P6 | advanced ⟳ | yes | PLUMBING, boot-critical, and there are TWO env names to reconcile. `apps/server/src/index.ts:21` reads `FS_DEV_MAX_TEXT_FILE_BYTES` and passes it as `AppOptions.maxTextFileBytes`; when that is undefined the service falls back to `resolveMaxTextFileBytes()` which reads a _different_ variable, `MAX_TEXT_FILE_BYTES` (apps/server/src/fs/service.ts:71), clamped to 1..2_147_483_647 (service.ts:68,75) with a `fs.invalid_max_text_file_bytes` warning on bad input. The resolved value is then re-read by app.ts:60 to construct GitService, which has its own independent `DEFAULT_MAX_TEXT_FILE_BYTES = 209_715_200` (apps/server/src/git/service.ts:110) — two constants that happen to agree today. Registering this key should collapse both env names and both constants onto one descriptor. Stored as `private readonly` in both services, so a live change does nothing: requiresRestart. |
| `files.treeConcurrency` | number | `32` | machine | P6 | internal ⟳ | yes | PLUMBING, boot-critical. `options.treeConcurrency ?? DEFAULT_TREE_CONCURRENCY` at service.ts:113, stored `private readonly`, passed per call as `{ concurrency: this.treeConcurrency }` into readTree (service.ts:157). Env today: `FS_TREE_CONCURRENCY` (apps/server/src/index.ts:22). Pure I/O-parallelism tuning for the local disk — internal visibility, machine scope, JSON-only. |
| `files.exclude` | string[] | `['node_modules', '.git', '.evlog', '.playwright-mcp', 'logs', 'dist', 'build', '.next', '.cache', 'coverage']` | application | P6 | advanced ⟳ | yes | PLUMBING across four consumers, and the widest-blast-radius key in this group. `defaultIgnoredNames` is a module-level const consumed by (1) the file tree via the derived `treeIgnoredNames` — which drops 'node_modules' so the tree still shows it (path.ts:18, tree.ts:103); (2) the watcher's ignore globs, computed at module import time into `watcherIgnoredChildGlobs` (apps/server/src/fs/watch.ts:23-26); (3) `fd` argv as `--exclude <name>` (apps/server/src/fs/search.ts:700); (4) `rg` argv as four `--glob !…` patterns per name (search.ts:739-743 via `ignoredDirectoryGlobArgs`, search.ts:790). Because it is baked into every fd/rg invocation regardless of the request, it falls squarely under the plan's spawn rule → NOT window, despite VS Code scoping its `files.exclude` per-resource. The watcher glob list is import-time, so requiresRestart. The tree-vs-search divergence (node_modules visible in tree, invisible to search) is real behavior that a single key would flatten — either register two keys or keep the derivation. |
| `search.defaultMatchMode` | enum(literal\\ | regex\\ | fuzzy) | `'literal'` | window | P6 | user | yes | PLUMBING. Naturally per-workspace — it is already persisted per root path in localStorage under `platform.workspace-state.v16.search:<rootPath>` (apps/web/src/lib/workspace-cache.ts:44,73-75, schema at workspace-cache.ts:170). Three places agree on 'literal' today: the store's `emptySearchBuffer` (search-buffer-state.tsx:727), the query builder's `options.matchMode ?? 'literal'` (apps/web/src/features/search/search-buffer-query.ts:29), and the server route default (apps/server/src/fs/contracts.ts:59). The registry becomes the seed for a _new_ buffer; the live per-buffer value stays runtime state. Delete the `matchMode` field from `cachedSearchBufferStateSchema` in the same pass or the two stores will fight. |
| `search.caseSensitive` | boolean | `false` | window | P6 | user | yes | PLUMBING. Per-workspace by nature, already cached per root (workspace-cache.ts:165). Reaches `rg`/`fd` only as the presence/absence of `--ignore-case` (apps/server/src/fs/search.ts:736, 690) — a fixed flag on a fixed binary, no injectable payload. Server route default also 'false' (apps/server/src/fs/contracts.ts:53). Note there is no smart-case behavior anywhere; `--ignore-case` is applied whenever caseSensitive is false, so a `search.smartCase` key would be a new feature. |
| `search.wholeWord` | boolean | `false` | window | P6 | user | yes | PLUMBING. Per-workspace, cached per root (workspace-cache.ts:182). Becomes `--word-regexp` on the rg argv (apps/server/src/fs/search.ts:737); fixed flag, no payload. Route default 'false' (apps/server/src/fs/contracts.ts:63). |
| `search.include` | string[] | `'' (stored as `includeGlobText`; becomes [] after workspaceSearchGlobPatterns)` | window | P6 | advanced | yes | PLUMBING plus a representation change. Today the UI holds one free-text field (`includeGlobText: ''`) split by `workspaceSearchGlobPatterns` (packages/contracts/src/workspace-search-match.ts:30) only when the filter row is open (`filtersVisible ? … : []`, apps/web/src/features/search/search-buffer-query.ts:26). A registry key should be `string[]`; keep the text field as the editor surface. Each glob becomes one or two `--glob <pattern>` argv entries on rg (search.ts:744-746, `globArgs` at search.ts:781-786) and is also matched in the fallback/index providers. Per-workspace by nature — this is the key users most want in `.platform/settings.json`. See notes for the spawn-rule carve-out this requires. |
| `search.exclude` | string[] | `'' (stored as `excludeGlobText`; becomes [] after workspaceSearchGlobPatterns)` | window | P6 | advanced | yes | PLUMBING plus the same representation change as search.include. Emitted as `--glob !<pattern>` (apps/server/src/fs/search.ts:747-749 via `excludeGlobArgs`, search.ts:762-764). Note the separate `CONTENT_INDEX_RG_EXCLUDE_GLOB_LIMIT = 512` truncation (search.ts:95, 769-772) applies to index-derived globs, not user globs — user globs are unbounded today, which is worth a `maxLength` in the schema. Per-workspace by nature; see notes for the suppression risk that comes with window scope. |
| `search.useIgnoreFiles` | boolean | `true` | application | P6 | advanced | **?** | NEW FEATURE. There is no toggle: .gitignore is always honored, on all three provider paths (rg's own default, the workspace index's `gitIgnored` flag at search.ts:866, and the fallback scanner via `isIgnoredSearchPath`). Adding it means threading a new flag through `searchQuerySchema` → `FindOptions` → conditional `--no-ignore` on rg argv and a bypass in `isIgnoredSearchPath`. Scoped `application`, deliberately NOT window even though VS Code scopes it per-resource: `false` un-hides gitignored files, which is exactly where `.env` and credential files live, and this is an agentic tool where those results land in a model context. A cloned repo must not be able to flip it. |
| `search.maxResults` | number | `200` | window | P6 | advanced | yes | PLUMBING, but the server caps it. `SEARCH_LIMIT = 200` is baked into every workspace search query (search-buffer-query.ts:29). The route validates `limit` with `integerQueryValueSchema('50', 200)` (apps/server/src/fs/contracts.ts:6, used at contracts.ts:52) — so the route's own default is 50 and anything above 200 is a 422/400, not a clamp. If the registry's schema allows >200 the route's upper bound must move in the same commit or the setting is silently unusable. The limit is enforced streaming-side at apps/server/src/fs/search.ts:255 and sets the `truncated` flag surfaced in the results header. |
| `search.quickOpen.maxResults` | number | `200` | window | P6 | advanced | yes | PLUMBING. An inline object literal inside `fetchQuickOpenFiles` — the whole quick-open query is hardcoded there (`matchMode: 'fuzzy'`, `includeContent: false`, `includeNames: true`, `streamNameMatchesEarly: false`, limit 200), consumed by apps/web/src/components/command-palette/use-command-palette-files.ts. Same 200 route ceiling as search.maxResults. Also note `nameRankCapacity` scales the fuzzy rank buffer off the limit (`limit * 4`, floored at 32, capped at 1000 — apps/server/src/fs/search.ts:86-88, 525-527), so raising this key raises server-side ranking memory too. |
| `search.preview.maxChars` | number | `240` | application | P6 | advanced | yes | PLUMBING, but there is no wire field today. `workspaceSearchPreview(line, columnIndex, endColumnIndex)` is called with no options object at apps/server/src/fs/search-shared.ts:100, so the `WorkspaceSearchPreviewOptions.maxChars` parameter (workspace-search-preview.ts:8,20) is dead. Either thread it from `FindOptions`/`searchQuerySchema` or resolve it server-side from the settings store and pass it at that one call site. Application scope: it is a wire-payload/rendering bound, not a per-project preference, and it directly sizes every search match sent over SSE. |
| `search.preview.contextChars` | number | `80` | application | P6 | internal | yes | PLUMBING, same dead-parameter problem as search.preview.maxChars — `contextChars` is only reachable through the unused options argument (workspace-search-preview.ts:23) and decides how much of the line precedes the match before the preview window is clamped to identifier boundaries. Internal: a tuning constant with no user story of its own; ship it JSON-only alongside maxChars or fold both into one descriptor. |
| `search.maxFileSizeBytes` | number | `1024 * 1024` | machine | P6 | advanced ⟳ | yes | PLUMBING, boot-critical. `this.maxSearchContentBytes = options.maxSearchContentBytes ?? 1024 * 1024`, stored `private readonly`, injected per search as `maxContentBytes` (apps/server/src/fs/service.ts:339) and emitted as `--max-filesize <n>` on the rg argv (apps/server/src/fs/search.ts:723-724). `FileSystemServiceOptions.maxSearchContentBytes` exists (service.ts:54) but apps/server/src/index.ts never sets it, so production always runs the 1MB default and there is no env override for it — unlike its two siblings. Machine scope: it is a per-box scan-cost limit and it reaches spawn argv, so the plan's rule forbids window. |
| `search.historyLimit` | number | `50` | application | P6 | internal | yes | PLUMBING. `SEARCH_HISTORY_LIMIT` trims both the query and replace history rings (`.slice(-SEARCH_HISTORY_LIMIT)`, search-buffer-state.tsx:1268) which are persisted per root path (`queryHistory`/`replaceHistory` in workspace-cache.ts:173-175). Internal: it exists to bound localStorage growth, not to express a preference. Application scope — a workspace file has no business sizing the user's local history buffer. |

### Notes

- SCOPE JUDGEMENT CALL — the one thing that needs an explicit ruling. The plan's hard rule ('any value that reaches process spawn/exec/env/keymap is application or machine, never window') collides with the fact that essentially every search preference ends up as argv on a spawned `rg` or `fd`. I drew the line at _what kind_ of argv: (a) fixed flags toggled by a boolean (`--ignore-case`, `--word-regexp`) and (b) per-query glob payloads the user can already type into the visible filter box today are window; (c) values that are server configuration baked into every invocation regardless of the request (`files.exclude` → `--exclude`/`--glob !…`, `search.maxFileSizeBytes` → `--max-filesize`) and (d) anything that _un-hides_ files (`search.useIgnoreFiles`) are application/machine. Rationale for (b): a glob is a separate argv element after `--glob`, spawned through `Bun.spawn` with no shell (apps/server/src/fs/search-tool-runner.ts), so it cannot select a binary, set an env var, or become a flag; and the identical capability is already exposed to anyone at the keyboard through the search filter row. If the reviewer reads the rule literally instead, move search.include/search.exclude/search.defaultMatchMode/search.caseSensitive/search.wholeWord to `application` — but then per-project search excludes, the single most-wanted workspace setting, become impossible.
- RESIDUAL RISK of window-scoped globs, stated plainly so it is a decision and not an oversight: a cloned repo shipping `.platform/settings.json` with `search.exclude: ['**/*.ts']` silently hides files from the search UI _and_ from the agent's search results. That is a suppression attack, not code execution, and the plan's Phase 4 'also modified in <scope>' indicator is the mitigation — the search panel must show that its filters came from workspace settings, not just the settings page. Do not ship window-scoped globs without that indicator.
- PER-WORKSPACE vs MACHINE, the split this group was asked for. Naturally per-workspace (window): search.defaultMatchMode, search.caseSensitive, search.wholeWord, search.include, search.exclude, search.maxResults, search.quickOpen.maxResults — all six of the first ones are ALREADY persisted per root path today, in localStorage under `platform.workspace-state.v16.search:<rootPath>` (apps/web/src/lib/workspace-cache.ts:44,73-75; schema `cachedSearchBufferStateSchema` at workspace-cache.ts:163-183). The product already believes these are per-project; the registry just moves where that belief is stored. Machine limits (never per-workspace): files.watcherEnabled, files.maxTextFileBytes, files.treeConcurrency, search.maxFileSizeBytes — all four are constructor-time `private readonly` fields on FileSystemService, three of them env-driven from apps/server/src/index.ts:18,21,22.
- BOOT-CRITICAL SET. files.watcherEnabled, files.maxTextFileBytes, files.treeConcurrency, search.maxFileSizeBytes and files.exclude are all read before or during `createApp`/module import and stored immutably. None can be delivered over the settings SSE. They belong on the plan's boot-critical list and all carry requiresRestart: true. `files.exclude` is the worst of them: `watcherIgnoredChildGlobs` is computed at _module import_ time (apps/server/src/fs/watch.ts:23-26), so it is frozen before any settings file is read.
- MIGRATION TRAP for Phase 6. The six search prefs live in a strictObject localStorage schema (`cachedSearchBufferStateSchema`, workspace-cache.ts:163) at CACHE_VERSION 16 (workspace-cache.ts:41). Removing the fields without bumping CACHE_VERSION makes every stored search buffer fail `v.safeParse` and get dropped by `readCacheEntry` (workspace-cache.ts:454) — which also throws away the cached match list. Per the repo's greenfield rule that is acceptable (delete the bad state), but bump CACHE_VERSION in the same commit so the failure is a clean drop rather than a per-user error toast via `reportError`.
- THE 200 CEILING is a real gate, not a detail. `limitQueryValueSchema = integerQueryValueSchema('50', 200)` (apps/server/src/fs/contracts.ts:6) rejects rather than clamps. Both search.maxResults and search.quickOpen.maxResults sit exactly at that ceiling today, so any registry schema that permits a larger value ships a setting that 4xx's the moment a user raises it. Either pin the schema max at 200 or raise the route bound in the same commit.
- TWO ENV NAMES FOR ONE VALUE. `FS_DEV_MAX_TEXT_FILE_BYTES` (apps/server/src/index.ts:21) and `MAX_TEXT_FILE_BYTES` (apps/server/src/fs/service.ts:71) both configure files.maxTextFileBytes, on different code paths, with only the second one validated and warned about. Independently, GitService keeps its own duplicate `DEFAULT_MAX_TEXT_FILE_BYTES = 209_715_200` (apps/server/src/git/service.ts:110) that happens to match. Collapse all three onto the descriptor when registering the key. Related to the plan's Phase 2 note that turbo.json has no PLATFORM*\*/FS*\* passThroughEnv entries — FS_WATCH, FS_TREE_CONCURRENCY and both max-bytes names have the same latent cache bug.
- DELIBERATELY EXCLUDED, with reasons. (1) `useWorkspaceIndex` (apps/server/src/fs/contracts.ts:62) is not a preference — it is chosen per call from the picker's scope (apps/web/src/components/file-picker/picker-search.ts:85) and the index itself is enabled only when a workspaceRoot is configured (apps/server/src/fs/service.ts:442-444). (2) Recents limits are three different call-site constants for two different UIs — 30 in apps/web/src/components/file-picker/data-helpers.ts:20, 40 in apps/web/src/lib/recent-folders-query.ts:7, route default 20 capped at 50 in apps/server/src/fs/contracts.ts:7,169 — so there is no single coherent key until those are unified; register it only if someone unifies them first. (3) Streaming batch cadence (SEARCH_FIRST_BATCH_DELAY_MS 0 / SEARCH_STEADY_BATCH_DELAY_MS 48 / SEARCH_FRAME_FALLBACK_MS 16, apps/web/src/features/search/search-buffer-runner.ts:21-23) and DIRTY_BUFFER_DEBOUNCE_MS 220 (use-run-dirty-search-buffer-overlay.ts:21) are frame-scheduling constants, not settings. (4) CONTENT_INDEX_RG_EXCLUDE_GLOB_LIMIT 512 (apps/server/src/fs/search.ts:95) bounds index-derived globs only, never user input. (5) There is no search-as-you-type debounce key to register: `useRunSearchBuffer` fires off the committed `query` in an effect (apps/web/src/features/search/use-run-search-buffer.ts:27) with no wait, so `search.searchOnType`-style keys would be new features.
- NO SMART CASE, NO FORMAT-ON-SAVE. `--ignore-case` is applied unconditionally whenever caseSensitive is false (apps/server/src/fs/search.ts:736); there is no smart-case path to expose. `editor.formatOnSave` — listed in the plan's Phase 6 paragraph next to the files.autoSave keys — likewise has zero occurrences at fda1523 (`rg formatOnSave` over apps/ + packages/ returns nothing); it belongs to the editor group but the plan's 'these three are new features, estimate separately' claim is confirmed on all three.
- The existing settings document (packages/contracts/src/settings.ts:117-121) contains only providerInstances, models and keybindings. Every key in this group is new to the registry — none is a rename of an existing stored key, so there is no read-compat concern, only the localStorage deletions noted above.

## lsp._, telemetry._, logs.\*

| Key | Type | Default | Scope | Phase | Vis | OK | Wiring |
| ---------------------------------- | ------------- | ------------------------------------------------------------------------------------------------------ | ----------- | ----------- | ---------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `lsp.enable` | boolean | `true` | application | P6 | user | **?** | NEW FEATURE. There is no global LSP on/off switch today. The only `enabled` in the LSP path is the hook's parameter default `enabled = true` (use-lsp-plugin.ts:24), and its sole caller passes `enabled: active` (apps/web/src/features/editor/components/editor.tsx:68) — i.e. it means 'this editor tab is focused', not 'LSP is on'. Wiring: AND a new setting into that prop, and gate the `/lsp/match` fetch (use-lsp-plugin.ts:35-60). Risk: `plugins` is a useMemo array handed to `useEditor` (editor.tsx:79-87, :107-124); per the plan's sharp edge the plugin array is built once per mount, so verify a live toggle re-applies rather than needing a remount. Scope is application (never window) because a false→true flip decides whether language-server child processes spawn at all. |
| `lsp.servers` | object | `{} (no overrides — lspConfigFromEnvironment returns null when neither env var is set)` | machine | P7 | advanced | yes | PLUMBING. Today the per-server override record comes only from `PLATFORM_LSP_CONFIG ?? FS_LSP_CONFIG` (registry.ts:615); with neither set `lspConfigFromEnvironment` returns `null` and `lspServersForEnvironment` returns the bundled list unchanged (:489-508). Replace that env read with a settings read. CORRECTION to the brief: the env var is NOT read once at construction — `lspServersForEnvironment()` is called from inside `matchLspServer` (registry.ts:475) on every /lsp/match request, so the record is already re-read per request and a settings-backed record needs no restart to affect NEW matches. ALREADY-RUNNING SESSIONS ARE THE PROBLEM: sessions are pooled in a module-level Map keyed `${server.id}${match.root}` (proxy-session.ts:77, :694-711, :719-721). Changing `command`/`args`/`env` for a server id does not change that key, so every already-connected editor keeps talking to the OLD child process, spawned with the OLD command and env, until it idles out (120s, :553) or the process dies. Setting `disabled: true` only removes the server from future matching (:496-500) — it does not kill a live session. There is no exported disposer: `PooledLspProxySession.dispose` is private (:574) and `removePooledSession` (:713) is internal. Wiring `lsp.servers` therefore requires a NEW exported `disposeLspSessionsForServer(id)` called on settings change, plus a client-side decision (reconnect the editor's LSP socket vs. show 'restart language server'). MACHINE SCOPE IS MANDATORY: `config.command` is passed to `spawnCommand` and `config.env` is spread over `process.env` (registry.ts:593-600) — arbitrary process execution with arbitrary environment. A cloned repo's .platform/settings.json must never reach this key. |
| `lsp.idleTimeoutMs` | number | `120_000` | machine | P6 | advanced | yes | PLUMBING. `DEFAULT_IDLE_TIMEOUT_MS = 120_000` (proxy-session.ts:79); `lspIdleTimeoutMs()` (:929-935) reads `PLATFORM_LSP_IDLE_TIMEOUT_MS ?? FS_LSP_IDLE_TIMEOUT_MS` and accepts any finite value >= 0. It is called fresh every time the idle timer is armed (:553), so a settings-backed value takes effect on the next arm with no restart. 0 means 'dispose immediately when the last connection drops' (used by the test at lsp/tests/proxy-session.test.ts:17). Machine scope: it controls how many language-server child processes stay resident, which is a RAM tradeoff per machine, and it governs process lifetime. |
| `lsp.experimental.tyForPython` | boolean | `false (truthy(undefined) === false)` | machine | P6 | advanced ⟳ | yes | PLUMBING, with a one-way latch to fix. `const useTyForPython = truthy(process.env.FS_EXPERIMENTAL_LSP_TY)` is evaluated at MODULE LOAD (registry.ts:55) — this is the one thing in this file that really is read once at construction. `experimentalFilteredServers` then ORs the per-call env with that module constant (`truthy(env.FS_EXPERIMENTAL_LSP_TY) \\                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | \\                                                                                                                                                                                                                                                                                 | useTyForPython`, :511), so once it was true at import it can never be turned back off in-process: pyright stays filtered out forever. That is why requiresRestart is true until the module-scope constant is deleted as part of the wiring. Chooses which Python binary spawns (spawnTy vs spawnNodePackageBin('pyright')) so it reaches process spawn → machine, never window. Only `PLATFORM*LSP_CONFIG`-style names were modernized here; this flag is still `FS*`-prefixed only. |
| `lsp.serverPriority` | string[] | `['deno', 'typescript', 'vue', 'eslint', 'oxlint', 'biome']` | application | P7 | internal | yes | PLUMBING. `serverPriority` (registry.ts:56) is the sort order applied to matched candidates (`compareServerPriority`/`serverPriorityIndex`, :675-682); ids not in the list sort last, and `matchLspServer` returns the FIRST candidate whose root resolves (:479-484). Wiring is a straight swap of the module const for a resolved value inside `matchLspServer`. Kept `internal` — it is an engineering tiebreak, not a knob a user should discover, but it must be registered because reordering it silently changes which server serves a .ts file. Not `window`: reordering picks which binary spawns (e.g. deno over typescript), which is the spawn-reachability rule even though no command string is involved. |
| `telemetry.enabled` | boolean | `booleanFromEnv(OBSERVABILITY_ENABLED) ?? environment !== 'test' → true in development and production` | application | P6 | user ⟳ | yes | PLUMBING on both ends, but asymmetric. SERVER: `observabilityEnabledFromEnv` (env.ts:4-7) feeds `ObservabilityConfig.enabled` (config.ts:55, :71), which `initializeObservabilityRuntime` uses once at boot to decide `initLogger({ enabled })` and whether to build a drain at all (runtime.ts:45-70) — hence requiresRestart. CLIENT: `clientLoggingEnabled()` (client-logging.ts:149-154) is also called on EVERY `emitClientLog` (:131), so the client half already honours a live change; only `initLogger`'s boot-time `enabled` is sticky. Note the client reads `import.meta.env.OBSERVABILITY_ENABLED`, which is not a VITE\_-prefixed name but IS explicitly injected by `define` in apps/web/vite.config.ts:22-25 defaulting to `''` — `booleanFromEnv('')` returns undefined, so the fallback `environment !== 'test'` is what actually applies in the browser. This is also the master off switch: it gates the PostHog drain regardless of the PostHog keys below (verified by packages/observability/src/tests/runtime.test.ts:18-27). |
| `telemetry.postHog.enabled` | boolean | `booleanFromEnv(OBSERVABILITY_POSTHOG_ENABLED) ?? Boolean(postHogApiKey) → false with no key` | application | P6 | advanced ⟳ | yes | PLUMBING. Default is derived, not literal: absent an explicit env value it is `Boolean(postHogApiKey)` (config.ts:87-88), and `postHogApiKey` defaults to `''` (:56-60, `stringFromEnv` returns `''` when nothing is set, :111-118), so an untouched install resolves to false. Keep that derivation in the registry — a flat `false` default would break the 'set a key, telemetry starts working' behaviour. Consumed once at boot by `createObservabilityDrain` (runtime.ts:48), so requiresRestart. |
| `telemetry.postHog.apiKey` | string | `'' (stringFromEnv over OBSERVABILITY_POSTHOG_API_KEY, POSTHOG_API_KEY, NUXT_POSTHOG_API_KEY)` | machine | P6 | advanced 🔒 ⟳ | yes | PLUMBING + SECRETS SPLIT. This is the only `sensitive: true` key in this group. Per the plan's Secrets section it must NOT live in ~/.platform/settings.json — it goes in the sibling ~/.platform/secrets.json (mode 0600) and the settings document holds only a presence marker, because /settings/raw, the 'Open Settings (JSON)' tab, and export all serve the settings file's real bytes. The plan is explicit that this split lands in Phase 2, before the first settings file is written, even though this key itself is registered in Phase 6. Wire it through the same `restoreRedactedSecrets` id+name path main already shipped (apps/server/src/settings/utils/redaction.ts). Machine scope: a credential, per-machine, never synced, and never workspace-settable. |
| `telemetry.postHog.host` | string | `'https://us.i.posthog.com'` | application | P6 | advanced ⟳ | yes | PLUMBING. Resolved from `OBSERVABILITY_POSTHOG_HOST ?? POSTHOG_HOST ?? NUXT_POSTHOG_HOST` falling back to the constant. Application scope, never window: this is the URL every log event is POSTed to, so a workspace-settable value would be a log-exfiltration channel. It is not literally spawn/exec/env, but it is the same class of hostile-input risk the plan's standing rule exists for — flag it explicitly in SERVER_CONSUMED_SETTINGS. |
| `telemetry.postHog.mode` | enum(events\\ | logs) | `'logs'` | application | P6 | internal ⟳ | yes | PLUMBING. Note the parser is deliberately lopsided — only the literal string 'events' (case-insensitive, trimmed) selects events mode; every other value including a typo silently means 'logs'. A registry enum schema fixes that by rejecting the typo instead of swallowing it. |
| `telemetry.postHog.eventName` | string | `'platform_log'` | application | P6 | internal ⟳ | yes | PLUMBING. Only meaningful when postHogMode === 'events'. Internal — it is a downstream-schema constant, not a preference. |
| `telemetry.postHog.distinctId` | string | `undefined (optionalStringFromEnv returns undefined for '')` | machine | P6 | internal ⟳ | yes | PLUMBING. The only key here whose default is `undefined` rather than a value, so the registry schema must be `v.optional(...)` with no default rather than defaulting to `''` — `''` and undefined are distinguishable downstream. Machine scope because it identifies an installation. Not marked sensitive (it is not a credential), but it is user-identifying, so keep it out of any wide event that logs setting values. |
| `logs.client.level` | enum(debug\\ | info\\ | warn\\ | error) | `import.meta.env.DEV ? 'debug' : 'info'` | application | P6 | advanced ⟳ | yes | PLUMBING. `clientLogMinLevel()` reads `import.meta.env.VITE_CLIENT_LOG_LEVEL`, accepts exactly the four evlog levels, and otherwise falls back to the DEV-conditional default. It is consumed once, as `minLevel` inside `initLogger` (client-logging.ts:69-81), and `initializeClientLogging` is latched by the module-level `initialized` flag (:49, :59-61) — so a live change needs either a new evlog setter or a reload. requiresRestart until that is addressed. Note `VITE_CLIENT_LOG_LEVEL` is NOT in turbo.json globalPassThroughEnv (turbo.json lists only OBSERVABILITY*\* at :14-33) — same latent bug class the plan calls out for PLATFORM*\*. |
| `logs.server.level` | enum(debug\\ | info\\ | warn\\ | error) | `NONE — no such default exists in code` | application | P6 | advanced ⟳ | **?** | NEW FEATURE, not plumbing. There is no server-side minimum log level anywhere: the server's `initLogger` (runtime.ts:50-68) passes `enabled`, `env`, `drain`, `pretty`, `redact`, `sampling`, `silent`, `stringify`, `plugins` — and no `minLevel`. The server expresses 'level' entirely through per-level sampling rates (`sampling.rates` at :59-65, sourced from `debugSampleRate`/`infoSampleRate`) plus the keep rules for status>=400 and duration>=slowMs. Two honest options: (a) drop this key and let users tune `logs.server.debugSampleRate`/`infoSampleRate` below, or (b) add a real `minLevel` to `initLogger` and to `ObservabilityConfig`. Do not register it as if it were a passthrough. Marked verified:false because the default could not be found in code. |
| `logs.server.console` | boolean | `booleanFromEnv(OBSERVABILITY_CONSOLE) ?? !production → true in development` | application | P6 | advanced ⟳ | yes | PLUMBING. Feeds two separate initLogger fields, inverted relative to each other: `pretty: config.consoleEnabled && environment !== 'production'` and `silent: !config.consoleEnabled` (runtime.ts:57, :66). Both are captured at boot → requiresRestart. Environment-conditional default; register the derivation, not a flat `true`. |
| `logs.server.dir` | string | `'logs'` | machine | P6 | advanced ⟳ | yes | PLUMBING, with a resolution rule to preserve. The raw value is relative by default and `resolveLogDir` (runtime.ts:239-244) turns a relative path into `<monorepoRoot>/logs`, falling back to `path.resolve()`; an absolute value is used as-is. Keep that behaviour when the value comes from settings, otherwise the log file silently moves relative to cwd. Machine scope: a filesystem path, never synced, and a workspace-settable log directory would let a cloned repo choose where the process writes files. Also relevant to the plan's home-consolidation work in Phase 2 — decide whether this should default under ~/.platform/ before registering it. |
| `logs.server.filePretty` | boolean | `false` | application | P6 | internal ⟳ | yes | PLUMBING. `booleanFromEnv(OBSERVABILITY_FILE_PRETTY) ?? false`. Internal — pretty-printing the JSONL file breaks the one-object-per-line contract AGENTS.md relies on for grep/jq, so this should stay JSON-only and searchable rather than being offered on the page. |
| `logs.server.maxFiles` | number | `14` | application | P6 | advanced ⟳ | yes | PLUMBING. Parsed by `positiveInteger` (:125-132), which rejects <= 0 and non-integers by falling back to the default — the registry schema should reject them instead so a bad value is a visible error, not a silent revert. Log retention in days-of-files; consumed by the fs drain built at boot. |
| `logs.server.maxSizeBytes` | number | `10_485_760` | application | P6 | advanced ⟳ | yes | PLUMBING. 10 MiB per file before rollover into the numbered continuations (`logs/2026-06-12.1.jsonl`) that AGENTS.md documents. Same `positiveInteger` fallback caveat as maxFiles. |
| `logs.server.infoSampleRate` | number | `production ? 25 : 100` | application | P6 | advanced ⟳ | yes | PLUMBING. Percentage, clamped to 0..100 by `percentage` (:143-150). Reaches `initLogger({ sampling: { rates: { info } } })` at runtime.ts:62. This plus debugSampleRate is what 'server log level' actually means today — see logs.server.level above. |
| `logs.server.debugSampleRate` | number | `production ? 0 : 100` | application | P6 | advanced ⟳ | yes | PLUMBING PLUS A MISSING READ. Unlike every sibling it has NO env override — the line is a bare `production ? 0 : 100` with no `observabilityEnv(env, ...)` call, so there is currently no way to turn debug logging on in a production build. Wiring it means adding the config read as well as the settings read. Note error and warn are hardcoded at 100 in runtime.ts:63-64 and are deliberately not settings. |
| `logs.server.slowMs` | number | `500` | application | P6 | advanced ⟳ | yes | PLUMBING. This is the sampling KEEP threshold — `sampling.keep: [{ status: 400 }, { duration: config.slowMs }]` (runtime.ts:59-61) — i.e. 'never drop an event slower than this'. It is a different 500 from the dashboard's `logs.dashboard.slowMs` and from the log reader's own `defaultSlowMs`; see notes. Do not collapse them into one key without deciding which consumer wins. |
| `logs.server.batchSize` | number | `50` | application | P6 | internal ⟳ | yes | PLUMBING. Drain batching for the PostHog pipeline. Internal engineering constant — registered and searchable per the plan's visibility rationale, not shown on the page. |
| `logs.server.batchIntervalMs` | number | `5_000` | application | P6 | internal ⟳ | yes | PLUMBING. Internal, same rationale as batchSize. |
| `logs.server.maxBufferSize` | number | `1_000` | application | P6 | internal ⟳ | yes | PLUMBING. In-memory drain buffer cap before events are dropped. Internal. |
| `logs.server.postHogRetries` | number | `2` | application | P6 | internal ⟳ | yes | PLUMBING. Parsed by `nonNegativeInteger` (:134-141), so 0 is a legal value here — unlike the `positiveInteger` keys. The registry schema must mirror that or 'no retries' becomes unexpressible. |
| `logs.server.postHogTimeoutMs` | number | `5_000` | application | P6 | internal ⟳ | yes | PLUMBING. Internal. |
| `logs.server.service` | string | `'platform'` | application | P6 | internal ⟳ | yes | PLUMBING. `observabilityEnv(env,'SERVICE')?.trim() \\                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | \\                                                                                                                                                                                                                                                                                 | 'platform'`. Reaches `initLogger({ env: { service } })`(runtime.ts:54). The web client hardcodes its own counterpart as`const serviceName = 'platform-web'` (apps/web/src/lib/client-logging.ts:24) with no env read at all, so this key covers the server only. Internal. |
| `logs.dashboard.defaultTimeRange` | enum(15m\\ | 1h\\ | 6h\\ | 24h\\ | all) | `'1h'` | window | P6 | user | yes | PLUMBING. `defaultLogsFilterState` is the sole seed of `useState<LogsFilterState>` in the panel (apps/web/src/features/logs/panel.tsx:24); nothing persists it, so filter choices are lost on every unmount today. Wiring = seed that useState from the resolved setting. The option list lives separately in log-toolbar.tsx:18-24 and must stay in sync with the registry enum. `window` scope is safe here — pure UI state that never reaches spawn, exec, env, or the keymap. |
| `logs.dashboard.defaultLevel` | enum(all\\ | debug\\ | info\\ | warn\\ | error) | `'all'` | window | P6 | user | yes | PLUMBING, same seed path as defaultTimeRange. Note the widened type: the underlying contract enum is `logDashboardLevelSchema = v.picklist(['debug','error','info','warn'])` (packages/contracts/src/log-dashboard.ts:4) and the filter adds a literal 'all' sentinel that `logDashboardFilters` translates to `levels: undefined` (log-filter-params.ts:34). The registry schema must be the union including 'all', not the contract picklist. |
| `logs.dashboard.defaultSource` | string | `'all'` | window | P6 | advanced | yes | PLUMBING. Free string, not an enum: the option list is fetched at runtime from the summary breakdown (panel.tsx:59, log-toolbar.tsx:114-119) and `logBreakdownOptionValues` (log-toolbar-options.ts:3-12) deliberately keeps a selected value that is not in the current list. So the registry schema is `string` with the 'all' sentinel, and a stale saved value must not be treated as invalid. Known values in practice are `be`, `client`, `keyboard` per AGENTS.md. |
| `logs.dashboard.defaultArea` | string | `'all'` | window | P6 | advanced | yes | PLUMBING. Same runtime-populated-options caveat as defaultSource (panel.tsx:56, log-toolbar.tsx:127-132). This is the highest-value `window`-scoped key in the group: a workspace pinning its own area filter is the realistic use. |
| `logs.dashboard.slowMs` | number | `500` | window | P6 | advanced | yes | PLUMBING. Always sent on the query (log-filter-params.ts:36) and used server-side purely to CLASSIFY events as slow for the summary count — `event.durationMs >= (filters.slowMs ?? defaultSlowMs)` in `isSlowEvent` (apps/server/src/observability/log-reader.ts:392-396). It never excludes rows. There is no UI control for it today, so wiring it adds the control as well as the read. Contract allows it: `slowMs: v.optional(nonNegativeIntegerSchema)` (packages/contracts/src/log-dashboard.ts:11). |
| `logs.dashboard.eventLimit` | number | `300` | window | P6 | internal | yes | PLUMBING. Hardcoded into the events query (`limit: 300`). The server has its own separate constants — `defaultEventLimit = 200` and `maxEventLimit = 1_000` (apps/server/src/observability/log-reader.ts:40-41) — so the client's 300 silently overrides the server default and a settings value above 1000 will be clamped, not honoured. Register the clamp as the schema's max so the UI cannot promise something the reader will not deliver. |
| `logs.dashboard.liveMaxEvents` | number | `500` | window | P6 | internal | yes | PLUMBING, two sites. The cap is written twice as an independent literal — a default parameter `maxEvents = 500` on `mergeLiveLogItems` (:30) and `options.maxEvents ?? 500` inside `mergeLiveLogEvents` (:50). Both must move to the setting in one pass or they will drift. It bounds the in-memory live-tail ring, and `mergeDetailsByRetainedEvents` (:81-95) drops detail records for evicted events, so raising it raises memory more than linearly. |
| `logs.dashboard.liveFlushMs` | number | `16` | window | P6 | internal | yes | PLUMBING. `setTimeout(flush, 16)` inside the default `scheduleLiveEventFlush`; the batcher already takes an injectable `scheduleFlush` (log-live-batcher.ts:4-6), so this is the cleanest wiring in the group — pass a settings-derived scheduler from use-log-live.ts. 16ms is a one-frame coalesce for setQueryData under a live SSE stream. Internal: a user has no reason to touch it, but it is exactly the kind of constant the plan wants registered and searchable rather than buried. |
| `logs.dashboard.staleTimeMs` | number | `1_000` | window | P6 | internal | yes | PLUMBING, two sites with the same literal (`staleTime: 1_000`) that must be changed together. This is one of the query-staleTime constants the plan's visibility rationale names explicitly as the reason `internal` exists. |
| `logs.dashboard.detailCacheEvents` | number | `5_000` | application | P6 | internal | yes | PLUMBING. Server-side cap on the log reader's detail cache. Application rather than window because it is a server-process memory bound, not per-window UI state — a workspace-settable server memory cap is the wrong shape even though it never reaches spawn. Internal: this is the 'cache cap' class the plan's visibility field was introduced for. |

### Notes

- CORRECTION to the brief's premise about apps/server/src/lsp/registry.ts: it does NOT read PLATFORM_LSP_CONFIG/FS_LSP_CONFIG once at construction. `lspServersForEnvironment(env = process.env)` (registry.ts:489) calls `lspConfigFromEnvironment` (:614) on every invocation, and it is invoked from inside `matchLspServer` (:475), i.e. once per /lsp/match request. The env read is already per-request. The ONE thing genuinely read once at module load is `const useTyForPython = truthy(process.env.FS_EXPERIMENTAL_LSP_TY)` (registry.ts:55), and because `experimentalFilteredServers` ORs it with the per-call env (:511) it is a one-way latch that cannot be un-set in-process. That latch, not the config read, is what forces requiresRestart on lsp.experimental.tyForPython.
- Server-definition count: the `lspServers` array holds 37 definitions (registry.ts:69-467), not 36. Only 36 are ever active at once because `experimentalFilteredServers` (:510-515) removes exactly one of the mutually-exclusive Python pair — `ty` normally, `pyright` when the TY experiment is on. Any registry test that asserts a count must account for that filter.
- WHAT A CHANGE TO lsp.servers MEANS FOR RUNNING SESSIONS (the brief's specific question): nothing, until the process dies. Sessions live in a module-level `pooledSessions` Map keyed `${match.server.id}${match.root}` (proxy-session.ts:77, :694-711, :719-721). Editing `command`, `args`, or `env` for a server id does not change that key, so `pooledLspProxySession` returns the EXISTING child process — spawned with the old command and old environment — to every new connection for that root. Setting `disabled: true` only removes the definition from `matchLspServer`'s candidate list (:496-500), which stops NEW roots from starting a server but does not touch a live one. The session only goes away on idle timeout (120s after the last connection drops, :553) or on process exit. There is no exported disposer: `PooledLspProxySession.dispose` is private (:574) and `removePooledSession` (:713) is module-internal. So wiring lsp.servers requires (a) a new exported `disposeLspSessionsForServer(id)` / `disposeAllLspSessions()`, (b) a settings-change hook that calls it, and (c) a client decision — reconnect the editor LSP socket automatically, or surface an explicit 'Restart Language Server' action. Without (a)-(c) the settings page will look like it works and silently do nothing for anyone with a file already open.
- THREE INDEPENDENT 500ms CONSTANTS, all meaning different things, none currently shared: (1) `DEFAULT_SLOW_MS = 500` (packages/observability/src/config.ts:49) — the sampling KEEP threshold, 'never drop an event slower than this'; (2) `defaultLogsFilterState.slowMs = 500` (apps/web/src/features/logs/log-filter-params.ts:19) — the dashboard's slow-count threshold sent on the query; (3) `defaultSlowMs = 500` (apps/server/src/observability/log-reader.ts:42) — the reader's fallback when the query omits slowMs. I registered (1) and (2) as separate keys and left (3) as the reader's fallback. Do not collapse them: (1) is a retention decision made at write time and (2) is a display decision made at read time. If they are unified, (3) has to be deleted, not aliased.
- logs.server.level is registered with verified:false on purpose. The brief asked for 'client and server log levels', but there is no server minimum level in the code: `initLogger` in packages/observability/src/runtime.ts:50-68 passes no `minLevel`, and `ObservabilityConfig` (config.ts:14-37) has no level field. The server's equivalent is per-level sampling (`sampling.rates`, runtime.ts:59-65) with error/warn hardcoded at 100. Either drop the key or treat it as new work in Phase 6 — do not register it as a passthrough.
- logs.server.debugSampleRate is the one observability config field with no env override at all: config.ts:70 is a bare `production ? 0 : 100` with no `observabilityEnv(env, ...)` call, unlike every sibling. Wiring it needs the config read added, not just the settings read. It is also why debug logging cannot currently be turned on in a production build by any means.
- PARSER LENIENCY THAT THE REGISTRY SCHEMAS SHOULD REPLACE, NOT PRESERVE: `positiveInteger` (config.ts:125-132) and `nonNegativeInteger` (:134-141) silently fall back to the default on a bad value, and `postHogMode` (:152-155) silently means 'logs' for anything that is not exactly 'events'. Under the plan's per-key-fallback read rule that is roughly the right behaviour for the FILE layer, but the registry schema should still reject the value so the settings page can say 'invalid' instead of showing the default as though the user's input were accepted. Also note the positive/non-negative split is real: postHogRetries legitimately accepts 0, the others do not.
- SCOPE JUDGEMENT CALLS. (a) Every lsp._ key is application or machine, none window — mandatory for lsp.servers (command + env spread over process.env at registry.ts:593-600 = arbitrary execution) and lsp.experimental.tyForPython (picks which Python binary spawns), and I extended it to lsp.serverPriority because reordering picks which binary spawns even without a command string, and to lsp.enable because it decides whether any server spawns. (b) lsp.idleTimeoutMs and lsp.servers are `machine` not `application`: they govern child-process lifetime, binaries, and paths, which the plan's definition of machine names directly. (c) telemetry.postHog.host is `application` even though it is not literally spawn/exec/env — it is where every log event is POSTed, so a workspace-settable value is a log-exfiltration channel. Add it to SERVER_CONSUMED_SETTINGS with an explicit comment so the registry test covers it; the literal wording of the standing rule does not catch it. (d) logs.server.dir is `machine` (filesystem path chosen by a cloned repo would be a write primitive). (e) Only logs.dashboard._ are `window` — pure client UI state that never reaches spawn, exec, env, or the keymap.
- EXCLUDED DELIBERATELY. `logs.dashboard.defaultSearch` (log-filter-params.ts:17, default `''`): a persisted default search silently hides rows on every panel open and is the kind of setting that generates 'the logs are broken' reports; leave it session-local. `defaultEventLimit = 200` / `maxEventLimit = 1_000` / `timelineBucketCount = 48` / `maxBreakdownItems = 12` (apps/server/src/observability/log-reader.ts:40-44): server-side reader constants with no user-visible meaning; maxEventLimit matters only as the clamp on logs.dashboard.eventLimit and is documented there. The client's `serviceName = 'platform-web'` (apps/web/src/lib/client-logging.ts:24) is hardcoded with no env read, so there is no client counterpart to logs.server.service. The `sensitiveFields` redaction set (client-logging.ts:28-47) and `maxStringLength = 2_000` (:26) are security policy, not preferences — deliberately not settings, and a workspace-settable redaction list would be an obvious hole.
- PHASE PLACEMENT. Everything here is Phase 6 (the plan's 'files / search / chat / lsp / telemetry / window batch') except two keys pushed to Phase 7: `lsp.servers`, which is a record of objects carrying command/args/env and needs the Phase 7 record editor plus the new session-disposal machinery, and `lsp.serverPriority`, which is a list-valued key needing the Phase 7 list editor. lsp.enable is the only key in this group that is genuinely new behaviour rather than plumbing, so it should be estimated with the plan's other feature-shaped keys (files.autoSave, editor.formatOnSave) rather than with the ~80 plumbing keys.
- requiresRestart is true for every telemetry._ and logs.server._ key because `initializeObservabilityRuntime` (runtime.ts:44-72) builds the drain and calls `initLogger` exactly once at boot, and true for logs.client.level because `initializeClientLogging` is latched by the module-level `initialized` flag (client-logging.ts:49, :59-61). The one asymmetry worth surfacing in the UI: telemetry.enabled is re-read live on the client (`clientLoggingEnabled()` is called on every `emitClientLog`, client-logging.ts:131) but not on the server — so turning telemetry off stops browser events immediately and server events only after a restart. Say that in the restart notice rather than implying both halves are stale.
- ENV NAMES NOT IN turbo.json globalPassThroughEnv (which lists only OBSERVABILITY\_\* at :14-33): PLATFORM_LSP_CONFIG, FS_LSP_CONFIG, PLATFORM_LSP_IDLE_TIMEOUT_MS, FS_LSP_IDLE_TIMEOUT_MS, FS_EXPERIMENTAL_LSP_TY, VITE_CLIENT_LOG_LEVEL, POSTHOG_API_KEY/POSTHOG_HOST (the un-prefixed aliases read at config.ts:58-59, :94-95). Same latent cache-correctness bug the plan already flags for PLATFORM_SETTINGS_FILE — fix them in the same Phase 2 turbo.json edit. Also note apps/web/vite.config.ts:22-25 explicitly `define`s `import.meta.env.OBSERVABILITY_ENABLED` defaulting to `''`; `booleanFromEnv('')` returns undefined (env.ts:16-24), so the browser correctly falls through to `environment !== 'test'`. That is working as intended, not a bug — but it means the client's telemetry.enabled default is decided by MODE, not by the server's resolved config, and the settings-backed version must not reintroduce that split.
- telemetry.postHog.apiKey is the only sensitive:true key in this group and it is the one that couples to the plan's Phase 2 secrets split. Registering it in Phase 6 is fine, but ~/.platform/secrets.json must already exist by then — the plan is explicit that moving secrets out of a settings file that already has them is a migration and there is no migration code. Add it to the test that asserts no sensitive value appears in /settings/raw, in an export, or in any log line.

## editor

| Key | Type | Default | Scope | Phase | Vis | OK | Wiring |
| --------------------------------------- | ------------ | ------------------------------------------------------------------------------------------------ | ----------- | ----------- | -------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `editor.fontFamily` | string | `"JetBrains Mono Nerd Font", "JetBrains Mono Variable", ui-monospace, SFMono-Regular, monospace` | window | P3 | user | yes | PLUMBING (app-side only), NOT live on its own. The editor takes no font option; the font arrives purely through CSS: `.app-editor-host .editor-virtualized { font-family: var(--font-mono) }` (packages/ui/src/styles/globals.css:528-533), with `--font-mono` declared at globals.css:74 and re-written onto document.documentElement at boot by applyDefaultMonoFontStack (apps/web/src/lib/default-nerd-font.ts:59-61). DO NOT bind this key to `--font-mono`: the file tree reads it (apps/web/src/components/workspace/file-tree/components/tree-pane.tsx:473) and `--font-heading` aliases it (globals.css:17). Add a new `--editor-font-family` used only by the editor host and write it from a settings effect. That alone repaints text but leaves geometry wrong: character width is measured once and cached (packages/editor-core/src/virtualization/virtualizedTextView.ts:192 -> browserMetrics.ts:11, cache key includes fontFamily at browserMetrics.ts:93-108), and the remeasure entry point VirtualizedTextView.refreshMetrics() (virtualizedTextView.ts:422) is unreachable because Editor.view is `private readonly` (packages/editor-core/src/editor/Editor.ts:200). So this needs EITHER a new editor-package passthrough (`Editor.refreshMetrics()`) OR an editor remount. Remount is affordable here: buffer, undo history and view session are app-owned (apps/web/src/features/editor/editor-render-document.ts:3-9), so only mount cost is paid, not user state. |
| `editor.fontSize` | number | `13` | window | P3 | user | yes | PLUMBING + a required editor-package passthrough (or remount). Same story as fontFamily: no editor option exists (apps/web/src/features/editor/components/editor.tsx:108-125 passes only cursorLineHighlight/document/keymap/onChange/plugins/rowPositioning/theme), the 13px comes from the package stylesheet rule on `.editor-virtualized`, and the app overrides font-size only for search-result hosts (packages/ui/src/styles/globals.css:619, 12px). Write a new `--editor-font-size` var on the editor host. Font size changes BOTH characterWidth and measured rowHeight, and both are frozen in view.metrics at construction (virtualizedTextView.ts:192); virtualization geometry will be wrong until re-measured. Needs `Editor.refreshMetrics()` exported from the package (view method already exists at virtualizedTextView.ts:422) or an editor remount. This is one of the two real cross-repo edits in this group. |
| `editor.lineHeight` | number | `24` | window | P3 | user | yes | LIVE PASSTHROUGH via an existing setter — the cheapest typography key. `Editor.setLineHeight()` exists (packages/editor-core/src/editor/Editor.ts:950) and forwards to VirtualizedTextView.setLineHeight (virtualizedTextView.ts:430-438), which applies new metrics immediately. Two gaps to close, both app-side: (1) the app never passes `lineHeight` (editor.tsx:108-125), so the effective value is whatever the CSS `--editor-row-height: 24px` measures to (fallback constant DEFAULT_ROW_HEIGHT = 24 at packages/editor-core/src/virtualization/browserMetrics.ts:6); (2) the React wrapper does not sync it — useControlledOptionSync (packages/editor-react/src/index.ts:564-596) syncs document/theme/hiddenCharacters/editability/keymap/rangeDecorations/rowGap/scrollMode/selection/scrollPosition/plugins and nothing else. Either add syncLineHeightOption to the wrapper or call `controller.getEditor()?.setLineHeight(v)` from an app effect. No remount, no new package algorithm. NOTE FOR THE UI: this is a row height in PIXELS, not a ratio — a user typing 1.5 gets a 1px line. |
| `editor.tabSize` | number | `4` | resource | P3 | user | yes | NEEDS A NEW EDITOR-PACKAGE SETTER — this is the schedule risk the plan names, and it is real. `tabSize` is captured `private readonly` in the Editor constructor (Editor.ts:264 declaration, :329 `this.tabSize = normalizeTabSize(options.tabSize)`) and pushed to the DOM exactly once, in the view constructor: `scrollElement.style.setProperty('--editor-tab-size', String(tabSize))` (virtualizedTextView.ts:313). Grep confirms NO setTabSize anywhere in packages/editor-core/src. The value fans out to three places that all cache: the CSS var (rendering), edit actions (Editor.ts:392 -> editActions.ts outdent), and visual-column math (displayTransforms.ts:218-290, used by row geometry/width scanning). A correct setter must rewrite the var, invalidate row geometry + max-visual-column caches, and re-derive rows. Fallback that ships today: editor remount keyed on the setting. Registering this key without either is registering it inert. |
| `editor.wordWrap` | boolean | `false` | resource | P3 | user | yes | LIVE PASSTHROUGH via an existing setter, plus a second-writer problem. `Editor.setWordWrap()` (Editor.ts:550) already works at runtime and is already driven by a keybinding: Alt+Z -> `editor.action.toggleWordWrap` (apps/web/src/keymap/default-bindings.ts:330, apps/web/src/keymap/command-registry.ts:286-288) -> Editor.ts:421. Only the INITIAL value is unwired (`wrap: options.wordWrap ?? false`, Editor.ts:351; the app passes nothing) and the React wrapper does not sync `wordWrap` (packages/editor-react/src/index.ts:564-596). App-side effect calling setWordWrap is enough — no package change. BUT the toggle command mutates editor state only, so after one Alt+Z the setting and the editor disagree; the command must write through the settings path. This belongs on the plan's 'Commands are a second writer' table alongside workspace.toggleDiffViewMode — it is currently missing from it. |
| `editor.minimap.enabled` | boolean | `true` | window | P3 | user | yes | PLUGIN ENABLE FLAG + a restructure of the lazy loader; NO remount needed, contrary to the plan's sharp-edge note. Today: loaded unless `editorPerformanceFeatureDisabled('minimap')` (editor-plugins.ts:157-163), inside nonCriticalEditorPluginLoaders(), whose result is memoized in MODULE-LEVEL state shared by every editor instance (editor-plugins.ts:82-83, 134-146); the URL disable-set is itself memoized once (apps/web/src/lib/editor-performance-trace.ts:76, 397-412). So re-reading the gate can never toggle anything. The live path that already exists: `Editor.setPlugins()` (Editor.ts:991 -> packages/editor-core/src/plugins.ts:584-603 adds, removes AND disposes) and the wrapper re-syncs whenever the plugin array identity changes (packages/editor-react/src/index.ts:593-596, samePlugins at :772-779). Work required: lift the minimap loader out of the module-cached wrapper plugin and into the array built at editor.tsx:80-88, and add the setting to the useMemo deps at editor.tsx:78-79 (today `[]`). Keep the lazy `import('@singapor/minimap')` — turning it on must not become a synchronous cost at mount. |
| `editor.scopeLines.enabled` | boolean | `true` | window | P3 | user | yes | PLUGIN ENABLE FLAG — identical mechanism and identical restructure to editor.minimap.enabled (same nonCriticalEditorPluginLoaders() body, editor-plugins.ts:148-180, same module-level cache at :82-83). Do both in one pass; separately they cost twice. Live once lifted into the editor.tsx plugin array; no remount, no package change. |
| `editor.syntaxHighlighting.enabled` | boolean | `true` | window | P3 | advanced | yes | PLUGIN ENABLE FLAG with real coupling — the one plugin toggle that is not free. createEditorSyntaxHighlightingPlugins() already returns `[]` when disabled (editor-plugins.ts:226-240), and it sits inside createCriticalEditorCorePlugins() memoized with `[]` deps (editor.tsx:78), so rebuilding that array on the setting makes setPlugins add/remove it live. Two cautions the registry entry must carry: (a) tree-sitter is ALSO the structure provider for folds and brackets — 'Tree-sitter stays for structure (folds/brackets)' (editor-plugins.ts:230-231) — so turning syntax off silently degrades editor.folding and editor.matchBrackets; the UI should say so or the keys should be linked. (b) The shiki worker owner and the tree-sitter provider are module singletons with explicit disposers (editor-plugins.ts:316-348); re-enabling must reuse them rather than leak a second worker. Advanced visibility: this is a perf escape hatch, not a preference. |
| `editor.scrollPersistence.enabled` | boolean | `true` | window | P3 | internal | yes | PLUGIN ENABLE FLAG — the cheapest of the perf gates. The gate is read INSIDE activate() (use-scroll-persistence-plugin.ts:36-48), not at module load, so the plugin object only has to be rebuilt (its useMemo deps are `[]` at :50) for Editor.setPlugins to re-activate it with the new answer. No package change, no remount. Registered internal on purpose: turning it off silently loses scroll restore across tab switches and project switches, which reads as a bug to a user. A fourth `?editorPerfDisable=` feature the plan's Phase 5 description does not mention. |
| `editor.decode.mode` | enum(off\\ | autoregressive\\ | parallel\\ | diffusion\\ | token) | `off` | window | P3 | advanced | yes | PLUGIN ENABLE FLAG + per-mode instance rebuild, same loader restructure as minimap/scope-lines. requestedDecodeMode() returns null when `?decode=` is absent (decode-mode.ts:10-15), so the effective default is OFF; DEFAULT_DECODE_MODE = 'diffusion' (decode-mode.ts:7) is only the alias for `?decode=1\\ | true\\ | on`. The mode is read from location.search once (editor-plugins.ts:168) and BAKED into the plugin instance via createDecodePlugin({ mode }) (editor-plugins.ts:171-176), so switching modes needs a new plugin instance, not an enable flag — the setting must be part of the plugin array's identity. Currently has no persistence of any kind. Advanced: it is an animation novelty on file open. |
| `editor.lineNumbers` | enum(on\\ | off) | `on` | resource | P3 | user | yes | LIVE plugin add/remove — plumbing only. createLineGutterPlugin() is called unconditionally at editor-plugins.ts:88; gate it on the setting and add the setting to the useMemo deps at editor.tsx:78. Editor.setPlugins disposes the removed gutter contribution cleanly (plugins.ts:584-603), and the gutter width recomputes on the next render pass. Keep the enum at on\\ | off: RELATIVE LINE NUMBERS ARE NOT SUPPORTED by the gutter package — LineGutterPluginOptions offers counterStyle/minLabelColumns/minWidth/startLine only (packages/editor-gutters/src/lineGutter.ts:37-46) — so a `relative` member would be a new feature, not a value. |
| `editor.folding` | boolean | `true` | resource | P3 | user | yes | LIVE plugin add/remove — plumbing only. createFoldGutterPlugin({ width: 16, icon, iconClassName }) at editor-plugins.ts:89-93; gate + deps exactly as editor.lineNumbers. Caveat: fold structure comes from the tree-sitter providers registered by the syntax plugins (editor-plugins.ts:230-231), so this key is a no-op while editor.syntaxHighlighting.enabled is false. |
| `editor.matchBrackets` | boolean | `true` | resource | P3 | advanced | yes | LIVE plugin add/remove — plumbing only. createBracketMatchPlugin({ style: { backgroundColor: 'var(--editor-bracket-match-background)' } }) at editor-plugins.ts:96-98; gate + deps as above. Boolean, not VS Code's always\\ | near\\ | never — the plugin has no proximity mode, so an enum would be inventing behavior. Also structure-dependent on tree-sitter (see editor.syntaxHighlighting.enabled). |
| `editor.occurrencesHighlight` | boolean | `true` | resource | P3 | advanced | yes | LIVE plugin add/remove — plumbing only. createOccurrenceHighlightPlugin({ style: { backgroundColor: 'var(--editor-occurrence-highlight-background)' } }) at editor-plugins.ts:99-101; gate + deps as above. Removal disposes the plugin's CSS Highlight registrations through the normal plugin dispose path (plugins.ts:584-603). |
| `editor.links` | boolean | `true` | resource | P3 | advanced | yes | LIVE plugin add/remove — plumbing only. createDocumentLinkPlugin() at editor-plugins.ts:102; gate + deps as above. No options today, so the key is strictly on/off. |
| `editor.renderLineHighlight` | enum(none\\ | gutter\\ | line\\ | all) | `all` | resource | P3 | advanced | yes | NEEDS A NEW EDITOR-PACKAGE SETTER or an editor remount — the second genuine cross-repo edit in this group, and it is NOT on the plan's sharp-edge list. `cursorLineHighlight` is constructor-only end to end: the app passes { gutterNumber: true, gutterBackground: ['fold-gutter'], rowBackground: true } (editor.tsx:109-113) -> Editor forwards it to the view once (Editor.ts:343) -> normalizeCursorLineHighlight freezes it into view state (virtualizedTextView.ts:251, 970-978) and every row render reads view.cursorLineHighlight (virtualizedTextViewRows.ts:1284, :1308). There is no setter and the React wrapper syncs nothing for it (packages/editor-react/src/index.ts:564-596). Enum mapping to the existing shape: none = all three off; gutter = gutterNumber + gutterBackground; line = rowBackground; all = today's value, hence the default. A setter must also force a row re-render pass, not just swap the object. |
| `editor.diff.viewMode` | enum(split\\ | stacked) | `stacked` | window | P3 | user | yes | LIVE PASSTHROUGH, app-only, zero editor-package work — this is why the plan puts it in the Phase 3 appearance batch (plan line 623). Consumers already read it reactively from the editor workspace store (apps/web/src/features/git/components/diff-view.tsx:30, apps/web/src/features/editor/components/compare-saved-view.tsx:21). Phase 3 work is three deletions plus a rewire: drop the localStorage key `platform.workspace-state.v<N>.diffViewMode` (apps/web/src/lib/workspace-cache.ts:56, writer at :271-273, reader at :374-377) and its persistence entry (apps/web/src/hooks/use-workspace-cache-persistence.ts:174-176), point the store at the setting, and make `workspace.toggleDiffViewMode` (apps/web/src/keymap/commands.ts:379-381) write through the settings path — it is already on the plan's second-writer table. |
| `editor.documentRetention.projectLimit` | number | `3` | application | P3 | internal | yes | PLUMBING, one call site. retentionForProjects already accepts `projectLimit` as an optional parameter defaulting to the module constant (document-retention.ts:26-38), but the only production caller passes neither cap (apps/web/src/features/editor/state/editor-commands.ts:263-268), so the constant is the live value. Wiring is: resolve the setting and pass it at editor-commands.ts:263. Takes effect on the next project switch — no restart, no remount. SCOPE IS APPLICATION DELIBERATELY: retention is computed as a UNION across parked workspace roots (document-retention.ts:39-51, editor-commands.ts:257-268), so a value read from one repo's .platform/settings.json would govern how long OTHER repos' documents survive. window scope would be incoherent, not merely risky. |
| `editor.documentRetention.byteBudget` | number | `64 * 1024 * 1024` | application | P3 | internal | yes | PLUMBING **plus a dead-code fix** — do not register this key as-is. The byte budget cannot take effect today: withinByteBudget returns the parked list untouched whenever `documentSizes` is undefined (document-retention.ts:76-82), and the production caller never passes documentSizes (editor-commands.ts:263-268). Only the tests exercise it (apps/web/src/features/editor/tests/document-retention.test.ts:40-64). So honoring the plan's 'a key is never registered inert' rule requires first threading a document-id -> byte-size map out of the editor document store into retentionForWorkspaces, THEN passing the setting. Estimate this as a small feature, not a passthrough. Same application-scope argument as projectLimit. |
| `editor.formatOnSave` | boolean | `false` | resource | P6 | user | **?** | NEW FEATURE — no default exists in code, so `false` is my proposal, not a verified reading. Confirmed absent at fda1523: grep for `formatOnSave\\                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | format-on-save\\                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | autoSave\\                                                                                                                                                                                                                                                               | auto-save`across apps/web/src, apps/server/src and packages/contracts/src returns ZERO hits. The plan already classifies it this way (Phase 6, plan lines 641-643). The parts exist but the join does not: the save path is saveEditorDocumentByPath (apps/web/src/features/editor/editor-save.ts:25) / saveAllEditorDocuments (:40), both ending in FileSyncService.save; the formatter is the editor command`editor.action.formatDocument`(apps/web/src/keymap/command-registry.ts:283-285, menu entry at apps/web/src/features/editor/utils/text-menu.ts:85-87) which resolves through the LSP plugin. New work: run format against the live document and AWAIT completion before the write; define a timeout and a failure policy for files with no attached formatter (silent skip vs. blocked save); make it not break saveAllEditorDocuments' sequential loop (editor-save.ts:48-51) or the dirty-check short-circuit at :34. Scope note: resource is correct and matches VS Code, and it does not violate the plan's spawn/exec rule — the key selects WHEN to format, never a binary or env; the formatter process itself comes from`lsp.servers`, which is application/machine. |

### Notes

- VERIFICATION BASIS: every default above was read at fda1523 (confirmed via git rev-parse). Editor-package paths resolve through the symlink packages/editor-core -> /Users/shaul/Desktop/D/Editor/packages/editor; likewise packages/editor-react and packages/editor-gutters. The repo-relative paths in `evidence` are valid and openable from the worktree root.
- CONFIRMED: the editor is constructed with NO typography or layout options. useEditor at apps/web/src/features/editor/components/editor.tsx:108-125 passes exactly cursorLineHighlight, document, keymap, onChange, plugins, rowPositioning, theme. No fontFamily, fontSize, lineHeight, tabSize or wordWrap anywhere in apps/web (grep for tabSize/wordWrap in apps/web returns only the two keymap entries for the Alt+Z toggle command).
- CORRECTION TO THE PLAN (line 728): 'The plugin array is built once per mount, so minimap / scope-lines / syntax toggles need plugin-level enable flags or an editor remount' overstates the cost. `Editor.setPlugins()` exists (Editor.ts:991) and the host adds, removes AND disposes correctly (plugins.ts:584-603); the React wrapper already re-syncs the plugin array whenever its identity changes (packages/editor-react/src/index.ts:593-596, samePlugins :772-779). The actual blockers are (a) the `useMemo(..., [])` deps at editor.tsx:78-79 and (b) the module-level caches nonCriticalEditorPlugins / nonCriticalEditorPluginsPromise (editor-plugins.ts:82-83) plus the memoized URL set disabledFeatureSet (editor-performance-trace.ts:76). Six of the plugin keys (lineNumbers, folding, matchBrackets, occurrencesHighlight, links, syntax) become live with app-side edits only. Re-estimate Phase 5 downward for those.
- THE TWO KEYS THAT REALLY NEED NEW EDITOR-PACKAGE API: editor.tabSize (no setTabSize exists at all; captured private readonly at Editor.ts:264/:329 and written to --editor-tab-size once at virtualizedTextView.ts:313) and editor.renderLineHighlight (constructor-only cursorLineHighlight, frozen at virtualizedTextView.ts:251/970-978). editor.fontSize and editor.fontFamily need only a small passthrough — VirtualizedTextView.refreshMetrics() already exists (virtualizedTextView.ts:422) but Editor.view is private (Editor.ts:200), so it is one export, not a new algorithm. The plan lists tabSize but NOT renderLineHighlight as a sharp edge; add it.
- REMOUNT IS A CHEAPER FALLBACK THAN IT LOOKS, and worth stating explicitly in the plan so Phase 5 has an escape hatch: buffer, undo history and view session are all app-owned and passed in (EditorRenderDocument at apps/web/src/features/editor/editor-render-document.ts:3-9; DocumentSession owns the history), and scroll position is persisted independently (use-scroll-persistence-plugin.ts). Keying the Editor component on tabSize/fontSize loses mount cost and any in-editor find state, not the user's edits or undo stack.
- CROSS-REPO COST IS THE ACTUAL SCHEDULE RISK IN THIS GROUP. Every 'new editor-package setter' above is a change in a DIFFERENT repository (/Users/shaul/Desktop/D/Editor) consumed through a symlink + bun link + root overrides, which historically requires a Vite dev-server restart to pick up. Budget that per package edit: two setters (setTabSize, setCursorLineHighlight) + one passthrough (refreshMetrics) + optionally two wrapper option syncs (lineHeight, wordWrap) in packages/editor-react.
- SCOPE RATIONALE: nothing in editor.\* reaches process spawn, exec, env, or the keymap, so the plan's hard rule does not force application/machine here. I split window vs resource on implementability rather than taste: `resource` for values the editor genuinely holds per instance (tabSize, wordWrap, the six plugin toggles, renderLineHighlight, formatOnSave); `window` for values implemented as one app-wide CSS variable or one module-level singleton (fontFamily, fontSize, lineHeight, minimap, scopeLines, syntax, decode, scrollPersistence, diff.viewMode). The two documentRetention keys are `application` because retention is computed as a union across parked workspace ROOTS — a workspace-scoped value would govern other repos' documents.
- DELIBERATELY EXCLUDED, and they should stay URL-only: `?editorPerfDisable=caret` and `=text` are CSS-only debug instrumentation injected into a <style> tag (editor-performance-trace.ts:379-386) that make the caret static and the text transparent — instrument, not preference. `?editorPerfLayout=absolute-rows` (editor-performance-trace.ts:97-102, consumed at editor.tsx:107) is an A/B row-positioning experiment. `?editorPerfTrace` installs a global profiling handle (editor-performance-trace.ts:78-91). Registering any of these as settings would ship a footgun with no user-facing meaning. Note this leaves `?editorPerfDisable=` alive after Phase 5, so the plan's phrasing 'replaces the ?editorPerfDisable= URL gates' is only true for minimap / scope-lines / syntax / scroll-persistence.
- ADJACENT FREE WIN, not registered here because it is outside the assigned group but worth one line in the plan: `hiddenCharacters` is ALREADY a wrapper-synced option (packages/editor-react/src/index.ts:574-577, EditorOptions at packages/editor-core/src/editor/types.ts:87) and the app passes nothing. A whitespace/control-character rendering setting would be a true zero-package-work live passthrough — the only one of its kind in the editor namespace.
- UNIT TRAP FOR THE UI: `editor.lineHeight` in this package is a ROW HEIGHT IN PIXELS (--editor-row-height: 24px, style.css:77; normalizeRowHeight/setLineHeight take px), not a ratio. Label and validate it as pixels, or every user who types 1.5 gets a one-pixel line and files a bug.
- SECOND-WRITER GAP THE PLAN'S TABLE MISSES: `editor.action.toggleWordWrap` (Alt+Z, default-bindings.ts:330) mutates Editor state directly via setWordWrap (Editor.ts:421) and persists nothing. Once editor.wordWrap is a setting, that command is a second writer in exactly the same way as workspace.toggleDiffViewMode — add it to the plan's Commands-are-a-second-writer table before Phase 5.
- INERT-KEY WARNING: editor.documentRetention.byteBudget cannot be honored today at all (documentSizes is never supplied — document-retention.ts:82 vs editor-commands.ts:263-268). Either thread a size map first or drop the key. It is the only key in this group that would silently do nothing after being wired 'correctly'.

## advanced-internal-tail

| Key | Type | Default | Scope | Phase | Vis | OK | Wiring |
| ---------------------------------------------- | ------------------- | ------------------ | ----------- | ---------------- | ----------- | --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `workbench.tooltip.delay` | number | `600` | window | P3 | advanced | yes | PLUMBING. `<TooltipProvider delay={600}>` is a literal in the root render. `main.tsx` renders it _above_ `<App/>` but _inside_ `QueryClientProvider`, so a settings read is possible only via a tiny wrapper component that calls the settings hook and forwards `delay`. Live once wired — base-ui/Tooltip re-reads the prop. |
| `workbench.commandPalette.recentCommandsShown` | number | `6` | window | P3 | advanced | yes | PLUMBING. `RECENTLY_USED_COMMANDS_SHOWN` is module-private and read inside `groupedCommandItems`; that function is pure and takes args already, so thread the value as a parameter rather than importing settings into a util (AGENTS.md forbids stateful imports in utils/). |
| `workbench.commandPalette.recentCommandLimit` | number | `30` | machine | P3 | internal | yes | PLUMBING. `RECENT_COMMANDS_LIMIT` bounds the persisted list in the write path of a non-React store (`recentCommandIds()` / `useSyncExternalStore`). The store has no provider, so the value must be pushed into it (a `setRecentCommandsLimit()` setter called from a settings effect), not pulled. |
| `workbench.state.persistDebounce` | number | `350` | machine | P3 | internal | yes | PLUMBING. `WORKSPACE_CACHE_WRITE_DEBOUNCE_MS` is the localStorage write debounce for tabs/layout/root-folder. It is consumed inside a hook, so a settings read is direct; confirm whether the debouncer instance is recreated when the value changes or needs a key. |
| `workbench.state.retainedProjects` | number | `8` | machine | P3 | internal | yes | PLUMBING. `WORKSPACE_SLICE_LIMIT` caps how many projects keep their tabs across restarts. Exported and used at write time in `workspace-cache.ts` (a util module) — pass it in from the persistence hook instead of importing settings there. |
| `workbench.query.staleTime` | number | `10 * 1000` | application | P6 | internal ⟳ | yes | PLUMBING, but the QueryClient is constructed at module scope, so a settings-driven value either needs `queryClient.setDefaultOptions()` on change or a reload. NOT window scope: a hostile `.platform/settings.json` setting this to 0 turns every mounted query into a per-render request storm. |
| `workbench.query.gcTime` | number | `5 * 60 * 1000` | application | P6 | internal ⟳ | yes | PLUMBING. Same module-scope construction problem as `workbench.query.staleTime`; `setDefaultOptions()` or reload. |
| `workbench.query.retry` | number | `1` | application | P6 | internal ⟳ | yes | PLUMBING. Same as above. Application scope for the same reason — retry count multiplies request volume against the local server. |
| `files.snapshotCache.staleTime` | number | `5_000` | application | P6 | internal | yes | PLUMBING. `FILE_SNAPSHOT_STALE_MS` is returned from `fileSnapshotQueryOptions()`, which is called per use — live once the function takes the value as an argument or reads a pushed module value. |
| `files.snapshotCache.gcTime` | number | `2 * 60 * 1000` | application | P6 | internal | yes | PLUMBING. Same call site as `files.snapshotCache.staleTime`. Note `apps/web/src/lib/tests/file-snapshot-query-cache.test.ts:22` asserts the constant directly; that test must move to asserting the resolved setting. |
| `files.snapshotCache.limit` | number | `64` | application | P6 | internal ⟳ | yes | PLUMBING. `FILE_SNAPSHOT_QUERY_CACHE_LIMIT` is baked into the cache policy installed once by `installFileSnapshotQueryCachePolicy(queryClient)` at module scope in `query-client.ts:15` — changing it live means re-installing the policy, otherwise it is restart-only. |
| `files.tree.prefetchStaleTime` | number | `5_000` | application | P6 | internal | yes | PLUMBING. Consumed at `apps/web/src/hooks/use-workspace-tree.ts:81` and `:128`; both are hook call sites, so a settings read is direct. |
| `files.tree.prefetchMaxBytes` | number | `1_048_576` | application | P6 | internal | yes | PLUMBING. Ceiling on which files the tree will speculatively read; consumed alongside the stale time in `use-workspace-tree.ts`. |
| `files.watcher.eventBatchDelay` | number | `100` | application | P6 | internal | yes | PLUMBING. `EVENT_BATCH_DELAY_MS` coalesces the filesystem event stream before invalidation. Hook-local, so live. Application scope: it governs how hard an external writer can drive refetches. |
| `files.recentFolders.limit` | number | `40` | machine | P6 | advanced | yes | PLUMBING. `RECENT_FOLDER_LIMIT` bounds the recent-folders query result. Machine scope because the list is filesystem paths on this machine. |
| `files.recentFolders.staleTime` | number | `30_000` | machine | P6 | internal | yes | PLUMBING. `RECENT_FOLDERS_STALE_TIME_MS` at the `useQuery` call site in the same file. |
| `files.maxTextFileBytes` | number | `209_715_200` | application | P6 | advanced ⟳ | yes | PLUMBING, and the only key in this group that is ALREADY overridable — via the `MAX_TEXT_FILE_BYTES` env var, parsed in `resolveMaxTextFileBytes()` at `apps/server/src/fs/service.ts:70` with an upper bound of 2_147_483_647 at `:68` and a `fs.invalid_max_text_file_bytes` process warning on bad input. Registering it means the registry becomes the source and the env read becomes a fallback (or is deleted). Note the identical constant is duplicated at `apps/server/src/git/service.ts:110` — register once, feed both. Server-side, resolved at service construction, so restart. |
| `git.status.staleTime` | number | `1000` | application | P6 | internal | yes | PLUMBING. Inline literal at the `useQuery` call. APPLICATION not window per the hard rule: this is the client-side rate limiter on a route that spawns `git status`, and a workspace-settable 0 is a spawn storm from cloned-repo input. |
| `git.status.serverCacheTtl` | number | `1_000` | application | P6 | internal ⟳ | yes | PLUMBING. `STATUS_CACHE_TTL_MS` is the server half of the same throttle (capacity 2_048 at `:119`). Module-scope const consumed by the cache the `GitService` builds, so restart unless the cache grows a setter. Application scope — directly gates `git` process spawns. |
| `git.branchRemoteState.staleTime` | number | `2_000` | application | P6 | internal | yes | PLUMBING. Inline literal at the `useQuery` call. Same spawn-rate argument as `git.status.staleTime`. |
| `git.pullRequest.pollInterval` | number | `60_000` | application | P6 | advanced | yes | PLUMBING. `PULL_REQUEST_POLL_MS` feeds BOTH `refetchInterval` (:19) and `staleTime` (:23) — one setting, two consumers, keep them tied. Advanced rather than internal: this spawns `gh` (network) on a timer and a user on a metered link will want it. Application scope is mandatory — it drives external process spawn. |
| `git.pullRequest.timeout` | number | `20_000` | application | P6 | internal | yes | PLUMBING. `GH_TIMEOUT_MS` bounds the `gh` child process. Server-side module const applied per spawn, so live once threaded through the service. Application scope — literally a process timeout. |
| `git.pullRequest.supportCacheTtl` | number | `60_000` | application | P6 | internal | yes | PLUMBING. `SUPPORT_CACHE_TTL_MS` caches the two `gh` probe processes per cwd (map at `:27`). Trimming candidate if the group needs to shrink. |
| `git.upstreamFetch.interval` | number | `15_000` | application | P6 | advanced ⟳ | yes | PLUMBING. `UPSTREAM_FETCH_INTERVAL_MS` is the default for the `AsyncThrottler` the scheduler constructs (options type already accepts overrides — `failureCooldownMs` at `:12`), so it is restart-scoped unless the scheduler is rebuilt. Application: it schedules background `git fetch` to a remote. |
| `git.upstreamFetch.failureCooldown` | number | `5_000` | application | P6 | internal ⟳ | yes | PLUMBING. `UPSTREAM_FETCH_FAILURE_COOLDOWN_MS`; already an injectable option on `UpstreamFetchSchedulerOptions`, so this is the cheapest key in the group to wire — the parameter exists, only the default needs to come from the registry. |
| `git.invalidationThrottle` | number | `2_000` | application | P6 | internal | yes | PLUMBING. `GIT_INVALIDATION_THROTTLE_MS` is a leading+trailing throttle so a runaway file-event stream cannot refetch git status per batch. Hook-local. Application scope — same spawn-rate rule. |
| `editor.documentRetention.projectLimit` | number | `3` | application | P5 | internal | yes | PLUMBING. `DEFAULT_PROJECT_LIMIT` is ALREADY a named parameter (`projectLimit` at `:30`) of `retentionForProjects`, so wiring is supplying it from the caller in `apps/web/src/features/editor/state/editor-commands.ts` rather than editing the util. How many projects keep their open documents alive across a switch. |
| `editor.documentRetention.byteBudget` | number | `64 * 1024 * 1024` | application | P5 | advanced | yes | PLUMBING. `DEFAULT_BYTE_BUDGET`, already a named parameter (`byteBudget` at `:28`). The second of the two deliberate ceilings — the file's own comment explains why a project count alone is defeated by one 100MB file. Advanced because it is the memory knob a user with huge files will actually reach for. |
| `editor.history.maxEntries` | number | `50` | application | P5 | internal | yes | PLUMBING. `MAX_EDITOR_HISTORY` bounds the MRU path list behind editor back/forward and tab reordering. Module-private in a state module; needs an argument or a pushed value. |
| `editor.colorTheme.previewSettleDelay` | number | `60` | application | P5 | internal | yes | PLUMBING. `PREVIEW_SETTLE_MS` is the debounce that stops a pointer-scrub down the theme list from queueing a shiki re-tokenize (~120ms/document) per row. The comment calls it 'the floor on how fast a preview can feel' — register it internal, do not surface it. |
| `editor.performanceTrace.maxEvents` | number | `5000` | machine | P5 | internal | yes | PLUMBING. `MAX_TRACE_EVENTS` caps the in-memory editor perf trace ring. Same module owns the `editorPerfTrace`/`editorPerfDisable`/`editorPerfLayout` URL params (`:67-69`) that Phase 5 is replacing with real settings — wire this key in the same pass so the URL-gate removal touches one file once. |
| `terminal.integrated.detachedSessionTtl` | number | `10 * 60 * 1000` | application | P5 | advanced ⟳ | yes | PLUMBING, and already an injectable constructor option — `TERMINAL_DETACHED_TTL_MS` is only the default for `detachTtlMs` at `:74`. How long a detached PTY child is kept alive before reaping. MUST be application/machine per the hard rule: this holds real child processes open. |
| `search.debounce` | number | `180` | application | P6 | advanced | yes | PLUMBING. `SEARCH_DEBOUNCE_MS` feeds `useDebouncedValue` from @tanstack/react-pacer. APPLICATION not window: search streams through the server and spawns `rg`, so a workspace-settable 0 is one ripgrep per keystroke from hostile input. |
| `search.maxResults` | number | `200` | application | P6 | advanced | yes | PLUMBING. `SEARCH_LIMIT`. Note the SAME number is duplicated as `MAX_DISK_SEARCH_LIMIT` at apps/web/src/features/search/search-providers.ts:19 — register one key and feed both, or they will drift. Application scope (bounds the `rg` result stream). |
| `search.quickOpen.limit` | number | `80` | application | P6 | internal | yes | PLUMBING. `SEARCH_LIMIT` in the file picker — a different limit from `search.maxResults` despite the identical local name. Distinct key on purpose. |
| `search.quickOpen.scopeTimeout` | number | `6000` | application | P6 | internal | yes | PLUMBING. `SEARCH_SCOPE_TIMEOUT_MS` bounds how long the picker waits on a search scope before giving up. The stream source is already injectable in this file, so wiring is contained. |
| `chat.cache.messageLimit` | number | `2_000` | application | P6 | internal | yes | PLUMBING. `CHAT_MESSAGE_CACHE_LIMIT`. Mirrors the server ceiling `MAX_THREAD_MESSAGES = 2_000` at apps/server/src/orchestration/read-model.ts:19 — register the client key and either register the server one too or assert they match in a test; a client cap above the server cap is silently dead. |
| `chat.cache.activityLimit` | number | `500` | application | P6 | internal | yes | PLUMBING. `CHAT_ACTIVITY_CACHE_LIMIT`; server twin `MAX_THREAD_ACTIVITIES = 500` at apps/server/src/orchestration/read-model.ts:20. |
| `chat.cache.checkpointLimit` | number | `500` | application | P6 | internal | yes | PLUMBING. `CHAT_CHECKPOINT_CACHE_LIMIT`; server twin `MAX_THREAD_CHECKPOINTS = 500` at apps/server/src/orchestration/read-model.ts:21. |
| `chat.cache.proposedPlanLimit` | number | `200` | application | P6 | internal | yes | PLUMBING. `CHAT_PROPOSED_PLAN_CACHE_LIMIT`. |
| `chat.threadDetail.maxSubscriptions` | number | `32` | application | P6 | internal | yes | PLUMBING. `MAX_CACHED_THREAD_DETAIL_SUBSCRIPTIONS` — how many live thread-detail sockets are kept warm. |
| `chat.threadDetail.idleEviction` | number | `15 * 60 * 1000` | application | P6 | internal | yes | PLUMBING. `THREAD_DETAIL_SUBSCRIPTION_IDLE_EVICTION_MS`. |
| `chat.threadDetail.prewarmLimit` | number | `10` | application | P6 | internal | yes | PLUMBING. `SIDEBAR_THREAD_DETAIL_PREWARM_LIMIT` — how many sidebar threads get their detail fetched speculatively. |
| `chat.projectionCache.threadLimit` | number | `200` | machine | P6 | internal | yes | PLUMBING. `CHAT_PROJECTION_CACHE_THREAD_LIMIT`. One of four ceilings on the localStorage boot snapshot (`platform.chat-projection`); the file's own comment budgets ~5MB of localStorage shared with drafts and workspace state. Machine scope — it bounds per-browser storage, never syncs. |
| `chat.projectionCache.transcriptLimit` | number | `3` | machine | P6 | internal | yes | PLUMBING. `CHAT_PROJECTION_CACHE_TRANSCRIPT_LIMIT`. |
| `chat.projectionCache.messageLimit` | number | `40` | machine | P6 | internal | yes | PLUMBING. `CHAT_PROJECTION_CACHE_MESSAGE_LIMIT` — only the visible head of a transcript is persisted; the detail stream replaces the slice on connect. |
| `chat.projectionCache.activityLimit` | number | `40` | machine | P6 | internal | yes | PLUMBING. `CHAT_PROJECTION_CACHE_ACTIVITY_LIMIT`. |
| `chat.projectionCache.persistInterval` | number | `500` | machine | P6 | internal | yes | PLUMBING. `CHAT_PROJECTION_CACHE_PERSIST_MS`. Deliberately a THROTTLE, not a debounce (a streaming turn mutates faster than any debounce window) — the descriptor description must say so or someone will 'fix' it. |
| `chat.rail.collapsedProjectLimit` | number | `200` | machine | P6 | internal | yes | PLUMBING. `MAX_COLLAPSED_PROJECTS`, applied as a `.slice(0, …)` on write at `:43` into `platform.chat-rail-collapse.v1`. Pure storage cap. |
| `chat.sessionRead.entryLimit` | number | `300` | machine | P6 | internal | yes | PLUMBING. `MAX_SESSION_READ_ENTRIES`, applied at `:52`/`:57` to the `platform.chat-session-reads.v1` read-marker map. |
| `chat.sessionRead.persistDebounce` | number | `300` | machine | P6 | internal | yes | PLUMBING. `SESSION_READ_PERSIST_DEBOUNCE_MS` — the write debounce paired with the cap above. |
| `chat.diffScope.entryLimit` | number | `200` | machine | P6 | internal | yes | PLUMBING. `THREAD_DIFF_SCOPE_LIMIT`; already a defaulted parameter (`limit = THREAD_DIFF_SCOPE_LIMIT` at `:93`), so wiring is supplying it at the call site. Backs `platform.chat-thread-diff-scope.v1`. |
| `chat.changedFiles.expansionLimit` | number | `200` | machine | P6 | internal | yes | PLUMBING. `CHAT_CHANGED_FILES_EXPANSION_LIMIT`; already a defaulted parameter at `:81`. Backs `platform.chat-changed-files-expansion.v1`. |
| `chat.inputDraft.persistDebounce` | number | `300` | machine | P6 | internal | yes | PLUMBING. `CHAT_INPUT_DRAFT_PERSIST_DEBOUNCE_MS` — how long typed-but-unsent text sits before it survives a reload. This one is a data-loss window, so it deserves a real description even at internal visibility. |
| `chat.promptStash.maxEntries` | number | `20` | machine | P6 | advanced | yes | PLUMBING. `MAX_PROMPT_STASH_ENTRIES`, applied as `.slice(0, …)` at `:120` — newest first, oldest silently evicted. Advanced rather than internal because eviction destroys user-authored text; a user who parks a lot of prompts has a real reason to raise it. |
| `chat.markdownHighlightCache.maxEntries` | number | `300` | application | P6 | internal ⟳ | yes | PLUMBING, restart-only as written: `MAX_HIGHLIGHT_ENTRIES` is a constructor argument to a module-scope `new ByteBoundedLru(...)` singleton at `:14`. Live resize needs a setter on `ByteBoundedLru`. |
| `chat.markdownHighlightCache.maxBytes` | number | `24 * 1024 * 1024` | application | P6 | internal ⟳ | yes | PLUMBING, same module-scope singleton. The byte bound is the primary one (a diff-heavy block outweighs thousands of one-line snippets), so if only one of the pair is registered, register this one. |
| `chat.providerList.staleTime` | number | `60_000` | application | P6 | internal | yes | PLUMBING. `PROVIDER_LIST_STALE_TIME_MS` at the `useQuery` options in the same file (`:21`). Application scope: the `/providers` route probes provider binaries. |
| `chat.providerList.gcTime` | number | `30 * 60_000` | application | P6 | internal | yes | PLUMBING. `PROVIDER_LIST_GC_TIME_MS` (`:17`). |
| `chat.authStatus.staleTime` | number | `15_000` | application | P6 | internal | yes | PLUMBING. `AUTH_STATUS_STALE_TIME_MS` (`:66`). Note `:8` also holds `LOGIN_POLL_INTERVAL_MS = 1_000`, which drives a `refetchInterval` during an active login (`:80`) — register it too if the group is allowed one more key. Application scope: this polls a route that shells out to `claude auth`. |
| `chat.commandCatalog.staleTime` | number | `5 * 60_000` | application | P6 | internal | yes | PLUMBING. `COMMAND_CATALOG_STALE_TIME_MS` (`:68`) — freshness of the composer's slash-command/skill catalog, which is read off disk by the server. |
| `chat.commandCatalog.gcTime` | number | `30 * 60_000` | application | P6 | internal | yes | PLUMBING. `COMMAND_CATALOG_GC_TIME_MS` (`:64`). Trimming candidate. |
| `chat.projectEntry.searchDebounce` | number | `140` | application | P6 | internal | yes | PLUMBING. `PROJECT_ENTRY_SEARCH_DEBOUNCE_MS` (already exported). Application scope — it paces a server-backed search. |
| `chat.sessionSearch.debounce` | number | `220` | application | P6 | internal | yes | PLUMBING. `SESSION_SEARCH_DEBOUNCE_MS` (already exported); paired with `SESSION_SEARCH_STALE_TIME_MS = 30_000` at `:22`, which is worth a sibling key. |
| `lsp.idleTimeout` | number | `120_000` | application | P6 | advanced ⟳ | yes | PLUMBING. `DEFAULT_IDLE_TIMEOUT_MS` governs how long a pooled LSP proxy session (a real language-server child process, pooled in the module-scope `pooledSessions` map at `:77`) survives with no client. MUST be application/machine per the hard rule — it is process lifetime. Restart-scoped as written because the pool is module state. |
| `lsp.diagnosticDelay` | number | `150` | application | P6 | advanced | yes | PLUMBING, and the cheapest LSP key to wire: `DEFAULT_DIAGNOSTIC_DELAY_MS` is only the default for `diagnosticDelayMs`, already declared on `TypeScriptLspSessionOptions` at `:47`. How long after a keystroke diagnostics are recomputed — genuinely user-visible, hence advanced not internal. |
| `providers.auth.defaultMethod` | enum(subscription\\ | console\\ | sso) | `'subscription'` | application | P7 | advanced | yes | PLUMBING. `DEFAULT_PROVIDER_AUTH_METHOD` selects which radio the sign-in dialog opens on; the union is `PROVIDER_AUTH_METHODS` at `:49` and the contract type is `providerSignInMethodSchema` at packages/contracts/src/provider.ts:177 — reuse that schema in the descriptor rather than re-declaring the union. APPLICATION scope is mandatory: each method maps to a distinct shell command (`claude auth login --console` / `--sso` / `--claudeai`, METHOD_COPY at `:57-70`) that gets spawned, so this reaches process exec and a workspace file must never set it. An SSO-first org will change this once and never again. |

### Notes

- ALL 68 keys above were verified by opening the defining file at fda1523 today. No key in this group is marked verified:false — every default listed exists in code at the cited line. Nothing here is a NEW FEATURE; the entire group is PLUMBING (hard-coded constants becoming registry-backed), which is why it is cheap despite its size.
- SCOPE JUDGEMENT — the hard rule bit harder than expected. Nine keys that read like harmless UI tuning actually pace process spawn and are therefore application, never window: git.status.staleTime / git.status.serverCacheTtl / git.branchRemoteState.staleTime (git status), git.pullRequest.pollInterval / .timeout / .supportCacheTtl (gh), git.upstreamFetch._ (git fetch), search.debounce / search.maxResults / search.quickOpen._ (rg), lsp.idleTimeout / lsp.diagnosticDelay (language-server children), terminal.integrated.detachedSessionTtl (PTY children), providers.auth.defaultMethod (claude auth login). A cloned repo setting search.debounce to 0 is one ripgrep per keystroke; setting git.status.staleTime to 0 is a git-status storm. I also put the plain TanStack Query defaults (workbench.query._, files.snapshotCache._) at application on the same reasoning one step removed — they all resolve to local-server requests, and a workspace-settable 0 is a self-inflicted DoS from hostile input. The only window-scoped keys I kept are the three that touch nothing but pixels: workbench.tooltip.delay and workbench.commandPalette.recentCommandsShown.
- SCOPE JUDGEMENT — machine vs application for storage caps. Every cap that bounds a localStorage payload (chat.projectionCache._, chat.rail._, chat.sessionRead._, chat.diffScope._, chat.changedFiles._, chat.promptStash._, chat.inputDraft._, workbench.state._, workbench.commandPalette.recentCommandLimit, files.recentFolders.\*, editor.performanceTrace.maxEvents) is machine, because it budgets a per-browser ~5MB quota that has no meaning on another machine and must never sync. If the plan would rather not spend `machine` on these, `application` is defensible for all of them — but do it uniformly, not case by case.
- PHASE ASSIGNMENT — the plan's Phase 3 is specifically the ten-key appearance batch, so I did not overload it. I assigned by where the CONSUMER lives, which is what the plan says the batches are actually for: workbench chrome + workspace-state persistence -> 3; editor + terminal -> 5; everything server-adjacent or infra (query client, files, search, chat, lsp, git) -> 6; providers -> 7. NOTE A GAP: git._ is a declared namespace in the Registry payload section but is named in NO phase batch (Phase 6 is 'files / search / chat / lsp / telemetry / window'). I put the seven git._ keys in Phase 6; the plan should either add git to that batch's name or say where it goes.
- TWO DUPLICATED CONSTANTS THAT WILL DRIFT IF REGISTERED CARELESSLY. (1) DEFAULT_MAX_TEXT_FILE_BYTES = 209_715_200 is defined TWICE — apps/server/src/fs/service.ts:66 and apps/server/src/git/service.ts:110. One key, two consumers. (2) The 200-result search cap is defined twice under two names — SEARCH_LIMIT at search-buffer-query.ts:4 and MAX_DISK_SEARCH_LIMIT at search-providers.ts:19. Same fix. Separately, the picker's own SEARCH_LIMIT = 80 (picker-search.ts:7) shares the local name but is a genuinely different limit — I gave it its own key on purpose.
- CLIENT/SERVER CEILING PAIRS. chat.cache.messageLimit / .activityLimit / .checkpointLimit (2_000 / 500 / 500) each have an exact server twin — MAX_THREAD_MESSAGES / MAX_THREAD_ACTIVITIES / MAX_THREAD_CHECKPOINTS at apps/server/src/orchestration/read-model.ts:19-21. Raising only the client key is silently dead: the server never sends more. Either register both halves under one id resolved on both sides, or add a test asserting they match.
- files.maxTextFileBytes IS THE ONLY KEY IN THIS GROUP THAT IS ALREADY OVERRIDABLE — via the MAX_TEXT_FILE_BYTES env var, with a real parser, an upper bound (2_147_483_647) and a process warning on bad input (apps/server/src/fs/service.ts:68-80). Registering it is a migration of an existing override channel, not a new one; decide explicitly whether the env var stays as a fallback or is deleted (greenfield rule says delete).
- THE CHEAPEST KEYS TO WIRE — six already have an injectable parameter and only need the default rerouted: editor.documentRetention.projectLimit / .byteBudget (named params of retentionForProjects), terminal.integrated.detachedSessionTtl (detachTtlMs ctor option), lsp.diagnosticDelay (diagnosticDelayMs on TypeScriptLspSessionOptions), git.upstreamFetch.failureCooldown (failureCooldownMs option), chat.diffScope.entryLimit and chat.changedFiles.expansionLimit (defaulted params). Start the batch here.
- THE MOST AWKWARD KEYS TO WIRE — five are captured at module scope and are restart-only unless a setter is added: workbench.query.{staleTime,gcTime,retry} (new QueryClient() at import time; needs queryClient.setDefaultOptions on change), files.snapshotCache.limit (baked into the policy installed once by installFileSnapshotQueryCachePolicy), chat.markdownHighlightCache.{maxEntries,maxBytes} (constructor args to a module-scope ByteBoundedLru singleton), lsp.idleTimeout (module-scope pooledSessions map), git.status.serverCacheTtl / git.upstreamFetch.interval (server caches/throttlers built at service construction). I marked all of these requiresRestart: true rather than pretending they are live.
- workbench.tooltip.delay has a structural wrinkle: <TooltipProvider delay={600}> lives in main.tsx ABOVE <App/> and cannot call a settings hook there. It sits inside QueryClientProvider, so the fix is a one-line wrapper component that reads the setting and forwards delay — not prop-drilling, and not moving TooltipProvider.
- EXCLUDED DELIBERATELY — pure algorithm constants, not settings, and registering them would be noise: all fuzzy-ranking offsets and penalties in chat/lib/model-picker-search.ts:20-33 and chat/lib/composer-command-search.ts:21-40; every ASCII codepoint and the three 4096-entry memo caps in features/search/search-sort.ts:5-17; the FNV hash seeds in search-result-items.ts:38-40; layout geometry (SIDEBAR*MIN/MAX_SIZE, BOTTOM_MIN/MAX_SIZE in workbench/utils/workbench-panels.ts:16-19, SESSION_RAIL*_ and TOOL*PANE*_ in chat-mode/utils/panels.ts:26-31, SEARCH_RESULT_CHARACTER_WIDTH and friends in search-results-view.tsx:38-42); duration arithmetic (chat-formatters.ts:17-20); CACHE_VERSION = 16 and every \*\_STORAGE_VERSION (schema versions, not preferences — bumping one is a code change with a drop policy); and the orchestration/reactor dedup ceilings (HANDLED_EVENT_ID_MAX, SEEN_RUNTIME_EVENT_ID_MAX, RETAINED_EVENT_LIMIT), which are correctness bounds on an event-sourced pipeline and should not be user-reachable at any visibility.
- EXCLUDED AS BELONGING TO ANOTHER GROUP, NOT AS UNREGISTERABLE: terminal scrollback (10_000, apps/web/src/features/terminal/terminal-panel.tsx:371) is a real user-visible terminal.integrated.\* setting for the Phase 5 batch, not an internal constant. Same for the theme key and the editor color theme.
-
- === STORAGE / RESET SECTION — localStorage keys that are UI MEMORY, not settings. These get clear-actions, NOT registry entries. ===
- platform.workspace-state.v16.\* — apps/web/src/lib/workspace-cache.ts:41-62. The big one: eight named keys (chatModePanels, chatModeSelection, diffViewMode, rootFolder, uiMode, wallpaperHidden, workbenchLayout, workspaces) plus two unbounded prefixes, `…v16.workspace:<root>` (per-project open tabs/documents, capped at 8 by WORKSPACE_SLICE_LIMIT) and `…v16.search:<root>` (per-project search buffers). A clear-action must enumerate by prefix, not by the fixed key list, or the per-project slices survive the reset. CAUTION: three members of this namespace — uiMode, wallpaperHidden, diffViewMode — become Phase 3 REGISTRY keys and must be deleted from the cache in that same pass, or the same value has two homes and last-writer wins.
- platform.chat-projection — apps/web/src/features/chat/state/chat-projection-cache.ts:26. Pure boot-paint cache; the detail stream replaces it on connect. Safest single clear-action on the page.
- platform.command-palette.recent-commands.v1 — apps/web/src/components/command-palette/state/recent-commands-store.ts:3. Command MRU ranking. Clearing it is 'forget what I have run'.
- platform.chat-rail-collapse.v1 — apps/web/src/features/chat-mode/utils/rail-collapse-storage.ts:4. Which project groups are collapsed in the session rail.
- platform.chat-session-reads.v1 — apps/web/src/features/chat-mode/utils/session-read-storage.ts:6. Per-thread read markers. Clearing it marks everything unread — the clear-action label should say so.
- platform.chat-thread-diff-scope.v1 — apps/web/src/features/chat/utils/thread-diff-scope-storage.ts:4. Per-thread diff scope selection.
- platform.chat-changed-files-expansion.v1 — apps/web/src/features/chat/lib/chat-changed-files-expansion-storage.ts:3. Which changed-file groups are expanded.
- platform.chat-input-drafts.v1 — apps/web/src/features/chat/lib/chat-draft-storage.ts:9. USER-AUTHORED TEXT, not UI memory in the disposable sense. Clearing it destroys unsent messages. It belongs in the Storage section but needs a confirm, and it must NOT be swept by a single 'Reset all local state' button without one.
- platform:prompt-stash:v1 — apps/web/src/features/chat/state/prompt-stash-store.ts:10. Also USER-AUTHORED TEXT (prompts parked with Mod+S), same confirm requirement. Note the key uses COLONS while every other key in the app uses dots — it is the odd one out and worth normalizing in the same pass that builds this section, given the greenfield no-migration rule.
- NOT UI memory, do not put these in Storage/Reset: `theme` (apps/web/src/components/theme-provider.tsx:76 — the unprefixed, unversioned app theme key the plan already calls out for deletion in Phase 3) and `platform.editor-color-theme.v1` (apps/web/src/features/editor/state/editor-color-theme-store.ts:30 — a real editor appearance setting). Both are settings that currently live in localStorage; they move into the registry, they do not get a clear-button.
- IGNORE: `platform-message-bubble-browser-theme` (apps/web/src/features/chat/components/message-bubble.browser.tsx:18) is a Playwright test fixture key, not shipped state.
- There is NO sessionStorage usage anywhere in apps/web/src — the Storage section only has to account for localStorage.

## git._ + window._

| Key | Type | Default | Scope | Phase | Vis | OK | Wiring |
| --------------------------------- | ------------------ | -------------------- | --------------- | ------- | ----------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `git.enabled` | boolean | `true` | application | P6 | user | **?** | PLUMBING (gate does not exist). Nothing is conditional today: the panel mounts unconditionally (git-changes-panel.tsx:9 and chat-mode/components/tool-pane.tsx:136), the status query is gated only on rootPath (features/git/hooks/use-status.ts:8), and tree decorations always render (file-tree/components/files-pane.tsx:10-16). Needs one gate feeding all three. APPLICATION, not window: the value decides whether git subprocesses spawn for a workspace at all, and a cloned repo setting git.enabled:false in .platform/settings.json would hide its own dirty state from you — VS Code's window-scoped git.enabled is exactly that footgun. verified:false — `true` is inferred from unconditional behaviour; no literal default exists in code. |
| `git.autofetch` | boolean | `true` | application | P6 | user | **?** | PLUMBING. Autofetch already exists and is always on: every status read fires `void this.upstreamFetch.schedule(...)` (service.ts:705), which throttles a real `git fetch <remote>` (runFetch at service.ts:176, executed via UpstreamFetchScheduler in git/upstream-fetch.ts:75-97). Contradicts docs/editor-parity-gap-matrix.md:223, which lists Auto-fetch as missing — the periodic fetch exists, only the setting and the all-repos/current choice do not. Reaches process spawn → application. verified:false — no literal default; on is unconditional. |
| `git.autofetchPeriod` | number | `15_000` | application | P6 | advanced | yes | PLUMBING, and the seam already exists: UpstreamFetchSchedulerOptions.intervalMs (upstream-fetch.ts:13,35) is simply never passed at the one construction site (service.ts:170-178). Milliseconds in code; if the UI shows seconds, convert at the widget, not in the registry. Live-changeable without restart because the throttler evaluates `wait: () => ...` per execution (upstream-fetch.ts:93) — but throttlers are cached per (common dir, remote) in a Map (upstream-fetch.ts:27,95), so the value has to be read through a closure, not captured in the constructor. |
| `git.autofetchFailureCooldown` | number | `5_000` | application | P6 | internal | yes | PLUMBING via the existing UpstreamFetchSchedulerOptions.failureCooldownMs (upstream-fetch.ts:34). Shortens the window after a failed fetch so a flaky remote retries sooner. Paces a network subprocess → application. |
| `git.confirmDiscard` | boolean | `true` | application | P6 | user | **?** | NEW FEATURE — there is no confirmation anywhere. Discard fires straight from the click handler (group-actions.tsx:45,76 → hooks/use-discard-paths-mutation.ts:11-16, use-discard-staged-paths-mutation.ts, use-discard-path-mutation.ts) and the app has no confirm primitive at all (the only window.confirm in the codebase is features/chat/components/chat-view.tsx:386). Ships with the dialog or not at all. APPLICATION despite being a UI dialog: the value decides whether a destructive `git restore`-class command runs unprompted, so a cloned repo must not be able to set it. verified:false — `true` is the intended new default, no default exists in code. |
| `git.confirmSync` | boolean | `true` | application | P6 | user | **?** | NEW FEATURE. Sync (pull+push) runs directly from the mutation with no prompt (use-sync-changes-mutation.ts:12, surfaced in features/git/components/header.tsx). Same application-scope reasoning as git.confirmDiscard: it gates an unprompted network mutation of the repo. verified:false — no default in code. |
| `git.confirmForcePush` | boolean | `true` | application | P6 | user | **?** | NEW FEATURE **and inert today** — force push does not exist. GitService.push builds only `['push']` or `['push','--set-upstream','origin',branch]` (service.ts:553-560); no route, contract, or UI carries a force flag (the only `force: v.optional(v.boolean(), false)` in git/contracts.ts:95 is worktree _removal_, unrelated). The plan forbids registering a key inert, so this key must land in the same commit as a force-push command or be dropped from the batch. Listed because docs/editor-parity-gap-matrix.md:223 names it in the missing git settings surface. verified:false. |
| `git.enableSmartCommit` | boolean | `false` | application | P6 | user | **?** | NEW FEATURE on an existing command. Commit runs `['commit','-m',message]` with no `-a` and no auto-stage (service.ts:436; the streaming twin at service.ts:467 is identical), so committing with an empty index just fails today. `false` preserves that. Reaches argv → application. verified:false — no default in code, the behaviour is simply absent. |
| `git.decorations.enabled` | boolean | `true` | window | P6 | user | **?** | PLUMBING. Decorations are unconditional: files-pane.tsx:11-16 always maps status into `gitStatusEntries` and hands them to TreePane (tree-pane.tsx:55,70,147), which renders the badges from features/git/status-symbols.ts. **The only git key that may be `window` scope** — its value decides whether A/M/D/U badges paint and reaches no argv, no env, no keymap. A hostile workspace file toggling it can hide badges but cannot cause or suppress an exec. verified:false — `true` is inferred from unconditional rendering. |
| `git.untrackedFiles` | enum(all\\ | normal\\ | no) | `'all'` | application | P6 | advanced | yes | PLUMBING into argv. The panel's status read hardcodes `--untracked-files=all` (service.ts:702). Two other `git status` call sites must NOT take this setting: service.ts:775 uses `--untracked-files=no` because it only needs branch info, and service.ts:798 uses `all` for pathspec/rename resolution — both are correctness reads, not preferences. Also note client-side grouping treats `index:'untracked'` specially (features/git/utils/change-rows.ts:24-30), so `no` must not strand that branch. Reaches argv → application. |
| `git.diff.contextLines` | number | `3` | application | P6 | advanced | yes | PLUMBING into argv at five sites, all hardcoded `--unified=3`: service.ts:214 (panel diff), :273 (blob/checkpoint diff), :945 (untracked synthesis), :1095 (object-id pair diff), plus the parallel path in the same block. Thread one resolved value or they drift. Reaches argv → application. |
| `git.diff.ignoreWhitespace` | boolean | `false` | application | P6 | advanced | yes | PLUMBING, with a live conflict to resolve first. Server default is `input.ignoreWhitespace ?? false` (service.ts:262 → `--ignore-all-space` at :274) and the routes agree (orchestration/routes.ts:140,156). But the chat checkpoint client hardcodes `true` at three call sites (features/chat/lib/checkpoint-diff-query.ts:46,59,148), and the git panel's own diff path (service.ts:207-217) does not accept the flag at all. Decide: one key that both paths honour, or an explicit exemption for checkpoint diffs. Reaches argv → application. |
| `git.path` | string | `'git'` | machine | P6 | advanced | yes | PLUMBING. argv[0] is the bare literal `'git'` in two places — runProcess (process.ts:82) and the spawn in runBoundedProcess (process.ts:260) — so the binary is always PATH-resolved with no override. MACHINE scope by the plan's own definition (paths, binaries, never synced); a workspace file naming a `git` binary would be arbitrary code execution from a cloned repo. Empty string should mean 'resolve from PATH', matching providerInstanceConfigSchema.binaryPath in packages/contracts/src/settings.ts. |
| `git.baseBranchCandidates` | string[] | `['main', 'master']` | application | P7 | advanced | yes | PLUMBING. `DEFAULT_BASE_BRANCH_CANDIDATES` is the ordered fallback when neither `branch.<name>.platform-base` nor the remote's HEAD answers (base-refs.ts:8,23-25); order is load-bearing (main before master). Phase 7 rather than 6 only because it is list-typed and `advanced` visibility means it needs the list editor. Names become git ref arguments → application scope. |
| `git.localTimeout` | number | `30_000` | application | P6 | internal | yes | PLUMBING, per-call so no restart: runProcess resolves `input.timeoutMs ?? defaultTimeoutMs(input.args)` on every invocation (process.ts:83). Budget for local verbs (status, diff, rev-parse, cat-file). Bounds a spawned process → application. |
| `git.networkTimeout` | number | `120_000` | application | P6 | internal | yes | PLUMBING, per-call. Applies to the NETWORK_ACTIONS set (clone, fetch, ls-remote, pull, push, remote — process.ts:28) and is also the fallback for non-git bounded spawns such as `gh` (process.ts:95). Bounds a spawned process → application. |
| `git.maxOutputBytes` | number | `10_000_000` | application | P6 | internal | yes | PLUMBING. Per-command output budget; exceeding it drops the partial output and raises git.OUTPUT_LIMIT_EXCEEDED (process.ts:38-44). GitService also carries its own copy via options.maxCommandOutputBytes defaulting to this constant (service.ts:158), so one key must feed both. Bounds server heap for a spawned process → application. |
| `git.diffConcurrency` | number | `4` | application | P6 | internal ⟳ | yes | PLUMBING; the option already exists (GitServiceOptions.diffConcurrency, consumed at service.ts:157 and used by mapWithConcurrency at :223). Captured in the constructor, so a live change needs a setter or a service rebuild — mark requiresRestart until one exists. Controls how many git subprocesses run at once → application. |
| `git.maxTextFileBytes` | number | `209_715_200` | application | P6 | internal ⟳ | yes | PLUMBING via the existing options.maxTextFileBytes (service.ts:159). Constructor-captured → requiresRestart. 200 MiB; decides what git considers renderable text rather than binary. |
| `git.statusCacheTtl` | number | `1_000` | application | P6 | internal ⟳ | yes | PLUMBING via the existing options.statusCacheTtlMs (service.ts:168). Collapses one burst of pane polls into one `git status`; raising it also delays autofetch scheduling, which hangs off the same read (service.ts:705). Constructor-captured in BoundedTtlCache → requiresRestart. |
| `git.statusCacheCapacity` | number | `2_048` | application | P6 | internal ⟳ | yes | PLUMBING; no option exists today, the constant is passed directly as BoundedTtlCache capacity (service.ts:166). Constructor-captured → requiresRestart. |
| `git.repositoryCacheTtl` | number | `60_000` | application | P6 | internal ⟳ | yes | PLUMBING via the existing options.repositoryCacheTtlMs (service.ts:163). Caches which repo root owns a path, misses included, so a non-repository folder cannot turn every poll into a `rev-parse`. Constructor-captured → requiresRestart. |
| `git.repositoryCacheCapacity` | number | `512` | application | P6 | internal ⟳ | yes | PLUMBING; passed directly as BoundedTtlCache capacity (service.ts:161), no option seam yet. Constructor-captured → requiresRestart. |
| `git.statusStaleTime` | number | `1_000` | application | P6 | internal | yes | PLUMBING (client query pacing). Application rather than window because each status read spawns `git status` and schedules the upstream fetch (service.ts:705); a workspace-settable value would let a cloned repo drive subprocess and network frequency. Keep it consistent with git.statusCacheTtl — a staleTime below the server TTL just serves the same cached answer. |
| `git.branchRemoteStaleTime` | number | `2_000` | application | P6 | internal | yes | PLUMBING (client query pacing). Deliberately fresh so a commit made in the terminal moves the ahead/behind counts without a reload (comment at use-branch-remote-state.ts:6-10). Drives server-side git reads → application. |
| `git.diffStaleTime` | number | `1_000` | application | P6 | internal | yes | PLUMBING (client query pacing) for the imperative fetchQuery behind 'open diff' (use-open-diff-document.ts:14-18). Do NOT fold the content-addressed diff queries into this key: use-diff-document-diffs.ts:43,53 and use-open-file-at-ref.ts:27 use `staleTime: Infinity` because they are keyed by object id, and that is correctness, not a preference. Drives a `git diff` spawn → application. |
| `git.pullRequest.pollInterval` | number | `60_000` | application | P6 | internal | yes | PLUMBING; PULL_REQUEST_POLL_MS feeds both refetchInterval and staleTime (use-pull-request-state.ts:19,23), so one key sets both. Each poll spawns `gh` on the machine → application, not window. |
| `git.pullRequest.timeout` | number | `20_000` | application | P6 | internal | yes | PLUMBING. GH_TIMEOUT_MS bounds the `gh` subprocess so a wedged CLI cannot hold a header render. Bounds a spawned process → application. |
| `git.pullRequest.supportCacheTtl` | number | `60_000` | application | P6 | internal | yes | PLUMBING. Caches the 'does `gh` work here' verdict per cwd in a module-level Map (pull-request.ts:27), keeping two probe processes off every read; the window is what makes a fresh `gh auth login` visible without a restart. Paces spawns → application. |
| `window.defaultWidth` | number | `1440` | machine | P6 | user ⟳ | yes | NEW FEATURE (nothing reads settings in the shell yet). The frame is a literal object in openMainWindow (index.ts:132-139). Needs `apps/desktop/src/bun/settings.ts` — **it does not exist** (apps/desktop/src/bun contains only index.ts, native-paths.ts, observability.ts, structured-errors.ts, vibrancy.ts) — to read ~/.platform/settings.json synchronously, honouring PLATFORM_SETTINGS_FILE, before `new BrowserWindow`. requiresRestart:true until an applyWindowSettings request exists on DesktopRPC (defineRPC handlers today expose only pickEntry, index.ts:123-130). This is a default size, NOT restored bounds — no bounds key exists or should. |
| `window.defaultHeight` | number | `960` | machine | P6 | user ⟳ | yes | NEW FEATURE, identical path to window.defaultWidth (same literal frame object, index.ts:132-139). Note frame also hardcodes `x: 0, y: 0` (index.ts:137-138) — position is deliberately NOT registered: per the plan, where the window was left is restored state, not a setting. requiresRestart:true. |
| `window.titleBarStyle` | enum(hiddenInset\\ | default) | `'hiddenInset'` | machine | P6 | advanced ⟳ | yes | NEW FEATURE plus a real cross-app coupling, not pure plumbing. The web titlebar pads `pl-[4.75rem]` whenever `isDesktop()` and unconditionally on style (apps/web/src/components/app-titlebar.tsx:31) — that gap exists solely to clear the hiddenInset traffic lights, so selecting 'default' leaves a 4.75rem hole unless the padding becomes conditional on the same key. That means a desktop-shell-only key has to reach the web app too. The default 'hiddenInset' is verified at index.ts:144; the **accepted value union is not** — node_modules is absent in this worktree, so electrobun@^1.18.1's BrowserWindow typings could not be opened. Confirm the union before writing the schema. |
| `window.nativeVibrancy` | boolean | `false` | machine | P6 | user ⟳ | yes | Ships READ-ONLY WITH A REASON (per the plan) — it needs the readOnly+reason machinery this phase adds, not a live control. `transparent: false` is forced at index.ts:153 with the measured justification in the comment at :145-152: CEF implements window transparency by switching to offscreen rendering, and transparent:true costs a 5.5MB CPU memcpy per paint (transparent:false produces zero OnPaint events). The NSVisualEffectView is already attached unconditionally (index.ts:157 → vibrancy.ts:25-44, material NSVisualEffectMaterialUnderWindowBackground = 21); it simply never shows through. So flipping this key alone would be a performance trap, which is exactly the reason string the UI must render. requiresRestart:true. |
| `window.trafficLightOffset` | object | `{ x: 0, y: 9 }` | machine | P6 | internal ⟳ | yes | PLUMBING, internal/JSON-only so it needs no widget (object-typed but not a Phase 7 concern). Positions the native macOS controls over our own toolbar and is only meaningful while titleBarStyle is 'hiddenInset'; it must be kept consistent with the `pl-[4.75rem]` reservation in apps/web/src/components/app-titlebar.tsx:31. Same shell-side read path and same requiresRestart:true as the rest of window.\*. |

### Notes

- PHASE PLACEMENT CAVEAT: the plan's phase list never names `git.*`. Phase 6 is literally 'files / search / chat / lsp / telemetry / window', yet the Registry payload section (plan:919) lists `git.*` among the v1 namespaces. I placed every git key in Phase 6 — the only batch whose consumers (server git service, git panel, file tree) are all live — except the list-typed `git.baseBranchCandidates`, which needs Phase 7's list editor. Flag this back to the plan author: either Phase 6's name gains 'git', or git gets its own batch.
- SCOPE, THE BIG JUDGEMENT CALL: git is an exec surface end to end, so 28 of the 29 git keys are `application`. The one exception is `git.decorations.enabled` (window) — it only decides whether status badges paint in the tree and reaches no argv, env, or keymap. `git.path` is `machine` (a binary path, per the plan's own machine definition). Anyone porting VS Code's window-scoped `git.*` defaults into this registry would be importing the vector the plan's hard rule exists to close.
- THE CONFIRM KEYS ARE APPLICATION ON PURPOSE. git.confirmDiscard / confirmSync / confirmForcePush render dialogs, which reads like window scope. They are application because the _value_ decides whether a destructive or network git command spawns unprompted. A cloned repo shipping `{"git.confirmDiscard": false}` in .platform/settings.json plus one mis-clicked toolbar button is uncommitted-work loss with no prompt.
- INERT-KEY RISK: `git.confirmForcePush` has nothing to confirm. Force push does not exist anywhere — GitService.push builds only `push` / `push --set-upstream origin <branch>` (service.ts:553-560), and no route, contract, or UI carries a force flag. The plan's rule is 'a key is never registered inert', so this key must ship with a force-push command or be cut. It is listed because the parity matrix names it; it is the single key here I would drop first.
- THE PARITY MATRIX IS WRONG ABOUT AUTOFETCH. docs/editor-parity-gap-matrix.md:223 lists 'Auto-fetch ❌ missing'. It exists and is always on: UpstreamFetchScheduler throttles a real `git fetch <remote>` per (git common dir, remote) off every status read (service.ts:705, upstream-fetch.ts:25-97). What is missing is only the on/off setting, the period, and VS Code's all-repos-vs-current choice. That makes git.autofetch/autofetchPeriod plumbing, not new features — a meaningful estimate difference.
- CONFLICTING DEFAULT FOUND — git.diff.ignoreWhitespace. Server and routes default `false` (service.ts:262, orchestration/routes.ts:140,156) but the chat checkpoint client hardcodes `true` at three call sites (features/chat/lib/checkpoint-diff-query.ts:46,59,148), and the git panel's own diff path (service.ts:207-217) does not accept the flag at all. I registered `false` (git's own behaviour, and what the panel does today). The registry cannot paper over this: decide whether checkpoint diffs honour the key or are explicitly exempted, before Phase 6.
- DELIBERATELY EXCLUDED — blame. `git.blame.*` appears in the parity matrix's 'diff/blame options', but a repo-wide grep for 'blame' in apps/web/src and apps/server/src returns nothing outside tests. There is no gutter, no route, no service method. Any blame key would be inert by definition.
- DELIBERATELY EXCLUDED — SCM count badge, checkout-safety/auto-stash, remote management, and tags. All are whole missing features in the matrix with zero code to gate; their settings belong to whichever phase builds the feature, not to this registry batch.
- DELIBERATELY EXCLUDED — identity and layout constants that would orphan on-disk state if changed: CHECKPOINT_IDENTITY (checkpoint-store.ts:44-49, deliberately not the user's git identity so hidden refs never look authored by them), SESSION_WORKTREES_DIRECTORY = 'platform-worktrees' (worktrees.ts:25), and BASE_CONFIG_KEY = 'platform-base' (worktrees.ts:28). Changing any of these strands existing refs/worktrees/config, and this repo has no migration code by policy.
- DELIBERATELY EXCLUDED — `staleTime: Infinity` on the content-addressed queries (use-diff-document-diffs.ts:43,53; use-open-file-at-ref.ts:27). Those are keyed by git object id, so infinite freshness is correctness, not a tunable. Registering them would invite a user to break diff rendering.
- DELIBERATELY EXCLUDED — MAIN_WINDOW_TITLE = 'Platform' (apps/desktop/src/bun/index.ts:29). It looks like a cosmetic `window.title` key but it is load-bearing: attachWindowVibrancy finds the AppKit window by matching this exact NUL-terminated string over FFI (index.ts:157, vibrancy.ts:32-34). A user-editable title would silently break vibrancy attach with only a desktop.vibrancy.window-not-found log to show for it.
- DELIBERATELY EXCLUDED — the vibrancy tuning constants: UNDER_WINDOW_BACKGROUND = 21, ATTACH_ATTEMPTS = 40, ATTACH_RETRY_MS = 50 (vibrancy.ts:9,13,14). With transparent:false forced, the attached NSVisualEffectView never shows through, so these are inert today. Revisit them together with window.nativeVibrancy if CEF ever gains transparency without OSR.
- NO BOUNDS KEY, AS INSTRUCTED. index.ts:137-138 hardcodes `x: 0, y: 0` alongside the size; I registered defaultWidth/defaultHeight but not x/y, because position is where-you-left-it state and belongs with session state, not settings.json (plan:635-638). Note this also means the current shell always opens at 0,0 — a UX bug, not a settings gap.
- VERIFICATION LINE I DREW: verified:true means I opened the file at fda1523 and saw a literal equal to the default I report. verified:false means the default is inferred from unconditional behaviour (git.enabled, git.autofetch, git.decorations.enabled) or the behaviour does not exist yet (the three confirm keys, git.enableSmartCommit). Every window.* default is a verified literal in apps/desktop/src/bun/index.ts; only the titleBarStyle *value union\* is unverified, because node_modules is absent in this worktree and electrobun's typings could not be opened.
- NO SENSITIVE KEYS IN THIS GROUP. Nothing under git._ or window._ is a credential, so none carry `sensitive: true` and none touch the secrets.json split. Closest call is git.path, which is a machine-scoped binary path — dangerous, but not secret.
- WINDOW KEYS HAVE A SECOND READER. Every window.* key is read by the desktop shell *before\* the server or the web app exists (`new BrowserWindow` at index.ts:132 happens after waitForHttp, but the read must be synchronous and file-based). So the registry must be importable from apps/desktop/src/bun with no server dependency, and the plan's test that shell and server resolve the same defaults from the same registry (plan:649) is the guard. requiresRestart:true on all five until an applyWindowSettings request exists on DesktopRPC — today the RPC exposes only pickEntry (index.ts:123-130).

# TUI command bindings

The shared command catalog now contains 198 rows. Web and TUI compose host handlers around
that metadata. Terminal defaults are authored with `platforms: ['tui']`; user overrides use
`keybindings.overrides`. Commands without a mounted handler do not enter the active palette.

This record preserves Plan 079's scope audit after completion. The runtime source is
[command metadata](../packages/client-core/src/commands/catalog.ts), and the terminal adapter is
[effective bindings](../apps/tui/src/commands/utils/bindings.ts).

## Installed foundation defaults

Control is explicit on every operating system. Two-stroke chords use a plain second stroke.
Settings edit/reset/scope actions apply within settings; dialog confirmation applies within a
dialog. Arrow navigation belongs to the shared wrapping Select. `?` opens help outside text inputs.
Open Address, Copy Address, raw settings JSON, and color/theme selection are also available from
the palette without default bindings. Development origin selection uses the launcher `--origin`.

| Command                        | Default     | Protocol |
| ------------------------------ | ----------- | -------- |
| `workspace.showQuickAccess`    | Mod+P       | legacy   |
| `workspace.showCommandPalette` | F1          | legacy   |
| `workspace.showCommandPalette` | Mod+K P     | legacy   |
| `workspace.showCommandPalette` | Mod+Shift+P | kitty    |
| `workspace.showSettings`       | Mod+K S     | legacy   |
| `workspace.quickOpenView`      | Mod+K V     | legacy   |
| `workspace.navigateBack`       | Mod+K B     | legacy   |
| `workspace.navigateForward`    | Mod+K N     | legacy   |
| `workspace.reconnect`          | Mod+R       | legacy   |
| `workspace.quit`               | Mod+C       | legacy   |
| `workspace.suspend`            | Mod+Z       | legacy   |
| `workspace.focusNextPane`      | Tab         | legacy   |
| `workspace.focusPreviousPane`  | Shift+Tab   | legacy   |
| `workspace.dismiss`            | Escape      | legacy   |
| `workspace.showShortcutHelp`   | ?           | legacy   |
| `settings.edit`                | F2          | legacy   |
| `settings.nextScope`           | F3          | legacy   |
| `settings.reset`               | F4          | legacy   |
| `settings.retry`               | F5          | legacy   |
| `settings.discard`             | F6          | legacy   |
| `dialog.confirm`               | F2          | legacy   |

## Original S4 scope audit

Audited 2026-09-05 against the current working tree. Source metadata was loaded with Bun from the actual web command table and default Linux preset, with no DOM, then recorded in `/work/tmp/platform-tui-command-audit.json`. This record makes no source changes. The following original scope table records the proposals made before implementation. The installed defaults above and the shared metadata are authoritative.

The live table has **179 production rows: 56 workspace and 123 editor**. `environment.devSwitchOrigin` adds one development-only row. The strategy and earlier research still say 142. All 180 rows have a disposition below. Numbering and navigation decisions come from `docs/tui-plan.md` sections 3.3, 10, and D3/D7.

## Foundation decisions

- Use explicit Linux-style Control resolution on every host. Never infer `Mod` from macOS. A Kitty capability probe may enable extra distinguishable keys, but every command with a Kitty extra below remains reachable through its legacy default or palette.
- Start with Ctrl+P for files, F1 or Ctrl+K p for commands, Ctrl+K s for settings, and named focus/dismiss actions. Ctrl+K is deliberately reserved as the app chord prefix even in app text inputs. Second strokes are plain letters or navigation keys, not Ctrl+S/Ctrl+J or control punctuation.
- Commands assigned to later slices are not inert foundation palette entries. Only register an available handler. A disabled registered command should show the exact current reason, such as no viewer tab, rather than claim success.
- Never register `workspace.toggleUiMode`, `workspace.showChatMode`, or `workspace.showWorkbenchMode`. New `workspace.openWorkbench` must take the selected project/worktree address and push a screen. `workspace.revealChat` pops to root. No global current project is introduced by a picker, quick access, or keyboard handler.
- The first file list may bring `toggleSidebarVisibility`, `focusFileTree`, and `findInFileTree` into 079. The strategy otherwise assigns the full tree to 080. A shell with no actual file list should not bind them yet.
- Keep twelve focus areas. A registration identifies the screen and selected environment/project in addition to its area. Text-entry status is a focus capability. Overlay close restores the captured origin; a stale/unmounted origin falls back to the current screen.
- Ctrl+Z belongs to job control. Do not advertise Ctrl+S, Ctrl+J, Ctrl+H, Ctrl+D, Ctrl+Z, Ctrl+Enter, Ctrl+Backspace, or Ctrl+punctuation as application defaults. Legacy Ctrl+I/Tab, Ctrl+M/Enter and Ctrl+[/Escape cannot be distinguished. Raw terminal mode does not restore distinctions already lost before the parser.
- The policy calls its collision list ten defaults but lists nine spellings. The live table also adds Ctrl+comma for settings and Ctrl+] for forward navigation, so validate the actual table rather than that count. The table below removes all these ambiguous application defaults.
- Ctrl+C and Ctrl+Z are terminal lifecycle actions. Process shutdown/suspend must restore modes, cursor and alternate screen. Kitty release events must not become command invocations. Terminal-face passthrough and attach get their own ownership policy in 080; do not let global shortcuts eat forwarded PTY input.

## Pure seams verified now

| Actual module                                                    | TUI seam and extraction constraint                                                                                                                                                                                                                                                                                                           |
| ---------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `@singapor/core/keymap`                                          | Public `buildKeymapTrie`, `trieStep`, `KeymapNode`, `KeymapBinding`, and chord types work without a DOM. The Editor sibling now owns these. Use this entry point, never its CSS-importing package root.                                                                                                                                      |
| `Editor/packages/editor/src/keymap/trie.ts`                      | Nodes contain ordered `candidates` and `descendants`. Preserve that order and command availability/fallthrough. `trieStep` is structurally DOM-free despite its `Pick<KeyboardEvent,...>` input annotation; supply `code: ''` if the terminal provides no physical code.                                                                     |
| `Editor/packages/editor/src/keymap/runtime.ts`                   | `createKeymapRuntime` remains DOM-bound: root element/document, keydown/keyup capture, focus events and composed paths. The TUI needs a terminal session adapter over the shared trie, or a host-neutral runtime extraction coordinated with Editor.                                                                                         |
| `apps/web/src/keymap/state/keymap-session.ts`                    | Current web adapter captures one bus snapshot per stroke, checks availability, passes Editor conditions and cancels on owner/input/document changes. Its predecessor chord-machine and keymap-trie files mentioned in the research no longer exist. Do not copy old semantics such as unconditional shortest-prefix winner or last-row wins. |
| `apps/web/src/keymap/utils/chord.ts`                             | Pure spelling/validation, max two strokes, five-second constant, first stroke Control/Meta. Extract with injected/explicit platform; TUI policy further requires Control for its authored prefixes.                                                                                                                                          |
| `apps/web/src/keymap/active-bindings.ts`                         | Pure calculations, but coupled to web command table and editor conditions. Extract by taking command metadata as input. Current pane arbitration sorts pane-specific before global, retains preset order and alternatives, and returns resolution diagnostics.                                                                               |
| `apps/web/src/keymap/default-bindings.ts`                        | Browser default builder now appends Editor preset packs plus browser reservations. TUI must use its audited host defaults, not filter the browser array and hope. No command-null browser reservations should enter the terminal table.                                                                                                      |
| `apps/web/src/keymap/table.ts`, `define-command.ts`, `types.ts`  | Metadata and IDs can be shared; web handlers, icon components, DOM target and feature-store runtime cannot. The metadata extraction should leave web handlers keyed by the same IDs. New IDs must enter this source of truth, not a second handwritten list that drifts.                                                                     |
| `apps/web/src/keymap/state/command-bus.ts`                       | Generic bus is reusable after errors and event factories become host ports. Runtime imports still pull web error reporting/toasts and wide-event defaults. TUI must preserve inspect/dispatch/settlement and captured origin semantics.                                                                                                      |
| `apps/web/src/keymap/utils/when.ts`                              | Pure enablement with file-backed document helpers and Editor conditions. Shared command metadata needs host-neutral conditions; TUI capabilities cannot falsely claim editorWritable.                                                                                                                                                        |
| `apps/web/src/keymap/utils/format-keys.ts`                       | Pure formatting with platform default. TUI must pass an explicit Control platform, including through commandShortcut, whose current convenience form infers it.                                                                                                                                                                              |
| `apps/web/src/features/command-palette/command-palette-utils.ts` | Pure matching/ranking/prefix grammar are reusable, but the file imports web command data, editor panel selection and document-id helpers. Extract the used item grammar and ranking together with their types; do not import web runtime merely for a string filter.                                                                         |
| `apps/web/src/lib/focus/state/service.ts`                        | Twelve areas, identity, captured origins and acknowledge/supersede protocol are portable. HTMLElement registrations, focusin and composedPath are host plumbing; TUI ownership must come from actual terminal widget focus.                                                                                                                  |
| `packages/tree/src/hooks/useFileTreeKeyboard.ts`                 | Arrow navigation, search, selection and rename keys live in a React hook, not the shared command table. `treeCommandFocusCandidate` is pure but only selects a focus candidate. Register new named list commands for the TUI instead of importing the hook or pretending all list actions already have command IDs.                          |

Bun smoke execution imported the public shared trie with `typeof document === 'undefined'`, matched Ctrl+P to quick access, armed Ctrl+K with one descendant, and completed plain s to settings. That proves the actual shared pure entry is usable. No application tests were run for this read-only audit.

The current root `PLAN.md` says 056/057 are complete and links `plans/057-delivery.md`; `plans/README.md` still has stale NEXT text for 057. The delivery record, not the stale research paths, describes the matcher now in use.

## Named actions missing from the current table

These are proposed additions, not aliases for an existing unrelated command. Their exact IDs must be agreed with the shared metadata extraction. Foundation should add only actions it implements.

| Proposed command              | Legacy and Kitty default                         | Scope / behavior                                                                                        |
| ----------------------------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------- |
| `workspace.openWorkbench`     | Enter on project/worktree row; otherwise palette | Push selected project/worktree address; required by D3, but current table has no such ID.               |
| `workspace.focusNextPane`     | Tab                                              | Cycle visible panes unless active overlay/text widget owns the key.                                     |
| `workspace.focusPreviousPane` | Shift+Tab                                        | Reverse cycle visible panes.                                                                            |
| `workspace.dismiss`           | Escape                                           | Cancel pending chord first, then overlay, then pane to parent, then root. At root do not silently quit. |
| `workspace.showShortcutHelp`  | ? in non-text panes                              | Registry-derived shortcuts/which-key overlay.                                                           |
| `workspace.quit`              | Ctrl+C                                           | Named renderer shutdown action with signal-safe restoration.                                            |
| `workspace.suspend`           | Ctrl+Z                                           | Named lifecycle suspend/resume action, not application undo.                                            |
| `fileTree.focusNext`          | ArrowDown or j                                   | File-list focus; j must not filter or move rows while text entry owns input.                            |
| `fileTree.focusPrevious`      | ArrowUp or k                                     | File-list focus.                                                                                        |
| `fileTree.focusFirst`         | Home or g                                        | First visible row. A bare g prefix is forbidden; use one stroke g.                                      |
| `fileTree.focusLast`          | End or Shift+g                                   | Last visible row.                                                                                       |
| `fileTree.openSelected`       | Enter                                            | Expand selected directory or open selected file through its domain action.                              |
| `fileTree.expandOrNext`       | ArrowRight or l                                  | Expand directory; move to first child when already expanded.                                            |
| `fileTree.collapseOrParent`   | ArrowLeft or h                                   | Collapse directory; otherwise parent.                                                                   |
| `fileTree.toggleSelection`    | Space                                            | Toggle selected row when multiple-selection behavior is implemented.                                    |

The existing defineCommand ID signature accepts only `workspace.*` and `environment.*`; file-tree command IDs need a deliberate extension rather than a cast. The focus union has `file-tree`, so that area can be reused immediately. Palette/list navigation also needs named commands if it shares this dispatch path; native text editing stays owned by the text widget.

## Per-command disposition

`79` means foundation, `80` workbench, and `81` agent view. Later phase bindings are proposals to ship when the handler ships. `external-editor` means the operation runs inside $EDITOR and has no app binding. `cell-editor-reserve` requires the deferred native editor. `none` means no default; supported commands remain palette-reachable when their handler exists. Kitty extras are added to the legacy defaults. Unless marked with `@area`, bindings are global but remain gated by command availability. Web defaults below are the actual default Linux preset, retaining authored `Mod` spelling.

### Workspace

| Command ID                          | Current web defaults       | Phase/disposition | Legacy default                | Kitty extra  | Behavior                                                                                        |
| ----------------------------------- | -------------------------- | ----------------- | ----------------------------- | ------------ | ----------------------------------------------------------------------------------------------- |
| `workspace.undoWorkspaceEdit`       | none                       | 80                | none                          | none         | Undo committed workspace operation; never take Ctrl+Z from job control.                         |
| `workspace.redoWorkspaceEdit`       | none                       | 80                | none                          | none         | Redo committed workspace operation; palette only.                                               |
| `workspace.showQuickAccess`         | Mod+P@any                  | 79                | Ctrl+P                        | none         | Open file quick access for the selected project or pushed workbench.                            |
| `workspace.showCommandPalette`      | Mod+Shift+P@any; F1@any    | 79                | F1; Ctrl+K p                  | Ctrl+Shift+P | Open commands mode; derive rows from registered available commands.                             |
| `workspace.showSettings`            | Mod+,@any; Mod+K Mod+S@any | 79                | Ctrl+K s                      | none         | Open settings search; use a plain second stroke, never Ctrl+S.                                  |
| `workspace.openFilePicker`          | none                       | 81                | none                          | none         | Project/folder picker; adding a project must not create a global current project.               |
| `workspace.openSearchEditor`        | none                       | 80                | Ctrl+K f                      | none         | Open search results in the pushed workbench.                                                    |
| `workspace.quickOpenPreviousEditor` | none                       | 80                | none                          | Ctrl+Tab     | Select previous viewer tab; Ctrl+Tab is enhanced-only.                                          |
| `workspace.quickOpenView`           | none                       | 79                | Ctrl+K v                      | none         | Open view mode with only mounted/implemented views.                                             |
| `workspace.gotoSymbol`              | Mod+Shift+O@any            | 80                | Ctrl+K o                      | Ctrl+Shift+O | LSP symbols in the active viewer.                                                               |
| `workspace.showAllEditors`          | none                       | 80                | none                          | none         | Open edt palette mode when viewer tabs exist.                                                   |
| `workspace.saveFile`                | Mod+S@any                  | 80                | Ctrl+K w                      | none         | Commit pending external-editor change through workspace-edit; disabled when nothing is pending. |
| `workspace.saveAllFiles`            | none                       | 80                | none                          | none         | Commit pending edits if the handoff model permits several; otherwise unavailable.               |
| `workspace.compareWithSaved`        | none                       | 80                | none                          | none         | Compare pending edit with server snapshot.                                                      |
| `workspace.openFileAtHead`          | none                       | 80                | none                          | none         | Read revision into viewer.                                                                      |
| `workspace.revertFile`              | none                       | 80                | none                          | none         | Discard pending handoff edit; expose only with an actual pending edit.                          |
| `workspace.reopenClosedEditor`      | none                       | 80                | Ctrl+K t                      | Ctrl+Shift+T | Restore last closed viewer tab.                                                                 |
| `workspace.toggleSidebarVisibility` | Mod+B@any                  | 80                | Ctrl+B                        | none         | Toggle files pane within workbench; can be brought into 79 if a file-list pane is included.     |
| `workspace.togglePanel`             | Mod+J@any                  | 80                | Ctrl+K j                      | none         | Toggle bottom panel; never Ctrl+J, which aliases line feed.                                     |
| `workspace.focusFirstEditorGroup`   | none                       | 80                | none                          | none         | Focus sole viewer group when present.                                                           |
| `workspace.focusSecondEditorGroup`  | none                       | fixed-layout      | none                          | none         | Unavailable in v1: no second resizable editor group.                                            |
| `workspace.focusThirdEditorGroup`   | none                       | fixed-layout      | none                          | none         | Unavailable in v1: no third resizable editor group.                                             |
| `workspace.focusEditor`             | none                       | 80                | Ctrl+K d                      | none         | Focus viewer; registered focus request must acknowledge actual focus.                           |
| `workspace.focusFileTree`           | Mod+Shift+E@any            | 80                | Ctrl+K e                      | Ctrl+Shift+E | Focus files; can be brought into 79 with initial file list.                                     |
| `workspace.findInFileTree`          | Mod+F@file-tree            | 80                | Ctrl+F@file-tree; /@file-tree | none         | Enter files filter; available in 79 if file list lands there.                                   |
| `workspace.revealActiveFileInTree`  | none                       | 80                | none                          | none         | Focus and reveal actual active viewer file.                                                     |
| `workspace.focusGit`                | none                       | 80                | Ctrl+K g                      | none         | Focus git pane in the pushed workbench.                                                         |
| `workspace.copyAddress`             | none                       | 79                | none                          | none         | OSC 52 plus available native clipboard port; preserve selected environment and address.         |
| `workspace.navigateBack`            | Mod+[@any                  | 79                | Ctrl+K b                      | none         | Pop view history; at workbench boundary restore exact originating rail state.                   |
| `workspace.navigateForward`         | Mod+]@any                  | 79                | Ctrl+K n                      | none         | Replay forward view history only if present; never choose a global project.                     |
| `workspace.revealChat`              | none                       | 81                | Ctrl+K a                      | none         | Pop to agent root, preserving its rail and stage selection.                                     |
| `workspace.revealTerminal`          | none                       | 80                | Ctrl+K r                      | none         | Open terminal pane; pass application shortcuts only while shell owns input.                     |
| `workspace.closeCurrentTab`         | none                       | 80                | Ctrl+W                        | none         | Close active workbench tab; unavailable in composer text entry.                                 |
| `workspace.toggleDiffViewMode`      | Mod+Shift+D@any            | 80                | none                          | Ctrl+Shift+D | Switch stacked/split if terminal width permits; palette is legacy default.                      |
| `workspace.toggleUiMode`            | Mod+Shift+M@any            | forbidden         | none                          | none         | D3 forbids construction around a mode toggle; never register in TUI.                            |
| `workspace.showChatMode`            | none                       | forbidden         | none                          | none         | D3 forbids legacy mode commands; use revealChat, which pops navigation.                         |
| `workspace.showWorkbenchMode`       | none                       | forbidden         | none                          | none         | D3 forbids legacy mode commands; new openWorkbench requires a project/worktree target.          |
| `workspace.newSession`              | Mod+Alt+N@any              | 81                | Ctrl+K c                      | Ctrl+Alt+N   | Create session scoped to selected project/worktree row.                                         |
| `workspace.nextSession`             | Mod+Alt+]@any              | 81                | Ctrl+K ArrowDown              | none         | Next session in current root rail order, only while root is displayed.                          |
| `workspace.previousSession`         | Mod+Alt+[@any              | 81                | Ctrl+K ArrowUp                | none         | Previous session in current root rail order, only while root is displayed.                      |
| `workspace.toggleSessionRail`       | Mod+Alt+B@any              | 81                | Ctrl+K h                      | Ctrl+Alt+B   | Toggle root rail; collapses into overlay on narrow terminals.                                   |
| `workspace.jumpToSession1`          | Mod+Alt+1@any              | 81                | Ctrl+K 1                      | none         | Select slot 1 in visible root rail; never cross-project global selection.                       |
| `workspace.jumpToSession2`          | Mod+Alt+2@any              | 81                | Ctrl+K 2                      | none         | Select slot 2 in visible root rail; never cross-project global selection.                       |
| `workspace.jumpToSession3`          | Mod+Alt+3@any              | 81                | Ctrl+K 3                      | none         | Select slot 3 in visible root rail; never cross-project global selection.                       |
| `workspace.jumpToSession4`          | Mod+Alt+4@any              | 81                | Ctrl+K 4                      | none         | Select slot 4 in visible root rail; never cross-project global selection.                       |
| `workspace.jumpToSession5`          | Mod+Alt+5@any              | 81                | Ctrl+K 5                      | none         | Select slot 5 in visible root rail; never cross-project global selection.                       |
| `workspace.jumpToSession6`          | Mod+Alt+6@any              | 81                | Ctrl+K 6                      | none         | Select slot 6 in visible root rail; never cross-project global selection.                       |
| `workspace.jumpToSession7`          | Mod+Alt+7@any              | 81                | Ctrl+K 7                      | none         | Select slot 7 in visible root rail; never cross-project global selection.                       |
| `workspace.jumpToSession8`          | Mod+Alt+8@any              | 81                | Ctrl+K 8                      | none         | Select slot 8 in visible root rail; never cross-project global selection.                       |
| `workspace.jumpToSession9`          | Mod+Alt+9@any              | 81                | Ctrl+K 9                      | none         | Select slot 9 in visible root rail; never cross-project global selection.                       |

### Appearance

| Command ID                   | Current web defaults | Phase/disposition | Legacy default | Kitty extra | Behavior                                                                       |
| ---------------------------- | -------------------- | ----------------- | -------------- | ----------- | ------------------------------------------------------------------------------ |
| `workspace.selectColorMode`  | none                 | 79                | none           | none        | Color mode palette; only wire after its consumer exists.                       |
| `workspace.selectColorTheme` | none                 | 79                | none           | none        | Theme palette over shared theme metadata.                                      |
| `workspace.setDarkTheme`     | none                 | 79                | none           | none        | Set actual theme through settings owner.                                       |
| `workspace.setLightTheme`    | none                 | 79                | none           | none        | Set actual theme through settings owner.                                       |
| `workspace.setSystemTheme`   | none                 | 79                | none           | none        | Resolve terminal-reported mode, using explicit fallback when unavailable.      |
| `workspace.toggleWallpaper`  | none                 | web-only          | none           | none        | Terminal has no wallpaper material; omit rather than writing an inert setting. |

### Editor

| Command ID                                          | Current web defaults                                   | Phase/disposition   | Legacy default                  | Kitty extra | Behavior                                                                                       |
| --------------------------------------------------- | ------------------------------------------------------ | ------------------- | ------------------------------- | ----------- | ---------------------------------------------------------------------------------------------- |
| `editor.undo`                                       | Mod+Z@editor                                           | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.redo`                                       | Mod+Shift+Z@editor; Mod+Y@editor                       | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.find`                                       | Mod+F@editor                                           | 80-viewer           | Ctrl+F@editor; /@editor         | none        | Viewer search, separate from composer input.                                                   |
| `editor.findReplace`                                | Mod+H@editor                                           | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.findNext`                                   | F3@editor                                              | 80-viewer           | F3@editor; n@editor             | none        | Next viewer match; plain n gated off in search input.                                          |
| `editor.findPrevious`                               | Shift+F3@editor                                        | 80-viewer           | Shift+F3@editor; Shift+n@editor | none        | Previous viewer match; shifted printable letter is legacy-safe.                                |
| `editor.goToDefinition`                             | F12@editor                                             | 80-viewer           | F12@editor                      | none        | LSP navigation from selected viewer location.                                                  |
| `editor.editor.action.goToImplementation`           | none                                                   | 80-viewer           | none                            | none        | LSP implementation results; palette only if the viewer implements it.                          |
| `editor.editor.action.goToTypeDefinition`           | none                                                   | 80-viewer           | none                            | none        | LSP type definition; palette only if implemented.                                              |
| `editor.editor.action.peekDefinition`               | none                                                   | 80-viewer           | none                            | none        | Viewer definition details; optional handler in 80, no inert palette row.                       |
| `editor.editor.action.revealDefinitionAside`        | none                                                   | 80-viewer           | none                            | none        | View target in tool pane only if that pane exists; fixed layout limits apply.                  |
| `editor.editor.action.showHover`                    | none                                                   | 80-viewer           | Ctrl+K i@editor                 | none        | LSP hover for selected viewer location.                                                        |
| `editor.editor.action.goToReferences`               | none                                                   | 80-viewer           | none                            | none        | LSP reference results; palette only.                                                           |
| `editor.closeFind`                                  | Escape@editor                                          | 80-viewer           | Escape@editor                   | none        | Close active viewer search before Escape moves focus to parent.                                |
| `editor.toggleFindCaseSensitive`                    | Alt+C@editor                                           | 80-viewer           | none                            | none        | Search option command; only enabled with viewer search open.                                   |
| `editor.toggleFindWholeWord`                        | Alt+W@editor                                           | 80-viewer           | none                            | none        | Search option command; only enabled with viewer search open.                                   |
| `editor.toggleFindRegex`                            | Alt+R@editor                                           | 80-viewer           | none                            | none        | Search option command; only enabled with viewer search open.                                   |
| `editor.toggleFindInSelection`                      | Alt+L@editor                                           | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.togglePreserveCase`                         | Alt+P@editor                                           | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.replaceOne`                                 | none                                                   | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.replaceAll`                                 | Mod+Alt+Enter@editor                                   | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.selectAllMatches`                           | Alt+Enter@editor                                       | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.selectAll`                                  | Mod+A@editor                                           | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.addNextOccurrence`                          | Mod+D@editor                                           | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.clearSecondarySelections`                   | Escape@editor                                          | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.deleteWordLeft`                             | Mod+Backspace@editor                                   | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.deleteWordRight`                            | Mod+Delete@editor                                      | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.deleteLines`                  | Mod+Shift+K@editor                                     | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.copyLinesUpAction`            | Mod+Alt+Shift+ArrowUp@editor                           | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.copyLinesDownAction`          | Mod+Alt+Shift+ArrowDown@editor                         | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.moveLinesUpAction`            | Alt+ArrowUp@editor                                     | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.moveLinesDownAction`          | Alt+ArrowDown@editor                                   | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.insertLineBefore`             | Mod+Shift+Enter@editor                                 | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.insertLineAfter`              | Mod+Enter@editor                                       | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.commentLine`                  | Mod+/@editor                                           | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.blockComment`                 | Mod+Shift+A@editor                                     | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.indentLines`                  | Mod+]@editor                                           | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.outdentLines`                 | Mod+[@editor                                           | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.insertCursorAbove`            | Alt+Shift+ArrowUp@editor; Mod+Shift+ArrowUp@editor     | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.action.insertCursorBelow`            | Alt+Shift+ArrowDown@editor; Mod+Shift+ArrowDown@editor | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.action.selectHighlights`             | Mod+Shift+L@editor                                     | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.action.changeAll`                    | Mod+F2@editor                                          | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.action.jumpToBracket`                | none                                                   | 80-viewer           | none                            | none        | Viewer location navigation if shared syntax supports it.                                       |
| `editor.cursorWordPartLeft`                         | Mod+Alt+ArrowLeft@editor                               | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.cursorWordPartRight`                        | Mod+Alt+ArrowRight@editor                              | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.action.trimTrailingWhitespace`       | none                                                   | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.sortLinesAscending`           | none                                                   | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.sortLinesDescending`          | none                                                   | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.joinLines`                    | none                                                   | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.duplicateSelection`           | none                                                   | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.transformToUppercase`         | none                                                   | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.transformToLowercase`         | none                                                   | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.transformToTitlecase`         | none                                                   | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.rename`                       | none                                                   | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.formatDocument`               | none                                                   | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.toggleWordWrap`               | none                                                   | 80-viewer           | none                            | none        | Viewer display command.                                                                        |
| `editor.editor.action.moveSelectionToNextFindMatch` | none                                                   | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.deleteBackward`                             | Backspace@editor                                       | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.deleteForward`                              | Delete@editor                                          | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.indentSelection`                            | Tab@editor                                             | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.outdentSelection`                           | Shift+Tab@editor                                       | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.cursorLeft`                                 | ArrowLeft@editor                                       | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.cursorRight`                                | ArrowRight@editor                                      | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.cursorUp`                                   | ArrowUp@editor                                         | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.cursorDown`                                 | ArrowDown@editor                                       | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.selectLeft`                                 | Shift+ArrowLeft@editor                                 | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.selectRight`                                | Shift+ArrowRight@editor                                | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.selectUp`                                   | Shift+ArrowUp@editor                                   | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.selectDown`                                 | Shift+ArrowDown@editor                                 | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.cursorWordLeft`                             | Mod+ArrowLeft@editor                                   | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.cursorWordRight`                            | Mod+ArrowRight@editor                                  | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.selectWordLeft`                             | Mod+Shift+ArrowLeft@editor                             | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.selectWordRight`                            | Mod+Shift+ArrowRight@editor                            | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.cursorLineStart`                            | Home@editor                                            | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.cursorLineEnd`                              | End@editor                                             | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.selectLineStart`                            | Shift+Home@editor                                      | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.selectLineEnd`                              | Shift+End@editor                                       | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.cursorPageUp`                               | PageUp@editor                                          | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.cursorPageDown`                             | PageDown@editor                                        | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.selectPageUp`                               | Shift+PageUp@editor                                    | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.selectPageDown`                             | Shift+PageDown@editor                                  | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.cursorDocumentStart`                        | Mod+Home@editor                                        | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.cursorDocumentEnd`                          | Mod+End@editor                                         | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.selectDocumentStart`                        | Mod+Shift+Home@editor                                  | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.selectDocumentEnd`                          | Mod+Shift+End@editor                                   | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.cursorUndo`                                 | Mod+U@editor                                           | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.cursorRedo`                                 | Mod+Shift+U@editor                                     | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.action.goToDefinition`               | none                                                   | 80-viewer           | none                            | none        | Existing hidden duplicate spelling; do not add a second advertised default.                    |
| `editor.editor.action.marker.next`                  | none                                                   | 80-viewer           | F8@editor                       | none        | Next diagnostic in viewer.                                                                     |
| `editor.editor.action.marker.prev`                  | none                                                   | 80-viewer           | Shift+F8@editor                 | none        | Previous diagnostic in viewer.                                                                 |
| `editor.editor.action.smartSelect.expand`           | Alt+Shift+ArrowRight@editor                            | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.action.smartSelect.shrink`           | Alt+Shift+ArrowLeft@editor                             | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.action.toggleTabFocusMode`           | Mod+M@editor                                           | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.action.autoFix`                      | Alt+Shift+.@editor                                     | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.inlineSuggest.commit`         | none                                                   | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.inlineSuggest.acceptNextWord` | none                                                   | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.fold`                                | Mod+K Mod+[@editor                                     | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.unfold`                              | Mod+K Mod+]@editor                                     | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.foldRecursively`                     | Mod+K Mod+Shift+[@editor                               | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.unfoldRecursively`                   | Mod+K Mod+Shift+]@editor                               | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.foldAll`                             | Mod+K Mod+0@editor                                     | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.unfoldAll`                           | Mod+K Mod+J@editor                                     | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.createFoldingRangeFromSelection`     | Mod+K Mod+,@editor                                     | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.removeManualFoldingRanges`           | Mod+K Mod+Shift+,@editor                               | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.deleteWordPartLeft`                         | Mod+Alt+Backspace@editor                               | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.deleteWordPartRight`                        | Mod+Alt+Delete@editor                                  | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.reindentlines`                | Mod+Alt+Shift+I@editor                                 | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.editor.action.reindentselectedlines`        | Alt+Shift+I@editor                                     | external-editor     | none                            | none        | D7 delegates buffer editing to $EDITOR; omit app default and active palette row in v1.         |
| `editor.cursorWordPartLeftSelect`                   | Mod+Alt+Shift+ArrowLeft@editor                         | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.cursorWordPartRightSelect`                  | Mod+Alt+Shift+ArrowRight@editor                        | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.cursorColumnSelectLeft`                     | Alt+ArrowLeft@editor                                   | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.cursorColumnSelectRight`                    | Alt+ArrowRight@editor                                  | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.cursorColumnSelectUp`                       | Mod+ArrowUp@editor                                     | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.cursorColumnSelectDown`                     | Mod+ArrowDown@editor                                   | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.cursorColumnSelectPageUp`                   | Mod+Alt+Shift+PageUp@editor                            | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.cursorColumnSelectPageDown`                 | Mod+Alt+Shift+PageDown@editor                          | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.foldLevel1`                          | Mod+K Mod+1@editor                                     | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.foldLevel2`                          | Mod+K Mod+2@editor                                     | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.foldLevel3`                          | Mod+K Mod+3@editor                                     | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.foldLevel4`                          | Mod+K Mod+4@editor                                     | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.foldLevel5`                          | Mod+K Mod+5@editor                                     | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.foldLevel6`                          | Mod+K Mod+6@editor                                     | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |
| `editor.editor.foldLevel7`                          | Mod+K Mod+7@editor                                     | cell-editor-reserve | none                            | none        | Native cursor/selection/folding state belongs to deferred cell editor; omit app default in v1. |

### Developer

| Command ID                    | Current web defaults | Phase/disposition | Legacy default | Kitty extra | Behavior                                                                        |
| ----------------------------- | -------------------- | ----------------- | -------------- | ----------- | ------------------------------------------------------------------------------- |
| `environment.devSwitchOrigin` | none                 | 79-dev            | none           | none        | Developer-only origin picker; use TUI host connection owner, never web handler. |

## Audit verification

All 180 current command rows are covered exactly once. Disposition counts: 79 = 12, 79-dev = 1, 80 = 23, 80-viewer = 19, 81 = 15, cell-editor-reserve = 66, external-editor = 38, fixed-layout = 2, forbidden = 3, web-only = 1.

S4 inventory and per-command disposition: PASS. The foundation was subsequently verified with native OpenTUI legacy/Kitty input events, in-process server routes, and a real PTY covering external editing, suspend/resume and quit. Physical Kitty keyboard hardware was not available; enhanced defaults remain gated by the terminal capability probe. Later-slice proposals in the original table remain unregistered.

# T3Code Parity — Second Sweep, Whole-Product

> **Measured 2026-08-12 against `b23ee6a`.** The previous document
> (`docs/t3code-chat-parity-gap-analysis.md`) measured the **chat** dimension and declared N0–N4 complete. This sweep is
> wider: providers, git, worktrees, terminal, desktop shell, settings, distribution. Every row below was traced end to
> end — a capability whose only callers are its own tests is recorded as **absent**, not present. Our files are
> kebab-case; reference paths are camelCase under `/Users/shaul/Desktop/D/refrences/t3code`.
>
> Effort scale: **S** ≤ 1 day · **M** 2–4 days · **L** 1–2 weeks · **XL** > 2 weeks.

---

## 1. Bottom line

**We are at roughly 65% of the reference's product, and roughly 75% of the parity we intend to build.** The previous
document's ~80% was honest _for the chat dimension it measured_, and that dimension survives this sweep largely intact.
The number drops because the denominator grew: outside chat, three whole subsystems are complete on the server and
reachable by nobody.

| Dimension                                              |                    Parity | Note                                                                                          |
| ------------------------------------------------------ | ------------------------: | --------------------------------------------------------------------------------------------- |
| Chat orchestration, timeline, approvals, checkpoints   |                      ~85% | Confirmed. Not re-litigated here.                                                             |
| Provider platform (instances, config, models, drivers) |                      ~40% | Two hardcoded instances; `reconcile()` never sees settings; two drivers; no text generation.  |
| Git workflow                                           |                      ~45% | Commit/push/pull/PR-create exist. No branch UI, no guards, no generated text, PR unreachable. |
| Worktrees / session isolation                          |                      ~15% | Full server service, **zero non-test callers**.                                               |
| Terminal                                               | ~55% of their feature set | One instance, memory-only history, no restart — but see below.                                |
| Settings surface                                       |                      ~20% | Panel mounts; two of three sections render permanent empty states.                            |
| Desktop shell                                          |                      ~15% | A dev launcher, not an app.                                                                   |
| Remote / distribution                                  |                       ~5% | Almost entirely by choice — see §5.                                                           |

### Where we exceed the reference

This is not a rounding note. The reference is a chat client with a terminal drawer. We are a workbench that also has
chat, and four dimensions of ours have **no counterpart at all** on the other side:

- **A real code editor.** `packages/editor-{core,react,tree-sitter,minimap,find,gutters,scope-lines,diff,lsp,panes,typescript-lsp}` (11 workspace packages), consumed at `apps/web/package.json:37-47`. The reference's _only_ editor is `apps/web/src/components/ComposerPromptEditor.tsx`, a Lexical prompt box — `ls apps/web/src/components | grep -i editor` there returns that one file.
- **Language servers.** `apps/server/src/lsp/{registry,installers,proxy-session,stdio-rpc,typescript}` plus install manifests. The reference has none: `grep -rniIl "language-server|vscode-jsonrpc|textDocument/" apps packages` over the whole reference returns **zero files**.
- **A multi-pane workbench.** `apps/web/src/features/workbench/`, `packages/tree` — resizable panels, sidebar tooling, persisted layout. (The tiling surface manager — `InteractionSurface`, drag-to-split — was removed in `21d30b57`, 2026-06-18.) The reference has no split panes at all: `grep -rlI "react-resizable-panels|ResizablePanel|tiling" apps/web/src` → zero.
- **Workspace search (79 files) and an in-app structured log viewer (29 files).** `apps/web/src/features/search/`, `apps/web/src/features/logs/`. The reference ships neither; its diagnostics story is a settings row that reveals the log directory in Finder (`apps/web/src/components/settings/SettingsPanels.tsx:1346-1379`). Ours is a filterable, live-tailing JSONL browser reachable from two surfaces (`apps/web/src/features/workbench/components/sidebar-panel.tsx:132`, `apps/web/src/features/chat-mode/components/tool-pane.tsx:54-59`).
- **Terminal rendering.** `ghostty-web` (`apps/web/package.json:61`) against their `@xterm/xterm` (`apps/web/package.json:33-34`). We render better; they manage terminals better (§ M-G).

The honest summary: **we have built the harder half of a product they have not attempted, and left unwired the easier
half they finished.** Every high-severity gap below is a wiring gap, not a build gap — except ACP.

### The failure mode, again

The previous document named it: _"a feature complete on both ends with nothing joining them, passing every test it
has."_ This sweep found it in **six new places**: the worktree service, provider instance reconciliation, model
preferences, `thread-detail-sync-store`, `WorkingRow`'s plan slot, and the `Project Scripts` palette group. It is the
single most reliable predictor of a false "closed" in this repo.

---

## 2. The gap list

Every surviving gap. `M` joins to the milestone in §3. Only `absent`/`partial` are listed; nothing classified
`present` or `diverged` appears.

| M   | id                                                | Title                                                         | Sev  | Eff | User-visible consequence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Ours                                                                                                                                                                                                                                                                      | Reference                                                                                                                                                            |
| --- | ------------------------------------------------- | ------------------------------------------------------------- | ---- | --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A   | `no-continuous-integration`                       | Nothing runs the tests before code lands                      | high | M   | A change that breaks the server suite, the two browser tests, or the desktop build reaches `main` and then the user. The only gate is a pre-commit hook that runs no tests.                                                                                                                                                                                                                                                                                                                                        | `lefthook.yml:1-23` (oxfmt/oxlint/typecheck on staged files; lines 21-23 are a commented-out bench gate marked "Move … to CI"); `git ls-files \| grep -c '^\.github'` → 0                                                                                                 | `.github/workflows/ci.yml:49-75` (Format/Lint/Typecheck/Test/Browser test/desktop build)                                                                             |
| A   | `attachment-bytes-uncapped`                       | Attachment blobs written with no check on decoded length      | med  | S   | A client declaring `sizeBytes: 1` can send an arbitrarily large data URL; the server decodes it whole into memory and writes it. One paste can fill the disk, and the size in the UI is a number the blob need not match.                                                                                                                                                                                                                                                                                          | `packages/contracts/src/chat-model.ts:52-55` (no `maxLength` on `dataUrl`); `apps/server/src/attachments/store.ts:38-70`; `apps/server/src/orchestration/engine.ts:409-461`                                                                                               | `apps/server/src/orchestration/Normalizer.ts:81-86`; `packages/contracts/src/orchestration.ts:138,164-166`                                                           |
| A   | `turn-input-uncapped`                             | No ceiling on user message text                               | low  | S   | A runaway paste writes an unbounded string into the append-only event log and every snapshot that thread will ever serve — permanently. No body limit refuses it earlier.                                                                                                                                                                                                                                                                                                                                          | `packages/contracts/src/orchestration-commands.ts:229-234` (`message.text` is bare `v.string()`); `apps/server/src/index.ts:27-36` (no `maxRequestBodySize`)                                                                                                              | `packages/contracts/src/orchestration.ts:135`; `packages/contracts/src/provider.ts:67-71`                                                                            |
| A   | `source-plan-reference-unvalidated`               | `thread.turn.start` accepts any `sourceProposedPlan`          | med  | S   | A stale client can cite a deleted or cross-project plan; the reference is persisted and the projection clears the actionable-plan flag on a thread that may not own that plan. The rail badge then describes state that never existed.                                                                                                                                                                                                                                                                             | `apps/server/src/orchestration/decider.ts:617-661` (copied straight to payload at :651); `projector.ts:232-237` latches `hasActionableProposedPlan:false` on whatever thread the client names; `read-model.ts:32-38` has no plan rows to validate against                 | `apps/server/src/orchestration/decider.ts:383-406`                                                                                                                   |
| A   | `revert-orphans-attachment-blobs`                 | Revert prunes messages, leaves image blobs                    | low  | S   | Every image on a reverted turn stays in `~/.platform/attachments` forever with no row referencing it. Revert-and-retry cycles leak disk nothing reclaims.                                                                                                                                                                                                                                                                                                                                                          | `apps/server/src/orchestration/projection-pipeline.ts:955-999` (never reads `attachmentsJson`); `thread-deletion-reactor.ts:57-63,125` only fires on `thread.deleted`                                                                                                     | `apps/server/src/orchestration/Layers/ProjectionPipeline.ts:376-440`                                                                                                 |
| A   | `dead-session-token-auth`                         | We ship an auth mode no client can satisfy                    | low  | S   | Turning on `FS_SESSION_TOKEN` locks the user out of their own UI: the web client never sends `Authorization`, and origin is checked before the token anyway. A configuration flag that can only break things.                                                                                                                                                                                                                                                                                                      | `apps/server/src/auth.ts:42,78-85,109-115,131-136`; `grep -rnI "Authorization\|Bearer" apps/web/src` → 0; the TODO at `auth.ts:185-187` concedes it                                                                                                                       | `apps/server/src/auth/http.ts:34,95,121`                                                                                                                             |
| A   | `login-shell-path-hydration`                      | Spawned tools inherit the launchd PATH                        | med  | S   | Launched from Finder/Dock, terminals open with a broken PATH and provider spawns fail "command not found" for CLIs the user has installed; SSH agent forwarding and Homebrew disappear.                                                                                                                                                                                                                                                                                                                            | `apps/desktop/src/bun/index.ts:92-97,115-118` (passes `...Bun.env` through; `withNode22Path` at :415-438 is nvm-only and Vite-only); `apps/server/src/terminal/service.ts:548-553` adds only `TERM`/`COLORTERM`                                                           | `apps/desktop/src/syncShellEnvironment.ts:19-109`; `apps/server/src/os-jank.ts:23-80`                                                                                |
| A   | `no-request-correlation`                          | No id ties a browser action to the server work it caused      | low  | S   | "Commands hang" cannot be answered: nothing joins a client wide event to the server wide event for the same interaction. This is the one half of tracing the previous plan explicitly accepted, and it was never built.                                                                                                                                                                                                                                                                                            | `apps/web/src/lib/client-logging.ts` has no `requestId`; the only server `requestId` (`provider/provider-service.ts:303-339`) is an approval id                                                                                                                           | `apps/server/src/observability/RpcInstrumentation.ts:36-89`                                                                                                          |
| A   | `no-connection-status-surface`                    | The banner never names the connection phase                   | med  | M   | When the socket dies the user sees `Chat connection failed.` at best and nothing at all while the phase is `offline` with a null error — no "Reconnecting (attempt 3)", no countdown, no manual Reconnect.                                                                                                                                                                                                                                                                                                         | `apps/web/src/features/chat/hooks/use-chat-shell-subscription.ts:16-21` (`ChatShellConnectionPhase` has no renderer); `chat-stage.tsx:72-76` renders only `shell.error`; `apps/web/src/features/chat/state/thread-detail-sync-store.ts` — **only reader is its own test** | `apps/web/src/components/WebSocketConnectionSurface.tsx:265-340`                                                                                                     |
| B   | `provider-instance-config-never-applied`          | The registry never reads settings                             | high | L   | Providers work only because the server boots a hardcoded pair. Nothing the user could ever save in settings would reach the running registry.                                                                                                                                                                                                                                                                                                                                                                      | `apps/server/src/provider/provider-adapter-registry.ts:406` — `void registry.reconcile(DEFAULT_PROVIDER_INSTANCES)` is `reconcile()`'s only non-test caller; `apps/server/src/app.ts:62-75` builds registry and `SettingsService` with nothing between them               | `apps/server/src/serverSettings.ts:528-566`; `provider/Layers/ProviderInstanceRegistryHydration.ts`                                                                  |
| B   | `settings-providers-section-inert`                | Settings › Providers is permanently empty                     | high | L   | The user cannot add a second Codex or Claude account, disable a provider, point one at a different binary, give it its own `CODEX_HOME`, or set env vars. The section reads "No provider instances configured yet." on every install.                                                                                                                                                                                                                                                                              | `apps/web/src/features/settings/components/provider-section.tsx:13-14`; `provider-row.tsx:20-25` (an enabled Switch and nothing else); the only writer `use-settings-actions.ts:51-59` maps over an always-empty array                                                    | `apps/web/src/components/settings/AddProviderInstanceDialog.tsx`; `ProviderInstanceCard.tsx:545-790`                                                                 |
| B   | `model-preferences-inert`                         | Model hide/order cannot be written and is never applied       | med  | M   | The user cannot hide a model they never use from the composer picker or pin one to the top. Settings › Models says "No model preferences yet." forever, because rows are built only from prefs that already exist. Even a populated preference would be ignored.                                                                                                                                                                                                                                                   | `apps/web/src/features/settings/utils/model-rows.ts:14-25`; `model-section.tsx:16-17`; `apps/web/src/features/chat/lib/provider-model-options.ts:102-128` (maps `provider.models` straight through)                                                                       | `apps/web/src/modelSelection.ts:88-113`; `components/settings/ProviderModelsSection.tsx:154-180`                                                                     |
| B   | `no-custom-models`                                | Cannot add a custom model slug                                | med  | M   | A new Codex slug, a proxy/OpenRouter model, or an internal build cannot be typed in, so it cannot be selected at all. `isCustom` exists in our contract and renders a badge, but every producer hardcodes `false`.                                                                                                                                                                                                                                                                                                 | `packages/contracts/src/provider.ts:85`; `apps/server/src/provider/adapters/utils/claude-models.ts:64,71,85,92`; `adapters/codex.ts:2567,2617`; `grep -rn customModels apps packages` → 0                                                                                 | `apps/web/src/components/settings/ProviderModelsSection.tsx:120-153`; `packages/contracts/src/model.ts:153-192`                                                      |
| B   | `no-provider-status-push`                         | Provider status changes never reach the client                | med  | M   | After `codex login` in a terminal, or installing a missing CLI, the composer keeps showing "Sign in required" and the old model list until a component remounts past a 60s stale window.                                                                                                                                                                                                                                                                                                                           | `apps/web/src/features/chat/lib/provider-query.ts:15-23`; `streamChanges` (`provider-adapter-registry.ts:190`) has zero non-test consumers; `packages/contracts/src/orchestration-ws.ts:284-291` has no provider frame                                                    | `apps/server/src/ws.ts:981-1004`; `packages/contracts/src/server.ts:205-207`                                                                                         |
| B   | `provider-env-vars-cleartext`                     | No secret handling for provider env vars                      | low  | M   | There is no way to give an instance an API key: any value would be stored unencrypted and echoed verbatim by `GET /settings`. Our own schema comment says so.                                                                                                                                                                                                                                                                                                                                                      | `packages/contracts/src/settings.ts:9-21`; `apps/server/src/settings/routes.ts:18` (no redaction); no secret store anywhere                                                                                                                                               | `packages/contracts/src/providerInstance.ts:104-108`; `apps/server/src/serverSettings.ts:65-86`; `auth/Services/ServerSecretStore.ts`                                |
| B   | `no-cli-version-gating`                           | Model catalog is not gated on the installed CLI               | low  | M   | The picker offers Claude models the installed CLI cannot run; the user selects one, sends a turn, and gets a provider-side failure instead of the model not being listed. Codex is unaffected (live catalog + real version).                                                                                                                                                                                                                                                                                       | `apps/server/src/provider/adapters/utils/claude-models.ts:46-60` documents the gap in-source; `adapters/claude.ts:216,236,2223` (`version: null` on every path)                                                                                                           | `apps/server/src/provider/cliVersion.ts`; `Layers/ClaudeProvider.ts:47,169-171`                                                                                      |
| B   | `no-general-preferences` ✅ **closed 2026-08-13** | Settings has a real preferences surface                       | med  | M   | 36 registered keys across Appearance, Editor, Terminal, Files, Search, Chat, Logs, Window, Providers, Models and Keyboard shortcuts, each wired to a live consumer. Per-setting reset and per-scope Reset all both ship. Colour mode, editor theme, wallpaper and diff mode now go **through settings** rather than through commands writing localStorage — the commands were rewired to the same write path. Residual: no clock format, no confirm-before-delete, no diff word-wrap, no default picker directory. | `docs/settings-reference.md`; `packages/contracts/src/settings/keys.ts`                                                                                                                                                                                                   | `apps/web/src/components/settings/SettingsPanels.tsx:839-1186`; `routes/settings.tsx:17-30`                                                                          |
| B   | `assistant-streaming-not-configurable`            | The buffered delivery path is dead code                       | low  | S   | The user always gets token-by-token text with no escape hatch on a slow machine. We built the alternative and nothing can select it.                                                                                                                                                                                                                                                                                                                                                                               | `apps/server/src/orchestration/provider-runtime-ingestion.ts:34,59,72,273` — `assistantDeliveryMode`'s only non-default caller is `tests/provider-runtime-ingestion.test.ts:42,58`                                                                                        | `packages/contracts/src/settings.ts:140`; `Layers/ProviderRuntimeIngestion.ts:1245-1285`                                                                             |
| B   | `editor-commands-not-rebindable`                  | Editor shortcuts cannot be rebound                            | med  | M   | For an editor-centred product, the largest command surface is fixed: find, replace, multi-cursor, fold, go-to-definition. The settings list filters them out because an override would silently do nothing.                                                                                                                                                                                                                                                                                                        | `apps/web/src/features/settings/components/keybinding-section.tsx:10-18`; cause is `app-runtime-content.tsx:18-22` (layers built from `defaultPlatformKeyBindings()`) vs `app-command-surface.tsx:41-46` (overrides resolved separately, later)                           | `apps/server/src/keybindings.ts:59-82`; `apps/web/src/keybindings.ts:193-220`                                                                                        |
| B   | `no-version-string`                               | The build version is nowhere in the app                       | low  | S   | A bug report has no version. (Log access is _not_ a gap — our in-app log viewer beats the reference's "Open logs folder".) Residual: no way to reveal the log directory in a file manager.                                                                                                                                                                                                                                                                                                                         | `grep -rn "APP_VERSION\|appVersion" apps/*/src packages/*/src` → 0 product hits; `apps/web/src/features/settings/panel.tsx:18-20` has no About                                                                                                                            | `apps/web/src/branding.ts:16-18`; `components/settings/SettingsPanels.tsx:1346-1379`                                                                                 |
| B   | `diff-word-wrap`                                  | No word-wrap toggle in the diff viewer                        | low  | S   | Long diff lines can only be read by scrolling horizontally; no toggle, no persisted default.                                                                                                                                                                                                                                                                                                                                                                                                                       | `apps/web/src/features/git/components/diff-view.tsx:55-62,71-77` (only `setFiles`/`setMode` pushed)                                                                                                                                                                       | `apps/web/src/components/DiffPanel.tsx:542-553,317-322,628`                                                                                                          |
| C   | `turn-start-worktree-bootstrap`                   | `thread.turn.start` cannot prepare a worktree or run setup    | high | L   | There is no "new session on a new branch" path at all. Every thread runs against the project root, so two concurrent agents edit the same checkout and stomp each other.                                                                                                                                                                                                                                                                                                                                           | `packages/contracts/src/orchestration-commands.ts:116-118` (`{createThread}` only); `decider.ts:694-717`; `chat-command-builders.ts:216` stamps `worktreePath: rootPath`; `grep -rni "setupScript\|runOnWorktreeCreate" apps packages` → 0                                | `packages/contracts/src/orchestration.ts:547-557`; `apps/server/src/ws.ts:395-435,455-465`; `apps/web/src/components/ChatView.tsx:2628-2633`                         |
| C   | `worktree-per-session`                            | Sessions cannot run in their own worktree                     | high | XL  | The user cannot start a task "on a new branch in a fresh worktree", pick a base branch, or clean one up. The whole worktree service exists and its only callers are its own tests.                                                                                                                                                                                                                                                                                                                                 | `apps/server/src/git/worktrees.ts:25,53-70,74-89,117-136` + `git/routes.ts:51-59`, callers: `git/tests/worktrees.test.ts` only; `grep -rni worktree apps/web/src` finds no client call                                                                                    | `apps/web/src/components/BranchToolbarEnvModeSelector.tsx:20-40`; `BranchToolbar.logic.ts:45-70`; `apps/server/src/git/Layers/GitCore.ts:1953-1954`                  |
| C   | `thread-branch-never-set`                         | A session's branch is always `null`                           | high | S   | Everything gated on `session.branch` never renders — creating a PR and seeing an existing PR's number have no other entry point. (Publish/push _are_ reachable from the Changes panel; that part of the previous claim was overstated.)                                                                                                                                                                                                                                                                            | `apps/web/src/features/chat-mode/components/stage-header.tsx:63`; the only writer is `chat-command-builders.ts:210` (`branch: null`); server `thread.meta.update` (`provider-runtime-ingestion.ts:158-168`) carries only `title`                                          | `apps/web/src/components/GitActionsControl.tsx:322-380`; `BranchToolbarBranchSelector.tsx:139-193`                                                                   |
| C   | `terminal-worktree-scoping`                       | The terminal opens in the project root, never the worktree    | med  | S   | With a session in a worktree, `git status`, `git diff` and `bun test` run against the wrong branch and the wrong files — silently.                                                                                                                                                                                                                                                                                                                                                                                 | `apps/web/src/features/chat-mode/components/tool-pane.tsx:82` passes the editor root; the worktree is used next door at `stage-header.tsx:66-68`; `apps/server/src/terminal/service.ts:143` `cwd: root.absolutePath`                                                      | `apps/server/src/terminal/Services/Manager.ts:37-40`; `Layers/Manager.ts:1636,1690,1815`                                                                             |
| C   | `project-scripts-unreachable`                     | Saved project scripts are unwritable                          | med  | M   | The palette's "Project Scripts" group is permanently empty — nothing can write `project.scripts`. A script cannot be marked as the worktree setup script, cannot carry an icon, and has no id to bind a key to. Only package.json discovery works.                                                                                                                                                                                                                                                                 | `packages/contracts/src/chat-model.ts:128-137` (schema is `{command,name}`, its own comment says scripts are _stored on the project_); `script-groups.tsx:27-28` filters on `saved`; the only `project.meta.update` producer is `chat-command-builders.ts:393-406`        | `packages/contracts/src/orchestration.ts:185-192`; `packages/shared/src/projectScripts.ts:11-37`; `apps/web/src/components/ProjectScriptsControl.tsx:85,223,305,455` |
| C   | `project-rename-unreachable`                      | Projects cannot be renamed or repointed                       | low  | S   | The project's name is fixed at creation; the only way out is to delete it (cascading every thread) and re-add the folder. The server accepts `title`/`workspaceRoot` and nothing sends them.                                                                                                                                                                                                                                                                                                                       | `apps/web/src/features/chat/lib/chat-command-builders.ts:393-406` (sole builder, `defaultModelSelection` only); server ready at `orchestration-commands.ts:60-71`, `decider.ts:245-263`                                                                                   | `apps/web/src/components/Sidebar.tsx:1843-1849`                                                                                                                      |
| D   | `branch-switching-ui`                             | No branch picker                                              | high | L   | The user cannot see what branches exist, switch, or create one without a terminal. Remote branches, the default branch and which worktree holds a branch are not even modelled.                                                                                                                                                                                                                                                                                                                                    | `apps/web/src/features/git/api.ts:65-84` `fetchBranches` — only caller is `tests/api.test.ts:52`; `mutation-keys.ts:2,4` (`checkout`/`createBranch` keys with no hook); `apps/server/src/git/service.ts:497-531` (plain `git branch --format`)                            | `apps/web/src/components/BranchToolbarBranchSelector.tsx:214-398,532-559`; `apps/server/src/git/Layers/GitCore.ts:1731-1900`                                         |
| D   | `git-init`                                        | A non-repo folder offers no way to initialize                 | med  | S   | Opening a folder that is not a repo shows a dead "No Git repository" label; the user must leave the app.                                                                                                                                                                                                                                                                                                                                                                                                           | `apps/web/src/features/git/panel.tsx:38-40`; `grep -rn init apps/server/src/git/{routes,service}.ts` → 0                                                                                                                                                                  | `apps/web/src/components/GitActionsControl.tsx:854-864`; `Layers/GitCore.ts:2156-2158`                                                                               |
| D   | `default-branch-guard`                            | Nothing warns before committing to `main`                     | med  | M   | A user can commit and push straight to the default branch with no prompt and no offer to move the work onto a feature branch. We do not model which branch is default in status.                                                                                                                                                                                                                                                                                                                                   | `packages/contracts/src/git.ts:17-28` (no `isDefaultBranch`); `apps/server/src/git/status.ts:6-26`; `commit-controls.tsx:66-78`                                                                                                                                           | `apps/web/src/components/GitActionsControl.logic.ts:280-328`; `Layers/GitManager.ts:1512-1546`                                                                       |
| D   | `stacked-git-actions`                             | The action ladder has two rungs and never reaches a PR        | med  | M   | Our primary button flips between Commit and "Sync Changes ↑N", which is real — but there is no Commit-&-push, no "Commit, push & PR", no Pull rung when merely behind, no "View PR", and no server composite reporting per-step results.                                                                                                                                                                                                                                                                           | `apps/web/src/features/git/components/commit-controls.tsx:22,50-78`; `utils/repository.ts:12-23`; `grep -rniE "stacked\|runStackedAction" apps` → only the diff-view mode                                                                                                 | `packages/contracts/src/git.ts:9-16,287-314`; `Layers/GitManager.ts:1548-1740`                                                                                       |
| D   | `pr-creation-has-no-review-step`                  | PR fires immediately with an empty body                       | med  | M   | Clicking "Pull request" runs `gh pr create` with the session title and no body. No edit, no base branch, no draft, no confirmation — though the server contract accepts all four.                                                                                                                                                                                                                                                                                                                                  | `apps/web/src/features/git/components/branch-actions.tsx:66-78`; `use-create-pull-request-mutation.ts:12` types base/body/draft; `apps/server/src/git/contracts.ts:102-108` validates them                                                                                | `apps/web/src/components/GitActionsControl.tsx:990-1200`; `Layers/GitManager.ts:1208-1305`                                                                           |
| D   | `disabled-action-reasons`                         | Unavailable git actions are hidden, not explained             | med  | S   | Detached HEAD, dirty tree, behind upstream, no origin, and "nothing to push" all look the same: an empty header. The `support` value that says _why_ is read and discarded.                                                                                                                                                                                                                                                                                                                                        | `apps/web/src/features/git/components/branch-actions.tsx:30,87-92,99-106`; `header.tsx:61-93` (always enabled)                                                                                                                                                            | `apps/web/src/components/GitActionsControl.tsx:117-180,884-900`                                                                                                      |
| D   | `status-missing-remote-context`                   | Status reports no origin, default branch, or host             | low  | S   | A repo with no remote silently offers actions that will fail, and non-GitHub hosts get no tailored path. (PR _support_ is already classified — that half is fine.)                                                                                                                                                                                                                                                                                                                                                 | `packages/contracts/src/git.ts:17-23,164-171`; `apps/server/src/git/service.ts:571-588` never inspects remotes                                                                                                                                                            | `packages/contracts/src/git.ts:203-221,47-54`; `packages/shared/src/git.ts:228`                                                                                      |
| D   | `long-action-progress-and-followups`              | Push/pull/PR are silent spinners                              | med  | M   | A slow network push looks identical to a wedged one: no phase, no elapsed time, no last output line. Push, pull and PR emit no success toast at all, so there is no surface for a "Create PR"/"Open PR" follow-up.                                                                                                                                                                                                                                                                                                 | `apps/server/src/git/routes.ts:78-111` (only `/commit-stream` streams); `use-push-remote-mutation.ts`, `use-create-pull-request-mutation.ts:16-17`; toasts exist only in `notify-mutation-error.ts:19` and `use-commit-mutation.ts:34,38`                                 | `packages/contracts/src/git.ts:377-434,55-80`; `GitActionsControl.tsx:575-696`                                                                                       |
| D   | `pull-not-fast-forward-only`                      | Pull runs a plain `git pull`                                  | low  | S   | On a diverged branch Pull can create a merge commit or stop mid-merge instead of refusing, and reports raw stdout rather than "Pulled" vs "Already up to date".                                                                                                                                                                                                                                                                                                                                                    | `apps/server/src/git/service.ts:541-546`; `components/header.tsx:75-83` (always enabled, no diverged check)                                                                                                                                                               | `Layers/GitCore.ts:1541-1580`; `packages/contracts/src/git.ts:316-321`                                                                                               |
| D   | `per-file-diff-stats`                             | Changes panel rows carry no ±counts                           | low  | M   | Picking what to commit means opening each file to learn whether it is a one-line tweak or a rewrite. (Turn and thread diffs _do_ show stats — that part was overstated.)                                                                                                                                                                                                                                                                                                                                           | `packages/contracts/src/git.ts:9-15`; `apps/server/src/git/status.ts` (porcelain v2 only, no `--numstat`); `components/file-row.tsx:39-68`                                                                                                                                | `packages/contracts/src/git.ts:210-220`; `Layers/GitCore.ts:1291-1310`                                                                                               |
| D   | `turn-diff-navigation`                            | The diff pane has no turn strip                               | low  | M   | Stepping back to an earlier turn's diff requires scrolling the transcript; the pane's Turn button always jumps to `latestTurnId` and there is no "All turns" chip inside it.                                                                                                                                                                                                                                                                                                                                       | `apps/web/src/features/chat-mode/components/tool-pane.tsx:99-121`                                                                                                                                                                                                         | `apps/web/src/components/DiffPanel.tsx:472-520,242-267`                                                                                                              |
| E   | `no-provider-text-generation`                     | Drivers expose no text-generation capability                  | med  | L   | The SPI has no slot to hang generation on, so commit messages, PR bodies, branch names and thread titles must all be human-written or truncated.                                                                                                                                                                                                                                                                                                                                                                   | `apps/server/src/provider/driver.ts:60-89` (`ProviderInstanceHandle = {adapter, dispose}`); `grep -rni "generateCommitMessage\|generateBranchName\|generatePrContent" apps` → 0                                                                                           | `apps/server/src/provider/ProviderDriver.ts:71`; `apps/server/src/git/Services/TextGeneration.ts:15-60`; `Layers/{Claude,Codex}TextGeneration.ts`                    |
| E   | `generated-commit-and-pr-text`                    | No generated commit message, PR body, or branch name          | med  | L   | A PR created by the app has the session title and a **completely empty body** — no Summary, no Testing. There is no way to ask for a commit message from the staged patch or a branch name from the request.                                                                                                                                                                                                                                                                                                       | `apps/server/src/git/commit-message.ts:3-20` is git's static `COMMIT_EDITMSG` block; `apps/server/src/git/pull-request.ts:82` passes `input.body ?? ''`; `branch-actions.tsx:73` sends `{title}` only                                                                     | `apps/server/src/git/Prompts.ts:25-68,82-111,155-173`; `Layers/GitManager.ts:1254`                                                                                   |
| E   | `thread-title-generation`                         | Claude threads keep a truncated prompt; renames get clobbered | med  | M   | A Claude thread's rail label is a fragment of the first message forever. And because ingestion applies provider names unconditionally, a Codex thread the user renamed by hand gets renamed back. `titleSeed` is written into the event log and read by nobody.                                                                                                                                                                                                                                                    | `grep -rn titleSeed` → producers `chat-command-builders.ts:141,192-219`, copied at `decider.ts:653`, **no consumer**; `provider-runtime-ingestion.ts:158-168` overwrites unconditionally; no Claude equivalent of `thread.metadata.updated`                               | `Layers/ProviderCommandReactor.ts:95-105,613-651,690-706` (`canReplaceThreadTitle` uses `titleSeed`)                                                                 |
| F   | `plan-right-panel`                                | No pinnable plan/tasks panel                                  | med  | M   | While the agent works a 12-step plan the full step list scrolls away inside the transcript; there is no panel to pin, no auto-open when a plan arrives, no per-turn dismissal, no narrow-width sheet. (The _current_ step is answered persistently by the rail subline — that part was overstated, and it is absent in the workbench chat side panel.)                                                                                                                                                             | `apps/web/src/features/chat-mode/utils/panels.ts:16-24` (git/files/editor/search/terminal/problems/logs — no plan tab); only plan render is `activity-row.tsx:82,115-140`                                                                                                 | `apps/web/src/components/PlanSidebar.tsx:1-330`; `ChatView.tsx:1933-2056,3501-3548`; `rightPanelLayout.ts:1-3`                                                       |
| F   | `working-row-plan-step-unreachable`               | The live step readout is built and never rendered             | med  | S   | The spinner only ever says "Working for 42s". The "3/7 — running the test suite" line `WorkingRow` can draw is unreachable because the parent never passes the plan it already computed — and the virtualizer reserves 72px for a row that renders ~52px.                                                                                                                                                                                                                                                          | `chat-timeline-items.ts:216,235-240,295-303` computes and sizes it; `timeline-row.tsx:96` renders `<WorkingRow latestTurn={…} />` with no `plan`; `working-row.tsx:8` defaults `plan = null`. Only caller passing a plan: `tests/working-row.test.tsx:20`                 | `apps/web/src/components/PlanSidebar.tsx:212-247`                                                                                                                    |
| F   | `shift-tab-plan-mode`                             | No keyboard toggle for plan mode                              | med  | S   | Flipping between Chat and Plan always costs a trip to a dropdown or typing `/plan` — you cannot toggle mid-sentence without mutating the prompt.                                                                                                                                                                                                                                                                                                                                                                   | `chat-input-submit-plugin.tsx:39-42` (`KEY_TAB_COMMAND` → menu commit only, no shift branch); the only Shift+Tab binding is `default-bindings.ts:387` (`editor.outdentSelection`)                                                                                         | `apps/web/src/components/chat/ChatComposer.tsx:1556-1563`; `ChatView.browser.tsx:2917`                                                                               |
| F   | `slash-model-command`                             | `/model` matches nothing                                      | low  | S   | The keyboard-first route into model selection does not exist. Worse, the commit path can only set an interaction mode, so a `/model` row alone would not reach the picker. **The previous plan marked this closed.**                                                                                                                                                                                                                                                                                               | `chat-input-logic.ts:41,82-97` (`'default' \| 'plan'`); `chat-input.tsx:269-288`                                                                                                                                                                                          | `apps/web/src/composer-logic.ts:5`; `ChatComposer.tsx:1440-1470`                                                                                                     |
| F   | `model-picker-keyboard`                           | No shortcut to open the picker, no jump keys                  | low  | M   | Switching models is mouse-only: a click into the composer footer plus a scroll.                                                                                                                                                                                                                                                                                                                                                                                                                                    | `apps/web/src/keymap/command-registry.ts` has no model command; `model-picker-row.tsx:17` deliberately renders no shortcut label                                                                                                                                          | `packages/contracts/src/keybindings.ts:23-48`; `ModelPickerContent.tsx:385-473,634`                                                                                  |
| F   | `user-input-digit-shortcuts`                      | Agent questions cannot be answered from the number keys       | low  | S   | Answering a blocking question always requires the mouse — a click per answer instead of a keystroke per answer.                                                                                                                                                                                                                                                                                                                                                                                                    | `pending-user-input-panel.tsx:105-126` (`onClick` only, no `<kbd>`, no listener)                                                                                                                                                                                          | `ComposerPendingUserInputPanel.tsx:88-119,145-160`                                                                                                                   |
| F   | `plan-save-to-workspace`                          | A plan cannot be written into the repo                        | low  | S   | The plan can be copied or downloaded to `~/Downloads`, but not saved as `docs/plan.md` in the workspace being worked on. The server route already exists.                                                                                                                                                                                                                                                                                                                                                          | `proposed-plan-card.tsx:63-77` (two menu items); `apps/server/src/fs/routes.ts:65-70` unused for this                                                                                                                                                                     | `ProposedPlanCard.tsx:88-156`; `apps/web/src/proposedPlan.ts:96-104`                                                                                                 |
| F   | `skill-inline-chip`                               | `$skill` stays raw text                                       | low  | M   | After picking a skill the prompt shows `$code-review` as plain, character-deletable text with no tooltip, so the user cannot re-check what it does before sending. Mentions get a chip; skills do not.                                                                                                                                                                                                                                                                                                             | `composer-skills.ts:77-79` returns a plain string; `chat-input-editor-actions.ts:185-227` builds nodes only for mention segments; `CHAT_INPUT_EDITOR_NODES = [ChatInputMentionNode]` (`chat-input-mention-node.tsx:94`)                                                   | `ComposerPromptEditor.tsx:106-111,251-310`                                                                                                                           |
| F   | `user-message-copy-button`                        | Copying a user message needs a right-click                    | low  | S   | Assistant messages get a hover copy button; user messages get none, and nothing on screen advertises the context menu.                                                                                                                                                                                                                                                                                                                                                                                             | `message-bubble.tsx:144-179` (copy gated on `!user`)                                                                                                                                                                                                                      | `MessagesTimeline.tsx:355-366`                                                                                                                                       |
| F   | `draft-attachment-loss-warning`                   | Pasted images vanish on reload, silently                      | low  | S   | Three pasted screenshots come back as text-only after a restart, with nothing having indicated they were memory-only. (Not persisting them is a deliberate, documented trade — this is only the missing warning, which the previous plan explicitly said to borrow.)                                                                                                                                                                                                                                               | `chat-draft-storage.ts:25-37` (explanation exists only in a code comment); `chat-input-attachment-list.tsx` renders no badge                                                                                                                                              | `apps/web/src/components/chat/ChatComposer.tsx:1911-1930`                                                                                                            |
| F   | `plan-implemented-rail-badge`                     | The source thread's rail row keeps a stale plan badge         | low  | S   | After splitting a plan into a new thread, the _rail_ row for the source thread keeps reporting "waiting" until a shell delta arrives; a second window watching that thread sees the plan card too. (The open thread itself resyncs correctly — that part was overstated.)                                                                                                                                                                                                                                          | `chat-projection-writers.ts:536-541,564-617` (detail writes never touch `sidebarThreadSummaryById`); `chat-projection-selectors.ts:136`; server writes cross-thread with no event (`projection-pipeline.ts:470-490`, `projector.ts:232-237`)                              | `Layers/ProviderRuntimeIngestion.ts:1066-1094`                                                                                                                       |
| F   | `checkpoint-capture-failure-silent`               | A failed capture says nothing on Claude turns                 | low  | S   | When the checkpoint for a Claude turn cannot be taken, the turn just has no diff and no revert point with no explanation. (Codex turns _do_ surface it as "Checkpoint missing/error" via the placeholder path.)                                                                                                                                                                                                                                                                                                    | `checkpoint-reactor.ts:139-172` folds the error into an outcome field; no `activity.append` in that file; the revert side _does_ append `checkpoint.revert.failed` (`provider-command-reactor.ts:366-377`)                                                                | `Layers/CheckpointReactor.ts:100-120,251,740,764`                                                                                                                    |
| F   | `palette-root-search-files-only`                  | Typing at the palette root searches files only                | med  | M   | Typing a session title gets "No matching files" unless the user already knows to type `sess ` first, and the empty root has no recent-sessions list — the conversation you were just in is not one keystroke away.                                                                                                                                                                                                                                                                                                 | `command-palette-utils.ts:235-245,281,293`; `command-palette-groups-factory.tsx:89-108`                                                                                                                                                                                   | `CommandPalette.logic.ts:210-278,330-346`                                                                                                                            |
| F   | `no-modifier-held-jump-hints`                     | Holding the jump modifier shows no digits                     | low  | S   | `Mod+Alt+1..9` session jumps exist but are invisible: the user has to count rows to guess which session is number 4. Nothing in the app tracks held modifiers (`grep addEventListener('keyup'` → 0).                                                                                                                                                                                                                                                                                                               | `default-bindings.ts:104-110`; `session-row.tsx` renders no ordinal                                                                                                                                                                                                       | `apps/web/src/shortcutModifierState.ts:29-83`; `keybindings.ts:277-301`                                                                                              |
| F   | `static-document-title`                           | The tab is titled "vite-monorepo"                             | low  | S   | Every window is labelled `vite-monorepo`; nothing says which project, session or file is open, and the `theme-color` meta is never synced.                                                                                                                                                                                                                                                                                                                                                                         | `apps/web/index.html:11`; `grep -rn document.title apps/web/src` → 0                                                                                                                                                                                                      | `apps/web/src/routes/__root.tsx:70-72`; `hooks/useTheme.ts:76-88`                                                                                                    |
| G   | `terminal-single-instance`                        | Exactly one terminal, hardcoded id                            | med  | M   | No second terminal, no split, no keeping a dev server in one shell while running commands in another — and the bottom panel and the chat tool pane fight over the same session. The server already keys sessions by id.                                                                                                                                                                                                                                                                                            | `bottom-panel.tsx:49` and `tool-pane.tsx:82` both pass `sessionId='terminal-1'`; no new/split/close command in `keymap/`                                                                                                                                                  | `ThreadTerminalDrawer.tsx:892-1210`; `terminal/Services/Manager.ts:35-47`                                                                                            |
| G   | `terminal-history-durability`                     | Scrollback is memory-only, TTL 10 minutes                     | med  | M   | Restarting the backend (every server edit under `--watch`) destroys every terminal and its output; leaving the tab closed 10 minutes kills the shell and whatever ran in it; the 256KB cap trims by chunk, so long build output truncates mid-escape-sequence.                                                                                                                                                                                                                                                     | `apps/server/src/terminal/service.ts:64-65,71,318-325,339-351`                                                                                                                                                                                                            | `terminal/Layers/Manager.ts:916-1050`; `Services/Manager.ts:69-73,528`                                                                                               |
| G   | `terminal-restart`                                | A dead shell is dead in place                                 | low  | S   | Type `exit` and the panel prints "Process exited 0" and stops; recovery depends on the panel happening to remount. (Clear and Reset _do_ exist in the right-click menu — that part was overstated.)                                                                                                                                                                                                                                                                                                                | `packages/contracts/src/terminal.ts:8-11` (`input\|resize\|dispose`); `service.ts:255-259` disposes on exit; `terminal-panel.tsx:354-357`                                                                                                                                 | `terminal/Services/Manager.ts:96-103`; `ThreadTerminalDrawer.tsx:586`                                                                                                |
| H   | `desktop-standalone-app`                          | The desktop app is a dev launcher                             | high | L   | The built `.app` locates this git checkout or aborts, then spawns `bun apps/server/src/index.ts` and a Vite dev server **from source**. You cannot run a stable copy of the workbench while refactoring the workbench — the app you are using is the code you are breaking.                                                                                                                                                                                                                                        | `apps/desktop/src/bun/index.ts:25,453-489` (`resolvePlatformRoot` throws otherwise), `:87-99` spawns TS source, `:101-120` spawns `vite --strictPort`; `electrobun.config.ts:1-34` has no packaging config                                                                | `apps/desktop/src/main.ts:718-743,1383`; `apps/server/src/config.ts:173-192`                                                                                         |
| H   | `single-origin-serving`                           | The server never serves the client                            | med  | M   | There is no single URL that is the product: even production runs `vite preview` on a second port, which is why the CORS allowlist is per-port.                                                                                                                                                                                                                                                                                                                                                                     | `apps/server/src/app.ts:103-112` (no static route, no `@elysiajs/static`); `scripts/prod.ts:58-81`; `apps/server/src/auth.ts:25-32`                                                                                                                                       | `apps/server/src/config.ts:173-192`                                                                                                                                  |
| H   | `desktop-native-integration`                      | The shell exposes only a file picker                          | med  | M   | No app menu (no Settings accelerator, no zoom, no About), no native context menu with working Cut/Copy/Paste, no OS-theme sync — and **external links have no system-browser handoff, so clicking a PR link opens it inside the CEF webview**.                                                                                                                                                                                                                                                                     | `apps/desktop/src/shared/rpc.ts:10-12` (`{pickEntry}` is the whole bridge); `markdown-link-menu.tsx:25` `window.open`; `branch-actions.tsx:50-64` bare `<a target=_blank>`                                                                                                | `apps/desktop/src/preload.ts:28-88`; `main.ts:921-989,1762-1782,1704`                                                                                                |
| I   | `no-acp-driver`                                   | No ACP support                                                | high | XL  | The user can only chat with Codex and Claude. Cursor Agent — and any agent speaking the Agent Client Protocol — cannot be added at all. The client half (agent-initiated `fs/read_text_file`, `terminal/create`, `session/request_permission`) is also missing.                                                                                                                                                                                                                                                    | `drivers/built-in.ts:17` registers `[codexDriver, claudeDriver]`; `provider/types.ts:96-97` has `'acp.jsonrpc'` in a union **with no producer**; no ACP dependency in `apps/server/package.json:31-49`                                                                    | `packages/effect-acp/src/{client,protocol,terminal}.ts`; `apps/server/src/provider/acp/*`; `Drivers/CursorDriver.ts:64`; `builtInDrivers.ts:44-49`                   |
| I   | `checkout-pull-request`                           | Cannot open an existing PR as a session                       | med  | L   | A user cannot paste a PR URL / `#42` and have the app resolve it, show its title and base→head, and check it out into the current checkout or a dedicated worktree.                                                                                                                                                                                                                                                                                                                                                | `apps/server/src/git/pull-request.ts:44-104` exports exactly `readPullRequest` + `createPullRequest`, both keyed on the current branch; no reference parser anywhere                                                                                                      | `PullRequestThreadDialog.tsx:35-291`; `apps/web/src/pullRequestReference.ts:6-29`; `Layers/GitManager.ts:1335-1500`                                                  |
| I   | `project-favicon-absent`                          | Projects have no visual identity                              | low  | M   | With more than a handful of projects the sidebar, palette and settings lists are an undifferentiated wall of text.                                                                                                                                                                                                                                                                                                                                                                                                 | `grep -rIln "project-favicon\|projectIcon" apps packages` → 0; `session-group-header.tsx:48-50` is a caret and a label                                                                                                                                                    | `project/Layers/ProjectFaviconResolver.ts:1-60`; `apps/web/src/components/ProjectFavicon.tsx:8-34`                                                                   |

---

## 3. Milestones

Foundation order still holds: **contracts → persistence → orchestration → transport → client store → UI.** A–B–C are
strictly ordered by dependency; F, G, H are independent of everything and of each other.

### M-A — Guardrails (no new surface until these hold)

`no-continuous-integration` · `attachment-bytes-uncapped` · `turn-input-uncapped` · `source-plan-reference-unvalidated` ·
`revert-orphans-attachment-blobs` · `dead-session-token-auth` · `login-shell-path-hydration` · `no-request-correlation` ·
`no-connection-status-surface`

Everything after this milestone adds reachable surface; this one makes sure a regression is caught, a paste cannot
exhaust the disk, a stale client cannot corrupt another thread's projection, and a broken socket says so. Two shapes to
copy from ourselves: measure the decoded byte length and persist _that_ (`Normalizer.ts:81-86`), and validate the plan
reference the same way `command-invariants.ts` validates everything else. `dead-session-token-auth` is a delete-or-fix
decision, not a build — per CLAUDE.md's greenfield rule, an auth mode nothing can satisfy should not survive the pass.

**Exit criterion — done when** a user can push a branch and watch the whole suite (including the two `*.browser.tsx`
tests that today run nowhere) pass or fail on it before merging; can paste a 200 MB image and get a clean rejection
instead of a dead server; and, when their laptop wakes, sees "Reconnecting — attempt 3" with a Reconnect button instead
of a conversation that quietly stopped updating.

### M-B — Make the settings document mean something

`provider-instance-config-never-applied` · `settings-providers-section-inert` · `model-preferences-inert` ·
`no-custom-models` · `no-provider-status-push` · `provider-env-vars-cleartext` · `no-cli-version-gating` ·
`no-general-preferences` · `assistant-streaming-not-configurable` · `editor-commands-not-rebindable` ·
`no-version-string` · `diff-word-wrap`

Order inside the milestone: **the registry↔settings link first** (`app.ts:62-75` currently builds both with nothing
between them), then the editor UI, then everything that reads the document. Building the UI first produces rows the
server ignores — the exact failure this codebase keeps producing. `provider-env-vars-cleartext` is a hard prerequisite
of the env editor, not a follow-up: do not ship a field that writes a token to a world-readable JSON the settings route
returns verbatim. `editor-commands-not-rebindable` is a wiring fix at
`app-runtime-content.tsx:18-22` — resolve overrides _before_ `editorKeymapLayersFromPlatform`, then delete the filter at
`keybinding-section.tsx:16-18`.

**Exit criterion — done when** a user can add a second Codex account on its own `CODEX_HOME`, disable it, type in a
model slug the probe never enumerated and select it in the composer, hide a model they never use so it leaves the
picker, rebind a find-in-file shortcut, and set the clock format — each surviving a restart, and the provider list
refreshing itself after `codex login` in a terminal without a reload.

### M-C — Session isolation: worktrees, end to end

`turn-start-worktree-bootstrap` · `worktree-per-session` · `thread-branch-never-set` · `terminal-worktree-scoping` ·
`project-scripts-unreachable` · `project-rename-unreachable`

The server half is done and has zero callers (`git/worktrees.ts`, routed at `git/routes.ts:51-59`). What is missing is
the command (`threadTurnBootstrapSchema` needs `prepareWorktree` + `runSetupScript`), the decider branch, the
`thread.meta.update` that stamps branch + worktreePath, and the composer's env-mode selector. `thread-branch-never-set`
is an S-sized fix with outsized leverage: it is what mounts `BranchActions` at all, which is what makes M-D's PR work
visible. Project scripts belong here because the setup script _is_ a project script with `runOnWorktreeCreate` set —
building the script editor twice would be waste; `project-rename-unreachable` rides the same project-meta dialog.

**Exit criterion — done when** a user can start a session on a new branch in its own worktree, watch the project's
setup script run in a terminal as part of that bootstrap, see the branch name in the session header, and have the
terminal beside that session already `cd`'d into the worktree so `git status` reports the agent's work and not the main
checkout's.

### M-D — The git workflow the branch unlocks

`branch-switching-ui` · `git-init` · `default-branch-guard` · `stacked-git-actions` · `pr-creation-has-no-review-step` ·
`disabled-action-reasons` · `status-missing-remote-context` · `long-action-progress-and-followups` ·
`pull-not-fast-forward-only` · `per-file-diff-stats` · `turn-diff-navigation`

`status-missing-remote-context` comes first inside the milestone — `hasOriginRemote` / `isDefaultBranch` /
`hostingProvider` on the status payload is the data `default-branch-guard`, `disabled-action-reasons` and the ladder's
rung selection all read. Extend `apps/server/src/git/service.ts:497-531` to the reference's shape (`--remotes`,
`origin/HEAD`, `worktree list --porcelain`, recency sort) rather than bolting a second query beside it.

**Exit criterion — done when** a user can open a folder that is not a repo and initialize it, create and switch
branches from the app, and take a change from "Commit" to an open pull request through one state-aware button that
streams each step's output, tells them exactly why any rung is unavailable ("Add an origin remote before pushing"), and
warns before the commit lands on `main`.

### M-E — Provider text generation

`no-provider-text-generation` · `generated-commit-and-pr-text` · `thread-title-generation`

One capability, four call sites. The previous plan rejected the commit-message call site alone as convenience; that
reasoning is sound for commit messages and wrong as a reason not to build the slot, because two of the other three
(thread titles, PR bodies) were _already accepted as open work_ in the same document. Add
`textGeneration` to `ProviderInstanceHandle` (`provider/driver.ts:60-67`), then `titleSeed` finally has a consumer and
the `canReplaceThreadTitle` guard stops manual renames from being clobbered by
`provider-runtime-ingestion.ts:158-168`.

**Exit criterion — done when** a user can leave the commit message blank and get one written from the staged patch,
open a PR whose body has Summary and Testing sections, and rename a thread by hand without the provider renaming it
back on the next turn — with Claude threads getting a generated title like Codex threads already do.

### M-F — Plan visibility and a keyboard-first composer

`plan-right-panel` · `working-row-plan-step-unreachable` · `shift-tab-plan-mode` · `slash-model-command` ·
`model-picker-keyboard` · `user-input-digit-shortcuts` · `plan-save-to-workspace` · `skill-inline-chip` ·
`user-message-copy-button` · `draft-attachment-loss-warning` · `plan-implemented-rail-badge` ·
`checkpoint-capture-failure-silent` · `palette-root-search-files-only` · `no-modifier-held-jump-hints` ·
`static-document-title`

Independent of every other milestone; almost entirely S-sized. Start with
`working-row-plan-step-unreachable` — it is two lines (`timeline-row.tsx:96` passes `item.plan`) and it also fixes a
20px-per-row virtualizer over-estimate. The plan panel goes in the existing tool-pane tab enumeration
(`chat-mode/utils/panels.ts:16-24`), which is why it is M and not L. `slash-model-command` needs the commit path at
`chat-input.tsx:269-288` to be able to open a surface, not just set a mode — that is the real work; the row is trivial.

**Exit criterion — done when** a user can run a 12-step plan with the step list pinned open beside the transcript,
watch the spinner say "3/7 — running the test suite", flip to Plan mode and back with Shift+Tab mid-sentence, open the
model picker and select a model entirely from the keyboard, answer a blocking agent question by pressing `2`, and find
a session by typing its title straight into the palette.

### M-G — Terminals plural

`terminal-single-instance` · `terminal-history-durability` · `terminal-restart`

The server already keys sessions by `(root, sessionId)` (`terminal/service.ts:134`); the client asks for
`'terminal-1'` in two places. Persist history to disk before adding instances — N terminals whose scrollback dies on
every `--watch` restart is worse than one.

**Exit criterion — done when** a user can keep a dev server running in one terminal while running tests in another,
restart a shell in place after it exits without remounting the panel, and find both terminals with their scrollback
intact after the backend restarts.

### M-H — The desktop app as an actual app

`desktop-standalone-app` · `single-origin-serving` · `desktop-native-integration`

`single-origin-serving` first: the server serving `dist/client` is what lets the packaged shell stop spawning Vite.
Justified not by distribution (see §5) but by self-hosting — today you cannot use the workbench while breaking the
workbench, because the app _is_ the working tree.

**Exit criterion — done when** a user can launch the packaged app from the Dock while the repo sits on a half-finished
branch and have it work, reach one URL that serves the whole product, use a real application menu and native
copy/paste, and click a PR link and land in their actual browser.

### M-I — Optional breadth, last

`no-acp-driver` · `checkout-pull-request` · `project-favicon-absent`

ACP is the only genuinely XL build left and the only settled decision I am reopening on cost grounds (§5). It should
not start until M-B lands: adding a third driver to a registry the user cannot configure produces an instance nobody
can create.

**Exit criterion — done when** a user can add a Cursor Agent instance in settings, pick `composer-2` from the composer,
and have that agent read files, run a terminal command and request permission through our own services.

---

## 4. What the previous plan got wrong

Nine "closed" claims do not survive. All nine share one root: they were verified as _built_, not as _reachable_.

1. **M10 — "`git-worktree-support`, `contracts-turn-start-bootstrap-worktree` — closed."** False. `git/worktrees.ts` has
   zero callers outside `git/routes.ts` and `git/tests/worktrees.test.ts`; `threadTurnBootstrapSchema`
   (`orchestration-commands.ts:116-118`) still has only `createThread`; and every thread is stamped
   `worktreePath: rootPath` (`chat-command-builders.ts:216`), which `session-provider.tsx:93-95` notes is the project
   root and permanent. No session has ever run in a worktree.
2. **N4 exit — "a branch can be pushed and a PR opened from chat."** Half true. Push and publish are reachable from the
   Changes panel (`git/components/header.tsx:85-93` → `--set-upstream`). The PR half is gated on `session.branch`
   (`stage-header.tsx:63`), which nothing ever writes, so `BranchActions` never mounts outside its own test.
3. **M8 — "`provider-settings-reconciliation`, `provider-multi-instance` — closed."** `reconcile()`'s only non-test
   caller passes a hardcoded constant (`provider-adapter-registry.ts:406`), nothing seeds `settings.providerInstances`,
   and the sole writer (`withProviderEnabled`) maps over an array that is always empty.
4. **N1 exit — "settings opens from a real entry point."** True, and it did not deliver the stated consequence. Two of
   the three sections render permanent empty states (`provider-section.tsx:13-14`, `model-section.tsx:16-17`) and the
   third excludes every editor command by design. Mounting the panel made **zero** new preferences reachable.
5. **N3 exit — "user keybindings can be edited and actually take effect."** Workspace commands only.
   `keybinding-section.tsx:16-18` filters out every `editor.*` id, and its comment says why.
6. **M4 — "`projection-cache-no-per-thread-status-or-error` — closed."** `thread-detail-sync-store.ts` is written by
   `thread-detail-subscriptions.ts:345,420` and read by `tests/thread-detail-subscriptions.test.ts:19`. Nothing renders it.
7. **M6 — "`composer-slash-model-command` — closed."** `/model` does not exist:
   `ChatInputSlashCommand = 'default' | 'plan'` (`chat-input-logic.ts:41`).
8. **N4 — "`git-stacked-actions` — closed."** A two-rung ladder shipped (Commit / Sync Changes). There is no
   commit+push, no PR rung, no Pull-when-behind rung, and no server composite reporting per-step results — and hook
   streaming, the reference's whole point, covers commit only.
9. **N4 — "project scripts landed with both ends attached."** The _discovery_ half landed. The contract's own comment
   (`chat-model.ts:128-133`) says scripts are stored on the project, the palette renders a `Project Scripts` group
   filtered on `saved` (`script-groups.tsx:27-28`), and nothing can ever write one. That group is dead UI.

Two accurate closures with residuals worth naming, not corrections: attachment blobs _are_ reclaimed on thread deletion
but not on revert (`revert-orphans-attachment-blobs`), and the plan-implement resync _does_ work for the open thread but
not for its rail summary (`plan-implemented-rail-badge`).

---

> [!IMPORTANT]
> **Two of §5's "settled" clusters were overruled by the user on 2026-08-12, after this sweep was authored.**
> URL routing and multi-environment backends are both being planned as work, not declined. §5's argument against
> routing rests on "no remote, no notifications, no external surface that could hold a link" — a premise that
> environments removes. The two decisions were coupled all along, and taking both together dissolves the objection
> to each. Read those two rows as the case that had to be answered, not as the current decision.

## 5. Deliberate non-parity

> **Environment decision superseded 2026-09-05.** The rows below record the earlier audit.
> [The environment strategy](environments-and-remote-plan.md) now selects connected machines
> with separate checkouts. Plan 077 supplies the runtime foundation; Plans 068 and 078 add
> scoped domain references and federation. Pairing remains deferred.

### Reaffirmed — settled previously, nothing in this sweep changes the reason

| Cluster                                                     | Findings dropped                                                                                                                                                                                    | Why still no                                                                                                                                                                                                                                                                                                                                                           |
| ----------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Remote access, pairing, device sessions**                 | `remote-device-pairing`, `no-device-pairing`, `server-lan-binding`, `headless-serve-pairing-output`, `desktop-network-exposure-control`                                                             | Settled twice. `assertLoopbackHost` (`apps/server/src/index.ts:24,97-101`) is not a gap — it is the decision expressed as an invariant that fails loudly. Nothing here is a defect; it is a product we chose not to be.                                                                                                                                                |
| **Multi-environment backends**                              | `multi-environment-backends`                                                                                                                                                                        | Settled as `transport-single-implicit-environment`. We are a local workbench against one server we ship. The `ChatEnvironment` seam stays; entity keys do not get scoped. Reopen only with remote access, never before.                                                                                                                                                |
| **URL routing / deep links**                                | `no-url-routing`                                                                                                                                                                                    | Our multi-pane workbench _is_ the "two things at once" answer, and it is better than two browser windows; the desktop shell opens one `BrowserWindow` (`apps/desktop/src/bun/index.ts:132,154`); and with no remote, no notifications and no marketing there is no external surface that could hold a link. The one cheap residual — the tab title — is kept as a gap. |
| **Responsive / mobile shell**                               | `no-responsive-shell`                                                                                                                                                                               | Settled as `composer-mobile-enter-newline`. The only reason the reference needs it is the phone-over-pairing story we are not building.                                                                                                                                                                                                                                |
| **Distribution and release engineering**                    | `cli-binary-and-subcommands`, `marketing-and-distribution`, `desktop-auto-update`, `no-license-or-contribution-files`, `no-product-telemetry`, `no-user-facing-docs`, `desktop-persistent-settings` | Zero external users, per CLAUDE.md. Every consequence in this cluster is "someone else cannot obtain, update or measure the app". **Trigger condition:** the day a second person is handed a build, this becomes M0 and `LICENSE` is the first file in it. Note `desktop-standalone-app` is _not_ in this cluster — it is kept for a self-hosting reason (§ M-H).      |
| **OTLP spans and metrics**                                  | `no-tracing-or-metrics` (span/metric half)                                                                                                                                                          | Our house style is wide events, stated in CLAUDE.md. The earlier decision also named the one real gap — client↔server correlation — and prescribed propagating a request id. That prescription was never executed, so it is kept as `no-request-correlation` in M-A. The exporter is not.                                                                              |
| **OpenCode driver**                                         | `no-opencode-driver`                                                                                                                                                                                | ACP is the general answer to "more agents"; OpenCode is a fourth bespoke adapter reaching models the other three already reach. Build ACP or build nothing.                                                                                                                                                                                                            |
| **Open in VS Code / Cursor / Zed**                          | `open-in-external-editor`                                                                                                                                                                           | We are the editor. Parity here is handing users a way out of our own product. The genuinely useful residual — the desktop bridge cannot hand a URL to the system browser, so PR links open inside CEF — is kept inside `desktop-native-integration`.                                                                                                                   |
| **Repository identity (remote, owner/name, canonical key)** | `repository-identity-absent`                                                                                                                                                                        | Settled as `contracts-repository-identity`; its purpose is matching one repo across machines, which needs multi-environment. And the stated consequence is already handled locally: `project-qualifiers.ts:8-33` attaches a distinguishing parent path segment to repeated folder names, rendered at `session-rail-model.ts:223-235`.                                  |
| **Draft image persistence**                                 | —                                                                                                                                                                                                   | Unchanged: `chat-draft-storage.ts:25-37` protects the text draft from the ~5 MB localStorage quota. Only the warning is borrowed (`draft-attachment-loss-warning`).                                                                                                                                                                                                    |
| **Commit-message generation as a standalone convenience**   | —                                                                                                                                                                                                   | Still agreed as convenience _on its own_. It rides M-E for free because the slot is being built for titles and PR bodies; it does not justify the slot.                                                                                                                                                                                                                |

### Reopened — the earlier reason expired, stated explicitly

- **`no-acp-driver`.** Previously deferred under `provider-missing-drivers` on cost grounds — and that row already
  said "ACP first — Cursor and Grok both ride it." The cost basis has since changed twice: the `ProviderDriver` SPI
  landed (a new driver is an implementation, not a fourth hardcoded singleton), and M-B makes instances configurable at
  all. The deferral was never a rejection; this plan schedules it (M-I) rather than re-deciding it.
- **`no-provider-text-generation`.** The earlier decision (`git-ai-commit-message`) evaluated one call site and judged
  it convenience — correctly. It did not weigh that the same driver capability is the missing prerequisite for thread
  title generation, which the _same document_ left open twice, and for PR bodies, which N4 shipped empty. Scoped as one
  capability with four call sites, it pays for itself; scoped as "AI commit messages", it does not.
- **`checkout-pull-request`.** Previously dropped because it "only pays off with a 'start a thread from a PR' flow and
  a git-host integration we do not have." Both preconditions now exist or are scheduled: `gh` integration shipped
  (`apps/server/src/git/pull-request.ts:106-127` probes auth and classifies failures), and M-C builds the worktree that
  would host the checkout. Ranked last in M-I and genuinely optional.

---

## 6. Convention notes for implementers

Per `CLAUDE.md`, applied to the paths above:

- **Kebab-case, group by feature then kind, no barrels.** New server work lands in the owning feature dir
  (`apps/server/src/git/`, `provider/`, `orchestration/`). New pure client modules go in `utils/`; do not grow
  `features/chat/lib/` further.
- **One component per file, one hook per file.** The provider-instance editor and the branch picker are the two places
  this will be tempting to violate — the reference does it in single 800- and 1 250-line files.
- **No prop drilling.** The plan panel reads from a narrow provider action, not props threaded through the tool pane;
  worktree/branch state belongs to the session provider that already owns `session.worktreePath`.
- **Never-nester.** The stacked-action rung selector, the branch-list merge (local + remotes + worktrees + default) and
  the reconcile diff are all guard-clause shaped. Keep nesting ≤ 3, no nested ternaries.
- **Theme tokens only.** Diff stats `diff-added`/`diff-removed`, blocked actions `warning`, failures `destructive`,
  plan-ready `info`. Provider status pills use `success`/`warning`/`destructive`, never a palette hue.
- **`tabular-nums`** on every number that moves: `+N/−M` diff stats, ahead/behind counts, plan step counters, reconnect
  attempt counts and countdowns, terminal restart counters.
- **Errors via `createStructuredError` / `defineErrorCatalog`.** New catalogs needed for worktree bootstrap, setup-script
  failure, attachment size rejection and branch checkout — never `new Error`.
- **Wide events, not extra lines.** Enrich the one event per operation: `worktreePath`/`baseBranch`/`setupExitCode` on
  bootstrap, `providerInstanceCount`/`reconcileAdded`/`reconcileRemoved` on reconcile, `declaredBytes`/`actualBytes` on
  attachment writes, `blobsReclaimed` on revert prune.
- **Tests are Vitest against the real in-process server** (`apps/web/test/fixtures.ts`, real `treaty` client,
  `MockProviderAdapter`, a real `git init` temp repo for every worktree and branch test). Run app tests with
  `bun --bun vitest`. `--project browser` hangs locally — which is precisely why M-A's CI job is the milestone that
  makes those two files mean anything.
- **Greenfield.** When M-B changes the settings document shape, or M-C changes what `worktreePath` means for existing
  threads, delete the stale state (the SQLite file, the projection cache, `chatModeSelection` in localStorage) or say
  what to delete. No migration code.

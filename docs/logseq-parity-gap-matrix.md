> [!WARNING]
> **Stale in one specific way — block surfaces no longer exist.** This sweep read the engine repo _before_ the block-surface system (`registerBlockProvider`, `EditorBlock*`, block rows and lanes) was deleted from `../Editor` on 2026-08-22 — ~4,200 lines across 57 files, zero production consumers. Roughly 15 rows below still name it as an available mechanism or recommend spiking it; **it is gone, and those rows should be read as pointing at the replacement primitive instead.** The engine ceiling those rows describe is real and, if anything, harder now: the only code path that ever produced a non-uniform row height went with it. What survived on purpose is the variable-row-height virtualizer machinery and `InlineReplacementSpec.render`. See [Engine prerequisite — the widget-row primitive](logseq-parity-implementation-plan.md#engine-prerequisite--the-widget-row-primitive).

> [!NOTE]
> **Generated gap matrix — 2026-08-22.** Companion to [logseq-port-map.md](logseq-port-map.md), which records where each row would live and what porting it would actually cost, and [logseq-parity-implementation-plan.md](logseq-parity-implementation-plan.md), which assigns every non-✅ row below to a wave. Produced by a 15-agent sweep: 9 agents surveyed Logseq domain by domain, 6 inventoried our own code (platform app, server, and the separately-published editor packages), and every have/partial claim was then re-opened and adversarially falsified against the files it cited — 40 of 132 such claims were downgraded, each with its refutation folded into the row's notes.

# Logseq Parity Gap Matrix

Reference (pinned at analysis time):

- `references/logseq` — logseq/logseq `fab2774` (2026-08-18)

Status is OUR coverage of the feature, not Logseq's. Effort = cost to build **in our stack** (React + our own editor engine + Bun/Elysia + SQLite), not the cost of transliterating their Clojure: S = days, M = 1–2 weeks, L = 3–6 weeks, XL = multi-month. Priority: core = daily expectation for a notes app, important = differentiating, niche = rarely used. Layer names the code that would own the row: `editor-core` / `editor-markdown` / `editor-new-package` live in the separate `@singapor` repo and must stay host-agnostic; `platform-web`, `platform-server` and `contracts` are this repo; `ambiguous` marks a row whose home cannot be fixed until the user settles a stated architectural fork. Source is the cheapest honest route: **port** = transliterate their code, which produces a derivative work carrying AGPL-3.0 obligations including the network-use clause; **reimplement** = rebuild from the behavioural/algorithmic description, no license exposure; **build-fresh** = Logseq is not a usable reference and the design starts from our own stack.

## Block/outliner data model, DataScript schema, transaction pipeline, undo/redo, file↔DB sync, and persistence

77 features — 0 have, 6 partial, 71 missing.

| Feature                                                         | Ours       | Effort | Priority  | Layer           | Source      | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --------------------------------------------------------------- | ---------- | ------ | --------- | --------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Parent-pointer tree**                                         | ❌ missing | M      | core      | platform-server | reimplement | Nothing tree-shaped exists for notes. packages/tree is a FILE tree: identity is the path string, structure is derived by splitting segments, order comes from a comparator with no per-node order field, and rows are uniform height (packages/tree/src/utils/model/publicTypes.ts:6-10, :28-36; utils/model/layout.ts:84-122) — it cannot back an outliner. New tables + a numbered migration; index parent for O(children) reads and keep a per-parent sorted child cache in the web projection so sibling lookup is O(log n) instead of Logseq's O(siblings) :avet scan.                                                                                                                                                                                                                                |
| **Pages are blocks**                                            | ❌ missing | M      | core      | platform-server | reimplement | One entity kind with an optional name/title, so a page can parent a page (namespaces) and top-level blocks parent to the page row. Cheap now, very expensive to retrofit later — every op signature (move, delete, insert target resolution) branches on page-vs-block. Our current world has no page concept at all; a note is a file path.                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Insert blocks**                                               | ❌ missing | L      | core      | contracts       | reimplement | Not in the inventory as a user-visible row but half the domain depends on it. The one invariant to keep from their code: the right-sibling order boundary is re-read from the live store at transaction time, never taken from the caller — a stale boundary passed from the renderer is what makes a new block sort after a concurrently inserted one. Also keep blocksWithLevel as a single O(n) pass with two id->level maps.                                                                                                                                                                                                                                                                                                                                                                           |
| **Move blocks**                                                 | ❌ missing | M      | core      | contracts       | reimplement | The whole point of :block/order is that a move is ONE attribute write on the moved block — no neighbour is touched. The only O(subtree) part is rewriting :block/page on descendants when the page changes; if we derive page from the ancestor chain at query time instead of denormalising, even that disappears. Reject :block/left and any prev-sibling variant outright.                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Indent with Tab**                                             | ❌ missing | M      | core      | contracts       | reimplement | Three behaviours that look optional and are not: append after the left sibling's existing last child, auto-expand a collapsed left sibling in the SAME transaction (otherwise blocks visibly vanish for a frame), and skip blocks already parented to the left sibling so repeated Tab is idempotent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Outdent with Shift-Tab (direct mode)**                        | ❌ missing | M      | core      | contracts       | reimplement | The right-sibling sweep is the difference between a correct outliner and one that appears to reorder the list when you outdent a middle item. It runs after the outdent move, reading the new state, and re-parents the former right siblings under the outdented block (or after its last child).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Move block up / down**                                        | ❌ missing | M      | core      | contracts       | reimplement | The up/down asymmetry is load-bearing: 'above X' means 'after X's last descendant', 'below X' means 'after X'. Getting it wrong makes Alt-Shift-Up jump whole subtrees. Also aborts when the anchor resolves to the page itself.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Drag-and-drop move to sibling or child**                      | ❌ missing | M      | core      | platform-web    | reimplement | packages/tree already has a working drag-drop interaction shape to copy (canDrag/canDrop/onDropComplete, packages/tree/src/utils/model/publicTypes.ts:202-247), but its model is path-keyed so it cannot be reused wholesale. Dropping onto a collapsed block must append after the whole subtree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Move refuses to create a cycle**                              | ❌ missing | S      | core      | contracts       | reimplement | Abort the whole operation with zero writes rather than partially applying. Cheap to implement, and it is the difference between a bad drag and an unrenderable graph (a cycle makes every recursive walk non-terminating).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Multi-block move keeps relative order**                       | ❌ missing | M      | core      | contracts       | reimplement | Requires read-your-writes inside one batch — the equivalent of their temp-conn. In our stack that is a staging layer over the pending write set, not a second SQLite connection. Reduce the selection to top-level blocks first (drop any block whose ancestor is also selected).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Enter splits a block at the cursor**                          | ❌ missing | L      | core      | ambiguous       | reimplement | Decide this before anything else in the interaction layer — it determines whether headings/live preview can vary row height (engine caps text rows at a uniform height, Editor repo packages/markdown/src/style.css:7) and whether undo can be structural. Behaviour either way: text before the caret stays, text after becomes a new block, inserted as first child when the block has children and is expanded, otherwise as the next sibling.                                                                                                                                                                                                                                                                                                                                                          |
| **Backspace at start merges into previous block**               | ❌ missing | L      | core      | ambiguous       | reimplement | The correctness bar is child preservation: the deleted block's children must be re-parented under the merge target, and the caret must land exactly at the join offset. Our current undo is text-only (Editor repo packages/editor/src/history.ts:24, snapshot stack capped at 200) and cannot record the re-parent, so a merge would be half-undoable until semantic undo exists.                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Delete removes the whole subtree**                            | ❌ missing | M      | core      | platform-server | reimplement | Their expandDeleteBlocksTx exists precisely because the renderer's idea of the subtree can be minutes old. In SQLite this is a recursive CTE over parent, which is cheaper and simpler than their BFS. Pages take a different path (detach parent/order/page, keep the entity) because Recycle supplies the rest.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Block uuid is immutable for life**                            | ❌ missing | S      | core      | platform-server | reimplement | Cheapest possible invariant and it underpins refs, undo replay and any future sync. In our stack the strongest form is to leave uuid out of the update op schema entirely, so it is unrepresentable rather than merely rejected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Deterministic journal uuids**                                 | ❌ missing | S      | core      | contracts       | reimplement | 00000001-YYYY-MMDD-0000-000000000000. Two devices or two imports creating the same day merge instead of duplicating. Cheap, high value, and impossible to retrofit once random journal uuids are in the wild.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Id-based references**                                         | ❌ missing | L      | core      | platform-server | reimplement | THE decision that makes page rename O(1) instead of O(referencing files) — the single biggest structural win over Obsidian, where rename rewrites every file. Implement the projection as an explicit function over a resolved-ref map, NOT as a getter trap with a swallowing try/catch (theirs silently yields blank titles). Do not substitute block refs inside titles or mutual refs recurse. Note this fights a file-authoritative model: a .md file containing [[uuid]] is unreadable outside the app, which is exactly the fork in 'Mirror is one-way'.                                                                                                                                                                                                                                            |
| **Automatic ref rebuilding**                                    | ❌ missing | M      | core      | platform-server | reimplement | Full replace of the ref set per touched block, not a diff. Sources: [[uuid]] tokens in the raw title, tags, link, property keys, and journal-day refs derived from datetime property values. Must remove self-refs and alias-refs or the renderer loops. Skip derivation for import/undo/remote paths or replayed data gets rewritten locally.                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Paste preserves indentation**                                 | ❌ missing | M      | core      | contracts       | reimplement | Get the ragged-indentation case right (2 spaces then 4 then 3): pop the stack down to frames at or below, and re-process the same block rather than consuming it. The engine exposes a paste hook (EDITOR_PASTE_HANDLER, Editor repo packages/editor/src/plugins.ts:474-510) with zero production consumers — usable if we go single-buffer.                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Copy re-mints uuids, cut and drag keep them**                 | ❌ missing | M      | core      | contracts       | reimplement | Copy must also rewrite refs BETWEEN copied blocks to point at the new copies, or a duplicated subtree silently links back to the original. Cut/drag keep uuids so inbound links survive. This is the rule Obsidian gets wrong by construction (copied ^ids duplicate).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Semantic outliner ops**                                       | ❌ missing | M      | core      | contracts       | reimplement | THE structural decision of the whole domain: every mutation is an [op, args] entry from a closed vocabulary, validated before ANY of the batch executes, and undo/sync/history consume ops rather than row diffs. Enforce the hard rule that op payloads carry uuids or idents, never numeric/local ids. Split the vocabulary like they do: only the structural + page ops enter history; collapse and property setters do not.                                                                                                                                                                                                                                                                                                                                                                            |
| **Semantic undo/redo**                                          | ❌ missing | XL     | core      | platform-server | reimplement | By far the hardest module in the domain (their construct.cljc is ~1300 LOC) and it must be designed alongside the op vocabulary, not bolted on. Key rules: delete inverts to insert with keepUuid AND keepBlockOrder (exact positions), indent rewrites to explicit per-block moves because a literal replay on a changed tree gives a different result, and every inverse must be uuid-only. Our existing editor undo (Editor repo packages/editor/src/history.ts:24) is a text snapshot stack with no way for a host to push a structural operation, so it cannot be extended — the outliner needs its own stack.                                                                                                                                                                                        |
| **Undo is all-or-nothing per transaction**                      | ❌ missing | S      | core      | platform-server | reimplement | Cheap to implement, expensive to omit — a partially replayed inverse corrupts the tree in a way the user cannot see or repair. Pair it with the skip-and-continue behaviour on replay: an entry whose targets no longer exist is skipped, not fatal.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Redo cleared by new edits**                                   | ❌ missing | S      | core      | platform-web    | reimplement | Also: a replayed history action must run with undo-generation disabled or undo loops forever. That flag belongs in the op metadata, so it is really a contracts detail surfaced here.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Startup rehydration**                                         | ❌ missing | S      | core      | platform-server | build-fresh | Logseq is no reference here: d/restore-conn exists to lazily rebuild an in-memory DataScript index from serialised nodes. We query rows on demand instead, so 'rehydration' collapses into the initial-data query. Effort is small precisely because we are not porting their storage protocol.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Order-key high-water mark restored at startup**               | ❌ missing | S      | core      | platform-server | reimplement | Explicitly reject their implementation: a module-level *max-key atom seeded by a reverse index seek is a hidden global, makes insertion non-reentrant, and violates our utils/ rule. Use SELECT MAX(order) WHERE parent = ? at insert time, or thread the boundary from a per-parent sorted child index. Same correctness, no global.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Partial initial hydration**                                   | ❌ missing | M      | core      | platform-server | reimplement | Their worker/UI split maps onto our server/Eden/React split almost exactly, and this is the architecture the survey recommends adopting. Copy the payload hygiene too: strip created-at/updated-at/tx-id from the initial payload (a large fraction of the bytes and pure noise for the renderer). Client-side storage is localStorage-only today (~5MB shared budget, apps/web/src/features/workspace/state/cache.ts:42-73), which is another reason the graph cannot live client-side.                                                                                                                                                                                                                                                                                                                   |
| **Lazy page loading with load status**                          | ❌ missing | M      | core      | platform-server | reimplement | Directly reusable design: the renderer must know whether expanding a node needs a round trip. Collapsed children are excluded by default so opening a page with a big collapsed subtree costs nothing. In SQL this is a depth-bounded recursive CTE plus an EXISTS child probe.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Mirror is one-way**                                           | ❌ missing | XL     | core      | ambiguous       | reimplement | Answer this before any other row in the file-sync section, because every mirror row below and the [[uuid]] ref decision hang off it. Our platform is currently file-authoritative end to end: apps/server/src/fs/watch.ts (native watcher fanned out over SSE), apps/server/src/fs/service.ts:229-258 (write with baseVersion/mtime guards), apps/web/src/features/editor/state/file-sync-service.ts. Choosing (a) means those stop being the source of truth for notes; choosing (b) means most of the Logseq DB design does not apply to us at all.                                                                                                                                                                                                                                                      |
| **File graph import**                                           | ❌ missing | L      | core      | platform-server | reimplement | Deterministic ordering matters (config, then logseq/, then assets, then doc files sorted) so an import is reproducible. Bypass per-transaction persistence and derivation for the bulk path — they report ~100x, and our equivalent is one big SQLite transaction with derivation deferred to a single post-pass. For us the higher-value target is an Obsidian vault import, which is the same pipeline with different frontmatter/link syntax.                                                                                                                                                                                                                                                                                                                                                           |
| **Markdown-to-block parsing**                                   | ❌ missing | L      | core      | platform-server | reimplement | What exists is a live markdown grammar and a working capture->inline-replacement pipeline; what does not exist is any AST or block extraction. Hard blockers named in the digest: the worker returns flat captures and never the Tree, there are only three query kinds (highlight/fold/injection) with no registerQuery, the fold query captures (list) not (list_item), and task markers are captured by nothing. So either the server parses markdown with its own parser (mdast/remark or tree-sitter in Bun), or we widen the shipped .scm files first. The level->parent stack machine itself is the contracts-side 'Paste preserves indentation' work. [downgraded by verification]                                                                                                                 |
| **Properties are database attributes**                          | ❌ missing | L      | core      | platform-server | reimplement | Their model (a real attribute per user property with a :db/ident) makes property queries index lookups instead of map scans — worth keeping as a goal, but DataScript's open schema makes it cheap for them and expensive for us. Relational equivalent: a property definition table + a typed property-value table keyed (block, propertyId), which gets the same index behaviour. Do not model properties as a JSON blob on the block or every query degrades to a scan.                                                                                                                                                                                                                                                                                                                                 |
| **Logical outdenting setting**                                  | ❌ missing | S      | important | contracts       | reimplement | Pure suppression of the sweep, so window scope is legal per the settings scope rule. Trivial once the sweep exists; do not register the key until the outdent op reads it (never-inert rule, keys.ts:11-27).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Move refuses page-into-block**                                | ❌ missing | S      | important | contracts       | reimplement | Use a typed structured error via createStructuredError, not Logseq's ex-info whose message is later matched by string in the op wrapper — that is exactly the fragile pattern our evlog error catalog rule exists to prevent. Do not swallow it silently as they do.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **No-op move detection**                                        | ❌ missing | S      | important | contracts       | reimplement | Cheap and disproportionately valuable: an idempotent drag must produce zero transactions, so it does not dirty the document, does not push an undo entry, does not rewrite a mirror file, and does not broadcast a delta.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Non-consecutive multi-selection**                             | ❌ missing | M      | important | contracts       | reimplement | consecutiveBlock? = share a page AND (b's left sibling is a, OR b's left sibling's subtree ends at a). lastChildBlock? terminates only because the parent chain is acyclic, so it depends on the cycle guard being real. Scattered selections switch move/delete/indent into different modes, so this is a prerequisite for correct multi-select, not a nicety.                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Enter on a collapsed parent**                                 | ❌ missing | S      | important | platform-web    | reimplement | Insert the sibling after the whole collapsed subtree, never as a hidden first child. Users notice this immediately because the typed text otherwise disappears.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Backspace on an empty block with children**                   | ❌ missing | S      | important | contracts       | reimplement | Without this, deleting an empty parent destroys a subtree — the single worst data-loss bug an outliner can ship.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Deleting a referenced block inlines its text into referrers** | ❌ missing | M      | important | platform-server | reimplement | Substitute ((uuid)), [[uuid]] and {{embed ((uuid))}} with the deleted block's text and retract the ref edge, so the referring sentence still reads. Build the pattern from an escaped uuid — their re-pattern string interpolation is a regex-injection shape. Skip referrers that are themselves being deleted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Cascading cleanup on delete**                                 | ❌ missing | M      | important | platform-server | reimplement | Loop until the retract set stops growing, tracking seen ids or mutual references never terminate. In SQL most of this is ON DELETE CASCADE on foreign keys, which is a real simplification over their hand-rolled fixpoint — only the derived cases (a Comments area whose every target died) need code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Soft delete to Recycle**                                      | ❌ missing | M      | important | platform-server | reimplement | Write the three breadcrumbs in the SAME transaction as the move, or restore becomes guesswork. Only the root gets new parent/order; descendants keep their internal edges, which is why restore is O(1) rather than O(subtree).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Restore from Recycle**                                        | ❌ missing | S      | important | platform-server | reimplement | Copy the refusal behaviour: if the original parent is gone or itself recycled, fall back to the original page, and if that is gone too refuse rather than dumping the block somewhere arbitrary. Emit retractions before adds for cardinality-one refs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Today's journal cannot be deleted**                           | ❌ missing | S      | important | platform-server | reimplement | Truncate blocks, keep the page entity. Small rule, but it prevents the daily note vanishing mid-day and it changes the shape of the delete-page inverse op (truncate inverts to block restores only, no page recreation).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Deterministic built-in uuids**                                | ❌ missing | S      | important | contracts       | reimplement | Makes schema entities stable across graphs and re-creations, which is what lets a built-in property survive an export/import round trip.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **id:: property carries uuids across import**                   | ❌ missing | S      | important | platform-server | reimplement | Also the Obsidian story: ^block-id suffixes are the same idea and should map onto the same slot, so an Obsidian vault's inbound block links survive import.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Duplicate id:: is re-minted, not honoured**                   | ❌ missing | S      | important | platform-server | reimplement | Mint a new uuid and strip the id:: line from the text. Obsidian's behaviour here is worse than Logseq's (duplicate ^ids coexist and links resolve arbitrarily), so this is a differentiator, not just hygiene.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Orphan page garbage collection**                              | ❌ missing | S      | important | platform-server | reimplement | Removing the last [[link]] to an empty page retracts that page. Without it, a graph accumulates phantom pages from every typo. Guard: never GC a page with content of its own, a class/property, or a built-in.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Paste into an empty block replaces it**                       | ❌ missing | S      | important | contracts       | reimplement | Small, but it also complicates the undo inverse: restoring a replaced target needs a delete + re-insert pair carrying the target's pre-paste payload. Decide it before writing inverse construction.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Undo restores cursor and selection**                          | ❌ missing | M      | important | platform-web    | reimplement | Undo taking the FIRST recorded cursor and redo the LAST is a small detail that makes structural undo feel native. Depends on the editing-surface fork: with per-block editors, restoring means focusing a row and setting an offset; with one buffer, it is a selection set.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Bounded undo history**                                        | ❌ missing | S      | important | platform-web    | reimplement | Do NOT copy their truncation: at 250 entries they keep the OLDEST 125 and drop the newest half, which is almost certainly a bug. Drop the oldest. Our text history already caps at 200 for a real reason — pinned snapshots retain deleted pieces (Editor repo packages/editor/src/history.ts:24) — but an op-based stack is small, so the cap can be much higher.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Large-page degraded loading**                                 | ❌ missing | S      | important | platform-server | reimplement | Their 100-child threshold flips a page into direct-children-only mode. Cheap insurance; measure our own threshold rather than copying the constant, and make it a settings key rather than a recompile.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Markdown mirror export**                                      | ❌ missing | L      | important | platform-server | reimplement | The renderer is the fiddly part: indentation by level, id:: lines, status markers, tag tokens re-materialised, page-level properties at the top, code fences and embeds. 714 LOC in their build. If the 'Mirror is one-way' fork lands on file-authoritative, this row inverts into a serializer used on save rather than a background mirror.                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Mirror filename safety and dedupe**                           | ❌ missing | S      | important | platform-server | port        | This is a pure lookup-table function (NFC normalise, strip <>:"\|?*\/ and control chars, trim trailing dots/spaces, cap at 160, reject Windows device names, ' (2)' suffixes ordered by uuid) with no Clojure runtime assumptions — one of the few places a near-literal transliteration is both easy and worth it. Note the AGPL exposure applies even to a 60-line function.                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Mirror write coalescing and rename cleanup**                  | ❌ missing | M      | important | platform-server | reimplement | We already have atomic write and app-write attribution (which is what stops a mirror write from looping back through the watcher). Missing: the per-page job map with a 1s debounce keeping the EARLIEST oldPath, the skip-if-byte-identical check (ours compares versions, not content), and delete-old-only-after-new-write-succeeds ordering. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Page title resolution on import**                             | ❌ missing | S      | important | platform-server | reimplement | Order: title:: property, then the decoded filename (triple-lowbar ___ -> /, %XX decoding), then the first heading. For an Obsidian import the equivalent is frontmatter title, then filename — and folders map onto page namespaces, which is a decision worth making explicitly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Import repair passes**                                        | ❌ missing | M      | important | platform-server | reimplement | Normalise journal uuids to their date-derived form and strip refs pointing at blocks that were never imported, so the import leaves no dangling ((uuid)) tokens. Run as one pass after all files, never per file.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Startup DB validation and auto-repair**                       | ❌ missing | L      | important | platform-server | reimplement | Copy the invariant list, not the implementation: a block with a page but no parent gets parent := page; a block with no page inherits the parent's; an entity with no page, parent or name is retracted; blocks missing a uuid get one. Express it as an array of {match, fix} rules (testable) rather than their 40-clause cond ladder. Mark repairs so they are excluded from undo. Most of these become unrepresentable if the schema has NOT NULL + foreign keys, which is a genuine advantage of a relational store.                                                                                                                                                                                                                                                                                  |
| **Duplicate order self-repair**                                 | ❌ missing | S      | important | platform-server | reimplement | The pragmatic complement to fractional ordering: let the invariant break, detect it cheaply, repair deterministically. Tie-break by uuid so independent peers converge. Only strictly needed once concurrent editing or sync exists — single-writer through one server makes duplicates near-impossible, so this can wait.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Parent cycle repair**                                         | ❌ missing | M      | important | platform-server | reimplement | Break by repointing the offending edge at the block's own page. Bound the iteration count for overlapping cycles. With a single-writer server and the move-time cycle guard this should be unreachable — treat it as a corruption backstop plus a loud structured error, not a routine pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Page-has-block-parent guard**                                 | ❌ missing | S      | important | platform-server | reimplement | Belt and braces over the op-level refusal. In SQL a CHECK constraint or a trigger expresses it declaratively, which is stronger than their per-transaction scan.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Page title validation**                                       | ❌ missing | S      | important | platform-server | reimplement | Reject leading hashtags, filesystem-hostile characters, collisions with an identically-tagged page, and plain pages named like a journal date. Surface as a structured error with a fix field so the UI can show what to change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Page rename rewrites inline references only where needed**    | ❌ missing | M      | important | platform-server | reimplement | The headline property (rename touches zero blocks) is free once refs are id-based; the actual code here is the delete path putting the plain title text back into referrers' raw titles. If the 'Mirror is one-way' fork lands file-authoritative, this row inverts into Obsidian-style rewrite-every-referencing-file, which is a completely different (and much larger) job.                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Per-transaction search index update**                         | ❌ missing | M      | important | platform-server | reimplement | We have NO content index anywhere: every content query re-spawns ripgrep (apps/server/src/fs/search.ts:247-291), the workspace index stores path + metadata + a 512-byte kind sniff only (apps/server/src/fs/workspace-index.ts:19-59), and there is no FTS5 virtual table in any migration. For a block graph the natural move is an FTS5 table upserted in the same transaction as the block write. Respect the settled search doctrine: ripgrep stays the correctness oracle and any semantic/structural mode is explicitly separate (docs/workspace-search-next-steps.md, status current).                                                                                                                                                                                                             |
| **Recycle 30-day auto-purge**                                   | ❌ missing | S      | niche     | platform-server | reimplement | Must go through the same cleanup as a hard delete or references dangle, and must not enter the op/undo log. A daily interval in the Bun process is enough; the retention window should be a settings key rather than a constant.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Built-in nodes are immutable**                                | ❌ missing | S      | niche     | platform-server | reimplement | Only matters once we have built-in classes/properties/pages. Copy the batch-level refusal (any built-in in the set aborts the whole operation) rather than silently skipping, and surface it as a structured error with a fix field.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Template insertion rewrites internal refs**                   | ❌ missing | M      | niche     | platform-server | reimplement | Mostly falls out of 'Copy re-mints uuids' once that exists; the extra work is template resolution and stashing the resolved payload in the op for undo/replay.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Ordered-list type inherits on insert**                        | ❌ missing | S      | niche     | contracts       | reimplement | Only meaningful once block properties exist. In a markdown-mirrored world the numbering also has to survive serialisation, so it couples to the mirror renderer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Undo of UI state**                                            | ❌ missing | S      | niche     | platform-web    | reimplement | Contentious behaviour (undo walking back a navigation surprises people). Our address layer already classifies state as address/preference/ephemeral (apps/web/src/features/address/utils/classification.ts:150-205), which is the right table to consult before adding this.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Duplicate journal day blocks the mirror**                     | ❌ missing | S      | niche     | platform-server | reimplement | Refuse to mirror either page and log a structured error rather than letting one clobber the other's file. Mostly unreachable if journal uuids are deterministic, which is the real fix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Property values as hidden child blocks**                      | ❌ missing | M      | niche     | platform-server | reimplement | The survey's clearest DO-NOT: this dual notion of children (outline children vs raw children) is named as the single largest source of accidental complexity in their outliner core, leaking into get-right-sibling, move, indent/outdent and delete. Store scalars inline; promote to a node only when the value is genuinely a reference. Row kept for faithfulness — recommended disposition is 'deliberately not built'.                                                                                                                                                                                                                                                                                                                                                                               |
| **Property value blocks cannot be outdented**                   | ❌ missing | S      | niche     | contracts       | reimplement | Moot if we reject property values as blocks (recommended). Listed only so the dependency is visible: choosing their property model drags this guard, and its siblings in move and delete, along with it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Comment blocks are structurally protected**                   | ❌ missing | M      | niche     | platform-server | reimplement | Only relevant if we ship block comments. The reusable idea is the general one: a block kind can opt out of being moved or being a move target, and an owning container dies when all its targets die. Build the kind mechanism, not the Comment special case.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Fractional-index sibling ordering**                           | 🟡 partial | S      | core      | contracts       | reimplement | We already ship a base-26 fractional index with the exact properties the survey wants (string compare, one write per move, returns null instead of throwing on corrupt input). Gaps vs Logseq: only two consumers today (chat rail reorder apps/web/src/features/chat-mode/utils/rail-reorder.ts:67, apps/server/src/orchestration/command-invariants.ts:16), no per-parent boundary lookup, no validation of remote keys, and generateSpreadOrderKeys is capped at 2 digits (675 slots) which is fine for a rail and wrong for a 5k-sibling page. Do NOT port clj-fractional-indexing or Logseq's global *max-key atom; thread boundaries explicitly. Evidence: packages/contracts/src/order-key.ts:58 (orderKeyBetween), :74 (generateSpreadOrderKeys), :107 (planPinnedReorder), :169 (sortByOrderKey). |
| **Collapse and expand**                                         | 🟡 partial | M      | core      | platform-web    | reimplement | What exists is grammar-driven text folding, per view, non-persistent, and driven by the first-wins syntax provider — plus the platform is currently logging 997 'crossing fold ranges' structural errors (editor.syntax.structural_error, logs 2026-08-17..22). An outliner leaning on the fold system inherits that. Copy the op-level detail: collapsing must skip the updated-at bump so folding does not dirty a document or trigger a mirror write. Evidence: Editor repo packages/editor/src/editor/foldState.ts:16-35 and packages/editor/src/editor/Editor.ts:2029-2106 (fold ranges + manual folds, per view); packages/gutters/src/foldGutter.ts:58 (toggle affordance).                                                                                                                         |
| **Batch transactions are atomic**                               | 🟡 partial | M      | core      | platform-server | reimplement | The transactional substrate exists; the outliner batch does not. We do NOT need their temp-conn clone — a SQLite IMMEDIATE transaction already gives read-your-writes across multi-step ops (move block 2 relative to just-moved block 1), which is a genuine simplification over the Clojure. What we must add is the single post-commit derivation pass (refs, delete expansion) and a single delta broadcast per batch. Evidence: apps/server/src/db/client.ts:27-48 (WAL bun:sqlite handle, busy_timeout); apps/server/src/db/schema.ts:48-53 (per-stream version unique index); apps/server/src/db/migrations.ts:40-47 (DDL + ledger in one IMMEDIATE transaction).                                                                                                                                   |
| **SQLite-backed persistence**                                   | 🟡 partial | M      | core      | platform-server | reimplement | Reject their design wholesale: we do NOT need a kvs(addr, content, addresses) table of transit-serialised index nodes — that exists only because DataScript needs a storage protocol. Real relational tables with indexes on (parent, order) give us the queries for free. Caveat: one SQLite file backs the entire platform today, so notes write traffic shares WAL and busy_timeout with chat event appends. Evidence: apps/server/src/db/client.ts:3-48 (bun:sqlite + drizzle, WAL, foreign_keys ON); apps/server/src/db/schema.ts:9-329; apps/server/src/db/migrations.ts:26-38 (ordered ledger).                                                                                                                                                                                                     |
| **Render delta broadcast**                                      | 🟡 partial | M      | core      | platform-server | reimplement | Best-fitting existing infrastructure in the whole domain: subscribe-with-snapshot-or-replay, coalesced deltas, a synchronized marker and a resume cursor are all built — but only for the chat aggregates, and aggregateKind is still ['project','thread'] (apps/server/src/db/schema.ts:37). Adding a notes subscription means a new subscribe* variant plus a projector; the pipeline runs a single named projector today. Broadcast canonical changed blocks + deleted ids, never raw row diffs. Evidence: apps/server/src/orchestration/streams.ts:98-116 (4000-event live tail, coalesced 25ms/512-event delta windows); apps/server/src/orchestration/ws-rpc.ts:40-58 (afterSequence resume, serverInstanceId invalidation); packages/contracts/src/orchestration-ws.ts:162-176.                     |
| **Versioned schema migrations**                                 | 🟡 partial | S      | important | platform-server | build-fresh | We already have the better version of this. Note the house rule tension: greenfield says no back-compat shims, but the migration ledger is forward-only and existing versions must never be renumbered — that applies to notes tables from the first migration onward. Evidence: apps/server/src/db/migrations.ts:26-38 (forward-only numbered ledger, currently 9 migrations), :40-47 (migration + ledger row in one IMMEDIATE transaction).                                                                                                                                                                                                                                                                                                                                                              |

## Markup: parsing, serialization, properties, and the file format

69 features — 0 have, 5 partial, 64 missing.

| Feature                                                                                    | Ours       | Effort | Priority  | Layer              | Source      | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------------------------------------------------------------ | ---------- | ------ | --------- | ------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Bullet-as-block file format**                                                            | ❌ missing | M      | core      | editor-new-package | reimplement | We would build this on tree-sitter list items, not mldoc byte offsets, so the utf8.cljs offset machinery is moot. Today the shipped fold query captures `(list)` not `(list_item)` (/Users/shaul/Desktop/D/Editor/packages/tree-sitter-languages/src/queries/markdown-folds.scm:3), so per-bullet structure is not even reachable from a plugin — the query file needs new captures before anything downstream works.                                                                                                                                                                                           |
| **Indentation encodes the outline tree**                                                   | ❌ missing | M      | core      | editor-new-package | reimplement | `with-parent-and-order` is described precisely enough to rebuild: an explicit stack with the three cases (sibling/child/outdent) and the do-not-consume rule on outdent-with-match. Our order-key primitive already exists — packages/contracts/src/order-key.ts:58 `orderKeyBetween` — so the fractional-index half is free; do NOT copy Logseq's module-level `*max-key` atom, it makes the function non-reentrant across pages.                                                                                                                                                                              |
| **Multi-line block continuation with 2-space hanging indent**                              | ❌ missing | S      | core      | editor-new-package | port        | AGPL exposure: this is one of the few places transliteration is genuinely cheaper than reimplementation — the survey ships exact worked examples for level=2 and level=3-with-tabs that should become the test table either way. Its known defect (a content-bearing indent region gets fully left-trimmed, silently losing code indentation) should be fixed, not reproduced.                                                                                                                                                                                                                                  |
| **`key:: value` block properties (markdown)**                                              | ❌ missing | M      | core      | editor-new-package | reimplement | Do not copy the `:block/properties` + `:properties-text-values` + `:properties-order` triple; the survey names it as the thing to replace with one ordered list of `{key, rawText, parsedValue}`. Also do not copy `insert-property`'s reparse-the-block-on-every-write: track property spans alongside the block.                                                                                                                                                                                                                                                                                              |
| **Page properties live in the pre-block**                                                  | ❌ missing | S      | core      | editor-new-package | reimplement | Our likely dialect choice is YAML front matter rather than a bare `key::` pre-block; the extractor should emit 'page properties' as one concept regardless of which surface syntax produced them, so downstream rows do not branch.                                                                                                                                                                                                                                                                                                                                                                             |
| **YAML front matter as page properties**                                                   | ❌ missing | S      | core      | editor-new-package | build-fresh | Promoted above Logseq's 'niche': for us YAML front matter is the dominant real-world convention (every Obsidian vault), not a compatibility shim, and it is the most likely home for page-level `title`/`aliases`/`tags`. Use a real YAML parser, not Logseq's single-colon line regex. [downgraded by verification]                                                                                                                                                                                                                                                                                            |
| **`id::` persists block identity in the file**                                             | ❌ missing | M      | core      | ambiguous          | reimplement | Logseq's own survey says the DB split is the better design and that storing `id::`/`collapsed::` in the file at all is a mistake. Note the Obsidian-compat argument for Path A: `^blockid` anchors are the ecosystem convention, and a DB-only identity is invisible to every other tool.                                                                                                                                                                                                                                                                                                                       |
| **`[[page ref]]` page references (incl. nested refs)**                                     | ❌ missing | M      | core      | platform-web       | reimplement | `[[…]]` is regular apart from one level of nesting, so the survey's advice holds: a thin scanner, not a grammar fork. Nested `[[a [[b]] c]]` must yield the outer name including the inner brackets AND the inner name — port that test case verbatim. Autocomplete has no home today: completion UI is not in @singapor/core, it lives in the LSP plugin.                                                                                                                                                                                                                                                      |
| **`#tag` and `#[[multi word tag]]`**                                                       | ❌ missing | M      | core      | platform-web       | reimplement | Ecosystem divergence worth deciding up front: in Obsidian tags are NOT pages. Logseq's tag-vs-pageref guards (`(^\|[^#])` before a page ref, and a punctuation lookahead after a tag) are the fiddly part and are where their string-replace bugs concentrate.                                                                                                                                                                                                                                                                                                                                                  |
| **`((uuid))` block references**                                                            | ❌ missing | L      | core      | platform-web       | reimplement | Engine blocker: inline replacements are hard-filtered to single-line spans (/Users/shaul/Desktop/D/Editor/packages/editor/src/inlineMap.ts:227), so a block ref can only render as one line of substituted text. Anything taller needs block surfaces (registerBlockProvider), which has ZERO production consumers and no browser tests — unproven work, not integration work.                                                                                                                                                                                                                                  |
| **TODO/DOING/NOW/LATER/DONE markers**                                                      | ❌ missing | M      | core      | editor-markdown    | reimplement | Our shipped queries capture NO task markers — `task_list_marker_checked`/`unchecked` appear in neither the markdown highlight nor the fold query — so even GFM `- [ ]` checkboxes are invisible to a plugin today. That query addition is the first unit of work and it also unblocks Obsidian-style checkboxes, which matter more to us than Logseq's keyword set.                                                                                                                                                                                                                                             |
| **Property values become page references**                                                 | ❌ missing | M      | core      | platform-web       | reimplement | Two rules worth copying verbatim: drop URLs and asset links before treating a value as refs (or every `https://…` becomes a page), and run the comma split over the PARSED plain segments, not the raw string, so commas inside `[[a, b]]` do not split. Their comma set includes the CJK full-width comma.                                                                                                                                                                                                                                                                                                     |
| **`alias::` and `tags::` page properties**                                                 | ❌ missing | M      | core      | platform-web       | reimplement | Aliases interact badly with the ref-substitution rows below — Logseq has to explicitly exclude alias targets from `:block/refs` to avoid loops. Design aliases as an index-level name→page mapping, never as a text rewrite.                                                                                                                                                                                                                                                                                                                                                                                    |
| **File name ⇄ page title codec**                                                           | ❌ missing | M      | core      | platform-server    | reimplement | Strongly consider NOT having a codec: Obsidian's 'filename IS the title' deletes this entire bug class, and Logseq's own `___` scheme is not a bijection — a page literally titled `a___b` round-trips to `a/b`, and a title containing `%` plus two hex digits is corrupted. Our fs layer already resolves and confines paths (apps/server/src/fs/path.ts:29) but has no title codec.                                                                                                                                                                                                                          |
| **Page title resolution order**                                                            | ❌ missing | S      | core      | platform-server    | reimplement | Their precedence (hardcoded `Contents` → `title::` property → decoded filename → first heading) is fine, minus the hardcoded case. Note the sanitized lower-cased lookup key vs the display title distinction — that split is what makes case-insensitive refs work and is worth adopting.                                                                                                                                                                                                                                                                                                                      |
| **Journal pages by date format**                                                           | ❌ missing | M      | core      | platform-web       | reimplement | Copy the two load-bearing decisions: derive the journal's id deterministically from the `yyyyMMdd` day int so two graphs merge, and memoize the title→date converter because it is called per ref, not per page.                                                                                                                                                                                                                                                                                                                                                                                                |
| **Rename-safe references (ids stored in text, titles rendered at display time)**           | ❌ missing | L      | core      | ambiguous          | reimplement | Whichever path: do it as a span-aware pass over parsed inline nodes, NOT string replace. The survey is unambiguous that Logseq's regex substitution is their largest source of corruption — a page named `foo` clobbering `foo-bar`, or a title rewritten inside a fenced code block — and that the sort-nested-and-longest-first ordering only papers over it. Also keep their escape hatch: when two pages share a title, do not substitute.                                                                                                                                                                  |
| **Duplicate `id::` repair**                                                                | ❌ missing | S      | important | platform-server    | reimplement | Logseq's repair strips the offending `id::` line with a regex that also eats preceding blank lines. Reuse our atomic-write + baseVersion guard (apps/server/src/fs/write.ts:12) so a repair cannot clobber a concurrent user edit, and log the repair as a wide event rather than silently rewriting a user's file.                                                                                                                                                                                                                                                                                             |
| **`collapsed:: true` persists fold state**                                                 | ❌ missing | S      | important | platform-web       | reimplement | Do the feature, reject the mechanism: writing `collapsed:: true` into the markdown is on the survey's do-not-copy list and produces diff churn on every collapse. Obsidian keeps fold state out of the file for the same reason. Caveat: our fold ranges are currently throwing `crossing fold ranges` in production (997 log events, 2026-08-17..22), so anything leaning on folds inherits that bug. [downgraded by verification]                                                                                                                                                                             |
| **`{{embed [[page]]}}` / `{{embed ((uuid))}}` transclusion**                               | ❌ missing | L      | important | platform-web       | reimplement | Same single-line ceiling as block refs, worse: an embed is inherently multi-line and editable in place. Logseq itself deprecated the macro in the DB version in favour of a `:block/link` attribute on an empty block — copy that model rather than the macro. Budget the first spike on proving registerBlockProvider under a real browser test, not on the feature.                                                                                                                                                                                                                                           |
| **`{{query …}}` simple queries**                                                           | ❌ missing | L      | important | platform-server    | reimplement | L buys property/tag/date filters over a SQLite index; Logseq parity (datalog semantics, result grouping, inline table editing) is XL. There is no content index of any kind today — every content query re-spawns ripgrep — so this row is really 'build the notes index' wearing a query costume.                                                                                                                                                                                                                                                                                                              |
| **Macro syntax `{{name args}}`**                                                           | ❌ missing | S      | important | editor-new-package | port        | AGPL exposure: `split-macro-arguments` is a ~40-line five-state scanner and the survey gives it line by line, so transliteration is realistically what happens — label it. Do NOT port `recover-inline-macros` (190 LOC): it exists only to repair fragmentation that mldoc causes, and our own scanner never fragments.                                                                                                                                                                                                                                                                                        |
| **`^^highlight^^` inline highlight**                                                       | ❌ missing | S      | important | editor-markdown    | reimplement | Blocker is the grammar, not the renderer: tree-sitter markdown has no `^^` node and our highlight query does not capture one, so this needs a text-level scanner alongside the capture-driven path. Pick the delimiter deliberately — Obsidian uses `==`, and supporting both is cheap while inventing a third is not.                                                                                                                                                                                                                                                                                          |
| **LaTeX `$inline$` and `$$displayed$$`**                                                   | ❌ missing | L      | important | editor-markdown    | reimplement | Two blockers, both engine-side. Inline `$x$` needs InlineReplacementSpec.render, which has no production producer anywhere. Display `$$…$$` needs a text row taller than one line, and text rows are uniform height by design — /Users/shaul/Desktop/D/Editor/packages/markdown/src/style.css:7 says so outright ('Row height stays fixed'). Plus a KaTeX dependency in a package that currently has none.                                                                                                                                                                                                      |
| **`SCHEDULED:` / `DEADLINE:` timestamps**                                                  | ❌ missing | M      | important | platform-web       | reimplement | Logseq's value shape (int `yyyyMMdd` when bare, a map when a time or repeater is present) is a wart worth fixing: use one typed datetime from the start. Note that the DB version disables org timestamp parsing entirely, so this is a file-version-only concept in their newer design.                                                                                                                                                                                                                                                                                                                        |
| **Property names themselves become pages**                                                 | ❌ missing | S      | important | platform-web       | reimplement | The on/off switch and exclude list are settings registry entries, not config.edn keys. Note the surprise this causes users: `year:: 1000` silently creates a `year` page, which is why Logseq shipped an excludelist almost immediately.                                                                                                                                                                                                                                                                                                                                                                        |
| **Scalar property value coercion (true/false/int, quotes force string)**                   | ❌ missing | S      | important | editor-new-package | port        | AGPL exposure: small enough that transliteration is the likely path — the survey hands over the exact oracle table (`year:: 1000` → 1000, `year:: "1000"` keeps the quotes, `file-path::` with a comma stays unsplit). Take the tests regardless of which path is chosen.                                                                                                                                                                                                                                                                                                                                       |
| **Invalid property names are quarantined**                                                 | ❌ missing | S      | important | editor-new-package | reimplement | Do not port the mechanism: Logseq decides validity by actually invoking the EDN reader on `"{:key nil}"`, which is meaningless for us. Write an explicit character class. Keeping invalid keys as literal text instead of dropping them is the behaviour worth preserving.                                                                                                                                                                                                                                                                                                                                      |
| **Property key normalization (case, `/`, space, `_` → `-`)**                               | ❌ missing | S      | important | editor-new-package | port        | AGPL exposure: four lines, but the aliasing rules (`custom-id`/`custom_id` → `id`, `last-modified-at` → `updated-at`) are decisions, not code. Decide early: aggressive folding means `My Key` and `my_key` are the same property, which users either love or find lossy.                                                                                                                                                                                                                                                                                                                                       |
| **Namespaced pages via `/`**                                                               | ❌ missing | M      | important | platform-web       | reimplement | The exclusion rules carry the real complexity: a title that is a valid journal date, a URL, or starts with `./`/`../` is not namespaced. Note the alternative our file tree already supports natively — folders — which gives hierarchy without a title codec.                                                                                                                                                                                                                                                                                                                                                  |
| **Assets and local file links**                                                            | ❌ missing | M      | important | platform-web       | reimplement | Inline images are the visible gap and they hit two engine walls at once: `![alt](url)` currently renders as its alt text (@singapor/markdown emits substituted TEXT only), and an image that grows its line needs variable row height, which text rows do not have. Logseq's PDF-annotation sidecar files (`hls__*.md` + `.edn`) are a separate, niche subsystem — skip. [downgraded by verification]                                                                                                                                                                                                           |
| **Markdown mirror (DB-as-truth, files as derived output)**                                 | ❌ missing | L      | important | platform-server    | reimplement | Core only if the DB-first fork is chosen; irrelevant under a text-first model. Note the honest cost: a mirror means the markdown is lossy by design (embeds expand inline, refs are re-rendered), so 'my notes are plain files' becomes 'my notes are exported as plain files'.                                                                                                                                                                                                                                                                                                                                 |
| **Safe file-stem generation**                                                              | ❌ missing | S      | important | platform-server    | reimplement | The full rule set is small and worth taking as-is: NFC normalize, replace `<>:"\|?*\/` and control chars, strip trailing spaces/dots, truncate to 160, reject Windows device names, and disambiguate collisions deterministically by id order. Emit collisions through createStructuredError, never `new Error`.                                                                                                                                                                                                                                                                                                |
| **Typed properties (`:default :number :date :datetime :checkbox :url :node :asset`)**      | ❌ missing | L      | important | platform-server    | reimplement | This is the row that turns notes into a database and it is what makes queries, sorting and property UI possible. Our migration ledger is forward-only (apps/server/src/db/migrations.ts:26) so the type set should be designed before the first migration lands, not widened later.                                                                                                                                                                                                                                                                                                                             |
| **Closed-value (enum) properties**                                                         | ❌ missing | M      | important | platform-server    | reimplement | Built-in Status and Priority as closed values is the right default and is how Logseq's DB version replaced org markers. Stable per-choice ids are what let a rename of 'Doing' not break every task.                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Property type inference from values**                                                    | ❌ missing | M      | important | platform-server    | reimplement | The reusable trick is `textWithRefs`: distinguishing `prop:: #a #b` (a node list) from `prop:: #a is #b` (prose) by erasing known ref names and checking what non-sigil characters remain. Build that alternation regex from ESCAPED, descending-sorted names — unescaped it both mis-matches and is a ReDoS surface on user data.                                                                                                                                                                                                                                                                              |
| **Markdown export options (indent style, strip emphasis/refs/tags/properties, level cap)** | ❌ missing | M      | important | platform-web       | reimplement | Their AST→simple-token pipeline (text_impl.cljs) is the single best spec in the whole domain for what each construct's markdown form should be — read it as documentation even if nothing is transliterated. Two rules to keep: links round-trip through their original source text and are never re-rendered, and nested emphasis must alternate `*`/`_` delimiters or it collapses.                                                                                                                                                                                                                           |
| **Dangling block refs are cleaned, not left broken**                                       | ❌ missing | M      | important | platform-server    | reimplement | Two behaviours worth copying: resolve forward refs in a second pass after all files load (so file order does not matter), and keep a block whose title becomes empty rather than deleting it. Never create placeholder entities for missing targets.                                                                                                                                                                                                                                                                                                                                                            |
| **Self- and cycle-reference guard**                                                        | ❌ missing | S      | important | platform-server    | reimplement | Mandatory, not optional: a block whose title contains its own id causes infinite display recursion (Logseq shipped and then regressed this). Needs both a self/alias exclusion when deriving refs AND a depth cap plus a fixed-point check in the title expansion loop.                                                                                                                                                                                                                                                                                                                                         |
| **`:PROPERTIES:`/`:END:` drawer properties (org)**                                         | ❌ missing | L      | niche     | editor-new-package | reimplement | Downgraded from Logseq's 'important' — org is not a daily expectation in a markdown-first notes app. Real fork: writing an org parser is L-to-XL, whereas depending on the npm `mldoc` package (separate repo, MIT-ish OCaml→JS, a dependency and not a derivative work) buys org support for roughly S. If org matters at all, take mldoc.                                                                                                                                                                                                                                                                     |
| **Org drawer auto-normalized to `key::` on read**                                          | ❌ missing | S      | niche     | editor-new-package | reimplement | Explicitly on the survey's do-not-copy list: Logseq fuses this into parsing, so the in-memory title differs from the file bytes for any markdown file containing a drawer. Build it as an opt-in normalize command, never inside the parse path.                                                                                                                                                                                                                                                                                                                                                                |
| **Org file directives (`#+TITLE:` etc.) become page properties**                           | ❌ missing | S      | niche     | editor-new-package | reimplement | Only worth doing if org support is taken at all. Logseq's synthetic `Properties` node is an invented AST node — in our design page properties are already a first-class field, so this collapses to a mapping table.                                                                                                                                                                                                                                                                                                                                                                                            |
| **`#+BEGIN_QUERY … #+END_QUERY` advanced datalog queries**                                 | ❌ missing | XL     | niche     | platform-server    | build-fresh | Logseq is not a usable reference here: the query text is EDN datalog against DataScript, which has no analogue in SQLite. If advanced queries are wanted, design a query surface for our store rather than porting theirs. One cheap thing to copy: ref extraction deliberately skips inside query blocks so query text does not create phantom page refs.                                                                                                                                                                                                                                                      |
| **User-defined macros from config**                                                        | ❌ missing | S      | niche     | platform-web       | reimplement | House rule forces the shape: the macro map is a `defineSetting` entry in packages/contracts/src/settings/keys.ts:30, never a config.edn or a new localStorage key. Scope matters — a macro that expands into anything execution-adjacent must not be `window` scope, since a workspace file ships inside a cloned repo.                                                                                                                                                                                                                                                                                         |
| **Built-in media macros (video/youtube/vimeo/tweet)**                                      | ❌ missing | M      | niche     | platform-web       | reimplement | Needs either InlineReplacementSpec.render (implemented in the engine, zero production producers — /Users/shaul/Desktop/D/Editor/packages/editor/src/displayTransforms.ts:192) or block surfaces. Note the engine's `hosting: 'hoisted'` mode exists precisely so a video keeps playing across row recycling.                                                                                                                                                                                                                                                                                                    |
| **`{{renderer …}}` plugin slot**                                                           | ❌ missing | L      | niche     | platform-web       | build-fresh | We have no plugin SDK at all. This row is 'build a plugin system', not 'add a macro'. Defer until there is a reason for third parties to exist.                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **`{{cloze …}}` spaced-repetition blanks**                                                 | ❌ missing | M      | niche     | platform-web       | reimplement | Cheap once the macro lexer exists — but the flashcard scheduling behind it is a separate, larger feature. Logseq's cloze complexity is almost entirely mldoc-repair code we will not need.                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Footnotes `[^name]` and definitions**                                                    | ❌ missing | M      | niche     | editor-markdown    | reimplement | Needs new captures in the shipped markdown queries; the current highlight query names nothing footnote-shaped. Rendering the definition on hover runs into the same single-line inline ceiling if the definition wraps.                                                                                                                                                                                                                                                                                                                                                                                         |
| **Org sub/superscript `_{}` and `^{}`**                                                    | ❌ missing | S      | niche     | editor-markdown    | reimplement | Only reachable through an org path or a scanner; not present in the markdown grammar. Low value outside chemistry/math notes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Org targets and radio targets (`<<x>>`, `<<<x>>>`)**                                     | ❌ missing | M      | niche     | platform-web       | reimplement | Radio targets are also a performance trap (a phrase index consulted on every render). If anything here is worth having it is the plain `<<target>>` anchor, which is S.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Org progress cookies `[3/5]` / `[50%]`**                                                 | ❌ missing | S      | niche     | platform-web       | reimplement | Trivial once tasks and the tree exist; meaningless before.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Org entities (`\alpha` → unicode)**                                                      | ❌ missing | S      | niche     | editor-markdown    | reimplement | A lookup table plus an inline replacement. Low value unless org support is taken.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Inline hiccup / inline HTML**                                                            | ❌ missing | L      | niche     | platform-web       | build-fresh | Do not implement hiccup at all — it is ClojureScript EDN read as markup and has no meaning in our stack. Raw inline HTML is the only portable half and it needs a sanitizer plus a stated threat model, because notes files arrive from clones and syncs.                                                                                                                                                                                                                                                                                                                                                       |
| **Priority `[#A]` / `[#B]` / `[#C]`**                                                      | ❌ missing | S      | niche     | platform-web       | reimplement | Cheap. Logseq's DB version maps A/B/C onto a closed-value property (high/medium/low), which is the better model — do that rather than persisting the bracket syntax as semantics.                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Repeating tasks (`.+1d`, `++1w`, `+1m`)**                                                | ❌ missing | M      | niche     | platform-web       | reimplement | The three repeater flavours (advance from completion / from scheduled / advance-and-skip-to-future) are the whole design and are worth copying as semantics. Org's cookie syntax is not — pick a syntax our users can read.                                                                                                                                                                                                                                                                                                                                                                                     |
| **`:LOGBOOK:` drawer with CLOCK entries**                                                  | ❌ missing | M      | niche     | platform-web       | reimplement | If tracked at all, store durations as data rather than as `CLOCK: [a]--[b] => H:MM` lines that must be stripped from every rendered title. The stripping pass is the only part of Logseq's code worth reading here.                                                                                                                                                                                                                                                                                                                                                                                             |
| **Optional extraction of multiple code fences into child blocks**                          | ❌ missing | S      | niche     | platform-server    | reimplement | Only meaningful if we build a Logseq/Obsidian importer at all. Skip until then.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Mirror dialect: `*` for properties, `-` for blocks**                                     | ❌ missing | S      | niche     | platform-server    | reimplement | Only exists if the mirror exists. The idea it encodes — properties must never be confusable with blocks — is worth keeping in whatever dialect we pick.                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Mirror open `:default` property values as nested blocks**                                | ❌ missing | M      | niche     | platform-server    | reimplement | Only reachable once properties can hold block-structured values, i.e. after typed properties. The recursion (a property value block carrying its own properties and children) is where the serializer gets genuinely hard.                                                                                                                                                                                                                                                                                                                                                                                      |
| **Duplicate journal day is a hard error**                                                  | ❌ missing | S      | niche     | platform-server    | reimplement | The principle generalizes past journals: refuse to write when two pages would collide, rather than silently overwriting. Use the feature's structured-errors catalog so the failure carries code/why/fix.                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Ref-valued properties are entities, not strings**                                        | ❌ missing | L      | niche     | platform-server    | reimplement | Niche as a feature, load-bearing as a design: it is what lets a property value be queried, deduplicated and carry its own properties. Costs a join everywhere and a serializer that can render a value entity back to text.                                                                                                                                                                                                                                                                                                                                                                                     |
| **Retroactive property type widening**                                                     | ❌ missing | M      | niche     | platform-server    | reimplement | Only matters with an importer. Their one-widening-per-file cap loses data on a property used three incompatible ways — if we build this, report every dropped value through a user-visible channel rather than capping silently.                                                                                                                                                                                                                                                                                                                                                                                |
| **OPML and HTML export**                                                                   | ❌ missing | M      | niche     | platform-web       | reimplement | Cheap once the markdown emitter and its remove-options exist; worthless before. Note our published-artifact and clipboard paths already exist for HTML-ish output elsewhere.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Legacy journal filename refs resolve to journals**                                       | ❌ missing | S      | niche     | platform-server    | reimplement | Pure importer compatibility — `[[2026_04_01]]` must land on the journal, not create a literal page, even when the referring file is read first. Skip unless a Logseq importer is built.                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Markdown heading blocks**                                                                | 🟡 partial | M      | core      | editor-core        | reimplement | Logseq's own heading model (level, size, marker, priority on one node) is not the interesting part for us; the interesting part is that we currently cannot make a heading look like a heading. Their `- # Title` bullet-scoped heading and the level-forced-to-1 rule are file-format quirks we should not inherit. Evidence: /Users/shaul/Desktop/D/Editor/packages/markdown/src/replacements.ts:37 — `HEADING_MARKER` hides `#` markers and carries the level in the replacement kind; but style.css:7 caps heading scale because 'Row height stays fixed: sizes below must fit within --editor-row-height'. |
| **Code fences as block display type**                                                      | 🟡 partial | M      | core      | editor-markdown    | reimplement | One rule is worth copying exactly: fence length = max(3, longest backtick run in content + 1), or content containing backticks closes the fence early. `(fenced_code_block) @fold` already exists in our fold query, so collapse works today. Evidence: /Users/shaul/Desktop/D/Editor/packages/tree-sitter-languages/src/queries/markdown-injections.scm:7 (fenced code injected and highlighted per language); packages/markdown/src/replacements.ts:36 (`text.literal` fences hidden in preview).                                                                                                             |
| **Whole-page rewrite on every save**                                                       | 🟡 partial | M      | core      | ambiguous          | reimplement | Worth stating plainly: Logseq's file version achieves round-trip fidelity by never round-tripping — block content is re-sliced from the original bytes and the AST is never rendered back during editing. Our text-first editor already has that property for free, and it is the strongest argument for staying text-first. Evidence: /Users/shaul/Desktop/D/platform/apps/server/src/fs/write.ts:12 (atomic tmp+rename writes guarded by baseVersion/expectedMtimeMs); apps/web/src/features/editor/state/file-sync-service.ts:18 (client save path with origin and writeId).                                 |
| **Blockquotes and `#+BEGIN_QUOTE`**                                                        | 🟡 partial | S      | important | editor-markdown    | reimplement | A quote 'display type' as a block-level property is a DB-version invention we probably do not want — plain markdown `>` round-trips everywhere and needs no property. Copy only the serializer detail: insert a blank line after a quote or following text gets absorbed. Evidence: /Users/shaul/Desktop/D/Editor/packages/markdown/src/replacements.ts:39 (`QUOTE_MARKER` substitutes `>` with `│`); tree-sitter-languages/src/queries/markdown-folds.scm:2 (`(block_quote) @fold`).                                                                                                                           |
| **Ordered list blocks**                                                                    | 🟡 partial | S      | niche     | editor-markdown    | reimplement | Logseq drives this from a `logseq.order-list-type::` property, which is a property-system detour around something markdown already expresses. Prefer native `1.` lists and skip the property entirely. Evidence: /Users/shaul/Desktop/D/Editor/packages/markdown/test/replacements.test.ts:122 — ordered list markers are captured but deliberately left as source, so `1.` renders as native markdown text.                                                                                                                                                                                                    |

## Outliner editing UX and keyboard model

72 features — 11 have, 19 partial, 42 missing.

| Feature                                                                                   | Ours       | Effort | Priority  | Layer              | Source      | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ----------------------------------------------------------------------------------------- | ---------- | ------ | --------- | ------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Enter at offset 0 of a non-empty item inserts an empty item ABOVE**                     | ❌ missing | S      | core      | editor-core        | reimplement | Cheap and high-value; it is the gesture that makes 'push this down and write above it' work without the mouse.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Enter on an item with visible children creates a first CHILD, not a sibling**           | ❌ missing | M      | core      | editor-new-package | reimplement | Logseq resolves hasChildren from the DB and AWAITS it before deciding. For us it is a synchronous scan of the following lines' indent — strictly simpler. The collapsed-parent-gets-a-sibling rule (never type into a hidden subtree) is the part worth copying exactly.                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Enter on an empty item outdents it instead of adding another**                          | ❌ missing | S      | core      | editor-core        | reimplement | Our current behaviour is Obsidian's (exit the list); Logseq's is one level per press, which is the correct outliner gesture at depth 4. One-line-ish change with a real UX delta. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Shift-Enter inserts a soft newline inside the item**                                    | ❌ missing | S      | core      | editor-core        | reimplement | Unverified that Shift+Enter is explicitly routed around listItemLineBreak; and a soft break inside a list item must be indented to the item's content column or markdown re-parses it as a new block. Test both. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Tab indents the item under its previous sibling (subtree rides along)**                 | ❌ missing | M      | core      | editor-new-package | port        | logseq.outliner.core/indent-outdent-blocks (~180 LOC) is the algorithmically hard-won part of this whole domain: the drop-while/take-while normalisation, the non-consecutive-selection refusal, and the auto-expand-in-the-same-transaction rule. AGPL — a literal transliteration is the tempting path and the one that carries exposure. Reimplement from the described algorithm. [downgraded by verification]                                                                                                                                                                                                                                                                                              |
| **Shift-Tab outdents, dragging the following siblings along (direct outdenting)**         | ❌ missing | M      | core      | editor-new-package | port        | Direct outdenting (right-siblings become children of the outdented item) is what stops the rest of the list appearing to jump above you. Obsidian never does this; it is a genuine differentiator, and it is the single subtlest algorithm in the inventory. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Escape leaves text editing and selects the enclosing item**                             | ❌ missing | S      | core      | editor-core        | reimplement | We have a text-range ladder; Logseq has a distinct selection MODE where the next keystroke is a structural command. Adopting a modal state is a real design commitment — say so before building the action bar on top of it. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Escalating Cmd+A (text → item → parent → page)**                                        | ❌ missing | S      | core      | editor-core        | reimplement | Cheapest way to ship it: make Cmd+A call smartSelect.expand when the current selection already covers the item, rather than building a second ladder. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Multi-item selection with Shift-Up/Down (text selection escalating to item selection)** | ❌ missing | L      | core      | editor-core        | reimplement | Copy the DIRECTION-STICKY rule exactly: reversing shrinks from the far end rather than growing the other way; getting that wrong is the classic multi-select bug. Reject their storage — Logseq keeps :selection/blocks as a vector of live HTMLElements and even synthesizes detached nodes for virtualized rows. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                                 |
| **Move item (with its subtree) up / down, hopping levels at boundaries**                  | ❌ missing | M      | core      | editor-new-package | port        | The non-obvious rule worth taking: moving UP past an item that has children lands you at the BOTTOM of that subtree (lastChildOrSelf), which is what makes repeated presses walk the visible outline instead of jumping levels. ~45 LOC, self-contained, but AGPL — reimplement from the description. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                                              |
| **Zoom in / zoom out on an item**                                                         | ❌ missing | L      | core      | ambiguous          | reimplement | Also needs a breadcrumb and history semantics. @singapor/scope-lines already ships createStickyScrollPlugin (sticky ancestor headers), which is the same ancestor-chain computation a zoom breadcrumb needs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Slash command menu at the caret**                                                       | ❌ missing | L      | core      | platform-web       | reimplement | Reuse rather than rebuild: the LSP completion controller already owns popup anchoring, filtering and the up/down/enter model over our editor. The TRIGGER SCANNER (open on `/` at line start or after whitespace; close on a space right after the slash; close after 2 chars past the last match; close on Backspace over the trigger) is pure text logic and is the part worth transliterating from their handle-last-input cond — order is load-bearing there.                                                                                                                                                                                                                                               |
| **Slash commands are data-driven step programs**                                          | ❌ missing | M      | core      | platform-web       | port        | The most portable single idea in the domain: `[label, steps, doc, icon, group]` with steps as `[:editor/input ...]` handled by a multimethod becomes `Record<StepName, (arg) => void \| Promise<void>>` almost mechanically. Port the SHAPE. Their commands/insert! splicing rules (replace the LAST occurrence of the trigger pattern rather than slicing at a remembered index; the CJK-adjacency space suppression; refuse a write that turns non-blank into blank) are the hard-won details.                                                                                                                                                                                                                |
| **`[[` opens page/node search and auto-closes the brackets**                              | ❌ missing | L      | core      | platform-web       | reimplement | Biggest hidden dependency in the domain: there is no note identity anywhere. Everything server-side is path-keyed (apps/server/src/db/schema.ts has no id/link/backlink table), and an external rename arrives as delete+create because neither watcher backend emits renames (apps/server/src/fs/watch.ts:342-347). A `[[page]]` index built on paths breaks on the first Finder rename. [downgraded by verification]                                                                                                                                                                                                                                                                                          |
| **`#` opens tag search**                                                                  | ❌ missing | L      | core      | platform-web       | reimplement | Copy the cancellation rules exactly — `##` cancels, `# ` cancels, Backspace over the `#` cancels, and typing `[[` while it is open upgrades to `#[[`. They are the difference between a tag popup that feels alive and one that fights you.                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Inline formatting toggles (bold / italic / highlight / strike / link)**                 | ❌ missing | S      | core      | editor-core        | reimplement | Two details make it feel right and both are one line: shrink the selection past leading/trailing whitespace BEFORE wrapping (so bolding 'word ' gives '**word** '), and test the delimiters OUTSIDE the selection so an already-bold selection unbolds whether or not the user selected the asterisks. ~80 LOC of pure string arithmetic — trivial enough that reimplementing costs less than reasoning about AGPL. [downgraded by verification]                                                                                                                                                                                                                                                                |
| **Pasting markdown inserts a correctly-indented subtree**                                 | ❌ missing | M      | core      | editor-core        | reimplement | EDITOR_PASTE_HANDLER has no production consumers in either repo — this would be its first. Their empty-target-is-replaced-not-appended rule is worth copying; the rest of paste-blocks is entity bookkeeping we do not have. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Drag an item by its bullet to reorder (sibling / nested / above)**                      | ❌ missing | L      | core      | editor-new-package | reimplement | Port the DROP-MODE HEURISTIC verbatim, it is the whole feature: pointer x > 50px from the row's left edge = drop as a CHILD, otherwise as a SIBLING; within 16px of the top of the first row = drop ABOVE. Reject their state storage — global atoms read by every block row means the separator re-renders the entire list on every dragover.                                                                                                                                                                                                                                                                                                                                                                  |
| **One shared keyboard model for every autocomplete popup**                                | ❌ missing | S      | core      | platform-web       | reimplement | Copy the specifics: wraparound, scroll the highlighted item to the vertical centre, shift+Enter as an alternate action, and Enter with NO matches falling through to a per-popup on-enter (that fallback is what makes 'create this page' work). [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Merge is refused when it would swallow a subtree**                                      | ❌ missing | S      | important | editor-new-package | reimplement | Without this, one Backspace silently re-parents a whole subtree under an unrelated item and the user cannot see what happened. Cheap guard, prevents the worst data-shaped surprise in the domain.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Backspace onto an EMPTY previous item deletes that item, not your text**                | ❌ missing | S      | important | editor-core        | reimplement | Matters much more under Path B (block ids/refs survive) than Path A (no identity to lose), but it also just feels correct: the blank line above you is the thing that should die.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Indent/outdent is blocked past the zoom root**                                          | ❌ missing | S      | important | platform-web       | reimplement | Trivial once zoom exists; meaningless before.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Alt+Up/Down selects whole items directly**                                              | ❌ missing | S      | important | editor-core        | reimplement | Binding conflict to settle first: Alt+ArrowUp is already ours for move-lines-up (/Users/shaul/Desktop/D/platform/apps/web/src/keymap/editor-commands.ts:279), which is itself the natural binding for move-item-up. One of the two must move.                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Drag-select items with the mouse**                                                      | ❌ missing | M      | important | editor-core        | reimplement | Skip their append/reverse-direction de-selection subtleties in v1; keep the anchor-to-hovered range. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Enter on an item selection re-enters editing**                                          | ❌ missing | S      | important | editor-core        | reimplement | Needed the moment Escape-to-select exists, or the mode is a trap.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Typing promotes an item to a code / quote / numbered block**                            | ❌ missing | S      | important | editor-core        | reimplement | Ours is markdown-native (the text stays ``` and >), theirs converts to a display-type property. Ours is simpler and round-trips to disk — a case where our model wins and their design should NOT be copied. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Mod+Enter cycles TODO status**                                                          | ❌ missing | S      | important | editor-core        | reimplement | editActions.ts already parses the leader including `[x]` (continuedLeader resets a ticked box on continuation), so the parsing half exists. Markdown has two states, not Logseq's four — decide whether Doing/Canceled get a syntax before building the cycle.                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Clicking a rendered task checkbox toggles it**                                          | ❌ missing | M      | important | editor-markdown    | build-fresh | Risk to state plainly: `InlineReplacementSpec.render` has ZERO production consumers anywhere — @singapor/markdown emits only hidden/substituted TEXT, and there are no browser tests for inline widgets (packages/editor/test/inlineWidgets.test.ts is happy-dom with faked rects). Also nothing in markdown-highlights.scm captures task_list_marker_checked/unchecked, so the capture has to be recovered by regex or a new query. This is the first real proving run of that extension point — budget for that, not for the checkbox.                                                                                                                                                                        |
| **Copy with no selection copies a reference to the current item**                         | ❌ missing | M      | important | platform-web       | reimplement | Blocked on the identity decision (Path A has no ids). Under Path A the honest substitute is a path+heading anchor link, not a uuid.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Cut items and paste back preserving their identity**                                    | ❌ missing | M      | important | platform-web       | reimplement | Their nicest touch: when the pasted set is exactly the set just cut, REVERT the delete transaction rather than re-inserting, so inbound references never dangle. Only reachable if we have a transaction log for structure — i.e. it depends on the undo-of-structural-ops row.                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Pasting HTML converts to markdown, plus a raw-paste escape hatch**                      | ❌ missing | M      | important | editor-core        | build-fresh | Use an MIT-licensed converter (turndown-class) rather than porting Logseq's html-parser. This is the single highest-value paste behaviour for a notes app — pasting from a browser and keeping bold/links/lists is what people notice on day one.                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Right-click on a bullet opens the item context menu**                                   | ❌ missing | M      | important | platform-web       | reimplement | Menu contents name host concepts throughout — open in sidebar, copy ref, export as, make flashcard, plugin items — and our menu primitives live in the platform. Start from the platform's existing tab/tree context-menu builders rather than a new mechanism.                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Floating action bar over a multi-item selection**                                       | ❌ missing | M      | important | platform-web       | reimplement | Keep their 200ms settle delay and the suppression rules (hidden while a dialog is open, hidden on any navigation keystroke) — without those a floating bar is a nuisance.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Chord (two-stroke) shortcuts**                                                          | ❌ missing | M      | important | platform-web       | reimplement | Only worth it if the notes mode really needs a `g j` / `t d` vocabulary. Their one useful lesson: a chord whose first key collides with an existing single-key binding must go DORMANT, not throw.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Document mode swaps Enter and Shift-Enter**                                             | ❌ missing | S      | niche     | contracts          | reimplement | Register it in the same pass that wires the Enter branch or not at all (house rule: no inert keys).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Backspace at 0 is blocked on protected first items**                                    | ❌ missing | S      | niche     | platform-web       | reimplement | Only becomes real once journals/zoom exist. Until then there is nothing to protect; do not build the guard framework early.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Up/Down with an active text selection collapses it first**                              | ❌ missing | S      | niche     | editor-core        | reimplement | Unverified either way in our engine; check before building. Cheap correctness detail, invisible until it is wrong.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Full-width CJK brackets trigger the same `[[` flow**                                    | ❌ missing | S      | niche     | editor-core        | port        | Their five-pattern table plus the ±4-char longest-match scan is small, exact, and hard to re-derive correctly. If we ever want CJK IME users, transliterate this specific function rather than guessing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **`((` block-reference typing (disabled in Logseq's DB version)**                         | ❌ missing | S      | niche     | platform-web       | build-fresh | Not a feature to build — a decision to record. Logseq DELIBERATELY killed `((` typing and now shows a 'use a node ref instead' warning, routing block refs through slash commands. That is a strong signal from people who shipped it: do not open with `((`.                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Copy a selection as a list of item references**                                         | ❌ missing | S      | niche     | platform-web       | reimplement | Only meaningful under Path B.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Jump mode (two-letter labels on visible items)**                                        | ❌ missing | M      | niche     | platform-web       | reimplement | Low value relative to cost; skip unless keyboard-only navigation becomes a stated goal.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Mobile: native key bridge, keyboard toolbar, and swipe-for-actions**                    | ❌ missing | L      | niche     | platform-web       | build-fresh | Merged from three inventory rows because they are one platform bet, not three features. Actively out of scope: WorkspaceView hard-codes `min-w-[1024px]` (apps/web/src/features/workspace/components/view.tsx:22), so the app is desktop-only by construction today. Do not cost these until that changes.                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Single editable block (raw source under caret, rendered elsewhere)**                    | 🟡 partial | L      | core      | ambiguous          | reimplement | Logseq's single-textarea trick exists to escape contenteditable. We already escaped it a different way (hidden input + virtualized rows + inline replacements), so the mechanism is not worth porting; the STATE MACHINE (what is revealed, when, and what commits) is. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/inlineMap.ts:108 (revealInlineMap restores source when the caret touches a replacement); packages/markdown/src/index.ts:20-44 (live-preview plugin, platform-wired at apps/web/src/features/editor/utils/plugins.ts:103).                                                                                                                                                   |
| **Enter splits the item at the caret and carries the list marker**                        | 🟡 partial | M      | core      | editor-core        | reimplement | We have the text half. Missing: the left-trim of the tail, and the sibling-vs-child placement rule (next row). Their compute-fst-snd-block-text is 6 lines of slicing — not worth the AGPL exposure. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/editActions.ts:557 listItemLineBreak (marker carried, ordered numbers advanced, `[x]` reset to `[ ]`); gated by languageConfiguration.ts:349-355 (MARKDOWN listMarkers: true).                                                                                                                                                                                                                                                          |
| **Backspace at offset 0 merges the item into the previous visible one**                   | 🟡 partial | M      | core      | editor-core        | reimplement | Logseq's version is 320 LOC across five transaction shapes because children are entities that must be re-parented. Ours is: delete the leader, then shift the following deeper lines left by one level. The five REFUSAL guards are the valuable part, not the transactions. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/commands.ts:65 deleteBackward (a flat buffer joins the two lines natively).                                                                                                                                                                                                                                                                                     |
| **Delete at end of an item pulls the next item up**                                       | 🟡 partial | S      | core      | editor-core        | reimplement | The one rule worth copying verbatim: refuse entirely when the current item is COLLAPSED, so forward-delete can never eat a hidden subtree. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/commands.ts:66 deleteForward (joins the next line natively).                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Tab / Shift-Tab over a multi-item selection indents the whole set**                     | 🟡 partial | M      | core      | editor-new-package | port        | On a text-backed outliner a line selection IS the block selection, so this is nearly free once single-item indent is right — one of the few places Path A beats Path B outright. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/commands.ts:72-73 (indentLines/outdentLines already apply across a multi-line selection).                                                                                                                                                                                                                                                                                                                                                                   |
| **Navigation follows the VISIBLE render order, not the tree**                             | 🟡 partial | M      | core      | editor-core        | build-fresh | Logseq queries document.getElementsByClassName('ls-block') on every arrow key and filters by offsetParent — correct-by-construction, O(document) per keystroke, and broken under windowing. We already have the flattened visible-rows array their own notes say to reimplement over. Explicitly reject the DOM-query approach. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/virtualizedTextViewLayout.ts:225-247 (display rows reflect wrapping and folds); editor/foldState.ts:16-35.                                                                                                                                                                                           |
| **Collapse / expand one item**                                                            | 🟡 partial | M      | core      | editor-core        | reimplement | Live hazard, not theoretical: `editor.syntax.structural_error` — 'Fold projections editor.folds.syntax and editor.folds.fallback contain crossing fold ranges' — fires 997 times in the platform logs 2026-08-17..08-22. Fix that before an outliner leans on folds, or every collapse inherits it. Persistence is the other open question: our fold state is per-view and in memory; Logseq persists :block/collapsed? per block as an undoable transaction. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/foldState.ts:16-35 (anchor-pair fold state, survives edits); editor/commands.ts:56-57 editor.fold/editor.unfold; markdown PROSE_FOLDING at languageConfiguration.ts:349-355.   |
| **Autopair of markup characters, with wrap-selection and type-over**                      | 🟡 partial | S      | core      | editor-core        | reimplement | Gaps are small and markdown-specific: `$$`, the `^^` and `;;` double-character triggers, and the triple-backtick fence special case. Extend MARKDOWN's PROSE_PAIRS rather than adding a parallel mechanism. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/autoClose.ts:1-40 (pairs per language, close-before set); inputSelectionController.ts:775 surroundSelection; editor/autoCloseStore.ts (tracks the inserted closer for type-over / paired delete).                                                                                                                                                                                                                                |
| **Copying items writes markdown + HTML + a private structured payload**                   | 🟡 partial | M      | core      | platform-web       | reimplement | Their in-memory fallback trick and ours are the same idea, arrived at independently, so the mechanism is settled. Under Path A the payload is just indented markdown and this row collapses to nearly nothing — a good argument for Path A. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/clipboardMetadata.ts:1-28 (private `application/x-editor-clipboard` type plus a text-keyed in-process fallback); editor/richText.ts:1-30 (HTML flavour on copy).                                                                                                                                                                                                                                 |
| **Undo/redo restores the caret, and covers structural operations**                        | 🟡 partial | M      | core      | editor-core        | port        | Under Path A most structural ops ARE text edits, so they land on our stack for free — another strong argument for Path A. Under Path B this becomes an XL problem. Their op-vector design ([record-editor-info, db-transact] with a before/after cursor pair, redo cleared on a new local edit, uninvertible ops SKIPPED rather than poisoning the stack) is worth transliterating as a design. Do NOT copy their overflow trim — `subvec result 0 125` keeps the OLDEST half, which is a bug. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/history.ts:24,71-109 (snapshot + selection stacks, cap 200); documentSession.ts:213-240 DocumentTransaction.                                         |
| **Heading size and item styling follow the text while typing**                            | 🟡 partial | L      | core      | editor-core        | build-fresh | The single hardest ceiling in this whole matrix, and it is ours, not Logseq's. Real heading sizes, an inline image that grows its line, a wrapping table cell — none are reachable without changing row-height layout. Everything else in the domain is weeks; this is the one that could be months, and it should be decided before any notes UI is designed around it. Evidence: /Users/shaul/Desktop/D/Editor/packages/markdown/src/replacements.ts:95-125 (heading markers hidden, level carried in the kind); packages/editor/src/plugins.ts:617 (kind becomes an `editor-inline-<kind>` row class).                                                                                                       |
| **Up at the very first item / Down at the last clamps instead of escaping**               | 🟡 partial | S      | important | editor-core        | reimplement | Only the not-editing half is real work, and it only exists if we adopt a selection mode with no caret. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/commands.ts:99-100 cursorUp/cursorDown (document-bounded).                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Collapse / expand one outline level at a time, and collapse/expand all**                | 🟡 partial | S      | important | editor-core        | build-fresh | Our foldLevelN is arguably better than Logseq's 'walk one level per press' — it is absolute rather than relative, and it is already bindable. Ship ours; do not port theirs. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/commands.ts:58-63 editor.foldAll / editor.unfoldAll / editor.foldRecursively / `editor.foldLevel1..7` (EDITOR_FOLD_LEVELS at commands.ts:6).                                                                                                                                                                                                                                                                                                                    |
| **Pasting or dropping files creates attachment references**                               | 🟡 partial | M      | important | platform-web       | reimplement | Cleanest hand-off in the whole domain: register a platform paste handler ahead of the built-in one, upload through the existing attachment route, return the workspace-relative path. Verification found drop is NOT wired at all (handleDrop reads only plain text), the built-in file handler emits `![name](name)` — a dead link with no bytes anywhere — and the 'existing attachment route' is chat-scoped with no POST. [downgraded by verification]                                                                                                                                                                                                                                                      |
| **Autocomplete popup anchored to the measured caret**                                     | 🟡 partial | S      | important | platform-web       | build-fresh | We get this for free by reusing the LSP completion controller's anchoring. Logseq measures the caret off their #mock-text mirror — irrelevant to us. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/plugins.ts:257-297 (getRangeClientRect, textOffsetFromPoint, container/scrollElement access); packages/lsp-plugin/src/plugin.ts:259 (a completion popup already anchors this way in production).                                                                                                                                                                                                                                                                                               |
| **Follow the link under the cursor (page / tag / URL)**                                   | 🟡 partial | M      | important | platform-web       | reimplement | Their nearest-link selection rule (prefer the match whose span contains the caret, else the closest) is 20 lines and worth copying. Blocked on the same missing page index as `[[`. Verification narrowed what exists to modifier-click on a bare http(s) URL in a visible row: URL_PATTERN is `https?://…` only, and the platform registers createDocumentLinkPlugin with no openLink override, so a followed link opens a browser tab and can never open a workspace file. [downgraded by verification]                                                                                                                                                                                                       |
| **Editing identity is (view, item) so multi-pane editing stays coherent**                 | 🟡 partial | M      | important | editor-core        | reimplement | Concrete gap, stated in the engine itself (packages/editor/src/editor/autoCloseStore.ts:20): nothing in core subscribes to buffer changes, so two views on one buffer do NOT live-update each other, and the platform wires no `buffer.subscribe` bridge. Two panes on the same note today will diverge on screen. Fix this before shipping any 'open in sidebar' gesture. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/documentSession.ts:165 (EditorViewSession owns selections, scroll and per-view metadata), :1191 createEditorBufferSession; /Users/shaul/Desktop/D/platform/apps/web/src/features/editor/state/workspace-document-service.ts:203 (one view per tab over a shared buffer). |
| **Emacs-style caret motions and kill commands**                                           | 🟡 partial | S      | niche     | editor-core        | reimplement | Also flags a structural blocker worth surfacing on its own: every outliner command (outline.indent, outline.zoomIn, outline.cycleTodo) hits the same closed EditorCommandId union. Either widen it once in editor-core, or accept that all notes commands live in the platform keymap and reach the editor through a capability token. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/commands.ts:105-108 cursorWordLeft/Right + selectWord*, :109-112 cursorLineStart/End + selectLineStart/End.                                                                                                                                                                                           |
| **Numbered-list items renumber and behave as items**                                      | 🟡 partial | S      | niche     | editor-core        | reimplement | We are AHEAD here: @singapor/markdown deliberately leaves ordered markers rendered as text, and continuation renumbers correctly. Logseq's version is a per-block property with special Enter/Backspace cases we do not need. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/editActions.ts:596-608 continuedLeader (advances the number, preserves the separator width) + renumberedItemEdits.                                                                                                                                                                                                                                                                                             |
| **Click-to-edit puts the caret at the clicked character in the RAW source**               | ✅ have    | S      | core      | editor-core        | build-fresh | Logseq's whole diff.cljs find-position two-pointer heuristic (and its five post-correction rules) exists only because the rendered DOM and the source string are different strings. In our model the buffer IS the source and replacements are spans over it, so the mapping is identity. Do NOT port this; it is dead weight. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/plugins.ts:257-378 (textOffsetFromPoint); displayTransforms.ts:192,202 (inline replacement is one atomic caret stop).                                                                                                                                                                                                |
| **Up/Down move by VISUAL row inside a wrapped item before crossing out of it**            | ✅ have    | S      | core      | editor-core        | build-fresh | In a flat buffer there is no 'block boundary' to cross, so this is free. Logseq needed textarea-cursor-first-row?/last-row? only because each block is its own textarea. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/virtualizedTextViewLayout.ts:225-247 (wordWrap splits a buffer line into wrap-segment display rows); editor/commands.ts:99-100 cursorUp/cursorDown.                                                                                                                                                                                                                                                                                                         |
| **Caret column memory across visual rows and across items**                               | ✅ have    | S      | core      | editor-core        | build-fresh | This is the row that kills Logseq's #mock-text mirror for us. Their entire caret-geometry module (~230 LOC rendering one hidden span per grapheme per keystroke, then reading offsetLeft/offsetTop) is answered by our existing layout. Do NOT port any of it — the inventory itself flags it as 'deliberately reject'. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/navigationTargets.ts:42 (offsetByDisplayRows(offset, rowDelta, goalColumn)).                                                                                                                                                                                                                                         |
| **Left at start / Right at end move into the adjacent item**                              | ✅ have    | S      | core      | editor-core        | build-fresh | Logseq's move-to-block-when-cross-boundary (including the save-before-crossing) is pure textarea-swap tax. Nothing to port. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/commands.ts:96-97 cursorLeft/cursorRight (continuous buffer, crosses line boundaries).                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Typed text is committed on an idle debounce, not per keystroke**                        | ✅ have    | S      | core      | platform-web       | build-fresh | Their 450ms/idle rule and the '#tag suppresses the save' hack exist because a save re-parses refs into a database mid-word. We save a file; there is nothing to re-parse. Do not import that coupling. The one idea to keep: force a flush at every STRUCTURAL boundary (indent, move, collapse, copy) so undo and any index see a consistent document. Evidence: apps/web/src/features/editor/tests/auto-save.test.tsx:54-55 (`files.autoSave: 'afterDelay'` + `files.autoSaveDelay`); features/editor/state/file-sync-service.ts:18-40 (writes guarded on baseVersion + expectedMtimeMs).                                                                                                                     |
| **Keyboard shortcuts grouped by context with guards**                                     | ✅ have    | S      | core      | platform-web       | build-fresh | Explicitly reject Logseq's design here — two competing window-level dispatchers (a Closure KeyboardShortcutHandler per handler group PLUS the editor's own raw keydown/keyup) with :before guards, which their own notes call the source of most subtle ordering bugs. One registry with explicit contexts is what we have. A notes surface adds one `FocusArea` member and typed target. Evidence: `apps/web/src/keymap/define-command.ts` (`CommandWhen`, command target, and per-binding pane); `apps/web/src/keymap/utils/when.ts` (the exhaustive evaluator); `apps/web/src/lib/focus/state/service.ts` (areas and deepest-target ownership).                                                              |
| **Caret motion and delete step by grapheme cluster**                                      | ✅ have    | S      | important | editor-core        | build-fresh | Logseq's safe-dec/safe-inc helpers are redundant for us. Nothing to do. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/graphemes.ts:1-25 (Intl.Segmenter boundaries shared by row measurement, caret stops and backwards delete, by explicit design).                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Pasting a URL over selected text creates a link**                                       | ✅ have    | S      | important | editor-core        | build-fresh | Ours also handles the multi-caret case (targets holding text get a link, empty ones get the URL). Not covered: pasting onto the URL half of an existing link, and their {{video}}/{{twitter}} macro wrapping — the latter is Logseq-macro-specific and should not be copied. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/pasteHandlers.ts:52-66 urlPasteHandler (absolute-URL check, per-target labelling, markdown link syntax at :27-30, registered for markdown at :44).                                                                                                                                                                                                              |
| **IME composition is never interrupted**                                                  | ✅ have    | S      | important | editor-core        | build-fresh | Re-test whichever new input rules we add (autopair extensions, `#`/`/` triggers) against an active composition — that is where Logseq accumulated its Rime/Pinyin special cases. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/input.ts:66 (`if (event.isComposing) return null`); inputSelectionController.ts:263-266,2662 (compositionstart/end tracked, guarded against a hidden-input refresh mid-composition).                                                                                                                                                                                                                                                                        |
| **Type-ahead is never lost during item creation**                                         | ✅ have    | S      | niche     | editor-core        | build-fresh | Logseq's whole pending-new-block buffer (typedText/tabIndent/delete replayed in a commit callback, ~70 LOC) is compensation for a worker round-trip on every Enter. Our edits are local and synchronous. Do NOT port it — and treat it as a warning: if a block-tree design (Path B) ever routes item creation through the server, this bug class comes back and costs a week. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/documentSession.ts:282 (applyEdits is a synchronous snapshot swap on the local piece table).                                                                                                                                                                         |
| **`(` only auto-pairs at a word boundary**                                                | ✅ have    | S      | niche     | editor-core        | build-fresh | Independent convergence on the same rule — good sign the rule is right. Nothing to do. Evidence: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/autoClose.ts:9-15 (AUTO_CLOSE_BEFORE, with the identical rationale in a comment).                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

## Queries, search, and the reference graph

58 features — 0 have, 10 partial, 48 missing.

| Feature                                                                                 | Ours       | Effort | Priority  | Layer           | Source      | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --------------------------------------------------------------------------------------- | ---------- | ------ | --------- | --------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Inline simple query `{{query …}}`**                                                   | ❌ missing | L      | core      | platform-web    | reimplement | Prerequisite risk sits in the engine: registerBlockProvider has zero production consumers, no browser tests, and its provider returns the FULL block list on every setContent AND every applyEdit (Editor.ts:646,670) — O(all blocks) per keystroke for a note full of queries. Prove block surfaces with a browser test before routing this through them.                                                                                                                                                                                                                                                                                                                                                      |
| **Boolean operators and/or/not with arbitrary nesting**                                 | ❌ missing | M      | core      | platform-server | reimplement | Do NOT port build-and-or-not-result: its list?/vector?/coll? shape-sniffing is the compiler recovering type information it threw away, and it collapses to a switch over a tagged IR (`{kind:'rule'\|'datoms'\|'and'\|'or'\|'not'}`). Skip add-bindings! and the not-clause reordering entirely — both are datalog repair passes. Their `(or (priority a) (not (priority c)))` is documented as NOT WORKING because they never emit or-join/not-join; our own evaluator gets it for free and should.                                                                                                                                                                                                            |
| **Page-reference filter `[[page]]`**                                                    | ❌ missing | M      | core      | platform-server | reimplement | The positional semantics are load-bearing and surprising: at top level or inside an `or` a page ref means DIRECT refs only (self-ref); nested inside an `and` it means refs inherited from ancestors (has-ref). Same syntax, different meaning by position — their nested-page-ref test depends on it. The `(and [[a]] [[b]])` → append `(or [[a]] [[b]])` trick exists only to give datalog a positive binding; drop it.                                                                                                                                                                                                                                                                                       |
| **Tag shorthand `#tag` and `(tags a b)`**                                               | ❌ missing | M      | core      | platform-server | reimplement | Two traps. (1) The lexical pre-transform rewrites ` #` → `#tag` and must NOT fire inside quoted strings or inside `[[page names]]` — they use a sentinel swap to protect both; a hand-written tokenizer makes the whole pass disappear. (2) `(tags Book)` matches structural DESCENDANTS of the tag, so it presupposes a class hierarchy we do not model. Blocks with a link set are excluded.                                                                                                                                                                                                                                                                                                                  |
| **Property filters `(property k v)` / `(property k)`**                                  | ❌ missing | L      | core      | platform-server | reimplement | The real cost is the prerequisite: we model no note properties at all. Copy one invariant verbatim — an unresolvable property ident must produce a clause that matches NOTHING (their ::no-property-found sentinel), never null, because a null ident matches EVERY property. Only properties marked public are visible to simple queries.                                                                                                                                                                                                                                                                                                                                                                      |
| **Task filter `(task todo doing done)`**                                                | ❌ missing | M      | core      | platform-server | reimplement | Arguments are case-insensitive and capitalized PER WORD ('in review' → 'In Review'), multiple arguments OR, vector form `(task [a b])` accepted, `(todo …)` is a legacy alias. Because task-hood is derived from the class graph, this filter is also the one that gets its own invalidation channel downstream.                                                                                                                                                                                                                                                                                                                                                                                                |
| **Journal date range `(between -7d +7d)`**                                              | ❌ missing | M      | core      | platform-server | port        | LOW license exposure — the relative-date grammar is a spec, not a creative artifact, and the survey writes it out fully. Needs a journal-page concept and a YYYYMMDD journal-day integer we do not have. Endpoints are sorted after conversion so argument order is irrelevant; both ends inclusive; `m` means MONTHS on this path.                                                                                                                                                                                                                                                                                                                                                                             |
| **Advanced (raw datalog) queries**                                                      | ❌ missing | XL     | core      | platform-server | build-fresh | Logseq's code is not a reference because the artifact IS datalog. Our escape hatch has to be designed fresh (parameterized SQL, or a typed IR the simple-query compiler also targets). Their validator exists for a reason worth copying: require a well-formed query shape and reject anything carrying function values across the execution boundary.                                                                                                                                                                                                                                                                                                                                                         |
| **Advanced query `:inputs` keyword resolution**                                         | ❌ missing | M      | core      | platform-server | port        | LOW exposure and fully spec'd, including the fiddly bits: `:today` must be resolved by the CALLER (the worker cannot know the renderer's timezone) and is validated in 10000101..99991231; the time-of-day suffix decoder clamps, so `today-25` yields 23:00 rather than erroring; unresolvable keywords pass through UNCHANGED rather than throwing (their own comment calls this possibly too subtle — consider erroring instead).                                                                                                                                                                                                                                                                            |
| **Visual query builder (chip clauses, filter/operator pickers, human-readable labels)** | ❌ missing | L      | core      | platform-web    | port        | Only the tree algebra ports (path-addressed add/append/remove/replace/wrap/unwrap over a nested vector, plus the two-way tree↔DSL conversion) — it is pure, low-exposure, and ~200 LOC. The chip UI is fresh work. Merged with the human-readable label pass here because a user sees one feature: chips must read `task: Todo \| Doing` and `Created 2020-12-26 ~ +1d`, resolving property idents to display titles and distinguishing journal-between from timestamp-between.                                                                                                                                                                                                                                 |
| **Query results render as a full table / list / gallery view**                          | ❌ missing | XL     | core      | platform-web    | reimplement | This is the single biggest row in the domain: multi-level major/minor sort, typed per-property filter operators, group-by any property (multi-valued rows appear in EVERY matching group), three layouts, column resize, shift-range selection, per-view search. We have no data grid at all. Do not copy their >10k-row index-order sort fast path — it silently drops rows that have no datom for the sort attribute. Tests must be browser-project: base-ui ScrollArea throws in happy-dom.                                                                                                                                                                                                                  |
| **Query results re-run automatically on relevant DB changes**                           | ❌ missing | L      | core      | platform-server | reimplement | Architecturally the most valuable idea in the domain: queries do NOT re-run per transaction; each declares a watch-key set derived from its compiled form, the writer computes the keys a change touched, and the store intersects. Copy their CONSERVATISM — an allowlist of analyzable predicates, anything else marked opaque ⇒ always re-run — so correctness never depends on the analysis being precise, only performance does. Transport already exists (apps/server/src/orchestration/streams.ts replay/subscribe with a resume cursor); keyed invalidation does not. Their 2s debounce is hardcoded as an `if` in the store — put it on the resource declaration instead.                              |
| **Linked references section**                                                           | ❌ missing | L      | core      | platform-server | reimplement | Nothing in our schema models a note, a block, or a ref — apps/server/src/db/schema.ts has exactly one fs table and it is a recents list. Mount the section only when the count is > 0 (they gate on a cheap count query), and reuse the same result view as queries so sorting/grouping/filtering come for free.                                                                                                                                                                                                                                                                                                                                                                                                |
| **Block references render inline and resolve recursively**                              | ❌ missing | L      | core      | platform-web    | port        | The string surgery is low-exposure and precisely spec'd, so port it. Non-obvious invariants: replace nested-bracket-bearing titles FIRST or a shorter title eats a substring of a longer one; bound the recursion at depth 10 AND carry a seen-set (mutual refs otherwise loop); leave unknown ids verbatim, never blank; a title containing a space cannot be written as a bare #tag. Safari has no lookbehind, which is why their capture-and-restore regex idiom exists — keep it.                                                                                                                                                                                                                           |
| **Property filters honour class-inherited defaults**                                    | ❌ missing | M      | important | platform-server | reimplement | A block tagged with a class that declares a property with a default matches `(property k <default>)` even though it never set the property. Selected at COMPILE time by comparing the queried value to the declared default, which is why the rule library has a doubled `-with-default` family. Their implementation cannot use `missing?` (entity reads return the default), hence a get-else sentinel — a SQL port needs the equivalent explicit null check.                                                                                                                                                                                                                                                 |
| **Priority filter `(priority high medium low)`**                                        | ❌ missing | S      | important | platform-server | reimplement | Only the first letter is capitalized here (unlike task, which capitalizes every word) — a small asymmetry that their tests pin. Unmatched arguments silently contribute nothing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Page filter `(page name)`**                                                           | ❌ missing | S      | important | platform-server | reimplement | Name is sanitized (trim boundary slashes, normalize path) and lower-cased before matching. Trivial once blocks carry a page id.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Timestamp range `(between created-at -1d today)`**                                    | ❌ missing | M      | important | platform-server | port        | Deliberately asymmetric with the journal path and easy to get wrong: epoch ms not YYYYMMDD, `>= start` and `< end` (not inclusive both ends), offsets applied to NOW not to today, `h` = hours and `n` = MINUTES while `m` still means months. Requires per-block created-at/updated-at columns.                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Full-text term inside a simple query**                                                | ❌ missing | M      | important | platform-server | reimplement | Theirs is a case-SENSITIVE substring scan documented in-source as 'could be slow' — a full pass with no index. Ours should compile to the FTS index instead and get the case-insensitivity for free. Our rg-based workspace search is file-granular and cannot be composed into a block query.                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Whole-query full-text mode (entire query in quotes)**                                 | ❌ missing | S      | important | platform-server | reimplement | A one-line escape hatch: if the whole string is quoted, bypass the DSL and run FTS capped at 30 results with semantic search off. Also the reason the reactivity analyzer must treat quoted queries as opaque (always re-run).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Dynamic template variables `<% today %>`**                                            | ❌ missing | S      | important | platform-server | port        | LOW exposure and directly portable; chrono-node is an npm package we can use for the natural-language fallback (`<% last friday %>`). Do NOT copy their dynamic-var `*current-db*` — pass a ResolveContext object, keeping the behaviour (resolve at compile time, which is what makes queries fast) without the untestable dynamic binding. An empty `<%%>` silently deletes the token.                                                                                                                                                                                                                                                                                                                        |
| **Query result count and foldable header**                                              | ❌ missing | S      | important | platform-web    | reimplement | Cheap except for one prerequisite: collapse state persists per block and per container, and we have no per-block state anywhere — the engine's document is one flat string with no node identity beyond Anchor. The collapse flag has to live in the notes store, keyed by block id, not by row.                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Advanced queries auto-inject built-in rules**                                         | ❌ missing | M      | important | platform-server | reimplement | Only meaningful if we adopt the underlying idea: predicates as DATA with declared dependencies, plus a transitive-closure computation at query time. That closure is what makes dependency-based cache invalidation possible later, so do not skip it even though we have no `%` argument to fill.                                                                                                                                                                                                                                                                                                                                                                                                              |
| **`:result-transform` post-processing**                                                 | ❌ missing | L      | important | ambiguous       | reimplement | The survey's own recommendation is to REJECT their design: they needed SCI plus function-bearing guards plus a validator, and still ship an interpreter. Make transforms declarative (sort / limit / group / pluck) and gate anything beyond that behind an explicit per-user opt-in.                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Built-in journal queries (DOING / TODO)**                                             | ❌ missing | S      | important | contracts       | reimplement | Register the key in the same pass that wires the consumer — a registered-but-inert default query is worse than none. Both queries collapse by default, show a count, and hide entirely when empty; the hide-when-empty rule is what keeps a fresh journal from looking broken.                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Builder clause actions: delete / wrap / unwrap / replace operator**                   | ❌ missing | M      | important | platform-web    | port        | Two details make or break it: replaceElement SPLICES when the replacement is a collection whose head is not an operator (that splice is what makes unwrap work), and loc `[0]` is special-cased in both wrap and unwrap because index 0 is the operator head, not a clause. Deleting the last clause in a group must remove the now-empty group too. Every mutation immediately rewrites the block's query text.                                                                                                                                                                                                                                                                                                |
| **Task queries get special invalidation**                                               | ❌ missing | M      | important | platform-server | reimplement | Task-hood is derived, not stored, so the invalidation must fire on status-property config, class-extends edges, class property declarations, and closed-value title edits — not just on a status change. Asymmetric cost is the design licence: a missed key is a stale UI (correctness bug), an extra key is one wasted re-run. Err toward extra.                                                                                                                                                                                                                                                                                                                                                              |
| **Exact-title fast path**                                                               | ❌ missing | S      | important | platform-server | port        | Standard SQLite idiom, no license exposure. Needs a `title COLLATE NOCASE` B-tree index alongside the FTS table. The win is the short-circuit: if a single-word query fills the requested limit from exact titles alone, skip the FTS and fuzzy passes entirely — which is the common case of typing a note's name.                                                                                                                                                                                                                                                                                                                                                                                             |
| **Alias matching in search results**                                                    | ❌ missing | M      | important | platform-server | reimplement | Requires an alias concept we do not model. The trick is worth copying: append alias titles to the indexed title so FTS ranks them as one document, then report which alias matched so the UI can show it beside the canonical title. Consequence for invalidation — adding an alias changes the ref scope of a DIFFERENT page, so it must force a broad reindex.                                                                                                                                                                                                                                                                                                                                                |
| **Linked references follow aliases and class descendants**                              | ❌ missing | M      | important | platform-server | reimplement | The reference set is the page plus all its aliases; a class page additionally pulls in structurally derived child classes, while EXCLUDING blocks merely tagged with the class itself (an instance is not a backlink). Invalidation consequence: an alias edit changes another page's ref scope, which is why their change analysis emits a global ref-scope key that busts every linked-references view.                                                                                                                                                                                                                                                                                                       |
| **Linked-reference include/exclude filters**                                            | ❌ missing | M      | important | platform-web    | reimplement | Interaction is unusual and worth matching exactly: click includes, shift-click excludes, click again clears; the filter icon is green for includes-only, red for excludes-only, amber for both. The co-occurring page list carries occurrence counts, is fuzzy-searchable (our fuzzyRank covers this) and virtualizes past 100 entries — packages/tree's uniform-height virtualization math is reusable. Filters persist on the page, not in UI state.                                                                                                                                                                                                                                                          |
| **Include filters satisfied by descendants, excludes only by ancestors**                | ❌ missing | L      | important | platform-server | reimplement | HIGH license exposure — the asymmetry IS the creative artifact, so rebuild from the description. The canonical case, straight from their docstring: with `foo > baz > bar`, include=bar and exclude=baz must NOT match foo, because bar is only reachable through an excluded branch. Excludes are checked against self+ancestors ONLY, so a parent is not excluded merely because a child mentions the excluded page. A naive implementation gets this wrong and users notice. Use an explicit stack, not recursion — these trees get deep.                                                                                                                                                                    |
| **Unlinked references section**                                                         | ❌ missing | M      | important | platform-server | reimplement | Explicitly do NOT port their implementation: it is an unindexed, unlimited full scan of every block title, survivable only because the UI first asks a capped boolean 'any at all?' question. Drive it off FTS from the start — phrase-query the page title, subtract blocks that already reference it, drop built-ins and linked blocks. Keep the cheap existence gate. Matching is mid-word substring, so a page named 'art' matches 'started'; that is intended.                                                                                                                                                                                                                                             |
| **Per-block reference count badge**                                                     | ❌ missing | M      | important | platform-web    | reimplement | The exclusions are the whole feature: the count drops hidden blocks, the block itself, blocks on the same page, and — for a class page — blocks merely tagged with that class. Click toggles inline expansion of the referencing blocks; shift-click opens them in a side pane. Rendering it inline at the end of block content is an inline-widget/decoration concern the engine already supports.                                                                                                                                                                                                                                                                                                             |
| **Search results show breadcrumbs and node icons**                                      | ❌ missing | M      | important | platform-server | reimplement | Their indexed title already embeds the ancestor chain, which is also why re-parenting forces a whole-subtree reindex — the two decisions are coupled. `uniqueTitle` matters the moment two notes share a name, which is common in a vault; our search groups by file path so nothing equivalent exists.                                                                                                                                                                                                                                                                                                                                                                                                         |
| **`(private-property …)` exposes hidden/built-in properties**                           | ❌ missing | S      | niche     | platform-server | reimplement | Identical to `(property …)` minus the public gate, surfaced behind a 'show built-in properties' checkbox in the builder. Cheap once the public gate exists.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **`(sample N)` random subset**                                                          | ❌ missing | S      | niche     | platform-server | port        | Trivial but has one sharp edge worth copying: the builder must return an EMPTY clause set, not null, or the enclosing `and` treats it as a failed clause and the whole query collapses.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Raw datalog clauses embedded in a simple query**                                      | ❌ missing | L      | niche     | platform-server | build-fresh | Logseq is no reference here because our engine is not datalog — the equivalent is exposing raw SQL or a typed IR. SECURITY: query text lives in a workspace file, so a raw-SQL escape hatch is an execution-reaching value shipped inside a cloned repository. Our own settings doctrine (packages/contracts/src/settings/registry.ts) says such values are never `window`-scoped; treat this the same way or gate it behind an explicit user opt-in.                                                                                                                                                                                                                                                           |
| **Advanced query page-ref equality rewriting**                                          | ❌ missing | S      | niche     | platform-server | reimplement | Sugar so users can write `(= ?x "[[Page]]")` against a multi-valued ref set and get containment semantics. Low value on its own; folds into whatever normalization pass the escape hatch already needs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **`:view` custom result renderer**                                                      | ❌ missing | XL     | niche     | ambiguous       | build-fresh | Their version SCI-evaluates a Clojure fn returning hiccup, catching exceptions into an inline error rather than crashing the page. In React the equivalent is a sandboxed component boundary or a template DSL — neither is a transliteration. Lowest value in the domain; defer until :result-transform's trust model is settled.                                                                                                                                                                                                                                                                                                                                                                              |
| **Group results by page (legacy)**                                                      | ❌ missing | S      | niche     | platform-web    | build-fresh | Logseq disabled this for DB graphs and for table mode, so their code is a dead file-graph path, not a reference. If we want it, it is one group-by option in whatever grid we build — not a separate feature.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Queries inside query results don't recurse**                                          | ❌ missing | S      | niche     | platform-web    | reimplement | Cheap and mandatory rather than optional: without it a query whose results contain a query (trivially, one that matches itself) hangs the renderer. Render a chip reading 'results for …' instead of executing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Command palette search with locale + pinyin initials**                                | ❌ missing | S      | niche     | platform-web    | port        | LOW exposure, fully spec'd with worked examples (设置→sz, 插入 block embed→crbe), and tiny-pinyin is an npm package available to us. Our fuzzyRank already takes label/path/keywords as separate fields (packages/contracts/src/fuzzy-rank.ts:4), so this is an added keyword source, not a new scorer. Cache the derived list and rebuild only on language change.                                                                                                                                                                                                                                                                                                                                             |
| **Semantic / vector search with hybrid ranking**                                        | ❌ missing | XL     | niche     | platform-server | reimplement | RRF itself IS a clean port (published algorithm, zero exposure): score = Σ weight/(60+rank+1), keyword 1.25, vector 1.0, ties by best rank then id — and it is worth adopting even with NO vectors, to fuse the exact/fuzzy/FTS lists we already have. Their term-match boost exists to stop a semantically-close-but-literally-absent hit outranking an exact lexical one. Our docs/structured-semantic-search-evaluation-plan.md is Status: draft but binds one rule: semantic must be an explicit separate mode that never silently alters exact results.                                                                                                                                                    |
| **Code-block-only search**                                                              | ❌ missing | S      | niche     | platform-server | reimplement | Needs a display-type or class on blocks. One implementation note worth keeping: request a much larger SQL limit (they use 300) because most candidates get filtered out afterwards, otherwise the group looks empty.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Plugin-provided search engines**                                                      | ❌ missing | XL     | niche     | ambiguous       | reimplement | apps/server/src/fs/search.ts:61 defines a one-method SearchProvider interface, but findInWorkspaceStream hardcodes the single disk provider, so there is no registry to plug into — the interface is a shape, not an extension point. Index mutations would also have to fan out to every registered engine. Lowest priority in the domain.                                                                                                                                                                                                                                                                                                                                                                     |
| **CLI search and query commands**                                                       | ❌ missing | M      | niche     | platform-server | build-fresh | No CLI surface exists at all. Real value is indirect: a CLI forces the query engine to be callable outside React, which our server-side placement already guarantees — so treat this as a test of the layering rather than a feature. Theirs notably uses substring datalog, NOT the FTS index, so its results differ from the app's; do not reproduce that split.                                                                                                                                                                                                                                                                                                                                              |
| **Transclusion / `{{embed}}` (deprecated upstream)**                                    | ❌ missing | XL     | niche     | editor-core     | build-fresh | Logseq is not a reference here at all. The three routes the diff plugin already proved are: folds, injected text rows, or synthesizing a separate document. A real embed needs either variable-height text rows or a block-surface row — and block surfaces have zero production consumers and no browser tests today. Score this as an engine-roadmap item, not a notes-feature item.                                                                                                                                                                                                                                                                                                                          |
| **Cmd-K global search modal with grouped results**                                      | 🟡 partial | M      | core      | platform-web    | reimplement | We already group commands, files, symbols, sessions, scripts, themes and color modes with per-group renderers (command-groups.tsx:13) and recents ordering. Missing: a notes/blocks group, per-group limits with a total matched count and a show-more affordance, cross-group keyboard navigation as one list, and 'Create page/tag' straight from the input. This is additive to an existing surface, which is why it is M and not L. Evidence: apps/web/src/features/command-palette/content.tsx:107.                                                                                                                                                                                                        |
| **Incremental search index sync on every change**                                       | 🟡 partial | L      | core      | platform-server | reimplement | Ours dedupes watch events, marks entries stale, flushes on a 25ms coalesce and falls back to a full rebuild past 500 events — good bones, but it only reindexes the changed path. The hard part is FAN-OUT: referrers, aliases (both directions), whole page subtrees on re-parent, whole entity trees on visibility flips, evaluated against BOTH before and after. Our watcher cannot supply 'before' — and external renames arrive as delete+create because only our own API emits `renamed` (apps/server/src/fs/service.ts:315). Invariant to copy: a block that became un-indexable must be actively DELETED from the index, not merely not re-added. Evidence: apps/server/src/fs/workspace-index.ts:496. |
| **Search filter groups and persisted last search**                                      | 🟡 partial | S      | important | platform-web    | reimplement | What exists is per-buffer query and replace HISTORY with cursor/draft navigation in the search buffer, persisted through the workspace cache — not a palette filter pin, and not a restored last-group. Persist any new state inside the versioned platform.workspace-state namespace (CACHE_VERSION currently 17), never as a fresh localStorage key; the ~5MB budget is already shared and the search payload is deliberately split out so a quota failure cannot evict open tabs. Evidence: apps/web/src/features/search/state/buffer-state.tsx:1154.                                                                                                                                                        |
| **Search snippets with highlighted terms**                                              | 🟡 partial | M      | important | contracts       | reimplement | HIGH license exposure on ensure-highlighted-snippet — reimplement from the spec rather than transliterating. Ours does one window with identifier-boundary snapping (240 char max, 80 char context) and emits no term markers in the payload. Missing: longest-term-first non-overlapping matching (so a short term cannot steal a longer term's characters), the 50-char prefix plus merge-vs-split two-window logic at 200 chars apart, break-character sentence snapping, and right-to-left marker insertion. Evidence: packages/contracts/src/workspace-search-preview.ts:14.                                                                                                                               |
| **Subsequence fuzzy fallback**                                                          | 🟡 partial | M      | important | platform-server | port        | Reuse our fuzzyRank (exact/prefix/word/substring/fuzzy tiers, multi-field, with a full comparator) rather than porting their gist-derived scorer — theirs even has a latent dead variable they warn you not to 'fix'. What we lack is the candidate pass: a `%a%b%c%` LIKE pattern with escaped metacharacters, run against PAGES FIRST so pages win the candidate budget, and skipped entirely when the query is multi-term and FTS already matched. Evidence: packages/contracts/src/fuzzy-rank.ts:35.                                                                                                                                                                                                        |
| **Search restricted to one page**                                                       | 🟡 partial | S      | important | platform-server | reimplement | Today we scope by directory root plus include/exclude globs — file-shaped, not note-shaped. Give the block index a `page` column and every pass takes `page = ?` for free; that is also what powers the palette's 'Current page' group. Evidence: packages/contracts/src/workspace-search.ts:31.                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Search index build with progress and idle gating**                                    | 🟡 partial | M      | important | platform-server | port        | Generic engineering, no license exposure. Ours is a path+metadata index with a real readiness state machine (cold/building/ready/stale/failed) but it is in-memory only, lost on restart, single-rooted, and a failed rebuild is swallowed into a search-degradation path (fallbackReason) rather than surfaced. Missing: content batching with a time budget and a yield between batches, pausing while the user types, percentage progress to the UI, a build-id check so a newer build supersedes an older one silently, and a user_version pragma written ONLY after full success so an interrupted build rebuilds next time. Evidence: apps/server/src/fs/workspace-index.ts:111.                          |
| **Hidden / built-in / deleted nodes excluded from search**                              | 🟡 partial | S      | important | platform-server | reimplement | Ours is a hardcoded ignore list (node_modules, .git, dist, logs…) plus .gitignore parsing — a vault gets no say, which is wrong for notes. Theirs is per-entity: soft-deleted, hidden, on a hidden page, or a private built-in. Apply at BOTH index and query time, and note the consequence for sync — a block that becomes hidden must be actively removed from the index even though none of its text changed, so the visibility verdict has to be compared before/after. Keep a dev flag that disables the filtering for debugging. Evidence: apps/server/src/fs/path.ts:5.                                                                                                                                 |
| **Namespace-aware search**                                                              | 🟡 partial | S      | niche     | platform-server | reimplement | Our fuzzy ranker already scores the path field separately from the label, which is the same user benefit for `Projects/Alpha`. Theirs adds a second `title MATCH ?` on the last path segment. Copy their TEST, not their code: bind-parameter counts must stay aligned across all four (namespace × page-scope) combinations, which is exactly the bug they unit-test for. Evidence: packages/contracts/src/fuzzy-rank.ts:4.                                                                                                                                                                                                                                                                                    |
| **Find-in-page (in-note / app-wide find bar)**                                          | 🟡 partial | S      | niche     | platform-web    | build-fresh | createEditorFindPlugin is in the critical plugin set, so find-within-a-note works today (capped at 19,999 painted matches). Their feature is Chromium's native webContents.findInPage, which does not exist for us — our desktop shell is CEF, not Electron. Note @singapor/find's engine is buffer-agnostic (FindTextSource) but its package exports only the plugin, so reusing findMatches elsewhere means widening that package's export map. Evidence: apps/web/src/features/editor/utils/plugins.ts:94.                                                                                                                                                                                                   |

## Page surfaces, journals, navigation, and graph views

61 features — 1 have, 6 partial, 54 missing.

| Feature                                                 | Ours       | Effort | Priority  | Layer           | Source      | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------------------- | ---------- | ------ | --------- | --------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Infinite journal scroll (home)**                      | ❌ missing | L      | core      | platform-web    | reimplement | Cost driver is not the query, it is mounting N editors: apps/web/src/features/workbench/components/code-panel.tsx:34-47 mounts only the active tab body, so a scroll of live day-editors needs its own keep-alive surface. Their virtualized-uuid-list design (render uuids, hydrate lazily) is the right shape for us too.                                                                                                                                                                                                                                                                                                       |
| **Today's journal auto-creation**                       | ❌ missing | S      | core      | platform-web    | reimplement | Do NOT copy their 3s polling loop plus the `edit-page-when-present!` 100ms DOM poll; use a scheduled timer to the next local midnight and a real 'surface mounted' promise. Skip creation while the workspace index is still building (apps/server/src/fs/workspace-index.ts:487).                                                                                                                                                                                                                                                                                                                                                |
| **Journal date format is a single graph-level setting** | ❌ missing | S      | core      | contracts       | port        | AGPL exposure: transliterating deps/common/src/logseq/common/date.cljs carries AGPL-3.0; the 28-formatter table and the two deliberate exclusions (dd.MM.yyyy mis-parses 2022Q4) are the valuable part and are fully described in the survey, so reimplementation is cheap. Titles derived from journal-day is what makes a format change free — copy that, do not store titles. Joda tokens map to date-fns but not 1:1.                                                                                                                                                                                                         |
| **Namespace pages as real hierarchy**                   | ❌ missing | M      | core      | platform-web    | reimplement | For us the honest question is whether namespaces are folders (free hierarchy, but 'a/b' and folder 'a' collide) or a parent field on a flat note store. Logseq's rule that intermediates are reused and that a chain never mixes kinds is worth keeping either way. Their root is a built-in Library page; ours would be the vault root.                                                                                                                                                                                                                                                                                          |
| **Page properties area**                                | ❌ missing | L      | core      | platform-web    | reimplement | Cheapest credible path is a React panel above the editor rather than an in-buffer surface, precisely because block surfaces have no browser tests and re-resolve the FULL block list on every applyEdit (Editor packages/editor/src/editor/Editor.ts:646,670). Bidirectional properties (values pointing at this page) need the same index as backlinks.                                                                                                                                                                                                                                                                          |
| **Linked references section**                           | ❌ missing | L      | core      | platform-server | reimplement | Add a links/refs table beside the existing drizzle schema (apps/server/src/db/schema.ts) with a numbered migration, fed incrementally from the existing watch stream (apps/server/src/fs/service.ts:450 already drives the workspace index that way). Reverse-ref navigation is DataScript magic with no equivalent for us — an explicit reverse index is mandatory, not an optimization.                                                                                                                                                                                                                                         |
| **Page deletion moves to Recycle**                      | ❌ missing | M      | core      | platform-web    | reimplement | Their kind-dependent dispatch is the design worth keeping: today's journal truncates, tags/properties hard-delete, everything else soft-deletes. Deleting a page must also drop it from favorites and clear the home-page setting if it was the home page. Partial plumbing only exists today: apps/server/src/fs/routes.ts:85 (POST /fs/delete).                                                                                                                                                                                                                                                                                 |
| **Refs degrade to plain text when a page is deleted**   | ❌ missing | M      | core      | platform-server | port        | AGPL exposure on deps/outliner/src/logseq/outliner/page.cljs. Two invariants must survive the port: the page's own blocks and self-references are excluded from the rewrite set, and the text update plus the ref retraction must land atomically or you get phantom backlinks or dangling [[uuid]]. Our optimistic-concurrency writes (apps/server/src/fs/write.ts:12) make the atomic part harder — a real batch route is the honest answer.                                                                                                                                                                                    |
| **Page view shell**                                     | ❌ missing | M      | core      | platform-web    | reimplement | Our workbench has no host for it: a notes surface is either a third WorkspaceUiMode value (apps/web/src/lib/ui-mode.ts:1, currently a closed two-value union with 7 consumers to update in one pass under greenfield rules) or a new editor document-id prefix. That choice gates almost every other row in this domain — settle it first.                                                                                                                                                                                                                                                                                        |
| **Keyed render-resource subscriptions**                 | ❌ missing | L      | core      | ambiguous       | port        | AGPL exposure on worker/handler/render_resource/*. The idea worth taking either way: a resource returns {value, watchKeys} so invalidation is per-key rather than per-transaction, and watch keys must OVER-approximate what was read. The biggest porting friction is that their cache keys are Clojure vectors with structural equality — TS needs an explicit key canonicalizer. Their rejection of functions and live entities in keys is a good guard to copy.                                                                                                                                                               |
| **UUID-based page references**                          | ❌ missing | L      | core      | platform-web    | port        | AGPL exposure on deps/db/src/logseq/db/frontend/content.cljs. This is the single highest-value port in the domain and the one place a naive reimplementation will be wrong: sortRefs must order identically in every direction or round-tripping is lossy; the (^\|[^#]) guard and the tag-side lookahead must both exist (Safari has no lookbehind, hence their captured left context); the recursive resolver needs both a seen-set and a fixpoint check because mutual refs are legal; and the cheap 'no ID_REF, return unchanged' bail-out is a performance requirement, not an optimization — it runs on every block render. |
| **Journal page identity by day int**                    | ❌ missing | S      | core      | contracts       | port        | AGPL exposure on deps/common/src/logseq/common/uuid.cljs and date.cljs, though both are tiny and fully specified. Two invariants: journal-day is a LOCAL calendar day (every conversion must use local-time constructors or midnight-adjacent journals land a day off), and the deterministic uuid 00000001-YYYY-MMDD-0000-000000000000 is what lets two devices create 'today' independently and converge. Their hash-derived built-in uuids need an exactly pinned 32-bit hash if we ever want import compatibility.                                                                                                            |
| **Journal virtualization with height memory**           | ❌ missing | M      | important | platform-web    | reimplement | Copy the ResizeObserver + cached-height-by-[root,day] placeholder trick verbatim in behaviour; it is what keeps scroll position stable. Skeleton-while-scrolling matters more for us than for them because each item may boot an editor instance. packages/tree's virtualization math is uniform-height and therefore NOT reusable here.                                                                                                                                                                                                                                                                                          |
| **Natural-language date pages**                         | ❌ missing | M      | important | platform-web    | reimplement | Strongest candidate in this whole domain for a new editor package: `@singapor/completion`, a host-agnostic trigger-char popup (items supplied by the host, geometry via getRangeClientRect, EditorCommandId is a closed union so it must own its own key handling). It earns a package because `[[`, `#`, `/` and the LSP path all need the same widget. chrono-node parses; the graph's date format renders.                                                                                                                                                                                                                     |
| **Journal navigation shortcuts**                        | ❌ missing | S      | important | platform-web    | reimplement | Each shortcut is one defineCommand object literal (id, title, keys, requires, run) — palette entry, menu availability and enablement all derive from it. Prev/next-journal day stepping must use local-time day arithmetic, not UTC.                                                                                                                                                                                                                                                                                                                                                                                              |
| **Today-only page sections**                            | ❌ missing | M      | important | platform-web    | reimplement | Blocked on a query engine and on scheduled/deadline parsing, neither of which exists in this domain or in our repos. Worth deferring until the query surface lands — the section is a consumer, not a feature in its own right.                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Rename validation**                                   | ❌ missing | S      | important | platform-web    | port        | AGPL exposure on transliterating deps/outliner/src/logseq/outliner/validate.cljs. Two rules are load-bearing and non-obvious: '/' is legal only when the title parses as a date (which is why validation is expensive — it runs the full 28-formatter path), and the same-name suppression when the only shared tag is the generic Page marker. Note their full-formatter path is ~50ms; do not run it on keystrokes.                                                                                                                                                                                                             |
| **Same-name pages disambiguated by tag**                | ❌ missing | M      | important | platform-web    | reimplement | This feature is only reachable if identity is id-based. On a filesystem-backed vault two notes cannot share a filename in one folder, so 'Apple #Company' and 'Apple #Fruit' need either distinct file paths with a display title in frontmatter, or a db-backed store. That model choice is a user decision that this row inherits rather than makes.                                                                                                                                                                                                                                                                            |
| **Namespace breadcrumb in the app header**              | ❌ missing | S      | important | platform-web    | reimplement | Reuse the same budget function as block breadcrumbs with the app-header preset (4 visible / 1 nearest). @singapor/scope-lines' sticky scroll is a different thing (in-buffer ancestor headers) and does not substitute. Our only breadcrumb today is the file-picker's (apps/web/src/features/file-picker/navigation/breadcrumbs.tsx:8) and it is path-only.                                                                                                                                                                                                                                                                      |
| **Library page lists top-level pages**                  | ❌ missing | S      | important | platform-web    | reimplement | If namespaces are folders this collapses to 'the vault root in the file tree' and is nearly free; if namespaces are a parent field it is a real surface. Their 'no parent means journal or built-in' invariant is a useful marker to preserve.                                                                                                                                                                                                                                                                                                                                                                                    |
| **Page aliases redirect**                               | ❌ missing | M      | important | platform-web    | reimplement | Ordering is the whole trick: resolve alias -> record the SOURCE in recents -> navigate, or recents fills with pages the user never sees. Backlinks must union over all alias ids, which the link index has to model up front rather than bolt on.                                                                                                                                                                                                                                                                                                                                                                                 |
| **Tag pages show a tagged-objects table**               | ❌ missing | L      | important | platform-web    | reimplement | This is really two features: the tag index (server) and a reusable saved-view table (web) that All-pages index also needs. Build the table once or it will be built three times.                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Linked-reference include/exclude filters**            | ❌ missing | M      | important | platform-server | port        | AGPL exposure on deps/db/src/logseq/db/common/reference.cljs. Port the asymmetry precisely: includes may be satisfied by a DESCENDANT, excludes are checked only against self-and-ancestors, and the unsatisfiable-includes prune is what keeps it from being O(graph). Their memo tables are scoped to one db value — ours must be scoped to one query run or they go stale.                                                                                                                                                                                                                                                     |
| **Unlinked references section**                         | ❌ missing | M      | important | platform-server | port        | Their implementation scans the whole :block/title index and is O(all blocks); the survey itself says it should become a real full-text index. We could ship v1 over the existing /fs/search/events stream (apps/server/src/fs/routes.ts:38) and only index later. Respect docs/workspace-search-next-steps.md: exact search results must never be silently altered by a fuzzier mode.                                                                                                                                                                                                                                             |
| **Page hover preview**                                  | ❌ missing | M      | important | platform-web    | reimplement | Suppress on the page you are already on, resolve aliases to the source first, and do not fire inside dialogs/popovers or inside another preview. If wiki-link tokens are rendered via InlineReplacementSpec.render (zero production consumers today), that mount can own its own hover handler instead.                                                                                                                                                                                                                                                                                                                           |
| **Recycle bin page with restore and permanent delete**  | ❌ missing | M      | important | platform-server | port        | AGPL exposure on deps/outliner/src/logseq/outliner/recycle.cljs. The genuinely good idea is reparenting rather than tombstone flags, so every existing query gets correct behaviour for free. Restore must refuse into a recycled parent, and GC (30 days) must not emit sync ops. If we go folder-based this is close to free — a move plus a sidecar record.                                                                                                                                                                                                                                                                    |
| **All-pages index**                                     | ❌ missing | M      | important | platform-web    | reimplement | The file tree is not a substitute: it is path-shaped, uniform-row-height and has no columns. Build the same saved-view table that Tag pages needs, once. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Right sidebar as an ordered stack**                   | ❌ missing | L      | important | platform-web    | reimplement | Explicitly reject their model: :sidebar/blocks keys off a heterogeneous :db/id (entity id, the string 'contents', a plugin keyword, a search string) and persists raw entity ids to localStorage. Use a tagged union over stable ids, and follow the existing document-id prefix scheme (apps/web/src/features/editor/utils/file-backed-document.ts:8) rather than inventing a fourth id vocabulary. Their module-level *drag-from/*drag-to atoms violate our conventions outright.                                                                                                                                               |
| **Right sidebar item kinds**                            | ❌ missing | M      | important | platform-web    | reimplement | This is the row that most argues for building an actual surface registry first, since the same closed-union pain already bites the workbench, the chat-mode rail and the editor document-id scheme (7 prefixes, 9 switch sites).                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Right sidebar item actions**                          | ❌ missing | S      | important | platform-web    | reimplement | Close/close-others/close-all and collapse/collapse-others map 1:1 onto our existing tab-menu shape; collapse state must key by item id, not index, so it survives reordering.                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Right sidebar resize and persistence**                | ❌ missing | S      | important | platform-web    | reimplement | Their drag-below-half-the-minimum-closes and drag-in-from-closed-reopens gestures are a nice detail we lack. Persist through the existing workbenchPanels slice, not a new localStorage key. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Left sidebar navigation list is user-configurable**   | ❌ missing | M      | important | platform-web    | reimplement | Blocked behind the same missing surface registry as the sidebar item kinds — 'which nav entries exist' is exactly the enumeration our closed unions cannot provide. Badge counts (their flashcards due count) need a live subscription. The checkbox selection itself must be a settings-registry entry, never the ad-hoc localStorage key Logseq uses.                                                                                                                                                                                                                                                                           |
| **Favorites list with drag reordering**                 | ❌ missing | M      | important | platform-web    | reimplement | We already own the right primitive: packages/contracts/src/order-key.ts:58 orderKeyBetween gives one write per drag with no neighbour rewrites, and planPinnedReorder (:107) is already used for the chat rail (apps/web/src/features/chat-mode/utils/rail-reorder.ts:67). Use it instead of Logseq's link-permutation trick.                                                                                                                                                                                                                                                                                                     |
| **Favorites stored as graph data**                      | ❌ missing | M      | important | ambiguous       | reimplement | Whichever home wins, favorites must be keyed by stable note id, not path, or every rename or external move silently unfavorites. Their read path filters recycled targets rather than cleaning up on delete — copy that laziness, it is correct.                                                                                                                                                                                                                                                                                                                                                                                  |
| **Custom home page**                                    | ❌ missing | S      | important | platform-web    | reimplement | The knob is a contracts entry (packages/contracts/src/settings/keys.ts:30), scope window is fine — it only selects a surface, it never reaches execution. Their :default-home {:sidebar [...]} pre-population is a separate, skippable idea.                                                                                                                                                                                                                                                                                                                                                                                      |
| **Zoom into a block as a route**                        | ❌ missing | L      | important | ambiguous       | reimplement | @singapor/diff's precedent is instructive: it wanted a row projection, did not get one, and synthesized its own text blob (packages/diff/src/lines.ts joinRenderLines). Any buffer-shaped zoom will face the same three options — folds, injected rows, or synthesize the text.                                                                                                                                                                                                                                                                                                                                                   |
| **Breadcrumb visibility budget**                        | ❌ missing | S      | important | platform-web    | port        | AGPL exposure on components/block/breadcrumb_model.cljs, but this is ~40 lines and the survey specifies it completely, so reimplementation is essentially free. Keep the rule that a single hidden segment is shown rather than ellipsed, and keep loadDepth per variant so a header breadcrumb does not pull a 16-deep chain.                                                                                                                                                                                                                                                                                                    |
| **Breadcrumb label extraction**                         | ❌ missing | S      | important | platform-web    | port        | AGPL exposure on the same file. The trap when porting: re-matches (whole string) vs re-find (substring) is used deliberately, and RegExp.test has re-find semantics — anchor every pattern that was re-matches. The 'code block shows its first comment line' fallback is a genuinely good detail.                                                                                                                                                                                                                                                                                                                                |
| **Shift-click opens in sidebar everywhere**             | ❌ missing | S      | important | platform-web    | reimplement | Cheap only if it is decided before the link components are written — retrofitting a modifier across page titles, favorites, recents, breadcrumbs and graph nodes is the expensive version. Our editor already reserves modifier-clicks for go-to-definition, so pick the modifier deliberately.                                                                                                                                                                                                                                                                                                                                   |
| **Global graph view with two modes**                    | ❌ missing | XL     | important | platform-web    | reimplement | There is no react-force-graph or cytoscape to lean on — their renderer is 2677 lines of PIXI plus a 1473-line layout module, and we have no graph/canvas dependency at all today. Two anti-patterns to reject up front: colours baked into derivation (forces a full rebuild on theme change; ours must be tokens resolved at render, per CLAUDE.md) and the uuid/asset-path label filter that launders upstream data bugs at the presentation layer.                                                                                                                                                                             |
| **Graph filters and controls**                          | ❌ missing | M      | important | platform-web    | reimplement | One implementation rule is load-bearing and easy to lose: a no-op filter must return the IDENTICAL object, because reference equality is how the renderer distinguishes 'no filter' from 'filter to this set'. Also keep 'selectedTagIds === null means all' distinct from '[] means none'.                                                                                                                                                                                                                                                                                                                                       |
| **Graph node interaction**                              | ❌ missing | S      | important | platform-web    | reimplement | Their aria-live selection panel that lists nodes as real buttons is the accessible-canvas pattern worth copying — a canvas graph is otherwise entirely unreachable by keyboard.                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Page-local graph**                                    | ❌ missing | M      | important | platform-web    | port        | AGPL exposure on worker/graph_view.cljs page mode and pixi/logic.cljs pageLayout. The second-order edges between neighbours are what make a local graph show structure instead of a star — that is the part to port, along with the BFS-depth ring placement with its golden-angle jitter.                                                                                                                                                                                                                                                                                                                                        |
| **Graph performance tiering**                           | ❌ missing | L      | important | platform-web    | port        | AGPL exposure on extensions/graph/pixi/logic.cljs (~700 LOC of layout). The thresholds and the analytic O(n) layouts (phyllotaxis, ring placement for isolates, cluster seeding) are the genuinely hard-won part and are fully specified in the survey. Per CLAUDE.md, measure before tuning — and note their fast/slow split is justified while the duplicated visibility predicates are not.                                                                                                                                                                                                                                    |
| **Page menu actions**                                   | ❌ missing | S      | important | platform-web    | reimplement | Each entry is a defineCommand plus a menu row, so cost scales with the number of actions, not with the menu. Publish/make-public are out of scope for us. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Today's journal cannot be deleted, only truncated**   | ❌ missing | S      | niche     | platform-web    | reimplement | The reason is a race with the auto-creator, not user intent — keep that comment when porting or someone will 'simplify' it away.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Convert page ↔ tag**                                  | ❌ missing | S      | niche     | platform-web    | reimplement | Only meaningful if tags are first-class entities rather than strings. Their guard list (blocked when a same-named counterpart exists, when the page has a namespace parent, when the tag has children) is the useful part.                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Batch page deletion dialog**                          | ❌ missing | S      | niche     | platform-web    | reimplement | The only detail worth copying is naming the pages that failed rather than reporting a count.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Right sidebar defaults to Contents**                  | ❌ missing | S      | niche     | platform-web    | reimplement | Worth questioning rather than copying — auto-pushing a Contents page is a Logseq habit, not an expectation. The keybindings are one defineCommand each.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Named heading routes (web only)**                     | ❌ missing | M      | niche     | platform-web    | port        | AGPL exposure on the slug derivation in worker/handler/page.cljs. The double conversion (uuid ref -> title ref -> bare prose) is the non-obvious part. Platform-forking the URL shape the way they do (web only) is not worth copying for us.                                                                                                                                                                                                                                                                                                                                                                                     |
| **Document title and anchor handling**                  | ❌ missing | S      | niche     | platform-web    | reimplement | Do not copy the 200ms anchor delay and 100ms scroll-restore timeout — those are empirical waits for their virtualized list. We already persist scroll positions per path (apps/web/src/features/workspace/state/cache.ts scrollPositionByPath) and can use a deterministic layout signal instead. Nothing in apps/web/src sets document.title today.                                                                                                                                                                                                                                                                              |
| **Graph time travel**                                   | ❌ missing | S      | niche     | platform-web    | reimplement | Needs a per-note created-at we do not currently record — fs birthtime is available from /fs/tree but is unreliable across copies and syncs. Disable rather than lie when the range is degenerate, as they do on the large-graph fast path.                                                                                                                                                                                                                                                                                                                                                                                        |
| **Pages can be excluded from the graph**                | ❌ missing | S      | niche     | platform-web    | reimplement | Exclusion is transitive over descendants in their version, which is why the derivation carries seen-sets on every ancestor walk. The toggle is a settings entry; the property is frontmatter.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **File-graph journal conventions (import only)**        | ❌ missing | S      | niche     | platform-server | reimplement | Only relevant if we ever import a Logseq 0.10.x vault. The reusable idea regardless: recognise journals by PATH (journals/YYYY_MM_DD.md) independently of the display format, and map each to the deterministic day uuid, so refs written under any title format resolve to the same note. Our own vault convention should be equally format-independent.                                                                                                                                                                                                                                                                         |
| **Page title inline rename**                            | 🟡 partial | M      | core      | platform-web    | reimplement | The whole value depends on refs being id-based. Without UUID-based page references this becomes Obsidian's problem — rewriting every referencing file on rename — which is the expensive, lossy path Logseq deleted. Also note apps/server/src/fs/service.ts:315 emits a real 'renamed' event only for renames through OUR API; an external rename arrives as delete+create. Evidence: apps/server/src/fs/routes.ts:76 (POST /fs/rename); packages/tree/src/utils/model/publicTypes.ts:202 (inline renaming in the file tree).                                                                                                    |
| **Page navigation and routing**                         | 🟡 partial | L      | core      | platform-web    | reimplement | Adding a note document kind means editing ~9 hardcoded sites (render branch, three enablement predicates, tab icon/name/title, cache pathForWorkspace, address token encode AND decode, palette label, an opener). Missing one gives a tab that renders but never persists, or persists but is unaddressable. Prefer id-in-URL over title-in-URL for the same reason Logseq does: titles change. Evidence: apps/web/src/features/address/utils/document-token.ts:20-86 (document token codec); features/address/utils/classification.ts:150-205 (address vs preference vs ephemeral).                                             |
| **Recent pages list**                                   | 🟡 partial | S      | important | platform-web    | reimplement | Two rules from their version are worth keeping: record the alias SOURCE not the alias, and do not reorder when the click came from the recents list itself. Ours is path-keyed today, so it will need re-keying to note id alongside UUID-based page references. Evidence: apps/web/src/features/workspace/state/cache.ts:259 (editorHistory + recentlyClosedEditorPaths, persisted per project); apps/server/src/fs/routes.ts:60 (GET /fs/recents with pickCount/lastPickedAt).                                                                                                                                                  |
| **All-files index**                                     | 🟡 partial | S      | niche     | platform-web    | build-fresh | Logseq is no reference here — theirs exists to expose config.edn/custom.css in a browser-only app. We are a real file workspace with a tree and an editor, so this is arguably already done. Evidence: apps/web/src/features/workspace/components/tree-pane.tsx:53 (file tree over the workspace); apps/server/src/fs/routes.ts:29 (GET /fs/tree returns size/mtime per entry).                                                                                                                                                                                                                                                   |
| **Left sidebar touch gestures and resize**              | 🟡 partial | S      | niche     | platform-web    | build-fresh | Not worth porting: apps/web/src/features/workspace/components/view.tsx:22 hard-codes min-w-[1024px], so the app is desktop-only today and the swipe half of this feature has no target. Resize already exists. Evidence: apps/web/src/features/workbench/components/layout.tsx:62-110 (desktop drag resize, persisted); no touch gesture code.                                                                                                                                                                                                                                                                                    |
| **Copy page URL**                                       | 🟡 partial | S      | niche     | platform-web    | reimplement | Their URL embeds the page uuid specifically so the link survives renames — ours is path-based today, so a copied link breaks on rename. That is a reason to land UUID-based page references before advertising shareable note links. Evidence: apps/web/src/keymap/workspace-commands.ts:516 (workspace.copyAddress copies a shareable link to the current location, origin-qualified and dev-param-stripped).                                                                                                                                                                                                                    |
| **Browser history back/forward**                        | ✅ have    | S      | core      | platform-web    | build-fresh | The only gap is Logseq's post-rename replace-instead-of-push rule, which matters once titles are addressable. Header arrow buttons do not exist, but the commands and history do. Evidence: apps/web/src/keymap/workspace-commands.ts:540-566 (workspace.navigateBack / navigateForward on Mod+[ / Mod+], driving real browser history with a popstate listener in the address layer).                                                                                                                                                                                                                                            |

## Tasks, assets, rich content, and knowledge features (flashcards/SRS, PDF annotation, whiteboards, renderers, templates)

64 features — 0 have, 3 partial, 61 missing.

| Feature                                                                      | Ours       | Effort | Priority  | Layer              | Source      | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---------------------------------------------------------------------------- | ---------- | ------ | --------- | ------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Task status as a closed-value property**                                   | ❌ missing | M      | core      | platform-web       | reimplement | Logseq's registry is data, not code (deps/db/src/logseq/db/frontend/property.cljs:300-321) — copying the SHAPE (one declarative map: type, closed values, icons, defaults, ui-position, queryable) carries no AGPL exposure, and it maps cleanly onto our defineSetting/contracts instinct. The key design win to keep: a task is NOT a block type, it is a block with a #Task tag and four properties, which deletes an entire category of ad-hoc block kinds. We have zero note/block model today, so this row is gated on that foundation.                                                                                                                                                                                                                                                                                                                                   |
| **Todo/Done render as a real checkbox (checkbox-state flag)**                | ❌ missing | M      | core      | editor-markdown    | reimplement | Logseq's elegant idea worth stealing: closed values carry :choice-checkbox-state true/false, and the checkbox UI asks 'which choice is checked?' instead of comparing to a literal DONE — so user-defined status sets work with zero extra code (property.cljs:305-318, components/property/value.cljs:2117-2130). For us the markdown form is the primary storage, so the checkbox is a text edit `[ ]`↔`[x]`, which is cheap; the cost is the missing grammar capture plus proving `render`.                                                                                                                                                                                                                                                                                                                                                                                  |
| **Priority as a closed-value property**                                      | ❌ missing | S      | core      | platform-web       | reimplement | DB version has Low/Medium/High/Urgent and no A/B/C; the importer maps legacy A→High, B→Medium, C→Low (exporter.cljs:488-501). If we ingest existing Logseq or org vaults we need that same table. Hidden-when-empty is the right default.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **TODO/LATER marker cycling**                                                | ❌ missing | S      | core      | platform-web       | reimplement | We already have the whole command substrate: defineCommand + derived id union + palette + keymap (apps/web/src/keymap/define-command.ts:101-128). The feature is the state machine plus the text rewrite. Logseq's: TODO→DOING→DONE→(none), LATER→NOW→DONE→(none), start state chosen by a :preferred-workflow config value — that is a settings-registry key for us. WAIT/WAITING/IN-PROGRESS are recognised but not in the cycle, a wart worth not copying.                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Scheduled and Deadline as datetime properties**                            | ❌ missing | M      | core      | platform-web       | reimplement | Keep them SEPARATE (plan-to-start vs finish-by) — collapsing them into one 'due' field is the mistake most note apps make and it breaks the agenda query and the repeat engine downstream. Store epoch-ms. Obsidian has no equivalent (frontmatter `due:` by convention), so this is differentiating even though it is table stakes for a task app.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Recurring tasks with three org repeater semantics (.+ / + / ++)**          | ❌ missing | M      | core      | platform-server    | reimplement | Zero license exposure: the semantics are documented publicly in docs/recurring-tasks.md and on orgmode.org. `.+` advances from COMPLETION, `+` from scheduled (may land in the past and stack overdue — documented, not a bug), `++` advances from scheduled in whole n-unit steps until strictly after now. Two traps from the survey: (1) do the arithmetic in UTC with whole units — that is what preserves day-of-week for weekly repeats, with no weekday fix-up code; (2) guard frequency > 0 before the advance loop or it spins forever. Note the file-based and DB implementations DISAGREE about what .+ and ++ mean; pick the DB/org semantics and normalise at import.                                                                                                                                                                                              |
| **Completing a repeating task reschedules it and resets to Todo**            | ❌ missing | S      | core      | platform-server    | reimplement | The user-visible invariant that makes or breaks this: a completion cycle must always end in the todo state WITH a strictly-changed temporal value, or the user sees a task that will not clear. Both writes land in one logical transaction so undo restores both halves. Watch the edge case: completing early (scheduled still in the future) still moves you forward one period rather than standing still.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Today's agenda (scheduled + deadline within N days) on the journal page**  | ❌ missing | S      | core      | platform-web       | reimplement | The exact predicate set IS the whole feature: (scheduled OR deadline) between today-00:00 and today+futureDays, AND status is neither done nor canceled, sorted by outline order, grouped by source page. Two behaviours to reconsider rather than copy: blocks with NO status are excluded entirely (a bare date on a non-task never shows, which surprises people), and only TODAY's journal renders the section. futureDays is a settings-registry key for us.                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Flashcards by tagging a block #Card**                                      | ❌ missing | S      | core      | platform-web       | reimplement | This is the whole reason Logseq's flashcards are good: no deck files, no import/export, no sync problem. Keep it. A multi-select command batch-tags every selected block in one transaction, which is what makes converting existing notes bearable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **FSRS scheduling with four ratings and a due preview per button**           | ❌ missing | M      | core      | platform-web       | reimplement | BEST VALUE-PER-EFFORT ROW IN THE DOMAIN, and license-clean: the FSRS algorithm is NOT Logseq code — it is the MIT-licensed cljc-fsrs library, whose JS equivalent `ts-fsrs` is also MIT. Only the ~200-line integration layer is Logseq's, and the survey describes all of it. Serialisation to copy: due lives in its OWN property as epoch-ms (so it is indexable), and the state map is the card minus due, plus last-rating. Two touches that make it feel right: run FSRS speculatively for all four ratings on every render so each button shows the date it would produce, and re-pull the block fresh before writing rather than trusting the in-memory copy.                                                                                                                                                                                                           |
| **Cloze deletion ({{cloze answer \\ cue}})**                                 | ❌ missing | M      | core      | editor-new-package | reimplement | Blocked on the same unproven plumbing as checkboxes: InlineReplacementSpec.render has zero production producers and no browser tests. Parse rule worth keeping: split on the LAST `\\` only, so an answer may itself contain the separator; render the revealed answer through the inline markup renderer so refs inside a cloze stay clickable; and guard the toggle with `event.target.closest('a')` so clicking a link inside a revealed answer follows it instead of re-hiding. Also note the reveal phase from the review UI OVERRIDES the local toggle.                                                                                                                                                                                                                                                                                                                   |
| **Three-phase card reveal cycle (init → show-cloze → show-answer)**          | ❌ missing | S      | core      | platform-web       | reimplement | Three lines of logic, big UX payoff: one 's' key advances the phase, the phase skips show-cloze when the card has no cloze, and rating buttons appear ONLY when everything is revealed (i.e. when the next phase would be init). show-answer must ignore per-block collapse state or a collapsed card shows nothing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Assets are first-class blocks (UUID filename, type/size/checksum)**        | ❌ missing | M      | core      | platform-server    | port        | Worth transliterating (it is ~40 lines and the survey says copy verbatim): filename is `assets/<block-uuid>.<ext>` so renaming the display title NEVER touches the filesystem, and checksum/type/size are REQUIRED properties so dedupe cannot be bypassed. We already have the two halves separately: sha256 content versioning (apps/server/src/fs/version.ts:11) and a blob store with mime-derived extensions (apps/server/src/attachments/store.ts:31-51) — but that store is chat-only, lives outside the workspace under ~/.platform/attachments, and has no dedupe. AGPL: this is Logseq code; the algorithm is simple enough to reimplement from the description if the user prefers a clean tree.                                                                                                                                                                     |
| **Paste or drop an image inserts an asset**                                  | ❌ missing | M      | core      | platform-web       | reimplement | EDITOR_PASTE_HANDLER has ZERO consumers in the Editor repo or the platform, so this is unproven plumbing, not wiring — budget for proving it. Precedent we do have: the chat composer already does paste/drop → compress → upload → attachment (apps/web/src/features/chat/utils/composer-drop.ts, image-compression.ts), so the client-side gesture is solved; the destination is not. Behaviour worth copying: if the current block is empty the first pasted file REUSES that block's id and replaces it (this is what makes 'paste into an empty bullet' feel right); with no editing block, land on today's journal.                                                                                                                                                                                                                                                       |
| **Built-in PDF viewer (toolbar, search, zoom, remembered page)**             | ❌ missing | L      | core      | platform-web       | reimplement | Cost is mostly pdf.js integration, not logic. Our platform makes the shell cheap: the document-id prefix scheme is the established way to open a non-file surface as a tab, and /fs/blob already serves the bytes. But the survey's checklist is real — a new document kind means hand-editing ~9 switch sites (render branch, enablement predicates, tab icon/label, cache persistence, address token letter, palette label, opener), because there is NO pane registry. Persist last page onto the asset, last zoom locally. Obsidian has a PDF view but no annotation, which is why the next three rows are the differentiator.                                                                                                                                                                                                                                              |
| **PDF text-selection highlights in five colours**                            | ❌ missing | L      | core      | platform-web       | port        | PROVENANCE MATTERS HERE: viewportToScaled/scaledToViewport/optimizeClientRects appear to derive from react-pdf-highlighter (MIT), NOT from Logseq — verify before assuming AGPL, because these are the one set of functions genuinely worth transliterating rather than rederiving. The design decision to copy: persist highlights as viewport-INDEPENDENT fractions (x1,y1,x2,y2 stored alongside the viewport width/height they were captured at), never pixels — then zoom, resize and DPI changes are free. optimizeClientRects merges the ragged DOM rects a selection produces: 5px same-line tolerance, 10px gap for adjacent, three passes (not a fixpoint), and it MUTATES its input. Also copy fixSelectionTextBreakline, which rejoins hyphenated line breaks and handles CJK (no space) vs Latin (space) — it is what makes a copied highlight read as a sentence. |
| **PDF annotations become real, linkable blocks**                             | ❌ missing | M      | core      | platform-web       | reimplement | The payoff is that an annotation is an ordinary block: linkable, taggable, queryable, draggable into other pages. That is the feature Obsidian users pay a community plugin for. Two things to copy: materialise the block LAZILY on first reference (highlights get ids at creation, blocks only when linked), and store the whole highlight map on the block alongside the indexed page/colour properties. One thing to fix: Logseq denormalises hl-value against the indexed properties and lets them drift — pick one source of truth. Also: if the colour cannot be resolved they silently create nothing, which is a bad failure mode.                                                                                                                                                                                                                                    |
| **LaTeX math rendering (inline and display)**                                | ❌ missing | M      | core      | editor-markdown    | reimplement | Mostly library wiring, and users treat it as table stakes (Obsidian ships MathJax). Copy three operational choices: lazy-load KaTeX (and mhchem) on first use behind a loading flag so a note with no math pays nothing; render with throwOnError:false and strict:false so half-typed input degrades instead of exploding; and reveal the source under the caret via the existing groupId mechanism so it stays editable. No AGPL exposure — this is a KaTeX integration, not Logseq logic.                                                                                                                                                                                                                                                                                                                                                                                    |
| **Templates as reusable note subtrees**                                      | ❌ missing | M      | core      | platform-web       | reimplement | Table stakes (Obsidian ships a core Templates plugin). Two ideas beyond Obsidian worth taking: template-applied-to makes a template AUTO-APPLY to blocks carrying given tags, and used-template records the provenance so you can find every note made from a template later. Adjacent capability we already have to substitute into: the engine's snippet sessions with tab stops and mirrors (packages/editor/src/plugins.ts:399-437) — that is the right vehicle for cursor placement after insertion.                                                                                                                                                                                                                                                                                                                                                                       |
| **Marker→status import mapping (custom markers become new choices)**         | ❌ missing | S      | important | platform-server    | reimplement | The clever half: unmapped markers (WAIT, WAITING, IN-PROGRESS) are not dropped and not coerced — the importer MINTS a new closed value on the status property, memoised per import so it is created once. That preserves user workflows and is the difference between an import people trust and one they don't. Table: TODO/LATER→todo, NOW/DOING→doing, DONE→done, CANCELED/CANCELLED→canceled (exporter.cljs:417-470).                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Org-style SCHEDULED:/DEADLINE: lines under a block**                       | ❌ missing | M      | important | platform-server    | reimplement | Only needed if we ingest existing vaults or want org compatibility. The parse result is either a bare yyyyMMdd int or {date-int, time, repetition [[kind][unit] n]} — the repetition triple is what feeds the repeat engine. If we own the markdown, prefer frontmatter/properties and treat these lines as an import path only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Declarative post-transaction command engine (rules produce more tx-data)** | ❌ missing | L      | important | platform-server    | reimplement | The survey is right that this beats sprinkling `if (status === done)` through handlers: 'repeating task' and 'property history' are both just entries in one vector, and because actions return tx-data rather than transacting, undo covers both halves. HARD PREREQUISITE we do not have: DataScript datoms {e,a,v,tx,added} have no equivalent in our stack — our write layer must expose an equivalent per-entity change feed before this engine can exist at all. Our orchestration event store is the natural home (schema.ts:32-64) but aggregateKind is still ['project','thread'] and the projection pipeline runs a single named projector, so this is real foundation work, not wiring.                                                                                                                                                                              |
| **Repeat driven by any two-valued property**                                 | ❌ missing | S      | important | platform-server    | reimplement | Nearly free once the checkbox-state flag exists, and it is the payoff for that design — a habit tracker driven by a custom `Watered?` property costs zero extra code. Also picks whether Scheduled, Deadline, or both get advanced. Build it in the same pass as the repeat rule; retrofitting the indirection later means touching every condition.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Repeat settings popover**                                                  | ❌ missing | S      | important | platform-web       | reimplement | Copy their labels, not the org syntax: 'Advance from completion' / 'Advance from scheduled' / 'Advance from scheduled, skip to future' is the only presentation of .+/+/++ users have ever understood. Plus 'Every [n] [unit]' and 'When [property] is [Done-choice]'.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Property change history (audit trail per property)**                       | ❌ missing | M      | important | platform-server    | reimplement | Logseq flags properties with :enable-history? (status and priority carry it by default) and detects change by ATTRIBUTE rather than value (the 'datom-attribute-check?' condition kind). For us the interesting alternative the survey does not consider: we have per-turn git checkpoints and a real git service, so note history could be git-derived rather than a parallel table — but there is no /git/log or /git/blame route today (git/routes.ts exposes no log verb), so either path is real work.                                                                                                                                                                                                                                                                                                                                                                     |
| **LOGBOOK clock in/out time tracking**                                       | ❌ missing | M      | important | platform-web       | reimplement | AGPL exposure if transliterated — the drawer insertion is hand-tuned text surgery (fixed ordering: title, SCHEDULED/DEADLINE, PROPERTIES, then the drawer; `subs 8` hard-codes the width of 'CLOCK: ['; the summary splitter depends on TWO spaces after '=>'). Do not port it. Also note the data-loss precedent: Logseq's own file→DB importer STRIPS the logbook and carries the clock data nowhere, so time tracking simply does not exist in current Logseq. If we build it, we are ahead of them. Timer displays must use tabular-nums.                                                                                                                                                                                                                                                                                                                                   |
| **Status and priority slash commands generated from the choice set**         | ❌ missing | S      | important | platform-web       | reimplement | The design point is that the menu is DERIVED from the property's closed values at runtime, each carrying its own icon — add a status choice and the menu grows with no code change. Build the slash menu generically (a provider list) the first time rather than hardcoding entries, because templates, dates, and embeds all want the same surface.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **New cards inherit their creation time as first due date**                  | ❌ missing | S      | important | platform-web       | reimplement | Small rule, large behavioural effect: a block newly tagged #Card is immediately due, and a backlog of old untagged notes sorts by age instead of all colliding at 'now'. Must agree with the due query's 'missing due counts as due' branch or cards flicker in and out of the queue — that pair of rules is the one real correctness trap in the flashcard feature.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Card decks as saved queries**                                              | ❌ missing | M      | important | platform-web       | reimplement | Nice property inherited free: because deck membership is a query and #Cards subclasses are included via structured children, `#Vocab extends Card` works as a deck with zero extra code. For us the blocker is upstream — there is no block query engine at all, so this row is really 'flashcards, once queries exist'. A blank deck query means global.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Duplicate attachments refused by checksum**                                | ❌ missing | S      | important | platform-server    | port        | SHA-256 the bytes, look for an existing asset with that checksum, and abort with a warning that NAMES and links the existing asset. We already hash file content for optimistic concurrency (apps/server/src/fs/version.ts:11) so the primitive is there. One design question the survey flags without answering: Logseq hard-refuses rather than silently inserting a reference to the existing asset — refusing is safer but makes the user do the work; inserting the existing one is friendlier and, given the filename is a uuid, harmless.                                                                                                                                                                                                                                                                                                                                |
| **Inline image rendering with drag-to-resize and persisted width**           | ❌ missing | L      | important | editor-core        | reimplement | Structural note, per our optimization rule: do not tune around this. The options are (a) lift the uniform-row-height constraint in the row height index + virtualization (the real fix, L, unlocks headings at true scale, tables, and live preview generally), or (b) render images as BLOCK rows below the text line, which already supports variable height and ResizeObserver measurement — cheaper, but images stop being inline and the design is visibly not Obsidian. Also: block surfaces and inline widgets have NO browser tests at all (only happy-dom with faked rects), so real measurement and scroll anchoring are unverified either way. Logseq persists {width} onto the asset block so the size follows the asset everywhere it is embedded — a better model than Obsidian's per-link `![[img\|300]]`.                                                       |
| **Assets resolve to a loadable URL per runtime**                             | ❌ missing | S      | important | platform-web       | reimplement | We are much better placed than Logseq here: they need assets://, file://, Blob URLs and a published-graph mode, plus a `/logseq__colon/` placeholder to survive Windows drive letters through URL parsing. We have one route that serves bytes for a confined path, so a single helper `assetUrl(relPath)` covers web and desktop. Two of their bugs to not inherit: percent-decode exactly once (guarded by a regex probe) and REVOKE blob URLs on unmount — Logseq never does, so a long session leaks. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                          |
| **Area (rectangle) PDF highlights captured as images**                       | ❌ missing | M      | important | platform-web       | reimplement | Capture recipe worth copying exactly: read the ALREADY-RENDERED page canvas, draw into an offscreen canvas at devicePixelRatio with imageSmoothingEnabled=false and alpha:false, toBlob, then run it through the ordinary asset pipeline. Consequence to document for users: sharpness depends on the zoom level at capture time. Guard: abort on a drag smaller than 10x10px, and disable text selection during the drag. The provisional image id must be replaced with the real asset id before persistence or the image cannot be resolved.                                                                                                                                                                                                                                                                                                                                 |
| **Jump from a highlight block back into the PDF**                            | ❌ missing | S      | important | platform-web       | reimplement | Small but it is what makes annotations feel connected rather than copied. Details that matter: scroll to a PDF point offset ~200px ABOVE the highlight so it is not glued to the top edge, then flash the element for ~1.2s, centring it first if it is off-screen. Round-trip navigation is the whole value proposition of in-app PDF annotation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **PDF highlight sidecar file for file-based vaults**                         | ❌ missing | M      | important | platform-server    | reimplement | MORE RELEVANT TO US THAN THE DB VERSION. Logseq's file form was assets/<key>.edn holding {:highlights [...] :extra {...}} plus a companion page for the annotation blocks. If our notes are markdown files, we need the equivalent (a JSON sidecar) unless annotations become properties inside the markdown, which gets ugly fast for area rects. Pretty-print whatever we choose — the file version used fipp specifically so the sidecar stayed diffable under git, which matters for a git-backed workspace like ours. Also fix their persistence trigger: they rewrite the WHOLE highlight array on every change and left 'incremental' as a TODO.                                                                                                                                                                                                                         |
| **Mermaid diagrams in fenced code blocks**                                   | ❌ missing | M      | important | editor-markdown    | build-fresh | OBSIDIAN-DELTA ROW: Logseq has NO mermaid anywhere (no dependency, no code path) and left it to plugins, while Obsidian ships it in code fences. So Logseq is no reference here at all. Risk to size honestly: registerBlockProvider has zero production consumers and its only tests are happy-dom with faked rects, so this would be the first real proof of block surfaces — valuable, but it is proving plumbing, not wiring. mermaid is a heavy dependency; lazy-load it per-document like the non-critical plugins already are.                                                                                                                                                                                                                                                                                                                                           |
| **Video embeds for YouTube/Vimeo/Bilibili/Loom**                             | ❌ missing | M      | important | editor-new-package | reimplement | Their matcher is four regexes shaped so group 3 is the domain and group 5 the id, plus a ?t= start offset, plus bare-id acceptance when the macro name supplies the provider hint. YouTube goes through the IFrame Player API specifically so timestamps can seek; the rest are plain iframes. Note youtube-nocookie is treated as its own provider — a privacy default we should probably make the DEFAULT rather than an alternative.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **{{renderer}}-style macro extension point**                                 | ❌ missing | M      | important | editor-new-package | reimplement | THIS IS THE ROW THAT JUSTIFIES @singapor/inline-macros. One parser for the `{{name args}}` form, one host-supplied renderer registry, one story for atomicity/caret-stops/reveal-under-caret, and one place where a missing renderer degrades gracefully. Explicitly REJECT their design: frontend.components.macro/macros is a global mutable atom that features swap! into at namespace load time (the SRS namespace registers 'cloze' on load), so LOAD ORDER IS THE API. Pass an explicit registry through the render config instead. Also copy their good failure mode: an unknown macro renders as a visible warning chip showing the original text — never silently disappears.                                                                                                                                                                                          |
| **User-defined text macros**                                                 | ❌ missing | S      | important | contracts          | reimplement | Cheap and it is the escape hatch that stops every small request becoming a feature request. Expansion is re-parsed as markup (inline when it is a single paragraph, block otherwise), which is what makes `{{sig}}` able to produce a link rather than literal text. Scope must be application: an expansion is text substitution, but if expansions can ever reach a command or a URL, a workspace-scoped map would be an injection vector.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Dynamic template variables (<%today%>, <%time%>, <%current page%>)**       | ❌ missing | M      | important | platform-web       | reimplement | The non-obvious part is the pre-pass, and it is the part that makes templates trustworthy: before inserting anything, scan every template block's title, raw title AND property text values for date variables, materialise the journal pages they name, and only then resolve — so `<%tomorrow%>` never produces a dangling ref. Resolution happens at APPLY time; the template block itself keeps the literal. Caveat they did not solve: NLP-parsed dates like `<%next friday%>` still dangle. Also note Logseq has TWO near-duplicate implementations with different semantics — do not merge them blindly if we port anything.                                                                                                                                                                                                                                             |
| **Quick capture from outside the app**                                       | ❌ missing | M      | important | platform-web       | reimplement | High value for a notes app: capture is where retention comes from. Options to expose as settings keys, following theirs: insert-today?, redirect-page?, prettify-url?, default-page. Media and tweet URLs get wrapped in their embed macros on the way in, which is a nice touch. For us the desktop shell already exists, so the cost is the protocol handler plus routing, not new infrastructure.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Agent-facing API over the notes graph (no in-editor AI)**                  | ❌ missing | M      | important | platform-server    | reimplement | WE ARE AHEAD HERE, in a way worth stating plainly: Logseq deliberately has NO LLM features in the note surface and instead exposes a machine-facing API (list/create/update pages and blocks, move blocks, insert trees, set properties) with read/write scopes. We already ARE an agent platform with a full chat/orchestration stack; what is missing is the notes domain to expose. Their restraint is the design lesson — resist putting an LLM in the editor and instead make the graph legible to the agent that already lives next to it. Concrete gap: aggregateKind is still ['project','thread'] (schema.ts:37) and the projection pipeline runs a single named projector, so a notes aggregate needs a migration plus either a second projector registration or its own pipeline. [downgraded by verification]                                                       |
| **Legacy [#A]/[#B]/[#C] priority markup in block text**                      | ❌ missing | S      | niche     | platform-web       | reimplement | AGPL exposure if transliterated — the offset computation is hand-tuned and would read as derivative. The survey describes it fully: skip past the LAST embedded heading prefix, then past the marker, then replace-FIRST /(\[#[ABC]\])?\s?/. Ordering is fixed: heading hashes, marker, priority, title. Only worth building as an import/compat path; our own storage should be a property, not inline markup.                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Legacy SM-5 spaced-repetition scheduler**                                  | ❌ missing | M      | niche     | platform-web       | reimplement | DO NOT BUILD. Logseq deleted it and moved to FSRS, and the survey documents why: the OF-matrix is float-keyed (so it grows unboundedly and mostly misses), it is persisted to a file OUTSIDE the graph so it desyncs on sync, it is global across all cards, and the implementation writes the matrix under the OLD ease factor then reads it under the NEW one — so the matrix is nearly dead weight and intervals are effectively initialInterval * ef^(repeats-1). A faithful port would have to reproduce that bug. The only reason to touch it: reading the six card-* properties when importing an old vault, which is a 20-line mapping into FSRS, not a scheduler.                                                                                                                                                                                                      |
| **Due-card badge with hourly refresh**                                       | ❌ missing | S      | niche     | platform-web       | reimplement | One real bug to avoid, which Logseq had to fix explicitly: store the interval canceller and cancel the previous one on remount, or intervals stack. Under our StrictMode-mounted app this bites twice as fast. Prefer invalidating a TanStack query on write over an hourly poll — we have the machinery and polling is the lazy answer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Card review keyboard shortcuts (s, 1-4, g f)**                             | ❌ missing | S      | niche     | platform-web       | reimplement | Do NOT copy their implementation: Logseq's handler locates the button by CSS class inside #cards-modal and synthesises a click. Bind to the action, not to the DOM. Add a `flashcards` FocusArea so 1/2/3/4 do not leak into the editor.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Flashcards can be disabled**                                               | ❌ missing | S      | niche     | contracts          | reimplement | Register it in the SAME pass that wires the consumer (the due-count poll and the entry point), per the never-inert rule. Cheap, but it is the difference between a notes app and a notes app with someone else's hobby bolted on. Scope is `application` (it gates a feature surface, not execution).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Pasted images get a timestamp title instead of 'image'**                   | ❌ missing | S      | niche     | platform-server    | reimplement | Exists because every clipboard names its payload image.png; without it a page of pastes shows ten blocks called 'image'. Two-line fix, disproportionate polish. Make the format a settings key rather than a literal.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Image lightbox with sensible next/prev order**                             | ❌ missing | S      | niche     | platform-web       | reimplement | One detail worth stealing: collect every image on the page, sort by y then x, then ROTATE the list so the clicked one is first — that is what makes next/prev feel right instead of arbitrary.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Asset alias directories outside the workspace**                            | ❌ missing | M      | niche     | platform-server    | reimplement | Genuinely niche, and it costs us more than it cost Logseq because our whole fs layer assumes a single confined root (apps/server/src/fs/path.ts:29-54 resolve → assertInside). If we build it, register each alias explicitly with a per-directory confirmation and resolve through the same assertInside check against the alias root. The aliases must be `application`/`machine`-scoped settings, never `window`.                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Missing-asset warning and remote re-download**                             | ❌ missing | S      | niche     | platform-web       | reimplement | Only the missing-file warning matters for us; the remote-download half presupposes a sync service we do not have and should not build for this. Keep it a quiet inline warning that names the expected path — a broken image icon tells the user nothing actionable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Zotero PDF path resolution**                                               | ❌ missing | S      | niche     | platform-server    | reimplement | Logseq DELETED the full Zotero extension (API client, item extractor, settings) and kept only path resolution plus two link macros — a good signal about what actually earned its keep. Build only the path resolution if anything. Resolving zotero-link:// against a configured data directory has the same confinement question as asset aliases.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Whiteboards (canvas where shapes are blocks)**                             | ❌ missing | XL     | niche     | platform-web       | build-fresh | RECOMMEND SKIPPING, and Logseq agrees — they removed the whole feature in Dec 2025 along with their vendored tldraw v1 fork. Their block↔shape sync was nonce-diffing with a full js->clj deep conversion on every tick, which is part of why it was slow. If we ever want a canvas: use current tldraw directly and store the document as an opaque asset. Do not make shapes blocks. Note the one idea that was good — shapes could carry refs, so a whiteboard participated in linked references — and that it is achievable far more cheaply by letting a canvas asset declare a flat list of referenced note ids.                                                                                                                                                                                                                                                          |
| **Schema-driven autocompletion inside config code blocks**                   | ❌ missing | M      | niche     | platform-web       | reimplement | Do not port — it is CodeMirror 5's registerHelper API and a malli schema walk, both of which we would rewrite anyway. The TECHNIQUE generalises and is the reason the survey included it: fold the token stream from document start to cursor into {currentPath, stateStack} to answer 'am I at a key or a value position, and under what path' with no parser. Their 'ignore-map' rule (a map nested inside a query is data, not config) is the insight that makes it correct. Our better answer for settings.json specifically is a JSON schema through the existing LSP path, not a hand-rolled scanner.                                                                                                                                                                                                                                                                     |
| **Calculator code blocks**                                                   | ❌ missing | M      | niche     | platform-web       | reimplement | Cheap in our stack if we lean on an existing expression library rather than writing an instaparse-equivalent grammar. Feature set that makes it useful: variables, percent, mixed numbers, scientific notation, exponent, modulo, PI/E — and per-line results. Niche, but it is the kind of thing that gets screenshotted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Evaluating code inside notes (sandboxed interpreter)**                     | ❌ missing | L      | niche     | platform-web       | build-fresh | Logseq is not a useful reference: their whole answer is SCI, a ClojureScript interpreter, plus a `call-api` bridge into the plugin API. For JS the equivalent is QuickJS-in-wasm or a worker with no network. Treat this as an explicit, off-by-default capability with a visible indicator; do not let it arrive as a side effect of the query engine the way Logseq's :view and :result-transform did. By our settings rule anything reaching execution is application/machine scope and cannot be configured from a workspace file.                                                                                                                                                                                                                                                                                                                                          |
| **Drag-resizable video embeds that rewrite the macro text**                  | ❌ missing | M      | niche     | editor-new-package | reimplement | Two techniques worth keeping even if the feature is niche. (1) Single source of truth: the width IS the `w=640` macro argument, no side store — so it survives copy/paste and git. (2) Caret preservation across a programmatic edit: compute the common-prefix length, then for each of selectionStart/selectionEnd keep it if it precedes the change, else max(changeStart, pos + lengthDelta). We will need that second one anywhere a widget edits text under a live caret. Also note they resolve WHICH occurrence was dragged by DOM index, because the same URL can appear twice in one block.                                                                                                                                                                                                                                                                           |
| **YouTube timestamp capture and seek**                                       | ❌ missing | S      | niche     | editor-new-package | reimplement | Purpose-built for taking notes against a video, and it is the kind of feature that wins a specific audience outright (Obsidian needs the Media Extended plugin). Cheap once video embeds exist. 'Nearest player above the cursor' is the right heuristic — do not overthink it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Pasting a media or tweet URL auto-wraps it as an embed**                   | ❌ missing | S      | niche     | platform-web       | reimplement | Same unproven-hook caveat as image paste: EDITOR_PASTE_HANDLER has zero consumers anywhere. Build both paste behaviours in one pass so the hook gets proven once. Also copy the small one: pasting a URL OVER a selection makes a markdown link with the selection as the label — that is the single highest-frequency paste interaction in a notes app.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Tweet embeds**                                                             | ❌ missing | S      | niche     | editor-new-package | reimplement | Trivial once the macro package exists. Copy their degradation rule: inside a table view, render the literal macro text instead of mounting an iframe per row — otherwise a query result with 50 rows mounts 50 third-party iframes. That rule should be general to ALL embeds in dense contexts, not special-cased to tweets.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Quick add panel**                                                          | ❌ missing | S      | niche     | platform-web       | reimplement | Cheap once capture routing exists — largely the same code behind a keybinding instead of a protocol handler. Worth building in the same pass as quick capture rather than separately.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Code blocks with a real editor and language modes**                        | 🟡 partial | S      | core      | editor-markdown    | reimplement | STRUCTURAL WIN — do not port. Logseq mounts a whole CodeMirror 5 instance per code block because their editor is a textarea; we would be adding an editor inside an editor. Our gap is chrome, not capability. Two caveats: the injection only fires for languages we have registered (7 grammars today), and language-aware editing INSIDE the fence (indent rules, brackets) follows the injected layer, which is worth a browser test. Evidence: /Users/shaul/Desktop/D/Editor/packages/tree-sitter-languages/src/queries/markdown-injections.scm:1 (fenced_code_block info_string drives an injection layer); markdown-folds.scm:4 (fenced_code_block is foldable).                                                                                                                                                                                                         |
| **Callouts/admonitions, quotes, and tables**                                 | 🟡 partial | M      | important | editor-markdown    | reimplement | OBSIDIAN-DELTA: build `> [!note]` callouts, not Logseq's org `#+BEGIN_NOTE` — Logseq itself deprecated #+BEGIN_QUOTE in DB graphs, and org admonition syntax has no audience in a markdown vault. The quote marker substitution proves the path works. Tables are the expensive part and are genuinely blocked on variable row height; a read-only rendered table as a block row is the cheaper interim. Their raw HTML and hiccup rendering (DOM-purified) is a category we should NOT copy — arbitrary HTML in notes is a security surface we do not need. Evidence: /Users/shaul/Desktop/D/Editor/packages/markdown/src/replacements.ts:95-125 (block-quote markers substituted width-for-width with │, with reveal-under-caret via groupId).                                                                                                                                |
| **Attachment size limit and extension requirement**                          | 🟡 partial | S      | niche     | contracts          | reimplement | We have the pattern but only for chat attachments and only as a constant, not a registered setting. Logseq rejects >100MB and rejects extensionless files (because the extension IS the stored asset type) BEFORE writing any bytes — check first, write second, or a rejected 300MB paste still costs a disk write. Scope this key `application`: it is a limit, not something a cloned workspace file should be able to raise. Evidence: /Users/shaul/Desktop/D/platform/apps/server/src/attachments/store.ts:31-51 (MAX_CHAT_ATTACHMENT_BYTES cap + unsupported-type rejection, chat pipeline only).                                                                                                                                                                                                                                                                         |

## Plugins, themes, config.edn, keybindings, and settings (extensibility & configuration surface)

66 features — 6 have, 7 partial, 53 missing.

| Feature                                                                     | Ours       | Effort | Priority  | Layer           | Source      | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --------------------------------------------------------------------------- | ---------- | ------ | --------- | --------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Plugin registration and lifecycle**                                       | ❌ missing | L      | core      | platform-web    | port        | The anchor row: everything else in this domain depends on it. Port the state machine and the invariants, which are the valuable part — asserted transitions (load only from unloaded/error), two dispose stacks run in insertion order with per-disposer try/catch, ids stable across reloads, a shared ready barrier so no plugin's main() runs while others are still constructing, and a broken package skipped so it does not occupy its id while any other failure still registers so the UI can offer removal. Note we already have a separate, trusted, in-realm plugin API for the editor (apps/web/src/features/editor/utils/plugins.ts:83) — decide explicitly whether third-party plugins ever reach it. AGPL exposure.                                       |
| **Host↔plugin duplex transport**                                            | ❌ missing | L      | core      | platform-web    | reimplement | Reimplement, do not port. Their postmate handshake (5 retries, 8s timeout, upgrade to a MessageChannel, then origin checks dropped) is a 2016 iframe design; we would start from a typed MessagePort or a worker. Keep three properties: every async call resolves, rejects, or times out — none hangs; correlation entries are garbage-collected; and the sandbox and in-process modes expose an identical call interface so no caller branches on mode. Reject their `api:call` string-mangling router outright (strip _/$, snake_case, four-way fallback lookup) — a typed router keyed by (namespace, method) removes a whole class of accidental API surface.                                                                                                       |
| **Plugin event hooks with subscription indexing**                           | ❌ missing | M      | core      | platform-web    | port        | Port the indexing, which is the design worth copying: the host keeps hook → {pluginId → opts} and only crosses the sandbox boundary for actual subscribers, installed automatically on first subscribe and uninstalled when the listener count hits zero. Without it a document-changed hook serializes on every keystroke. Reject the uuid-encoded-into-hook-name trick.                                                                                                                                                                                                                                                                                                                                                                                                |
| **Enable / disable a plugin without uninstalling**                          | ❌ missing | S      | core      | platform-web    | reimplement | Logseq stores `disabled` inside the plugin's own settings blob so enable is settings.set + load. Our equivalent is a `plugin.<id>.disabled` key in the settings registry, which also gets us scope enforcement for free. Disabling must clear every command, keybinding, UI item and theme the plugin registered — that teardown is the actual work.                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Plugin settings persisted per plugin**                                    | ❌ missing | M      | core      | contracts       | port        | The hard mismatch: our registry is static and compile-time typed, theirs is a per-plugin JSON file with a runtime schema. Keep the dynamic entries in a separate document from the typed table so the static guarantees are not diluted. Port the store semantics — change(next, prev) events in both directions, schema defaults that never overwrite a user value, patch with array-replace-not-concat — and reject their reference-equality `set` (mutate-then-set is a silent no-op). AGPL exposure.                                                                                                                                                                                                                                                                 |
| **Auto-generated per-plugin settings form**                                 | ❌ missing | M      | core      | platform-web    | port        | We are further along than Logseq here: our widget kind is type-checked against the value type and the stored value is validated, where theirs is advisory (Object.assign of defaults, no coercion). What is missing is the dynamic half — a schema that arrives at runtime. Port their widget taxonomy (string/number/boolean/enum/object/heading, inputAs for color/date/range/textarea, enumPicker) and their rule that an unhandled type renders a visible error instead of vanishing. Do not port the 1000ms debounce on uncontrolled inputs; it drops the last keystrokes on unmount. AGPL exposure. [downgraded by verification]                                                                                                                                   |
| **Sandboxed plugin main UI window**                                         | ❌ missing | L      | core      | platform-web    | reimplement | Reimplement rather than port: their lsp:// URL scheme exists only to give each plugin a distinct origin for postMessage checks, encoding filesystem paths into hostnames. We should hand each plugin one opaque origin (blob/srcdoc with a null origin plus an explicit MessagePort) and drop path encoding entirely. Keep the behaviour of restoring editor focus when the panel hides.                                                                                                                                                                                                                                                                                                                                                                                 |
| **HTML injection into named UI slots and CSS selectors**                    | ❌ missing | L      | core      | platform-web    | reimplement | Explicitly do NOT port the implementation. DOMPurify-sanitized innerHTML with ALLOW_UNKNOWN_PROTOCOLS and iframe allowed is a very wide hole, and it gives plugins no component model. Take only the identity and lifecycle rules — one node per (pluginId, key), update-in-place on re-registration, null template means removal, `reset` clears the slot — and express the content as a serializable declarative UI descriptor instead of an HTML string.                                                                                                                                                                                                                                                                                                              |
| **Event delegation from injected HTML back to the plugin**                  | ❌ missing | M      | core      | platform-web    | reimplement | Their data-on-click="method" plus a hand-rolled event serializer is a consequence of choosing HTML strings. With a declarative descriptor the callback is a named action id on the node and the payload is typed. Keep one detail: the bounding rect is only serialized when the node opts in, because getBoundingClientRect forces layout.                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Plugin-injected global stylesheets**                                      | ❌ missing | S      | core      | platform-web    | port        | Trivial to port (keyed style tag, replace text on re-register, remove on unload). The real question is policy, not mechanism: unrestricted plugin CSS can override our theme tokens wholesale, so decide whether injected styles are scoped to the plugin's own containers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Plugin slash commands**                                                   | ❌ missing | L      | core      | platform-web    | reimplement | Most of this row's cost is the missing primitive, not the plugin API: there is no trigger-character completion menu anywhere in the app (the LSP completion controller lives in @singapor/lsp-plugin and is not a general menu). Logseq's declarative action-step vector is the piece worth taking — it lets a plugin script the editor (insert text, move caret, open a picker) without shipping a function across the sandbox.                                                                                                                                                                                                                                                                                                                                         |
| **Plugin command-palette entries**                                          | ❌ missing | S      | core      | platform-web    | port        | We already have the half Logseq is proudest of — a searchable palette with usage-based ranking. What is missing is a non-static row source: PlatformCommandId is derived from a compile-time array (apps/web/src/keymap/table.ts:11), so runtime ids need the id type widened to admit a `plugin.<id>/<key>` branded form. Their unregister-purges-history rule is worth copying so an uninstalled plugin stops influencing ranking. [downgraded by verification]                                                                                                                                                                                                                                                                                                        |
| **Unified command registration with placements**                            | ❌ missing | M      | core      | platform-web    | port        | Port the shape: placements expand into N registrations returning ONE idempotent unregister, and plugin teardown removes everything by plugin id regardless of whether the plugin called it. Two fixes to make while porting — pick a single key normalization (theirs disagrees between SDK and host and `Commands.execute` papers over it by applying both), and actually implement the `when` field, which Logseq stores and never evaluates. Our CommandRequirement lattice (apps/web/src/keymap/define-command.ts:32) is exactly the layer they are missing. AGPL exposure.                                                                                                                                                                                          |
| **{{renderer}} macro slots (inline widget anchors in note text)**           | ❌ missing | L      | core      | editor-markdown | reimplement | Two engine facts block this today: InlineReplacementSpec.render has zero producers anywhere (only plumbing and tests reference it), and inline replacements are hard-filtered to single-line spans (packages/editor/src/inlineMap.ts:227), so a multi-line widget cannot be one replacement. Anything taller than a text row also needs the block-surface path, which has no production consumer and no browser test. This is the row that decides whether embeds are reachable at all.                                                                                                                                                                                                                                                                                  |
| **Theme plugins with a light/dark pair**                                    | ❌ missing | M      | core      | platform-web    | port        | Port the state machine, it is small and every rule in it earns its place: eject before inject so there is never a window with two custom stylesheets; a theme with no url means the default and must null the current theme rather than inject nothing; restore at startup with both persistence and events suppressed; on uninstall clear only the mode whose selection belonged to that plugin. Note this can ship WITHOUT a plugin runtime as plain theme files — a much cheaper first cut. AGPL exposure.                                                                                                                                                                                                                                                            |
| **Per-workspace custom CSS file**                                           | ❌ missing | S      | core      | platform-web    | reimplement | Mechanically trivial (read file, inject keyed style tag, replace on workspace switch). One caveat their design ignores: CSS is not inert — url() in a stylesheet is a network request, so a workspace-scoped stylesheet inside a cloned repo can beacon. Their `:custom-css-url` config override (a remote stylesheet named by a workspace file) should not be reproduced.                                                                                                                                                                                                                                                                                                                                                                                               |
| **Plugin marketplace browsing and install**                                 | ❌ missing | L      | important | platform-server | reimplement | Nothing plugin-shaped exists anywhere in the repos. We already have the pieces this needs (path-confined fs at apps/server/src/fs/path.ts:29-54, attachments-style blob store at apps/server/src/attachments/store.ts:31-51), so the cost is the registry contract and the install/uninstall reconciliation, not plumbing. Ranks below the runtime: a marketplace with nothing to run is dead weight.                                                                                                                                                                                                                                                                                                                                                                    |
| **Install plugin from a GitHub release URL**                                | ❌ missing | S      | important | platform-server | reimplement | Cheap once install exists: fetch package.json, require a `plugin` block, derive a stable id from owner_repo, reuse the marketplace install path. Logseq routes GitHub repos through its own proxy (plugins.logseq.io/r2) — we would hit GitHub directly or not at all.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Load unpacked plugin (developer mode)**                                   | ❌ missing | S      | important | platform-server | reimplement | This is the feature that makes plugin authoring possible at all, so it lands with the runtime rather than after it. Keep Logseq's rule that a dev entry point is stripped for packages installed into the managed root — a marketplace package must not be able to point at a dev server.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Reload a plugin in place**                                                | ❌ missing | M      | important | platform-web    | port        | Port the lifecycle discipline, not the code: two dispose stacks (runtime cleared on every unload, registration only on uninstall), run in insertion order with per-disposer try/catch, and a bounded beforeunload so a hung plugin cannot wedge the app. The `effect: false` theme unregister during reload is what stops a visible theme flash. AGPL exposure — libs/ has no LICENSE and the repo root is AGPL-3.0.                                                                                                                                                                                                                                                                                                                                                     |
| **Automatic and manual update checks**                                      | ❌ missing | M      | important | platform-server | reimplement | Logseq samples 16 random enabled plugins on the auto path and 32 on an explicit check purely to stay under GitHub's rate limit. If we host our own index we can check all of them in one request and delete the sampling entirely — a case where their design is a workaround, not a pattern.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Declarative plugin list synced to a file (plugins.edn)**                  | ❌ missing | M      | important | platform-server | port        | Worth porting for one trick: the diff projection is derived FROM the schema (version/repo/theme/effect), so the file format and the diff can never drift. A version change then reads as install+uninstall of the same id, which is the desired replace semantics. Note the Clojure version relies on structural map equality for set-difference — in TS this needs a serialized projection key or a keyed deep-equal. AGPL exposure.                                                                                                                                                                                                                                                                                                                                    |
| **Draggable and resizable plugin panels with remembered geometry**          | ❌ missing | M      | important | platform-web    | port        | Port one specific algorithm: geometry is persisted as pixels PLUS the viewport it was captured at, then restored as a percentage of the current viewport, clamped to 99% and floored below the header. That is what keeps a panel on-screen after a monitor change. Also port the rule that once the user has dragged a panel, the plugin may no longer move it. AGPL exposure.                                                                                                                                                                                                                                                                                                                                                                                          |
| **Toolbar and page-bar buttons with user pinning**                          | ❌ missing | M      | important | platform-web    | reimplement | The blocker is structural: apps/web/src/features/workbench/utils/panels.ts:3 declares WorkbenchSidebarTab as a closed union and the renderer is a hand-written if-chain, so nothing can contribute a surface at runtime today. Pinning state (`<pluginId>:<key>` strings, purged on uninstall) is a settings key.                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Plugin entries in block, page and context menus**                         | ❌ missing | M      | important | platform-web    | reimplement | Fix one bug while porting: Logseq keys these registrations as `<label>_<pluginId>`, so changing a label orphans the old entry. Key on an explicit id. Their PDF-highlight variant has no analogue for us.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Plugin-defined keyboard shortcuts**                                       | ❌ missing | M      | important | platform-web    | reimplement | Our model is already better than theirs on the axis they got wrong: they install and uninstall whole native handler instances per context, we resolve one table against a FocusArea (apps/web/src/keymap/active-bindings.ts:66). The gap is dynamic ids again — the override document is keyed by a closed command-id schema (packages/contracts/src/settings.ts:100). Their global/non-editing/editing mode maps onto our FocusArea + CommandRequirement pair.                                                                                                                                                                                                                                                                                                          |
| **Cross-plugin command invocation**                                         | ❌ missing | S      | important | platform-web    | reimplement | Cheap once ids are namespaced `plugin.<id>/<key>`. Skip their key-normalization compensation entirely by normalizing once at the registration boundary.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Invoking built-in app commands from a plugin**                            | ❌ missing | S      | important | platform-web    | reimplement | We have the whole target — 44 workspace commands plus the editor set, with stable ids and one dispatch point. Logseq hand-maintains a fixed enum of ~35 invokable ids; we can expose the real table filtered by a flag on the command, which stays in sync automatically. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Replacing how a block renders**                                           | ❌ missing | XL     | important | editor-core     | reimplement | The hard ceiling: text rows have a UNIFORM height (packages/editor/src/virtualization/virtualizedTextViewLayout.ts:229 returns variable only for block rows), and @singapor/markdown's own stylesheet says content must fit within --editor-row-height. registerBlockProvider has zero production consumers and its tests are happy-dom with faked rects. Also port their matching discipline: predicates must be synchronous (an async one is rejected, not awaited, because matching happens during render) and a throwing predicate returns false rather than breaking the frame.                                                                                                                                                                                     |
| **Custom rendering of a block's properties area**                           | ❌ missing | L      | important | editor-core     | reimplement | The declarative condition DSL ({has}/{equals}/{in}/{not}/{any}/{all} over property idents) is small and worth taking, with one change: an unrecognised operator should fail closed with a warning, not fail open and match everything as Logseq does. Note we have no property model at all, so 'block properties' is itself unbuilt.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Fenced code block renderers**                                             | ❌ missing | L      | important | editor-markdown | reimplement | @singapor/markdown already hides fence markers via inline replacements, so the recognition half exists; the missing half is rendering a component below or instead of the code, which runs into the same uniform-row-height ceiling as macro slots. The source/rendered toggle and live re-render on edit are the UX worth copying.                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Sandboxed per-plugin file storage**                                       | ❌ missing | M      | important | platform-server | port        | We already run the same pattern for the workspace root, so this is a second root plus a per-plugin subdirectory. Port their discipline — one checkpoint function every read/write/unlink/list passes through, listings return relative paths so the host layout never leaks, and a directory listing is re-validated before deletion. Fix their gap: path.relative is lexical, so a symlink out of the storage dir still passes; use a realpath check. [downgraded by verification]                                                                                                                                                                                                                                                                                      |
| **HTTP client that bypasses CORS**                                          | ❌ missing | M      | important | platform-server | port        | The retry/cache/abort pipeline is genuinely worth transliterating: exponential backoff `delay * factor^attempt`, LRU touch by delete-then-reinsert on a Map, responses cached as clones so a consumer cannot poison the cache, abort short-circuited before any transport work. Security-wise this hands plugins an unrestricted egress path — it needs an allowlist decision, and our auth today is origin-allowlist-only bound to loopback (apps/server/src/auth.ts:22). AGPL exposure.                                                                                                                                                                                                                                                                                |
| **User macros as text expansion**                                           | ❌ missing | M      | important | platform-web    | port        | 38 LOC, the most portable unit in the whole domain — but port the behaviour, not the bugs: arguments are split on whitespace despite the docs' comma example (so `{{poem red,blue}}` is ONE argument and $2 never fills), the regex requires at least one argument so a zero-arg macro never expands, and sequential replacement lets an argument containing $n be re-expanded. Do it as a single regex pass with a replacer function. AGPL exposure.                                                                                                                                                                                                                                                                                                                    |
| **Chord (multi-key) shortcuts**                                             | ❌ missing | M      | important | platform-web    | port        | Our bindings are a single hotkey string per command (packages/contracts/src/settings.ts:100 stores one nullable string), and the recorder captures one chord, not a sequence. Port the concept but NOT the resolution: Logseq lets a chord whose prefix collides with a simple key go dormant with a debug log, which is a real usability bug they document. With our own dispatcher we can resolve by longest-match with a timeout instead.                                                                                                                                                                                                                                                                                                                             |
| **Plugin system master switch**                                             | ❌ missing | S      | important | contracts       | build-fresh | Logseq is barely a reference — theirs is a localStorage boolean. For us it is a `plugins.enabled` key, and per our scope rule it must be application/machine (it decides whether third-party code executes), never window. Also register it in the same pass as its consumer: a switch that gates nothing is worse than no switch.                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **In-realm React renderer registration**                                    | ❌ missing | M      | important | ambiguous       | build-fresh | Logseq is an anti-reference here: Experiments.* reaches window.top to hand the host a React component plus the host's React/ReactDOM, works only because the plugin shares the host realm, and is explicitly documented as unstable and marketplace-discouraged. Note that our own React-into-block-surface path (packages/react/src/editorBlocks.ts:48) has zero production consumers and no browser test, so even the trusted route is unproven.                                                                                                                                                                                                                                                                                                                       |
| **GitHub API rate-limit suppression on update checks**                      | ❌ missing | S      | niche     | platform-server | port        | Only needed if we query GitHub per plugin. With a single hosted index this row disappears. If kept, classify by response status, not by substring-matching the message the way Logseq does.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Per-plugin log console**                                                  | ❌ missing | S      | niche     | platform-web    | reimplement | We have structured wide-event logging end to end (evlog + logs/*.jsonl + a logs panel), so this is a `pluginId` field on plugin lifecycle events plus a filter, not a new ring buffer. Their 500-entry buffer surviving reload is the one property worth keeping: a crash-on-load must still be inspectable.                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Startup performance table and slow-plugin warning**                       | ❌ missing | S      | niche     | platform-web    | port        | Falls out for free if registration emits one wide event per plugin with a duration, which is our house logging style anyway. The 6-second user-facing warning threshold is the only design decision here.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Broken package detection with one-click cleanup**                         | ❌ missing | S      | niche     | platform-server | port        | Maps directly onto our conventions: their typed IllegalPluginPackageError carrying `{url, packageJsonPath}` is exactly a createError entry with code/why/fix, and the `fix` field is what powers the Uninstall button. A plain thrown string loses the ability to clean up, which is the whole point.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Bulk removal prompt for hoarded disabled plugins**                        | ❌ missing | S      | niche     | platform-web    | reimplement | Triggers at 100 disabled plugins with a 30-day snooze. Genuinely rarely used; list it for completeness and skip it until someone actually accumulates that many.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **before/after command invocation hooks**                                   | ❌ missing | S      | niche     | platform-web    | port        | `CommandBus` is already a single chokepoint that settles one wide `command.dispatch` event, so the observation half exists; what is missing is letting a plugin subscribe. Cheap, but it hands plugins a hook into every user action — worth gating.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Per-block render slots (subscribe to one block by id)**                   | ❌ missing | M      | niche     | editor-core     | reimplement | The document is a flat piece table with no block/node tree and no per-block state, so 'a block' is a host projection onto row numbers today. Reject Logseq's design detail of encoding the uuid into the hook name; keep only the idea of indexing subscriptions so unsubscribed blocks cost nothing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Plugin-provided app routes**                                              | ❌ missing | M      | niche     | platform-web    | build-fresh | Logseq is no reference here because we deliberately have no router (apps/web/src/App.tsx:7 forbids one above the store provider). Our analogue is the synthetic document-id scheme: a plugin surface would mint `plugin:<id>/<view>` and open as an editor tab. That means teaching ~9 hardcoded switch sites, since there is no document-kind registry.                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Right-sidebar and always-mounted daemon renderers**                       | ❌ missing | M      | niche     | platform-web    | reimplement | Same closed-union blocker as toolbar items. The daemon renderer matters more than it looks: only the ACTIVE editor tab's body is mounted (apps/web/src/features/workbench/components/code-panel.tsx:40), so a plugin needing background state has nowhere to live without it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Replaceable search backend**                                              | ❌ missing | L      | niche     | platform-server | reimplement | The interface shape already exists but there is no registry — a second provider means editing findInWorkspaceStream. Before building this, note the binding rule from docs/workspace-search-next-steps.md and the search evaluation plans: ripgrep stays the correctness oracle and an alternate engine must never silently alter exact-search results. A plugin-supplied backend that can change what exact search returns violates that. [downgraded by verification]                                                                                                                                                                                                                                                                                                  |
| **Git access for plugins**                                                  | ❌ missing | S      | niche     | platform-server | reimplement | The capability exists internally and is deliberately unrouted. Exposing arbitrary git argv to plugins is arbitrary code execution with extra steps (git supports hooks and `-c core.pager`); if built at all, expose a fixed verb set rather than Logseq's execCommand passthrough. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Reading resolved theme values from a plugin**                             | ❌ missing | S      | niche     | platform-web    | build-fresh | Only needed because sandboxed plugin UI cannot inherit the host stylesheet. If we choose declarative UI descriptors rendered by the host (see the HTML-injection row), the host applies tokens itself and this row disappears entirely.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **custom.js with a re-asked consent prompt**                                | ❌ missing | M      | niche     | ambiguous       | reimplement | Logseq eval()s a file from inside the graph after a confirm() that is re-asked every 7 days, and `:custom-js-url` lets a config file pull remote code. This is a straight code-execution channel out of a file that ships inside a cloned repository. If we build it, the 7-day TTL is the wrong knob (it reads as a bug to users); a permanent per-path grant is honest. Obsidian has no user-JS hook at all, which is a defensible position.                                                                                                                                                                                                                                                                                                                           |
| **Executable view and transform functions in configuration**                | ❌ missing | L      | niche     | ambiguous       | reimplement | Logseq's :query/views and :query/result-transforms hold Clojure lambdas evaluated by SCI when a query renders. Reproducing that faithfully means shipping an interpreter whose sandbox escape is a documented feature. Recommend: declarative transforms only, and say so explicitly rather than leaving the field open.                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Custom slash commands from configuration**                                | ❌ missing | M      | niche     | platform-web    | reimplement | Depends entirely on the slash menu existing. Once it does, this is one registry key holding label→insertion pairs, plus the same action-step vocabulary plugins use. Their last-wins dedup by label (so a user command can deliberately override a built-in) is a deliberate design choice worth keeping, but it means load order decides the winner.                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Generated typed SDK bindings for plugin authors**                         | ❌ missing | S      | niche     | contracts       | build-fresh | Logseq needs this only because its host is ClojureScript and its SDK is TypeScript — they walk emitted .d.ts files with ts-morph to generate CLJS wrappers. We are TypeScript on both sides, so a typed router plus a published .d.ts IS the SDK, with no generation step. Listed for completeness; the real work is publishing a versioned API package, not code generation.                                                                                                                                                                                                                                                                                                                                                                                            |
| **Theming via a documented CSS variable surface**                           | 🟡 partial | M      | core      | platform-web    | reimplement | We already have the thing Logseq documents — a semantic token set that flips light/dark automatically, plus material utilities. What is missing is the CONTRACT: which tokens a third-party theme may rely on, versioned, versus which are internal. Their scoping hooks (html[data-theme]/[data-color]/[data-font] plus platform classes) are worth matching. Our surface-opacity/blur/saturation knobs have no Logseq equivalent and a theme author would need to be told about them. Evidence: /Users/shaul/Desktop/D/platform/packages/ui/src/styles/globals.css (778 lines: light :root, .dark, and the @theme inline map); CLAUDE.md (token-only styling rule, status/diff tokens, surface material utilities).                                                    |
| **Appearance settings (mode, accent colour, fonts)**                        | 🟡 partial | S      | core      | contracts       | reimplement | We already exceed Logseq on surface material and match on mode and editor font. Missing: an accent colour (their html[data-color] radix scale) and an app-wide font toggle distinct from the editor font. Both are one registry entry plus a token, once the token exists in globals.css. Note theirs live in localStorage and follow the machine; ours are scoped registry keys, which is strictly better. Evidence: packages/contracts/src/settings/keys.ts:31 (workbench.colorTheme dark/light/system), :91 (editor.fontFamily), :103 (editor.fontSize), :40-82 (surface opacity/contentOpacity/blur/saturation/wallpaper).                                                                                                                                           |
| **Customising, disabling and multi-binding shortcuts**                      | 🟡 partial | S      | core      | contracts       | port        | We have override and disable, and we already draw the distinction Logseq draws (null = no shortcut, absent = use the default) with two separate buttons. Missing: multiple bindings for one command — their `["ctrl+k" "up"]` form. Our override document stores one hotkey, so a command with two defaults loses both when overridden (documented at apps/web/src/keymap/active-bindings.ts:47). Evidence: packages/contracts/src/settings/keys.ts:517 (keybindings.overrides, widget 'keybindings'); packages/contracts/src/settings.ts:100 (record of commandId → nullable hotkey); apps/web/src/features/settings/components/keybinding-row.tsx:53 (Unbind vs Reset deliberately separate).                                                                          |
| **Deprecation warnings for config keys that no longer apply**               | 🟡 partial | S      | important | contracts       | port        | We report a removed key as merely unknown, which tells the user nothing about why it stopped working. Port the shape: a separate non-blocking pass with a table of key → human reason, independent of validation so an invalid file still gets its parse error AND a valid-but-outdated one still gets its warning. Cheap, and it is the difference between 'unknown key' and 'this no longer applies because X'. AGPL exposure (deps/common is under the AGPL root). Evidence: packages/contracts/src/settings/resolve.ts:63 (unknown-key diagnostic fires for a removed key, but carries no reason).                                                                                                                                                                   |
| **Keymap settings page with keystroke-based search**                        | 🟡 partial | S      | important | platform-web    | port        | We have the page, text search, a keystroke recorder, and the right invariant — rows come from the RESOLVED table, so the list says what is in force rather than what was asked for. Missing is the 'press the keys' FILTER mode (record a chord, see every command bound to it). Their recorder state machine is worth porting for that: modified keys commit instantly, unmodified keys accumulate on a 400ms timer, backspace drops one step before commit and clears after. Evidence: apps/web/src/features/settings/components/keybinding-section.tsx:24 (rows read back out of the resolved table), :28 (text search over ~90 rows); components/widgets/chord-recorder.tsx:1.                                                                                       |
| **Shortcut conflict detection and guided reassignment**                     | 🟡 partial | M      | important | platform-web    | port        | This is the single most portable algorithm in the domain and we have only the first third of it. We detect exact shadowing and name the winner; missing are chord-prefix shadowing, cross-context overlap as a non-blocking amber warning, and undo of a reassignment. Their key rule is subtle and correct: prefix overlap is a conflict only WITHIN one context, because two contexts own independent prefix trees; and editing-only vs non-editing-only never conflict at all. Compare parsed key vectors, never strings. AGPL exposure. Evidence: apps/web/src/keymap/active-bindings.ts:38 (CommandKeyBindingRow.shadowedBy); features/settings/components/keybinding-row.tsx:32 (renders 'Shadowed by X'), :44 (conflictCount).                                    |
| **Raw JSON settings editor and open-in-default-app**                        | 🟡 partial | S      | niche     | platform-web    | reimplement | The app-settings half already ships as a real editor tab with our own engine, which is strictly better than Logseq's embedded CodeMirror. Extending it to a per-plugin document is one more prefix in the document-id scheme plus a save route. Evidence: /Users/shaul/Desktop/D/platform/apps/server/src/settings/routes.ts:46 (GET /settings/raw), :47 (POST /settings/raw); apps/web/src/features/settings/utils/document.ts:3 (settings-json: document scheme).                                                                                                                                                                                                                                                                                                      |
| **Layered configuration (defaults ← global ← workspace)**                   | ✅ have    | S      | core      | contracts       | port        | Ours is stronger than theirs on the axis that matters: precedence is enforced per layer AND per scope, so a workspace file physically cannot set an execution-reaching key — Logseq has no such boundary, which is exactly why their config.edn can name binaries and eval code. What is left to port is documentation, not code: their merge rule is 'maps merge one level, everything else replaces', which is surprising for nested values and only documented in a template file's prose. Ours should state its merge strategy explicitly (registry already carries a per-key merge: 'replace' \| 'record'). Evidence: packages/contracts/src/settings/resolve.ts:20 (SETTINGS_LAYER_ORDER user/workspace/policy), :36-46 (SCOPES_BY_LAYER), :57 (layerAllowsScope). |
| **Keyboard shortcut registry with contexts**                                | ✅ have    | S      | core      | platform-web    | port        | We have the registry and, crucially, the modern version of their dispatch: they install and uninstall whole native handler instances per context (which is why refresh! drops modal handlers and why chord/simple-key collisions silently go dormant), while we resolve one table against a FocusArea per event. Their ~200-command taxonomy and six context groups are still a useful checklist of what a notes app must bind — we have ~44 workspace commands plus the editor set. Evidence: apps/web/src/keymap/define-command.ts:32 (CommandRequirement lattice), :36 (CommandKeyDefault.pane scopes a binding to a FocusArea); keymap/active-bindings.ts:66; keymap/table.ts:11 (one command table).                                                                |
| **Configuration validation with readable errors**                           | ✅ have    | S      | important | contracts       | port        | Already shipped, and with a property Logseq lacks: one bad key never takes down the document, because the same document carries the keybindings the running app needs. Their five-way ordered cond (empty file is valid; duplicate-key gets its own message; not-a-map gets a custom message rather than a schema error) is worth checking our jsonc path against — apps/server/src/settings/layer.ts:224 already reasons about what jsonc-parser recovers from a broken document. Evidence: packages/contracts/src/settings/resolve.ts:63 (SettingsDiagnostic: unknown-key \| scope-not-allowed \| invalid-value, never thrown), :66-70 (carries id + layer + detail); apps/web/src/features/settings/components/diagnostics-banner.tsx, malformed-banner.tsx.          |
| **Config knobs surfaced in the settings UI with comment-preserving writes** | ✅ have    | S      | important | platform-web    | port        | Both halves already ship. Logseq uses borkdude.rewrite-edn for exactly the same reason we use jsonc-parser — a programmatic write must not destroy hand-written comments. Nothing to port; listed so the matrix does not read as a gap. Evidence: apps/server/src/settings/json-document.ts:91 (applyEdits + modify from jsonc-parser); apps/web/src/features/settings/components/page.tsx:183 (rows generated by grouping descriptors on category).                                                                                                                                                                                                                                                                                                                     |
| **Platform-correct shortcut display**                                       | ✅ have    | S      | important | platform-web    | port        | Covered. Two of their display rules are US-layout-specific and should NOT be copied: `(`/`)` expanding to shift+9/shift+0, and shift+/ displaying as `?`. Their instinct to print 'cmd' rather than 'meta' because that is what the keycap says is worth keeping. Evidence: apps/web/src/keymap/active-bindings.ts:1 (detectPlatform/normalizeRegisterableHotkey/parseHotkey from @tanstack/react-hotkeys); keymap/define-command.ts:38 (per-key platforms list); features/settings/components/widgets/chord-recorder.tsx:10.                                                                                                                                                                                                                                            |
| **Modifier-key correction from KeyboardEvent.code**                         | ✅ have    | S      | niche     | platform-web    | port        | Solved, and our guard is more careful than theirs — we refuse the code fallback when the layout already prints a Latin letter, where Logseq falls back whenever any modifier is held. Their motivating cases (macOS Option+key producing a Unicode character, Windows AltGr) are the same ones our fallback covers. Evidence: apps/web/src/keymap/active-bindings.ts:88 (printed-key match first, then a physicalKeyName(event.code) fallback), :96-99.                                                                                                                                                                                                                                                                                                                  |

## Graph lifecycle: multi-graph, sync (RTC/db-sync), export/publish, import, desktop/mobile/web builds, backup & startup

69 features — 2 have, 6 partial, 61 missing.

| Feature                                                                 | Ours       | Effort | Priority  | Layer           | Source      | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ----------------------------------------------------------------------- | ---------- | ------ | --------- | --------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Create a new vault**                                                  | ❌ missing | S      | core      | platform-web    | reimplement | POST /fs/create-folder and /fs/write already exist (apps/server/src/fs/routes.ts:77, :74) so the missing part is the flow: validate the name, refuse a duplicate, seed a config file plus a journals/pages layout, open it, create today's journal, and stamp created-at. Logseq's version rejects invalid names case-insensitively after prefixing; our vault names are directory names, so the collision rule is the filesystem's, which is simpler and one fewer invariant to get wrong.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Remove a vault from the list**                                        | ❌ missing | S      | core      | platform-web    | reimplement | Eviction already happens silently when a project falls past WORKSPACE_SLICE_LIMIT (apps/web/src/features/workspace/state/cache.ts:344) — it removes the slice and the search buffer — but there is no user-facing 'forget this vault'. Logseq's version also refuses to remove the last/demo graph with a dedicated error code and switches away with a toast naming both graphs. Our greenfield rule means no migration for removed slices; just drop them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Content index build with progress after open**                        | ❌ missing | L      | core      | platform-server | reimplement | The index we have is path-and-metadata only — basename, extension, a character bag, a 512-byte content-kind sniff, size, mtime, stale bit. There is no text in it, so every content query re-spawns ripgrep, and it is in-memory only, single-rooted, and fully rebuilt on every workspace open with no persistence and no user-visible progress. For notes this is the single highest-leverage server gap: titles, links and backlinks all need a persisted content index. Copy Logseq's scheduling rule — build on idle, reschedule while the user types — because it is what keeps indexing from competing with editing. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                             |
| **Create a remote (synced) vault**                                      | ❌ missing | L      | core      | platform-server | reimplement | There is no identity anywhere in the product: apps/server/src/auth.ts:31 hands out a constant local principal and the guard is an origin allowlist bound to loopback. So this row is blocked on accounts, not on sync mechanics. Logseq's two useful rules: refuse when a remote graph of the same name exists (tell the user to delete it rather than guessing), and make the encrypted/unencrypted choice immutable at creation so the checksum's attribute set never has to change under a live graph.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Initial vault upload (framed snapshot streaming)**                    | ❌ missing | L      | core      | platform-server | port        | The framing algorithm is worth porting verbatim in spirit: 4-byte big-endian length prefix over the uncompressed payload, gzip wrapping the framed stream rather than each frame, a binary search for the largest row prefix that fits a 1MB budget, reset=true exactly once and finished=true exactly once. For us the payload is markdown files plus a derived index, not database pages, which actually simplifies it — a file is a natural frame boundary, and the 'single row too large' fatal case becomes 'single file too large', which we already bound (FS_DEV_MAX_TEXT_FILE_BYTES). Reuse toSse/streaming plumbing (apps/server/src/sse.ts:15) for progress.                                                                                                                                                                                                                                                                                                                                              |
| **Download a remote vault**                                             | ❌ missing | L      | core      | platform-server | port        | Port the staged error reporting above all: Logseq attaches the stage name (:init, :fetch-pull, :fetch-snapshot-download, :prepare-e2ee, :stream-snapshot, :prepare-import, :finalize-import) to every failure, and says outright that this is what made download failures debuggable — exactly our house 'if the logs do not explain it, fix the logs first' rule. Also port the content-encoding sniff (check for the gzip magic 1f 8b rather than trusting the header) and staging into a temp location before touching the target vault.                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Live sync over WebSocket**                                            | ❌ missing | XL     | core      | platform-server | reimplement | We have the transport shape but none of the semantics: apps/server/src/orchestration/ws-rpc.ts:40 already gives a versioned WS RPC with a serverInstanceId that invalidates stale resume cursors, and orchestration/streams.ts:75 keeps a 4000-event live tail so a reconnecting client is replayed rather than re-snapshotted. That is the right substrate — a notes aggregate would widen the aggregateKind enum (apps/server/src/db/schema.ts:37, currently only project\|thread) and reuse the pipeline. Note the deliberate rejection from the survey: Logseq is not a CRDT, convergence rests entirely on the server being the sole serializer, and two offline clients editing one title cannot merge. If we want real offline-first multi-device merge, copy their normalization and checksum but put an op-based CRDT or per-attribute LWW with hybrid logical clocks underneath.                                                                                                                           |
| **Offline editing queue**                                               | ❌ missing | L      | core      | platform-server | port        | Port the table shape from client_op.cljs: one row per user action with tx_id (stable identity across rebases), created_at (preserved on rewrite so a rebased row keeps its queue position — getting this wrong sends it to the back of the queue), explicit pending/failed flags never inferred from row absence, and both the forward and inverse forms persisted at write time rather than reconstructed at rebase time. The rows would ride our existing event store: append-only, streamVersion-unique per aggregate, command receipts for idempotency (apps/server/src/db/schema.ts:32-93) — almost all of it is already built for chat.                                                                                                                                                                                                                                                                                                                                                                        |
| **Batched upload of local edits**                                       | ❌ missing | M      | core      | platform-server | port        | Small and worth copying exactly: at most 50 pending entries per batch, and only when online, socket open, nothing in flight, and local t equals last-known remote t — and when it skips, it logs exactly why (pending count, ws state, both t values). That last part is the whole debuggability story and matches our wide-event logging rule: one enriched event per attempt, not a narrow line per condition.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Stale-upload rejection and pull**                                     | ❌ missing | S      | core      | platform-server | port        | We already run the same idea one layer down: apps/server/src/fs/write.ts:47 rejects a write whose baseVersion or mtime moved with FILE_CHANGED, and apps/server/src/db/schema.ts:48 enforces a unique (aggregate, streamVersion). Logseq's version is the same check on a monotonic integer t — reject with reason 'stale', return the current t, and have the client pull immediately instead of retrying. Nothing is lost because the queue is durable. Cheap once the queue exists.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Rebase of unsynced local edits**                                      | ❌ missing | XL     | core      | ambiguous       | reimplement | Do not transliterate apply_txs.cljs: it is 1924 lines, carries a retry loop for 'the pending set changed while we were rebasing', ~8 distinct drop-stale-op filters, and an explicit undo-history wipe, and Logseq's own ADR 0008 exists because these bugs kept escaping tests and had to be reproduced with a browser-tab simulator. Reimplement from the described shape instead: one pending row = one user action, replayed as a small closed set of semantic operations (save, insert, move, delete, transact) rather than as datoms, dropped whole if any op inside it becomes invalid. The one clever detail worth copying literally is the left-sibling walk — when an insert's target was deleted remotely, walk its old left-sibling chain until you find a sibling that still exists.                                                                                                                                                                                                                    |
| **Sync convergence checksum**                                           | ❌ missing | M      | core      | contracts       | port        | The most portable module in the entire survey (429 LOC, selfContained: yes) and the highest value per line. Additive rolling hash over (entityUuid, attr, value) triples: FNV and DJB over UTF-16 code units, Math.imul plus \|0 so it is deterministic across JS engines, component-wise 32-bit wrapping add/sub so it is commutative and therefore order-independent, updatable in O(changed entities). Two details are load-bearing and easy to miss: reference values are normalized to the target's uuid (so eids never leak in), and a duplicated uuid must switch to counted multiset tuples or set-difference silently loses a tuple and the checksum drifts permanently. Also copy the dev-only assertion that recomputes after every local tx and throws on drift with the offending tx metadata.                                                                                                                                                                                                          |
| **Portable wire identity for transactions**                             | ❌ missing | M      | core      | contracts       | port        | Called out by the survey as the single most load-bearing convention in the whole sync design: never put a local id on the wire. Every entity reference becomes a uuid lookup ref, and entities created inside the transaction become string tempids (the stringified uuid). Everything else — the checksum, the rebase, the server's ability to validate — depends on this. If we sync notes at all, do this first and do it verbatim in spirit. The ordering rules matter too: schema-defining items first, delete-then-recreate of the same uuid must lead, ordinary deletions must go last so datoms referencing the doomed entity still apply.                                                                                                                                                                                                                                                                                                                                                                   |
| **Attachment sync**                                                     | ❌ missing | L      | core      | platform-server | reimplement | We have a local blob store for chat attachments (apps/server/src/attachments/store.ts:39 writeAttachmentFromDataUrl, size-capped, mime-derived extension) but nothing that syncs it and no notion of an asset owned by a note. Logseq's policy is the reusable part: a dedicated op queue with ~10 concurrent workers, skip assets over 100MB with a log rather than failing the sync, drop ops whose entity/type/checksum is missing, and — the user-facing bit — list assets whose local file could not be read in the status panel so the user can see exactly what is missing instead of a silent gap.                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Optional end-to-end encryption per vault**                            | ❌ missing | L      | core      | platform-server | port        | Ports almost mechanically to TypeScript — it is already WebCrypto promise chains. RSA-OAEP-4096 per user, private key wrapped with PBKDF2-SHA256 600k plus AES-GCM under a user password, AES-GCM-256 per vault wrapped to each member's public key. Two invariants to preserve exactly: the iteration count is version-tagged inside the envelope (never derived from config), and the set of encrypted fields must agree with the checksum's attribute set or the two sides never converge. Also copy the tolerant decrypt — a value that is not valid ciphertext passes through unchanged, which is what lets plaintext and mixed vaults work. Note the flip side of 'optional': the choice is immutable after creation precisely so the checksum shape is stable.                                                                                                                                                                                                                                                |
| **Encryption password prompt and local persistence**                    | ❌ missing | M      | core      | platform-web    | reimplement | Ask once, unwrap the private key, optionally persist encrypted so it is not asked every session. The one detail that is pure UX gold: distinguish a wrong password (WebCrypto raises OperationError) from a real crypto failure and say 'wrong password' instead of logging a crash. Headless runtimes take the password as a flag and persist it encrypted against the refresh token. The secret must NOT enter the settings document — it goes to the secret store.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Title to filename normalization and collision allocation**            | ❌ missing | S      | core      | platform-server | port        | Directly needed the moment a user can create a page by typing a title, and it is a nest of platform traps we would otherwise rediscover one bug report at a time: NFC-normalize; replace path separators, the Windows-invalid set < > : " \| ? * and control characters with _; strip trailing spaces and dots (Windows silently drops them); truncate to 160 characters; then REJECT rather than invent a fallback if the result is blank or a reserved device name (CON, PRN, AUX, NUL, COM1-9, LPT1-9). Determinism matters because the same vault mirrored on macOS and Windows must choose the same names. One correction while porting: implement the persisted title-to-path index the ADR specifies, not the shipped code, which recomputes duplicate suffixes by sorting uuids and therefore renumbers everyone's files when the alphabetically-first duplicate is deleted.                                                                                                                                 |
| **Import a foreign markdown vault (Logseq or Obsidian)**                | ❌ missing | M      | core      | platform-server | reimplement | Our file-native architecture makes the hard 90% of Logseq's 3169-line exporter unnecessary — opening an Obsidian or Logseq folder already reads every markdown file. What is missing is the convention layer: reading logseq/config.edn or an Obsidian config, converting :: properties and front matter, mapping which tags become classes, handling hls__ PDF-annotation pages, and reporting per-file warnings. The survey is explicit that this module is the most expensive to transliterate and the cheapest to reimplement, and that copying it is clear AGPL derivative-work exposure while rebuilding the pipeline shape is not. Take the shape: config first, deterministic partition and sort so dependencies import first, sequential per-file import with progress and per-file error capture, a pre-pass reserving ids for entities referenced before they exist, post-passes for cross-file reference repair, and a structured notification channel instead of throwing. [downgraded by verification] |
| **Automatic timed vault backups**                                       | ❌ missing | M      | core      | platform-server | port        | We already have a real, careful snapshot mechanism — per-turn git checkpoints into hidden refs that never appear in git log, attributed to the app rather than the user, captured without disturbing the staged index. It is scoped to agent turns, not to time, and there is no retention policy. Porting Logseq's backup filesystem discipline gives us the rest, and their reservation trick is genuinely clever: mkdirSync WITHOUT recursive, treating EEXIST as 'try suffix+1', which is an atomic directory reservation with no lock file. Plus temp-file-then-rename for the payload (which we already do for writes), a metadata file as the ordering authority, and a childPath guard using path.relative to reject escapes. [downgraded by verification]                                                                                                                                                                                                                                                   |
| **Remote vault entries merged into the switcher**                       | ❌ missing | M      | important | platform-web    | reimplement | Logseq merges remote-owned graphs into the same list, deduped by name, and only when the major schema version matches the running app; remote-only entries render as 'download' rather than 'open'. Cheap once a remote exists, meaningless before. Blocked on there being any remote at all — we have no accounts (apps/server/src/auth.ts:31 is a constant local principal with no identity).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Vault validation and repair**                                         | ❌ missing | M      | important | platform-server | reimplement | Logseq validates the whole DB against a Malli schema, gates EDN export on it, and offers a --fix CLI variant. Our analogue would validate a notes index against its valibot contracts (packages/contracts is already the schema home) and repair by reindexing from the files, which is strictly safer than repairing in place because the markdown is the source of truth. Worth building early: it turns 'my vault is weird' into a reproducible report.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Schema migration on vault open**                                      | ❌ missing | M      | important | platform-server | port        | We have a real, correct migration engine — but it is for the single process-wide platform database (~/.platform/fs-metadata.sqlite), not per-vault. Logseq's engine is per-graph, ordered, forward-only, stamps the version inside the same transaction as the step, and — the part worth copying — refuses to migrate and shows a blocking 'your app is outdated' notice when the DB is AHEAD of the app, which happens when a peer on a newer build synced a migration to you. It also enqueues each migration as a pending sync op so migrations propagate. Only the last two matter once sync exists. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                                               |
| **Vault registry (stable identity across renames)**                     | ❌ missing | M      | important | platform-server | reimplement | Everything we key today is a root path (apps/web/src/features/workspace/state/cache.ts:66 workspaceSliceStorageKey), so moving or renaming a folder loses its tabs, its search buffer and any URL pointing at it. Logseq keeps a registry mapping each graph to a remote uuid and a local uuid, upserted on every switch. Add one table (vault_id, current_path, created_at, last_seen_at) plus an upsert on the existing switch chokepoint — that single change unblocks deep links, the switcher's dedupe, and later the remote merge.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Open a vault in a second desktop window**                             | ❌ missing | L      | important | platform-web    | build-fresh | The shell opens exactly one window (apps/desktop/src/bun/index.ts:123 openMainWindow, called from both branches at :75 and :85). The blocker is not window creation, it is that our web state assumes one active workspace root per process: activateWorkspaceRoot / claimWorkspaceOpenGeneration are module singletons, and localStorage keys are not window-scoped. Electrobun is not Electron, so Logseq's window.cljs is no reference — build fresh.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Partial-batch failure reporting**                                     | ❌ missing | M      | important | platform-server | port        | The server names the entries it already applied, the one that failed, and (when known) the missing block uuids; the client marks the applied prefix done and rolls back only the failing entry rather than discarding the whole batch. Without this a single bad entry costs the user the other 49. Pairs with our existing structured-error discipline — this is exactly a createError with code/why/fix carrying successIds and failedId as fields.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Undo history invalidated by an incoming merge**                       | ❌ missing | M      | important | editor-core     | reimplement | Logseq simply clears the whole undo/redo stack in a finally block after any rebase, because the recorded inverses no longer describe the database. Crude but correct, and it is the honest default. We cannot even do the crude thing today: the stack is host-closed (packages/editor/src/history.ts:24, snapshots plus selection sets, capped at 200) with no API to clear it, no way to set undoGroup, and no way to push a host operation. Minimum engine ask: a public clearHistory() on the buffer, plus a way to mark a set of applied edits as non-undoable so a remote apply does not become an undo step for the local user.                                                                                                                                                                                                                                                                                                                                                                               |
| **Incoming-transaction sanitization**                                   | ❌ missing | S      | important | contracts       | port        | 208 LOC, mostly self-contained, and it is the cheap defence that keeps a merge from leaving orphans: drop ops for attributes deleted by a migration (an append-only set that must be updated whenever a migration deletes a property, or old clients' transactions fail forever), drop ops targeting entities retracted in the same transaction, and — the important one — complete a subtree delete by appending retractions for every descendant not otherwise touched. Must be idempotent; running it twice must produce the same result.                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Convergent sibling-order repair**                                     | ❌ missing | S      | important | contracts       | port        | We already have production fractional indexing with the right failure posture — return null and let the caller rewrite the section rather than throw. Two deltas versus Logseq: ours is base-26 with two digits (675 slots) where theirs is base-62 unbounded, and we have no duplicate-order repair. The repair is 46 LOC and deterministic by construction: group duplicates by (parent, orderValue), sort the tied siblings by uuid string, generate n fresh keys strictly between the duplicate and the next-greater sibling value. Because it is a pure function of (value, next value, sorted uuids), every peer that sees the same duplicate produces byte-identical keys, so it converges instead of ping-ponging. One correction to make while porting: take the MINIMUM sibling value greater than the duplicate, not the first one the index happens to yield — the shipped code takes the first, which is a latent bug. [downgraded by verification]                                                     |
| **Parent-cycle detection and repair**                                   | ❌ missing | M      | important | contracts       | port        | Real hazard in any outliner sync: A moves under B while B moves under A. The algorithm is sound (iterative DFS with an explicit stack, a remote-informed victim choice preferring a safe target outside the cycle, a guard rejecting a safe target that is itself a descendant of the victim, bounded at 16 iterations with edge-key dedupe). Two warnings from the survey: it has NO production caller in Logseq — tests only — and the doc describing its protocol is stale, so do not assume they ship it. Port the algorithm on its merits, not on the assumption it is battle-tested. Port to an explicit stack loop, not recursion, so a deep tree cannot blow the stack.                                                                                                                                                                                                                                                                                                                                      |
| **Live presence of collaborators**                                      | ❌ missing | L      | important | platform-server | reimplement | Who is connected and which block each is editing, with avatars beside the edited block. The one implementation detail worth keeping is storing presence as a WebSocket attachment so it survives the relay hibernating rather than in a process map. Our stream hub (apps/server/src/orchestration/streams.ts:98) already does snapshot-or-replay fan-out with a synchronized marker, so the transport is free; the cursor-position half also needs the editor to expose remote selection decorations, which the CSS Custom Highlight path already supports for background colours.                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Sync status panel**                                                   | ❌ missing | M      | important | platform-web    | reimplement | Connection state, pending-local count, pending-server count, pending asset ops, files that could not be read, local and remote fingerprints, last error. Note the platform constraint from the digest: there is no surface registry — sidebar and bottom tabs are closed string unions rendered by if-chains (apps/web/src/features/workbench/utils/panels.ts:3), so adding this panel means editing the union, the icon list, the renderer branch and the FocusArea mapping by hand. Budget that friction for every new panel in this domain.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **On-demand and backfill attachment download**                          | ❌ missing | M      | important | platform-server | reimplement | Eager on desktop and CLI where storage is cheap, lazy elsewhere, plus an explicit 'download missing attachments' action to backfill. Straightforward once attachment sync exists; the design question is which platform 'owns' eager download in a multi-device setup, which Logseq answers with an explicit owner predicate rather than letting every client race.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Invite collaborators by email**                                       | ❌ missing | L      | important | platform-server | reimplement | Blocked on the absence of any identity system (apps/server/src/auth.ts:31). The detail worth keeping: for an encrypted vault, inviting must immediately wrap the vault key to the invitee's public key, and an email with no account gets a specific 'that user does not exist yet' warning rather than a generic failure.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Member list, remove member, leave vault**                             | ❌ missing | M      | important | platform-server | reimplement | Roles (manager/member), who invited whom, manager-only removal, and self-removal as 'leave'. Note the key-management consequence Logseq accepts: revoking a member deletes their wrapped key but does NOT rotate the vault key, so a removed member who kept a copy of the data can still read what they already had. Decide that explicitly rather than inheriting it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Delete a remote vault**                                               | ❌ missing | S      | important | platform-server | reimplement | Deletes server-side and drops/recreates the per-vault tables without touching the local copy — which is the right default and worth stating in the UI, because 'delete' next to a vault name reads as 'delete my notes'.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Self-hosted sync relay**                                              | ❌ missing | L      | important | platform-server | reimplement | Logseq's relay runs on Cloudflare with a Node adapter for self-hosting, and the client exposes commands to point at your own deployment. For us the natural shape is inverted and easier: our server is already a plain Bun/Elysia app (apps/server/src/app.ts:76), so the relay is another Elysia app rather than a port of a Durable Object. Register the endpoint as a settings key — and note the house scope rule: a URL that selects where data is sent is application/machine scope, never window, because a workspace file ships inside a cloned repository.                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Bootstrap gating while a vault is still uploading or importing**      | ❌ missing | S      | important | platform-server | reimplement | Small but prevents a whole class of half-synced-vault bugs: while the initial snapshot is in flight the relay answers 409 'not ready' rather than serving a partial graph, and the vault shows as not-ready in the list. Maps cleanly onto our structured-error convention — a code with why/fix rather than a bare 409.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Export vault as an archive**                                          | ❌ missing | M      | important | platform-server | reimplement | Merges Logseq's export-as-SQLite, export-as-zip and export-whole-graph-as-Markdown, because for a file-native vault a user sees one thing: 'give me my notes in a file'. jszip is already a server dependency (apps/server/package.json:47, used today only by the fonts service) so the mechanics are free. Copy two details: STORE rather than deflate for already-compressed assets, and a staged progress readout (collecting → zipping → finalizing) — our toSse helper (apps/server/src/sse.ts:15) makes the progress stream three lines. Downgraded from Logseq's 'core' because our vault is already a plain folder the user can copy.                                                                                                                                                                                                                                                                                                                                                                       |
| **Export a selection as Markdown, OPML or HTML**                        | ❌ missing | M      | important | ambiguous       | reimplement | For us copy-as-Markdown is nearly free since the source IS markdown; the substance is OPML and HTML, plus the option set Logseq exposes (indent as dashes/spaces/none, strip properties, strip page refs) and a preview dialog before copying. If it lands platform-side it is a utils module plus a dialog; if it lands in the engine it needs a stable public API and its own tests in the singapor repo.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Structural interchange export and import (cross-vault transfer)**     | ❌ missing | L      | important | platform-server | reimplement | Logseq's graph-agnostic EDN is their cross-graph transfer and backup format: id-free, entities described by title/uuid, relationships by name, edge pages included as SHALLOW copies so the export does not transitively drag in the whole graph, implied tags omitted, closed-value sets unioned with referenced-but-undeclared values, timestamps off by default so exports are diffable. That SHAPE is the valuable part and ports directly to JSON via packages/contracts valibot schemas; the 1455-line implementation is not worth transliterating. Also copy the gate: refuse a whole-vault export when validation fails rather than emitting a broken bundle.                                                                                                                                                                                                                                                                                                                                                |
| **Publish public pages as a static site**                               | ❌ missing | L      | important | platform-server | reimplement | The reusable design is the closure rule, not the code: take pages explicitly marked public, add their tags, their property entities and page-valued property targets and aliases — and STOP. One level deep, deliberately, because that is what keeps the bundle small and stops private content leaking in because it happened to be tagged. Two traps: aliases of private pages must also be private, and forgetting the tags-property entity breaks all tag rendering. Their single-giant-inlined-HTML approach is a poor fit for us; a static site generator over our markdown is more natural. Note the survey's warning that Logseq has two coexisting publishing systems — pick one.                                                                                                                                                                                                                                                                                                                          |
| **Publish a single page to a hosted service**                           | ❌ missing | XL     | important | platform-server | reimplement | Multi-month not because of the client code but because it means operating a service: upload page content plus referenced assets, optional page password, short URL written back onto the page, images uploaded at multiple resolutions for responsive serving, unpublish deletes server-side and removes the property. Blocked on accounts, same as everything else on the remote side. Realistically choose between this and the static-site route rather than shipping both.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Import an exported archive**                                          | ❌ missing | S      | important | platform-server | reimplement | Straight inverse of the archive export and cheap with jszip already present. Copy the small robustness details: find the shallowest entry that looks like the vault root rather than assuming a layout, and handle escaped path separators in asset entries. Path confinement is non-negotiable — every extracted path must pass through the existing assertInside guard (apps/server/src/fs/path.ts) or a crafted archive escapes the workspace.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **List, restore and delete backups**                                    | ❌ missing | M      | important | platform-server | port        | Restore, diff and delete all exist for checkpoints and are wired into the chat revert flow, but there is no vault-level backup browser showing name, time, size and source. Port the retention policy detail that is easy to get wrong: prune PER SOURCE, so automatic backups never prune manual ones and vice versa, and treat the metadata file as the ordering authority with mtime only as a display fallback. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Two-phase backup retention (dense recent, sparse old)**               | ❌ missing | S      | important | platform-server | port        | 146 LOC, selfContained: yes, and it is the difference between 12 useless backups from the last hour and 12 useful ones spanning a week. Sort newest-first by timestamp parsed from the filename, keep one per hour for the first six distinct hours, then one per day until the keep count is reached, delete the rest. The detail worth keeping: unparseable filenames get unique bucket keys so they are never silently deduplicated against each other, and the newest file is always kept. Pair it with the one-per-hour rate limit that a manual backup bypasses.                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Per-file version history**                                            | ❌ missing | M      | important | platform-server | reimplement | We are one route away and then some. apps/server/src/git/service.ts:345 already exposes repositoryRunner(path) returning a scoped run(args) with output caps, and 'log' is already in the read-only verb allowlist — but there is no /git/log and no /git/blame route (apps/server/src/git/routes.ts:22-111), so no history reaches the UI. compare-saved: documents only diff the buffer against the file on disk, not against history. For a notes app 'show me this note last Tuesday' is a daily expectation, and for a git-backed vault it is nearly free.                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Quick capture from the OS**                                           | ❌ missing | M      | important | platform-web    | reimplement | Push a URL, title or snippet into today's journal or a default page, with options to redirect and to prettify URLs. Depends on a journal concept we do not have. Two platform blockers: no protocol handler or global hotkey registration exists in apps/desktop, and the destination page is a settings key — which by our scope rule is application scope, not window, since a workspace file must not be able to redirect captures.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Vault garbage collection (orphaned store pages)**                     | ❌ missing | S      | niche     | platform-server | build-fresh | Logseq's GC walks the SQLite kvs table for addresses unreferenced by any other row's addresses array, excluding DataScript index roots. That is specific to a persistent-tree page store; if our notes mode stores markdown files plus a derived index, this is moot and the equivalent maintenance is 'drop index rows whose file no longer exists'. Do not port the algorithm — port the idea of a dev-only sweep command.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Per-browser-tab vault identity**                                      | ❌ missing | S      | niche     | platform-web    | reimplement | Logseq stores the open graph per browser tab in sessionStorage so a reload keeps it and two tabs can hold two graphs without a ?graph-id= on every route. Our startup resolution is single-valued (rootFolder in the versioned localStorage cache, apps/web/src/features/workspace/state/cache.ts:59), so two tabs fight. Genuinely niche for a desktop-shell-first product; becomes core if the web build is ever a real target.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Persistent-storage permission request (OPFS)**                        | ❌ missing | S      | niche     | platform-web    | build-fresh | Architecturally moot for us: the durable state is real files on a real filesystem behind the Bun server, not browser-evictable OPFS. The only evictable thing we have is the localStorage UI cache, which is already versioned and explicitly disposable (apps/web/src/features/workspace/state/cache.ts:42). Listed for completeness; do not build it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Oversized note bodies offloaded to attachments**                      | ❌ missing | S      | niche     | platform-server | reimplement | Logseq offloads any block title over 4096 UTF-8 bytes as a .txt asset, ships a reference, and rehydrates it after apply, purely to bound individual transaction payloads. For a markdown-file model this is largely moot — a note IS a file and files already have a size ceiling — so treat it as a reminder to bound per-message payload rather than as a feature to build.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Change encryption password**                                          | ❌ missing | S      | niche     | platform-server | port        | Cheap because it re-wraps only the user's private key — the vault AES key is untouched, so no data is re-encrypted and no peer needs to do anything.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Sync and vault CLI**                                                  | ❌ missing | M      | niche     | platform-server | build-fresh | We have no CLI at all today — the only entries are the server (apps/server/src/index.ts) and the desktop shell. Because our services are already injectable (createApp accepts injected database, lsp pool, pty factory), a CLI is a thin wrapper rather than a parallel implementation. Logseq's command set is a good checklist (status/start/stop/upload/download/remote-graphs/backup list\|create\|restore\|remove) but the code is no reference.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Pause and resume uploads (one-way sync debug switch)**                | ❌ missing | S      | niche     | contracts       | build-fresh | Stops local batches from uploading while still pulling and rebasing, so one-way scenarios can be reproduced. Ours would be visibility 'internal' or 'advanced'. Trivial code, real diagnostic value once a rebase pipeline exists. Register it in the same pass as the consumer or not at all.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Copy structural data for a block, page or ontology to the clipboard** | ❌ missing | S      | niche     | platform-web    | reimplement | Dev-facing affordance that piggybacks entirely on the interchange format. Nearly free once that exists, and disproportionately useful for filing bug reports.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Export a debug dump for bug reports**                                 | ❌ missing | S      | niche     | platform-server | build-fresh | Logseq dumps all datoms as Transit with a matching importer that rebuilds a graph from the dump. Our equivalent would be the index plus the file list, and honestly our JSONL structured logs (logs/YYYY-MM-DD.jsonl) already carry most of the diagnostic value. Low priority; the format is store-specific so their code is no reference.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Copy a block or page as an image**                                    | ❌ missing | M      | niche     | platform-web    | build-fresh | Optional transparent background, crop to a region, honour current zoom. Note the engine constraint that shapes the result: text row height is uniform (packages/editor/src/virtualization/virtualizedTextViewLayout.ts:229 — hasVariableRows is true only for block rows), so a captured note will not have real heading sizes or inline images until that changes. The screenshot will look exactly as flat as the editor does.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Backlinks on published pages**                                        | ❌ missing | M      | niche     | platform-server | reimplement | Uploading the page-reference edges (source page, source block content, target page) alongside the payload so the published site renders linked mentions. Trivial once a link index exists; impossible before, since we have no link table anywhere today (apps/server/src/db/schema.ts has no link or backlink table).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Exclude comments from published output**                              | ❌ missing | S      | niche     | platform-server | reimplement | A one-line publishable-block predicate in Logseq; presupposes a comments feature we do not have. Listed for faithfulness.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Hourly browser backup to a user-chosen folder**                       | ❌ missing | M      | niche     | platform-web    | build-fresh | Exists in Logseq because a web graph lives in evictable OPFS and would otherwise be unrecoverable. Our data is already on the real filesystem, so this is moot for the desktop shell and only relevant if a pure-web build ever ships. The one idea worth stealing regardless of tier is the content-equality skip: compare before writing and do nothing when nothing changed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Mobile app**                                                          | ❌ missing | XL     | niche     | platform-web    | build-fresh | Merges Logseq's mobile stale-worker recovery, mobile download-progress screen and the mobile half of quick capture, because a user sees one feature: 'the app on my phone'. All of their mobile-specific machinery (reload the app if an old DB worker still holds its lock, reload on SQLite errors) exists because their engine runs a database inside the mobile webview — an architecture we would not reproduce, since our server is a native process. Also note apps/web hard-codes a 1024px minimum width (apps/web/src/features/workspace/components/view.tsx:22), so nothing in the current UI is mobile-viable as-is.                                                                                                                                                                                                                                                                                                                                                                                      |
| **Local vault list in a switcher**                                      | 🟡 partial | S      | core      | platform-web    | reimplement | We remember the last 8 project roots plus a 40-entry recents list, but a 'vault' is just any directory — no name normalization, no case-insensitive dedupe, no per-vault metadata (created-at, last-seen-at, schema version), and no 'this one is a notes vault' marker. Logseq's list is deduped by graph name and carries createdAt/lastSeenAt; that metadata is what makes the switcher, deep links and the registry work. Add a vault descriptor before building anything else in this domain. Evidence: apps/web/src/features/workspace/state/cache.ts:62 (workspaceIndex storage key), :344 (writeWorkspaceIndexCache, newest-first, WORKSPACE_SLICE_LIMIT 8); apps/web/src/lib/recent-folders-query.ts:15; apps/server/src/fs/service.ts:382.                                                                                                                                                                                                                                                                 |
| **Vault open/restore sequence**                                         | 🟡 partial | M      | core      | platform-server | reimplement | We have a working open sequence for a code workspace: resolve the root, rebuild the path index, subscribe the watcher, restore tabs and scroll. A notes vault needs steps we do not have — per-vault schema check and migrate, seed on first open, stamp identity, start the notes index — and the ordering rule Logseq learned the hard way: schema-defining data before anything that depends on it, migrations before first render and before sync starts, and a snapshot download must skip both seeding and migration because the snapshot already contains them. Evidence: apps/server/src/fs/service.ts:450 (openWorkspaceRoot); apps/server/src/fs/watch.ts:51 (FileChangeHub); apps/web/src/features/workspace/hooks/use-open-root.ts:35; apps/web/src/features/editor/providers/state-provider.tsx:23.                                                                                                                                                                                                     |
| **Conflict record and warning UI**                                      | 🟡 partial | M      | core      | platform-web    | reimplement | We detect and surface file-level conflicts today and can open both sides as a diff tab, which is a real, working conflict story at file granularity. Logseq's is finer and its rule is the one to keep: last-writer-wins on the value, but never discard the loser silently — persist it with its timestamp in a conflicts table unique on (block, attr, value) so repeated rebases do not duplicate it, and show a per-block warning the user can copy from and dismiss, auto-hiding conflicts whose value already equals the current text. Evidence: apps/web/src/features/editor/state/conflict-state.tsx:24 (FilesystemConflict store); features/editor/utils/conflict-diff-document.ts:1; apps/server/src/fs/write.ts:47 (FILE_CHANGED); apps/server/src/fs/app-save-marker.ts:1.                                                                                                                                                                                                                               |
| **Deep link to a vault, page or block**                                 | 🟡 partial | M      | important | platform-web    | reimplement | In-app addressing is real and well-designed: every piece of state is explicitly classified as address/preference/ephemeral with a stated reason, and tabs encode through one token codec. What is missing is (a) resolving a vault by a stable id rather than by root path, and (b) an OS-level scheme — apps/desktop has no CFBundleURLTypes and no protocol handler, and openMainWindow (apps/desktop/src/bun/index.ts:123) takes no launch URL. Logseq's ADR 0017 rule is the one to copy: an explicit-but-unresolvable graph id must be reported, never silently opened as the current graph. Evidence: apps/web/src/features/address/utils/document-token.ts:20; utils/classification.ts:150; hooks/use-projection.ts:140.                                                                                                                                                                                                                                                                                      |
| **Multi-writer safety across tabs and windows**                         | 🟡 partial | M      | important | platform-server | port        | This is where our architecture is strictly better than Logseq's and the gap row should be read as 'do not port'. Logseq needs Web Locks master election over BroadcastChannel (shared_service.cljs, 393 LOC) precisely because each browser tab holds its own OPFS SQLite connection. Our tabs are thin clients of one loopback Bun process that owns the filesystem and the database, so election is unnecessary. What is still missing is client-side: two tabs editing the same document have no shared buffer (the engine explicitly does not subscribe to buffer changes — packages/editor/src/editor/autoCloseStore.ts:20), so tab B does not repaint when tab A edits. That is a real bug surface with a single-writer server underneath it. Evidence: apps/server/src/db/client.ts:27 (single process-wide handle, WAL, busy_timeout 5000); apps/server/src/fs/write.ts:24, :47.                                                                                                                             |
| **Automatic reconnect with backoff and a staleness watchdog**           | 🟡 partial | S      | important | platform-web    | port        | We have a tested exponential ladder and a heartbeat teardown, which is most of the value. Missing versus Logseq: random jitter (they add up to 250ms, which matters when many clients reconnect after a relay restart), and the identity re-check before a timer fires — their reconnect timer and stale-socket watchdog both verify the client is still the same (repo, graphId, socket object) before acting, so a stale timer cannot kill a newer connection. Add jitter and the identity guard when this moves out of chat. Evidence: apps/web/src/features/chat/utils/stream-reconnect.ts:6 (STREAM_RECONNECT_DELAYS_MS ladder 250ms→16s), :9; features/chat/transport/orchestration-rpc-client.ts:765.                                                                                                                                                                                                                                                                                                         |
| **Switch vault (workspace root)**                                       | ✅ have    | S      | core      | platform-web    | reimplement | This is our best-developed piece of the whole domain and it is genuinely equivalent to Logseq's graph-switch: a single chokepoint that supersedes in-flight opens by generation, resets the tree, restores per-project tabs from the slice cache, and rebuilds the workspace index. What Logseq does additionally on switch and we do not: cancel the backup timer, reload per-graph config/shortcuts, restart sync, and log total switch duration. Any notes state must be torn down here or it will bleed across vaults. Evidence: apps/web/src/features/workspace/hooks/use-open-root.ts:35 (useOpenWorkspaceRoot); apps/server/src/fs/routes.ts:65 (POST /fs/workspace-root); apps/server/src/fs/service.ts:450.                                                                                                                                                                                                                                                                                                 |
| **Markdown files on disk as the durable form**                          | ✅ have    | S      | core      | platform-server | port        | Read this row as 'do not build Markdown Mirror'. Logseq's mirror exists because SQLite is their source of truth and they wanted to look like a vault from the outside — it is explicitly one-way, never read back, and excluded from import and watching. We are the other way round: files ARE the truth, so the projection is free and, unlike theirs, editing a file actually works. The consequence is that a Logseq-parity notes mode should derive its index FROM the files rather than treating files as output. The one piece of their mirror worth taking is the filename algorithm, tracked as its own row. Evidence: apps/server/src/fs/write.ts:12 (temp file then atomic rename), :47 (baseVersion/mtime optimistic concurrency); apps/server/src/fs/watch.ts:51; apps/server/src/fs/service.ts:229.                                                                                                                                                                                                    |

## Obsidian delta — the Obsidian feature surface Logseq's codebase will not teach us

56 features — 9 have, 3 partial, 44 missing.

| Feature                                                    | Ours       | Effort | Priority  | Layer           | Source      | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ---------------------------------------------------------- | ---------- | ------ | --------- | --------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **YAML frontmatter properties**                            | ❌ missing | M      | core      | platform-server | build-fresh | Logseq has ZERO frontmatter code — grep confirms the only 'yaml' hits are a CodeMirror highlight mode registration and issue-template URLs. Round-trip safety is the real cost: preserve key order and comments on rewrite, or every property edit churns the file and re-triggers the watcher. Only the `---` fence highlighting is editor-markdown work, and tree-sitter's markdown grammar already injects it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Properties panel with typed editors**                    | ❌ missing | L      | core      | platform-web    | reimplement | Logseq's frontend/components/property/{value,config,dialog,default_value}.cljs is a usable design reference over DB properties. Every control edit must be expressed as a text edit to the frontmatter block so undo and external-change reconciliation keep working. Two hosting options, both platform: a React header outside the editor host (safe), or an editor block surface anchored to row 0 (registerBlockProvider, which has NO production consumers anywhere).                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Split panes**                                            | ❌ missing | L      | core      | platform-web    | build-fresh | Do NOT build a second window manager: prior tiling work exists in project history (features/workbench InteractionSurface) but is not on this branch. Retrofitting splits after panes ship is the brutal path — the pane tree has to become the addressing unit for tabs, layout persistence, and the URL projection in one pass. Logseq has no split-pane component at all. Workbench layout is a fixed two-level ResizablePanelGroup today and the split API is hard-stubbed to false (apps/web/src/features/editor/state/commands.ts:138-155).                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Backlinks pane with filters**                            | ❌ missing | L      | core      | platform-server | port        | deps/db/src/logseq/db/common/reference.cljs is the best port target here and its filter semantics are hard-won: EXCLUDES test only effectiveRefs (self+ancestors) so a parent does not vanish because a child mentioned an excluded page, while INCLUDES may be satisfied by descendants EXCEPT through an excluded ref (allowedSubtreeRefs prunes at the exclude). Memoize both or the traversal goes exponential. Run the whole thing on ids and materialize entities only at the end. Note the keystone prerequisite: a per-file note metadata index (headings, links, embeds, tags, frontmatter, section line ranges, plus a derived link graph patched incrementally from watch events) which is not itself an inventory row.                                                                                                                                                                                                                  |
| **Search operator syntax**                                 | ❌ missing | L      | core      | platform-server | build-fresh | Logseq has no search syntax at all — scoping is clickable group chips (cmdk/core.cljs:56) and the only operator handling is FTS5 AND/OR/NOT leaking through get-match-input (search.cljs:355). Design: structural terms (file:/path:/tag:) filter FIRST to shrink the candidate set, then FTS the free-text remainder, then intersect. Binding house constraint from docs/workspace-content-engine-evaluation-plan.md: ripgrep stays the correctness oracle, false negatives are unacceptable, and any semantic mode must be explicitly separate. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                                      |
| **Daily notes**                                            | ❌ missing | S      | core      | platform-web    | reimplement | Port the timezone discipline verbatim from frontend/template.cljs: clone new Date(), setHours(0,0,0,0), then setDate(getDate()+offset), and build the YYYYMMDD key by zero-padded concatenation — NEVER via toISOString, which yields yesterday's note for users east of UTC every evening. That single bug is the most common in date-template code. Obsidian's daily notes are discrete files, unlike Logseq's infinite journal feed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Templates**                                              | ❌ missing | S      | core      | platform-web    | port        | deps/outliner/src/logseq/outliner/template.cljs is small and portable: scan /<%([^%].*?)%>/g (the [^%] guard leaves <%% as an escape), resolve today/yesterday/tomorrow/time/current-page case-insensitively, fall back to natural-language date parsing, and pass unknown placeholders through UNCHANGED — never blank them, a visible typo beats silent content deletion. Format time with hourCycle 'h23'.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Wikilink autocomplete on [[**                            | ❌ missing | M      | core      | platform-web    | reimplement | Typing # inside must offer the target's headings and ^ its blocks — neither exists in Logseq (no headings model, block refs are ((uuid))), so only the trigger/menu mechanics are borrowable. Do NOT copy Logseq's eager page creation on accept; unresolved links stay unresolved until the user creates the note. Completion UI is explicitly NOT in editor core (the controller lives in @singapor/lsp-plugin), so either generalize that controller or hand-roll one in the platform.                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Aliases**                                                | ❌ missing | M      | core      | platform-server | reimplement | Logseq's integration is the model: worker/search.cljs:558 concatenates alias titles into the indexed title so an alias is searchable, :563 reports which alias matched, and reference.cljs:275 folds the alias set into the linked-reference id set at step 1. Do all three or the feature is half-broken in a way users experience as data loss.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Heading links and block ids**                            | ❌ missing | L      | core      | platform-server | build-fresh | Logseq gives no reference: its ((uuid)) refs are DB-backed and never break, which is precisely the hard part we must solve in text. Budget for: heading links breaking on rename (Obsidian accepts this), a generated short base-36 token for ^ids, and the fact that an anchor write to a closed file is an edit the user did not make — it must go through the server's atomic-write chokepoint with its own conflict/undo story.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Embeds and transclusion**                                | ❌ missing | L      | core      | ambiguous       | reimplement | markdown_mirror.cljs:321 embed-target shows the recursive-inline shape (render the target's subtree at the current indent and splice), and its depth cap exists because ref cycles are real — bound the recursion or a note embedding itself hangs the render. `![[image.png\|300]]` and `![[file.pdf#page=3]]` are separate sub-features that only need inline widgets, not nested editors.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Tag hierarchy and inline tags**                          | ❌ missing | M      | core      | platform-server | reimplement | Both halves have references: markdown_mirror.cljs:37 simple-hashtag-re plus :248 tag-token for the '#tag' vs '#[[Multi Word]]' decision (bare form only when the title has no whitespace and none of #[](<>).,;:'"`), and deps/common/src/logseq/common/util/namespace.cljs for hierarchy. Dedupe case-insensitively and skip tags already present in the text, as their decorator does.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Community plugin API with custom views**                 | ❌ missing | XL     | core      | platform-web    | reimplement | HIGHEST AGPL EXPOSURE IN THE SURVEY: libs/src/*.ts is 5,585 LOC of already-TypeScript plugin host, so copying is a licensing decision about the whole application, not a module. Read for shape, write our own transport/sandbox/teardown. Minimum table stakes derived from what load-bearing Obsidian plugins need: index read + change subscription, custom view types bound to extensions, code-fence renderers, command+hotkey registration, settings schema, CSS/theme injection, file CRUD through our atomic-write path. Note Logseq CANNOT host an Excalidraw-class plugin — its iframe plugins may only inject into host slots — so decide sandbox-vs-capability BEFORE shipping, not after.                                                                                                                                                                                                                                              |
| **Dataview-class querying over the index**                 | ❌ missing | XL     | core      | platform-server | port        | Two port targets: query_dsl.cljs for compiler STRUCTURE (parse → template expand → per-form dispatch → boolean composition → central date coercion) and deps/db/src/logseq/db/common/view.cljs (724 lines) for the sort/filter core. From view.cljs steal the closed-value ordering idea outright — enum properties sort by declared position so High/Medium/Low sorts by severity, not alphabetically — plus: precompile the filter predicate ONCE per filter set, match ref-valued filters by target id not by rendered title, and treat nil/[]/'' identically in the empty predicate or every filter subtly lies.                                                                                                                                                                                                                                                                                                                                |
| **Link-preserving rename and move**                        | ❌ missing | L      | core      | platform-server | build-fresh | Logseq skips exactly the hard part: its refs are uuid-based so a rename rewrites nothing. Ours must be a multi-file transaction with a preview and a single undo. Two existing hazards: apps/server/src/fs/service.ts:315-326 emits a real 'renamed' event only for renames through OUR API — external renames arrive as delete+create, so an out-of-band move silently orphans backlinks; and writeIfChanged-style skip-if-identical is required or the rewrite storm re-triggers the watcher and the reindexer.                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Source mode**                                            | ❌ missing | S      | important | platform-web    | build-fresh | The rendering half is already free (do not install the markdown plugin); the missing half is a per-pane toggle, its persistence per file, and a global default — a mode flag on EditorTabRecord plus a settings key, not an engine change. The plugin array is memoized per languageId in apps/web/src/features/editor/components/editor.tsx:80-93, so a mode toggle must invalidate it. Logseq has no mode concept at all. [downgraded by verification]                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Reading view**                                           | ❌ missing | M      | important | platform-web    | reimplement | We already have a second markdown pipeline in apps/web/src/features/chat/components/assistant-markdown.tsx (react-markdown + custom remark plugins); reading view should either reuse it or replace it, not become a third. Logseq's deps/publish/src/logseq/publish/render.cljs is the nearest reference and it renders block trees, not documents. Cannot be an editor package: it must expand embeds, resolve wiki-links against the vault, collect footnotes, and render callouts.                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Typed property schema across the vault**                 | ❌ missing | M      | important | platform-server | port        | deps/db/src/logseq/db/frontend/property/type.cljs is 253 lines and is the single best port target in this domain: types :default :number :date :datetime :checkbox :url :node :asset, with cardinality (one/many) and closed values as orthogonal axes rather than separate types. Its ref-typed (:node) properties are strictly better than Obsidian's link-as-text and cost nothing once a link index exists. The descriptor union itself belongs in packages/contracts — but it is note user data, NOT a settings-registry entry.                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Linked panes**                                           | ❌ missing | M      | important | platform-web    | build-fresh | Blocked by a real engine gap for the two-editors-on-one-file case: nothing in core subscribes to buffer changes (packages/editor/src/editor/autoCloseStore.ts:20 says so outright), so a second view sharing an EditorTextBuffer will not repaint when the first edits — the host must observe buffer.subscribe and re-drive it. For informational panes (outline/backlinks/graph) that gap does not apply.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Hover link preview**                                     | ❌ missing | M      | important | platform-web    | reimplement | Logseq HAS this and its timing is well tuned — port the interaction, not the code: frontend/components/block.cljs:882 popup-preview-impl, :963 page-preview-trigger. LSP hover/peek already proves the overlay mechanics in our stack (registerViewContribution gives container, scrollElement, getRangeClientRect and textOffsetFromPoint); the new part is resolving the link and rendering note content in the popup.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Unlinked mentions**                                      | ❌ missing | M      | important | platform-server | reimplement | Logseq's version (reference.cljs:309) is a full scan over every block title with no index — fine at a few thousand notes, not at 50k. Back ours with FTS instead: phrase-search the title, then subtract notes that already reference it. There is no full-text index today (every content query re-spawns rg), so this row inherits that build. The one-click 'Link' button is a text rewrite through the existing atomic-write path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Outgoing links pane**                                    | ❌ missing | S      | important | platform-web    | build-fresh | Unresolved links must be a first-class displayable state. Logseq has no concept of one because [[foo]] creates page foo eagerly (cmdk/core.cljs:99) — do not port that eagerness; it silently fills a vault with empty notes. Pure read of one file's already-computed metadata record, split into resolved and unresolved.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Outline (headings) pane**                                | ❌ missing | M      | important | platform-web    | build-fresh | We already extract document symbols for the palette (apps/web/src/features/command-palette/document-symbols.ts, symbol-groups.tsx) — the pane is a second consumer of the same data, and markdown headings can come from tree-sitter captures rather than LSP. Logseq is no reference: it has no heading hierarchy, only a per-block :logseq.property/heading integer. @singapor/scope-lines sticky scroll already renders ancestor headers and is the closest existing behaviour.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Tags pane**                                              | ❌ missing | M      | important | platform-web    | reimplement | Namespace nesting is the only piece with a reference: deps/common/src/logseq/common/util/namespace.cljs, used for namespace-aware search in worker/search.cljs:391. Counts must merge inline #tag and frontmatter `tags` into one number or the pane immediately looks wrong.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Local graph with depth control**                         | ❌ missing | L      | important | platform-web    | port        | worker/graph_view.cljs:981 build-page-graph is the shape: root + refs + mentions + tags, plus a SECOND-DEGREE pass adding edges among the neighbour set — that pass is what makes it look like a neighbourhood instead of a star, and it is O(k^2) with a query per neighbour, so precompute an adjacency set or cap k. Emit semantic node kinds and let the renderer resolve theme tokens; Logseq wrongly bakes dark-theme colours into graph construction. Sized L because this row pays for introducing a graph renderer at all.                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Global graph with filters, groups and display settings** | ❌ missing | M      | important | platform-web    | port        | Port the bounding, not just the build: graph_view.cljs:660 switches algorithms above 10,000 pages and hard-caps at 20,000 links, and orphan visibility must be computed AFTER link collection from the full linkedPageIds set, not from the capped list. Their 20k truncation is silent — ours should say 'showing 20,000 of N'. Bulk-prefetch node attributes in one pass per attribute; per-node lookups are the whole frame budget at 10k nodes. Sliders and toggles are settings-registry entries.                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Canvas**                                                 | ❌ missing | XL     | important | platform-web    | build-fresh | NO reference available: Logseq's tldraw whiteboards were deleted at this commit (refactor: separate og and db version #12276) — only two welcome PNGs and graph-parser test fixtures survive. Build against the published .canvas spec. A card that embeds a live note reuses the embed machinery, so do embeds first or the cards are static text.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Embedded search queries in a note**                      | ❌ missing | L      | important | platform-web    | reimplement | Logseq's src/main/frontend/worker/query_dsl.cljs (812 lines) is the structural reference — parse, expand template vars, compile per-form via a dispatch table, compose booleans with explicit scoping, coerce dates centrally — but its datalog target is wrong for us; ours compiles to an index predicate plus an FTS sub-query. Their raw-datalog escape hatch is an injection surface; ours must not be able to read outside the vault index. Both hosting routes are platform-driven: registerBlockProvider (zero production consumers, re-resolves the FULL block list on every applyEdit) or reading-view-only rendering.                                                                                                                                                                                                                                                                                                                    |
| **Bookmarks**                                              | ❌ missing | M      | important | platform-web    | reimplement | Use packages/contracts/src/order-key.ts:58 orderKeyBetween for drag-reorder — fractional base-26 keys, one write per drag, no neighbour rewrites, convergent; it returns null rather than throwing on corrupt input so the caller falls back to a section rewrite. Logseq has flat page favorites only (no groups, no bookmarked searches or headings).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Callouts**                                               | ❌ missing | M      | important | editor-markdown | build-fresh | Logseq has ZERO callout code (grep across .cljs and .css returns nothing). Two engine frictions: the multi-line span ban means the box is styled per row rather than as one decoration, and uniform row height caps how much the title row can differ. Nested callouts need the fold source to compose, but fold ranges cannot be contributed independently — createSyntaxSession is first-provider-wins (packages/editor/src/plugins.ts:946-953), so an extra fold source must wrap the tree-sitter provider.                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Footnotes**                                              | ❌ missing | M      | important | editor-markdown | reimplement | Logseq has footnote AST handling in EXPORT only (handler/export/{html.cljs:113,opml.cljs:156,text_impl.cljs:259}) — no authoring or rendering UI, so treat it as parser reference at best. The tree-sitter markdown query does not name footnote nodes; captures are lossy (all markers lump into punctuation.special), so expect regex re-discrimination the way packages/markdown/src/replacements.ts:36-39 already does.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Plugin marketplace with install/update/disable**         | ❌ missing | L      | important | platform-web    | reimplement | frontend/components/plugins.cljs + handler/{plugin,plugin_config}.cljs is a reasonable UX reference. Downloading and executing third-party code is the one place our 'no downloads from untrusted sources' posture needs an explicit, user-facing trust decision — restricted mode should be the default, as Obsidian does. Cannot precede the plugin host itself.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Templater-class scripted templates**                     | ❌ missing | L      | important | platform-web    | build-fresh | Logseq is not a reference — its template.cljs is five hardcoded regex substitutions with no user variables, no scripting, no prompts, no cursor placement. State the gap plainly rather than extending the regex. If plugins are Node-privileged (Obsidian's choice) this is nearly free; if sandboxed, it needs a capability broker. The file-create hook must wrap our fs write path so content can be rewritten BEFORE write.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Style Settings and CSS snippets**                        | ❌ missing | M      | important | platform-web    | reimplement | House constraint: our theme is token-based (packages/ui/src/styles/globals.css) and CLAUDE.md forbids raw palette colours in components — user snippets are the sanctioned escape hatch, so they must be injected as a separate late layer rather than allowed to fight tokens. Logseq's provideStyle/provideTheme (libs/src/LSPlugin.ts:1148,1161) shows the per-plugin tagging that makes clean teardown possible; get teardown wrong and disabling a snippet leaves its CSS forever.                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Attachment handling**                                    | ❌ missing | M      | important | platform-web    | reimplement | The existing ~/.platform/attachments store is chat-scoped and content is written from data URLs — a vault attachment must land in the VAULT (root / same folder as note / per-note subfolder, a contracts setting), not in the app's private directory, or the folder-of-files promise breaks. Logseq's handler/assets.cljs has no per-note folder policy. 'Find unreferenced attachments' falls out of the link index for free. The engine's EDITOR_PASTE_HANDLER already exposes the real DataTransfer and has no consumers anywhere.                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Stacked tabs**                                           | ❌ missing | M      | niche     | platform-web    | build-fresh | Currently impossible to prototype because only the active tab body is mounted (code-panel.tsx:34-47); stacked tabs need N live editor views side by side, which also means N EditorViewSessions over shared buffers and the cross-view repaint gap above.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Saved workspaces (layouts)**                             | ❌ missing | S      | niche     | platform-web    | build-fresh | localStorage is the only client persistence (CACHE_VERSION 17, 8-project eviction cap, ~5MB shared budget) — named layouts are small enough to live there, but they must be a separate key so a quota failure elsewhere cannot evict them, the same split already used for search buffers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Pop-out windows**                                        | ❌ missing | L      | niche     | platform-web    | build-fresh | Two editors over one EditorTextBuffer do not live-update each other today (autoCloseStore.ts:20). Cross-window would additionally need the buffer to be the sync unit across window boundaries — either a shared worker or server round-trip. Genuinely three-sided (workbench + desktop shell + engine), which is why it is expensive for a niche feature. Defer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Random note**                                            | ❌ missing | S      | niche     | platform-web    | build-fresh | Logseq has no random command anywhere in modules/shortcut/config.cljs. Scope it to the current folder/search filter or it resurfaces templates and attachments. One command over the note list already in the workspace index; hours of work.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Unique note creator (Zettelkasten prefixer)**            | ❌ missing | S      | niche     | platform-web    | build-fresh | No Logseq equivalent — its markdown_mirror:90 disambiguates duplicate stems with ' (n)' suffixes, the opposite strategy. Reuse the same filename sanitizer as the Vault row so a timestamped name cannot collide with a reserved device name. A create command with a timestamp-format and folder setting calling the existing POST /fs/create-file.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Comments %%…%%**                                         | ❌ missing | S      | niche     | editor-markdown | build-fresh | No Logseq equivalent. Inline %%…%% is trivially a single-line replacement; the block form crosses lines and hits the same single-line filter as callouts, so hide it per row. Stripping it from export/publish is a one-line filter in whichever renderer produces that output.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Sync**                                                   | ❌ missing | XL     | niche     | platform-server | build-fresh | Logseq's deps/db-sync is the WRONG SHAPE — it syncs DataScript transactions over an op log (Cloudflare D1 worker), not files; only checksums, atomic writes, and a per-file version log transfer. Note docs/delta-db-implementation-plan.md is paper-only (no packages/crdt, aggregateKind is still ['project','thread']), so nothing exists to build on. The single v1 requirement is that every write already goes through one atomic chokepoint — it does.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Publish**                                                | ❌ missing | L      | niche     | platform-server | reimplement | deps/publish/src/logseq/publish/{render.cljs (1611 lines),routes.cljs,index.cljs,meta_store.cljs} is the same feature with block-shaped content; the reusable parts are the meta store, per-note opt-in, and asset copying. The renderer itself is our reading-view renderer run headless, so building reading view first makes this much cheaper.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Mobile parity**                                          | ❌ missing | XL     | niche     | platform-web    | build-fresh | src/main/mobile/ is a structural reference only (separate bottom-tabs shell over the shared worker/DB). Concrete blocker today: apps/web/src/features/workspace/components/view.tsx:22 hardcodes min-w-[1024px], so every mode is desktop-only by construction. Our editor packages are already host-agnostic, so half the layering discipline is free. The architectural requirement lands on the index: keep it platform-neutral (no React, no server client imports) so a mobile shell is a UI project rather than a fork.                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Live Preview editing mode**                              | 🟡 partial | L      | core      | editor-markdown | build-fresh | What exists is marker hiding and text substitution; what is missing is everything visual — InlineReplacementSpec.render has ZERO producers anywhere, so an ![alt](url) renders as its alt text (packages/markdown/src/replacements.ts:146-170). Real Live Preview needs: variable text-row height, multi-line decoration spans, and widget interactions expressed as text edits so undo stays source-level. Logseq contributes nothing — its editor swaps one focused block for a <textarea> and never had to solve any of this. Evidence: packages/markdown/src/index.ts:20-45 (production plugin); replacements.ts:95-125; packages/editor/src/inlineMap.ts:108 revealInlineMap; apps/web/src/features/editor/utils/plugins.ts:103-106.                                                                                                                                                                                                           |
| **Dockable left/right sidebars**                           | 🟡 partial | L      | core      | platform-web    | build-fresh | The blocker is not layout, it is that there is NO surface registry: WorkbenchSidebarTab and WorkbenchBottomTab are closed string unions rendered by hand-written if-chains, so every notes pane (backlinks, outline, tags, graph) is a union member plus an icon button plus a branch plus a FocusArea mapping. Add the registry before adding the fourth pane, not after. Evidence: apps/web/src/features/workbench/components/sidebar-panel.tsx:49-78,121-146 (5-tab sidebar); bottom-panel.tsx:34-53; workbench/utils/layout.ts:1-35 (resizable, persisted geometry).                                                                                                                                                                                                                                                                                                                                                                            |
| **Quick switcher**                                         | 🟡 partial | S      | core      | platform-web    | port        | frontend/common/search_fuzzy.cljs:49 score is the single most directly portable algorithm in the reference and is ~120 LOC: clean (strip [ ](<>)/\_ and spaces) + NFKC + accent-strip, then a subsequence walk where consecutive matches earn an increasing bonus and a miss costs 0.1, returning 0 (hard reject) if the query is not a subsequence. The additive prefix(+1010)/substring(+1000) bonuses make ranking effectively tiered — that tiering is why it beats Levenshtein. Cache the normalized candidate on the index record; do not recompute per keystroke. Note the vestigial idx accumulator is never added to the score — keep it or drop it, do not half-use it. Missing: alias matching, a create-note affordance, folder-path disambiguation, modifier-open-in-split. Evidence: apps/web/src/features/command-palette/quick-open-groups.tsx, use-command-palette-files.ts, file-palette-row.tsx; state/recent-commands-store.ts. |
| **Vault = plain folder of files**                          | ✅ have    | S      | core      | platform-server | reimplement | The one genuinely portable piece is markdown_mirror's normalizeStem: NFC, replace <>:"\|?*\/ and controls with _, strip trailing dots/spaces (Windows drops them silently and two notes collide), truncate to 160, reject CON/PRN/AUX/NUL/COM1-9/LPT1-9. Truncate on UTF-8 byte length, not JS chars. Everything else in Logseq's file layer is the wrong arrow direction (files are its output). Residual work is note-specific policy (which extensions count as notes, filename derivation, per-vault ignore rules — the ignore list is hardcoded today at apps/server/src/fs/path.ts:5-18). Evidence: apps/server/src/fs/routes.ts:26-86; fs/service.ts:450-465; fs/watch.ts:51-105; fs/path.ts:29-54.                                                                                                                                                                                                                                          |
| **Document-first editing (no forced outliner)**            | ✅ have    | S      | core      | editor-core     | build-fresh | Logseq is the inverse model and is an active anti-reference here: src/main/logseq/common/export/file.cljs:269 prefixes every block with indentation + '-'. Do not port its serializer wholesale; the parts worth keeping are fence-length computation (max(3, longest backtick run + 1)), heading-prefix idempotence, and continuation-line indentation. The risk runs the other way — if a block/outliner mode is ever wanted, the engine offers no node identity beyond Anchor. Evidence: packages/editor/src/pieceTable/tree.ts:1 and public/document.ts:1 (one flat UTF-16 string, no block/node tree); apps/web/src/features/editor/components/editor.tsx:80-130.                                                                                                                                                                                                                                                                              |
| **Tabs**                                                   | ✅ have    | S      | core      | platform-web    | build-fresh | Logseq has nothing here — its entire multi-view model is a flat :sidebar/blocks list (state.cljs:226). A notes surface joins by minting a prefixed document id; there is NO registry, so the ~9 switch sites listed under the document-id scheme must all be taught the new kind or the tab renders but silently fails to persist or address. Only the ACTIVE tab's body is mounted (code-panel.tsx:34-47), so any new note surface with expensive state must own a store outside the component tree. Evidence: apps/web/src/features/workspace/utils/tab-model.ts:36-50; workbench/components/sortable-editor-tab-button.tsx; keymap/workspace-commands.ts:394; workspace/state/cache.ts:42-76.                                                                                                                                                                                                                                                    |
| **Search results with context and per-file grouping**      | ✅ have    | S      | core      | platform-web    | port        | Settled decisions a notes surface must respect: exactly one search buffer per workspace root keyed search-buffer:<root>, one result row per MATCH not per line, open-buffer matches before disk matches. If snippets need improving, ensure-highlighted-snippet (search.cljs:265) is worth porting — longest-term-first non-overlapping matching, descending-order sentinel splicing, sentence-boundary window starts, and at most two highlighted windows on purpose. Residual work is note-specific: sort by modified date, copy-results-as-links, snippet quality. Evidence: apps/web/src/features/search/components/file-group.tsx, result-file-header.tsx, highlight.tsx, utils/sort.ts; apps/server/src/fs/search.ts:117-135.                                                                                                                                                                                                                 |
| **Command palette**                                        | ✅ have    | S      | core      | platform-web    | reimplement | Logseq's cmdk/core.cljs is still worth reading for ORDERING policy: group order is a pure function of the input string (leading '/' means scope picker, mid-string '/' means path), one global monotonic itemIndex across all groups so arrow-keys cross group boundaries with no group-aware code, and the create affordance suppressed on exact sanitized-title match so 'Create page Foo' never sits above the actual page Foo. Notes commands join by adding object literals, not by new infrastructure. Evidence: apps/web/src/keymap/command-registry.ts:13-41; keymap/table.ts:11-17 (44 workspace commands); features/command-palette/command-palette-groups-factory.tsx; recent-commands-store.ts.                                                                                                                                                                                                                                         |
| **File explorer with folder operations**                   | ✅ have    | S      | core      | platform-web    | build-fresh | Logseq has no user folders at all, so nothing to port. Caveat for notes reuse: @workspace/tree identity IS the path string, sibling order comes from a comparator (no per-node order field), and rows are uniform height — fine for a file tree, wrong for an outliner. Git-status decoration and per-row decoration slots are reusable for tag or unresolved-link badges. Evidence: packages/tree/src/utils/model/publicTypes.ts:202-247 (dragAndDrop, renaming.onRename, sort comparator); apps/web/src/features/workspace/components/tree-pane.tsx:155,179; apps/server/src/fs/routes.ts:26-86; workspace/utils/tree-pane-state.ts:17,41.                                                                                                                                                                                                                                                                                                        |
| **External change reconciliation**                         | ✅ have    | S      | core      | platform-server | build-fresh | Two residual gaps to close for notes. (1) External renames arrive as delete+create (both watcher backends can only produce created/changed/deleted), so link maintenance must tolerate an unlinked pair. (2) /fs/events has no resume cursor and no batching, unlike the orchestration WS — a vault-wide git checkout will flood it. Keep every write on the single atomic chokepoint including skip-if-identical (markdown_mirror.cljs:553); that one check is what kills watcher feedback loops and, later, sync churn. Evidence: apps/server/src/fs/watch.ts:51-105,159-201; fs/write.ts:12-62; fs/app-save-marker.ts:1-139; apps/web/src/lib/file-snapshot-query-cache.ts:7-45; features/editor/state/file-sync-service.ts:18.                                                                                                                                                                                                                  |
| **Find and replace in file**                               | ✅ have    | S      | important | editor-core     | build-fresh | Logseq is behind us here — frontend/components/find_in_page.cljs is 115 lines of Electron find-in-page with no replace. One limit worth knowing: FIND_MATCHES_LIMIT is 19,999 painted matches (packages/find/src/search.ts:10), and findMatches/FindTextSource are NOT exported from the package, so reusing the engine for multi-note search means widening the exports map. Evidence: packages/find/src/plugin.ts:220-221 (onReplaceOne/onReplaceAll), :241-242, :170-175; apps/web/src/features/editor/utils/plugins.ts:83-108; features/search/utils/replace-runner.ts (vault-wide replace, beyond Obsidian core).                                                                                                                                                                                                                                                                                                                              |
| **Rebindable hotkeys with conflict detection**             | ✅ have    | S      | important | platform-web    | build-fresh | Logseq's modules/shortcut/config.cljs (956-line flat {command-id {:binding :fn}} map, multi-binding per command) validates the flat-registry shape we already use. Per CLAUDE.md, command enablement predicates stay in keymap/ next to the registry — never in lib/ or components/. A notes surface only needs to add a FocusArea so its bindings get their own scope. Evidence: packages/contracts/src/settings/keys.ts:517-530; apps/web/src/features/settings/components/keybinding-row.tsx:44 (conflictCount); features/settings/utils/keybinding-rows.ts:27; keymap/default-bindings.ts + active-bindings.ts.                                                                                                                                                                                                                                                                                                                                 |

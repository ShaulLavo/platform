> Historical research from before Plan 068. The implemented domain and current contracts are
> documented in [Session domain](../session-domain.md).

# Completeness critique — what a TUI plan author still cannot answer

Read: all ten listed reports plus `codex-tui.md` (present in the folder, not on the list). Every gap below was first checked by grepping the reports, then confirmed or refuted against source under `/work/projects/platform` on 2026-09-05. Where a report contradiction could be settled by reading code, it is settled here and is not a gap.

## 1. Contradictions settled (use these, do not re-derive)

| Topic                         | Reports disagree                                                                                                                     | Truth                                                                                                                                             | Evidence                                                                                                             |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| Orchestration WS path         | web-features `WS /orchestration/ws`; server-surface `/orchestration/rpc`                                                             | `/orchestration/rpc`                                                                                                                              | `apps/server/src/orchestration/ws-rpc.ts:91`; `apps/web/src/features/chat/transport/orchestration-rpc-client.ts:713` |
| Attachment URL                | web-features `GET /chat-attachments/:fileName`; server-surface/hard-problems `/attachments/:fileName`                                | `/attachments`                                                                                                                                    | `packages/contracts/src/chat-model.ts:94` (`CHAT_ATTACHMENT_URL_PREFIX`)                                             |
| Settings write                | hard-problems §0.1 and plans-constraints A8 `POST /settings`; server-surface `POST /settings/write`                                  | `/settings/write`                                                                                                                                 | `apps/server/src/settings/routes.ts:37-39`                                                                           |
| Quick open                    | web-features "`GET /fs/quick-open` style query"                                                                                      | no such route; `fetchQuickOpenFiles` streams `GET /fs/search/events` with `includeNames: true, matchMode: 'fuzzy', entryType: 'file', limit: 200` | `apps/web/src/lib/file-server.ts:204-235`                                                                            |
| `workspace.jumpToSession1..9` | web-features "(unbound, hidden)"; keymap-commands `Mod+Alt+1…9`                                                                      | bound `Mod+Alt+1…9` on every platform                                                                                                             | ran `defaultPlatformKeyBindings('linux')` (`scratchpad/understand/tty-collisions.ts`)                                |
| `thread-status.ts` reuse      | client-core §7.2 moves it into `client-core/chat/derive`; plans-constraints A7 says 068 deletes it (status becomes server-projected) | sequencing: extract only after 068, per plans-constraints (b)                                                                                     | `docs/session-domain.md`; `apps/web/src/features/chat/utils/thread-status.ts:12-33`                                  |
| Navigation model              | web-features §9 "clone the toggle today"; plans-constraints A2 "the TUI may not add a mode toggle"                                   | unresolved — a product decision, see gap 2                                                                                                        | `docs/product-vision.md:26-35,119-124` vs `apps/web/src/lib/ui-mode.ts`                                              |

## 2. Coverage audit

- **`apps/web/src/features`**: all 15 directories (`address chat chat-mode command-palette editor environments file-picker git logs menus search settings terminal workbench workspace`) are covered by web-features §3; top-level `hooks/`, `providers/`, `state/` (application runtime) are covered by plans-constraints D2 and web-features §1.1. No feature is missing.
- **Server routes**: server-surface §3 covers every mount in `apps/server/src/app.ts:173-232` (health, orchestration HTTP+SSE+WS, terminal, lsp, providers, attachments, fonts, wallpaper, settings, git, fs, observability). No route is missing.
- **Plans**: plans-constraints read 077/068/078/069/056/057 only. Not read, per `plans/README.md`: 066, 067, 071, 073, **074** (Bun-native PTY: deletes the JSON-lines bridge, allows non-UTF-8 bytes — `plans/074:34,84,114,127-128`), 075 (WebGPU renderer fallback for the web pane), **076** (the dev server is `bun --watch` and leaks children on reload), **067** (`GET /terminal/theme` whitelisted Ghostty appearance — `plans/067:65-78,103-113`). 074, 067 and 076 bear directly on the TUI's terminal pane, theme, and dev loop.
- **Reference subsystems**: opencode, crush, t1code and codex are read deeply. The one load-bearing item read from types only is OpenTUI `EmbeddedTerminalRenderable` — the scratchpad `otui/` contains smoke tests for core/React/Solid/markdown/Ink/vt but none references `EmbeddedTerminal` (gap 3).
- **TUI-specific concerns**: Windows/ConPTY — covered from the reference side (codex-tui §8, opencode-tui §14), open question in framework-options §13; our server only has a win32 shell list (`apps/server/src/terminal/service.ts:537-538`) and CI runs `ubuntu-latest` only (`.github/workflows/ci.yml:24,59`). SSH/tmux/kitty — covered. Auth token entry — moot (origin allowlist only, no token mode; all reports agree). Single-binary distribution — half covered (gap 5). Hot reload — only t1code's `dev-tui.ts` is described (gap 7). Accessibility/screen readers/`NO_COLOR` — zero mentions in any report (gap 8). Testing — pieces exist, composition does not (gap 7).

## 3. Gaps (8)

### G1 `post-068-wire-contract` — Which wire vocabulary is the plan written against?

Every report documents today's protocol v4: `thread.*` commands, `ThreadId` = `thread-<uuid>`, `OrchestrationThreadShell`, client-derived `waiting|working|failed|idle` (server-surface §4.7-4.8, web-features §3.2, client-core §7). plans-constraints A3/A7 and (b) say the TUI must be scheduled strictly after 068, which deletes that vocabulary with no alias, bumps the protocol version, renames the modules client-core proposes extracting, and moves status server-side. No report enumerates 068's locked shapes: `session.*`/`worktree.*` command and event names, the shell snapshot with `worktrees[]`, `ScopedSessionRef`/`scopedSessionKey`, the `needs-input|working|settled` attention projection, receipt aggregate rules, the `@<environmentId>` address segment, and `SessionWorktreeTarget` (069). A plan whose transport/projection/rail/composer phases cite today's contracts is a second consumer 068 must rewrite, which 068 forbids (`:596-598`).
Where: the completed domain is documented in `docs/session-domain.md`; current contracts live in `packages/contracts/src/{orchestration-commands,orchestration-events,orchestration-snapshots,orchestration-ws,chat-ids}.ts`. Physical worktree lifecycle remains in `plans/069-worktree-lifecycle.md`.

### G2 `root-navigation-model` — Toggle parity or vision stack?

web-features §9 recommends cloning the current two-mode toggle (`uiMode` chat|workbench, `workspace.toggleUiMode`/`showChatMode`/`showWorkbenchMode`, the chat-mode tool pane with 7 tools, a global current project). plans-constraints A2 says the vision forbids exactly that for any new front end (root = agent view; IDE pushed from a project/worktree row; "no toggle and no current-project setting"; non-goal at `:119-124`). The task brief carries both ("clone every web feature" and "agent-view-first root with IDE pushed"). Which commands survive, what the address `mode` token means for TUI links, and which persisted keys (`uiMode`, `chatModeSelection`, `workbenchPanels`, `chatModePanels`) have TUI equivalents all depend on this call; there is no TUI lane in `PLAN.md` to settle it.
Where: `docs/product-vision.md:12-13,26-35,85-89,119-124`; `apps/web/src/lib/ui-mode.ts:1-3`; `apps/web/src/keymap/workspace-commands.ts:1098-1141`; `apps/web/src/features/chat-mode/components/tool-pane.tsx:24-100`; `apps/web/src/features/address/utils/grammar.ts:19,36-59`; `apps/web/src/features/workspace/state/cache.ts:57-108`; `PLAN.md:125-155` (environments lane; no TUI entry).

### G3 `embedded-terminal-unverified` — Does OpenTUI's `EmbeddedTerminal` work as our terminal pane?

hard-problems §1.3-1.4 makes `EmbeddedTerminalRenderable` the recommendation ("essentially zero new code beyond the socket adapter") and framework-options §11.4 endorses it, but both read only the `.d.ts`; hard-problems states "Neither of them uses `EmbeddedTerminal`; I could not verify which version introduced it", and nothing in `scratchpad/otui/` exercises it. Unverified: `write()` with chunk-unaligned UTF-8 strings from `{type:'output'}` (and raw bytes after plan 074); whether `onTerminalResize` fires from Yoga relayout and maps to `{type:'resize'}`; how `handleKeyPress`/`encodeKey` negotiate the kitty protocol for the inner program while the host renderer also runs `useKittyKeyboard`; mouse forwarding; `screen()` as a test oracle; repaint after the server's 256 KiB mid-alt-screen replay; and whether it renders under `createTestRenderer` in `bun --bun vitest`.
Where: `scratchpad/otui/node_modules/@opentui/core/renderables/EmbeddedTerminal.d.ts` (`write`, `onData`, `encodeKey`, `onTerminalResize`, `screen`), `zig.d.ts:736-761`; upstream `anomalyco/opentui` `packages/core/src/examples/` (embedded terminal demo); `packages/contracts/src/terminal.ts:8-17`; `apps/server/src/terminal/service.ts:60-65,260-351,491-560`; `plans/074-bun-native-pty.md:84-128`; a smoke script in `scratchpad/otui/` against the running dev server with `Origin` per `apps/desktop/src/bun/index.ts:202,303`.

### G4 `terminal-hostile-bindings` — Which defaults cannot survive a terminal, and what is `Mod` on macOS?

Running `defaultPlatformKeyBindings('linux')` (`scratchpad/understand/tty-collisions.ts`): of 100 command bindings, **10 collide with C0/tty controls** — `Mod+S` saveFile (XOFF), `Mod+J` togglePanel (LF), `Mod+[` navigateBack and outdentLines (ESC), `Mod+H` findReplace (BS), `Mod+D` addNextOccurrence (EOF), `Mod+Z` undo (SIGTSTP), `Mod+K Mod+S` settings, `Mod+Enter`, `Mod+Backspace`; **51 need kitty/`modifyOtherKeys`** (Ctrl+Shift+letter, Ctrl+punctuation, Ctrl+Alt+…, Shift+Enter/Tab); **12 are Alt-prefixed** (ESC-prefix ambiguity against the 5 s chord timeout). keymap-commands §8.3 names the classes but no report enumerates them, chooses the mac table (Cmd is invisible; `Mod`→Ctrl collides with readline), or resolves how a TUI default table coexists with the single `keybindings.overrides` record (application scope, one chord per command, `platforms` has no `tui` value) without a second binding store.
Where: `apps/web/src/keymap/default-bindings.ts:23-30,84-108`; `keymap/define-command.ts:50-58` (`platforms`); `keymap/utils/chord.ts:14-34`; `packages/contracts/src/settings/keys.ts:594-608`; `packages/contracts/src/settings.ts:108-123`; `scratchpad/understand/{dump-bindings,tty-collisions}.ts`; OpenTUI `lib/parse.keypress.d.ts:5-24`, `parse.keypress-kitty.d.ts`, `renderer.d.ts:135-146`; codex `tui/src/key_hint.rs:119-135`, `keyboard_modes.rs:162-215`; opencode `src/config/index.tsx:101-136`.

### G5 `server-launch-and-distribution` — Attach, supervise, or embed — and does the server compile?

The references show three models (t1code attach-only env vars / supervised child; opencode in-process worker; codex embedded-or-daemon). Our facts: the desktop spawns `bun --env-file=.env apps/server/src/index.ts` from source, releases ports, computes `SERVER_ALLOWED_ORIGINS` with `allowedOriginsForWebPort`, and probes `/health` with a synthetic `Origin` (`apps/desktop/src/bun/index.ts:41-108,202,245-320`); prod builds `apps/server/dist/index.js` with `--external @parcel/watcher` and runs it under `bun` (`apps/server/package.json:19-22`, `scripts/prod.ts:58-63`); the PTY bridge spawns a real Node via `resolveNodeBinary()` and `Bun.resolveSync('@lydell/node-pty', import.meta.dirname)` (`apps/server/src/terminal/service.ts:607,782-794`). framework-options §9.5 compiled the TUI alone (99.5 MB). Nobody checked whether `apps/server` survives `bun build --compile` (native `@parcel/watcher`, node-pty + Node until 074, `bun:sqlite` migrations, `~/.platform`), which origin string the TUI presents and who adds it to the allowlist, whether the TUI logs via `/_log/ingest` or `@workspace/observability` (client-core Q5), or the remote path (078's SSH launcher is desktop-only and unbuilt; a TUI-owned `ssh -L` is "a new decision for root PLAN.md").
Where: files above; `apps/server/src/index.ts:18-47,116-137`; `apps/server/src/home.ts:4-18`; `scripts/runtime-network.ts:58-67`; `plans/074-bun-native-pty.md`; `plans/078-federated-environments.md:96-97,170-173`; `docs/environments-and-remote-plan.md:152-196`; `references/t1code/apps/tui/src/serverSupervisor.ts:127-361`, `scripts/bundle-server-dist.mjs:88-114`; `references/opencode/packages/opencode/src/cli/cmd/tui.ts:200-290`, `script/build.ts:52-197`; `references/codex/codex-cli/bin/codex.js:118-260`.

### G6 `theme-and-colour-degradation` — What is the TUI's colour source of truth and degradation ladder?

The web palette is 511 CSS custom properties in oklch with `color-mix` translucency (`packages/ui/src/styles/globals.css:173-227,385-416,532`), selected by `workbench.colorTheme` dark/light/system and `workbench.palette` (`packages/contracts/src/settings/keys.ts:32-60`); the syntax theme is a bundled VS Code theme in localStorage (`apps/web/src/features/editor/state/color-theme-store.ts`); plan 067 adds `GET /terminal/theme` sanitized from the user's Ghostty config. No report maps these to truecolor/256/16 terminals, says how "system" resolves in a terminal (OSC 10/11, mode 2031 via `renderer.themeMode`) versus `prefers-color-scheme`, decides between deriving the UI palette from the host's 16 colours (opencode `generateSystem`) and shipping one, or handles `NO_COLOR`/`FORCE_COLOR`/`TERM=dumb` (OpenTUI's JS checks only `OPENTUI_*` env; `capabilities.rgb/ansi256` is exposed, policy is ours). CLAUDE.md's token rule (status = `destructive/info/success/warning/diff-*`) needs a terminal equivalent.
Where: files above; `apps/web/src/features/settings/utils/apply-appearance.ts:46-80`; `plans/067-integrate-ghostty-config-appearance.md:65-78,103-113`; OpenTUI `types.d.ts:63-86`, `lib/terminal-palette.d.ts`, `syntax-style.d.ts:23-33`, `renderer.d.ts:460-461`; opencode `src/theme/index.ts:95-111,241-299,360-469`, `src/context/theme.tsx:222-233`; codex `tui/src/terminal_palette.rs:7-70`, `diff_render.rs:1-33`, `styles.md`; crush `internal/ui/styles/quickstyle.go:20-79`.

### G7 `tui-test-and-dev-harness` — How are TUI tests composed under repo rules, and what is `dev:tui`?

Rules: real in-process Elysia via `createApp`/`server/testing`, `bun --bun vitest`, no socket to our own server, mock only PTY/LSP children. framework-options §9.4 proved OpenTUI headless under `bun --bun vitest`; codex-tui §9 and opencode-tui §15 give VT100-backend and fake-transport patterns. Unanswered: driving the three WS surfaces without sockets — only `/orchestration/rpc` has an in-process factory (`apps/web/test/factories/in-process-orchestration-socket.ts`); `/terminal` and `/lsp` upgrades have none and the terminal service's PTY factory must be injectable (`service.ts:597-690`); whether `apps/web/test/{fixtures,env,factories}` become a shared `test/` package so `apps/tui` does not import across apps; how `createTestRenderer`/`captureCharFrame` coexist with React `act()` warnings; and the dev loop — the server runs `bun --watch` (`apps/server/package.json:19`; plan 076 zombie issue) while a `bun --watch` TUI (t1code `scripts/dev-tui.ts:248-260`) restarts the process and drops alt-screen state; nothing defines `bun run dev:tui` or how `turbo dev` (`scripts/dev.ts`) hosts it.
Where: `apps/web/test/fixtures.ts:31-39`, `test/env/dom.ts:21-26`, `test/server.ts:16-17`, `test/factories/{in-process-orchestration-socket,orchestration-socket,chat-transport}.ts`; `apps/server/src/testing.ts:1-19`; `apps/web/vitest.config.ts`, `vitest.browser.config.ts`; `apps/server/src/terminal/service.ts:597-690`; `apps/server/src/lsp/routes.ts:105-135`; `scripts/dev.ts`; `turbo.json`; `plans/076-watch-reload-child-reaping.md`; `references/opencode/packages/tui/test/fixture/tui-sdk.ts:18-109`, `test/app-lifecycle.test.tsx:10-61`; `references/codex/codex-rs/tui/src/test_backend.rs:13-25`; `references/t1code/apps/tui/src/index.tsx:50-76`.

### G8 `accessibility-and-plain-mode` — What is the accessible/plain mode, and is it in scope?

No report mentions screen readers, reduced motion, or monochrome. The web app has `role="status"`/`aria-live` on loaders and the chord indicator (`packages/ui/src/components/spinner.tsx`, `apps/web/src/keymap/components/pending-chord-indicator.tsx`) and CLAUDE.md requires loaders to slow rather than freeze under reduced motion; OpenTUI's type surface has no accessibility API. Alt-screen TUIs are opaque to VoiceOver/NVDA/Orca, which is one reason codex writes history into native scrollback with a copy-friendly `/raw` mode and opencode has `animations_enabled` with a `⋯` spinner fallback. The plan needs: screen mode (alt-screen vs `split-footer` + scrollback — framework-options §13 open question) treated as an a11y input, an animations-off/`NO_COLOR` mode, OSC 9/terminal-title notification policy, and which of these are registered settings keys (`window` vs `application`) versus env — all subject to the "no inert key" rule.
Where: `CLAUDE.md` Loading And Empty States; `packages/ui/src/components/{spinner,orbit-loader,ring-loader,loading-state}.tsx`; OpenTUI `renderer.d.ts:40-60` (`screenMode`), `scrollback.d.ts`; codex `tui/src/history_ui.rs`, `history_cell/mod.rs:171-290` (`raw_lines`), `insert_history.rs:1-5,105-215`; opencode `src/component/spinner.tsx:17-24`, `src/attention.ts`; `packages/contracts/src/settings/keys.ts` (registry rule).

## 4. Lesser items (not gaps, note for the plan)

- Windows: native Windows is undecided; server support is a shell-list only (`terminal/service.ts:537-538`), CI is Linux-only, OpenTUI tests Windows x64 under Bun. Decide WSL-only vs native in the plan's Scope.
- `WatchClientMessage` is dead contract (server-surface §11); do not implement it.
- Terminal sessions are exclusive-attach (`service.ts:310-316`); "one id, two doors" with the web needs a server change (hard-problems §1.4) — already identified, needs a phase.
- `features/chat/state/server-connection-store.ts` and `server-restart-invalidation.ts` are dead (client-core §1); delete in 077 close-out, not in the TUI plan.

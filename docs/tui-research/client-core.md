> Historical research from before Plan 068. The implemented domain and current contracts are
> documented in [Session domain](../session-domain.md).

# client-core: how much of the web client is React-free, and where the shared package boundary should sit

Repo: `/work/projects/platform`. All paths below are repo-relative unless absolute. Line numbers are from the working tree at HEAD `e9cfba70` plus the uncommitted plan-077 work (the `features/chat/environment/` folder is deleted in the working tree and `transport/chat-transport.ts`, `transport/create-chat-transport.ts`, `state/active-transports.ts`, `providers/transport-*.tsx` are the untracked replacements).

## 1. Summary

- `apps/web/src/lib/` is 7,995 non-test lines across 51 modules. By line count roughly **58% is pure** (runs in Bun with no shim), **17% needs a shim** (a Vite `import.meta.env` read, a `localStorage` call, a `toast`, or a `window.addEventListener` that is already guarded), and **25% is DOM/React-bound** and belongs to the web app only (focus service, file-open-intent, platform bridge, fonts, clipboard, React error reporting, file icons).
- The chat feature's non-component layers are larger and cleaner than `lib/`: `transport/` (997 lines) is portable except for four `WebSocket`/`MessageEvent` global references that already have an injection seam (`createSocket`, `orchestration-rpc-client.ts:53`); `state/` (4,541 lines) is portable except for six `localStorage` sites and three guarded `beforeunload` hooks; `utils/` (10,570 lines) is roughly 80% pure domain logic (projection writers, timeline derivation, command builders, pending approvals, model selection) with a well-separated DOM tail (image compression, Lexical editor actions, clipboard serialization, DOM fragment links).
- `packages/contracts` (36 modules, one `.` export, deps `valibot` + `minimatch`) and `packages/observability/env` are already runtime-neutral and are the correct foundation. Nothing in contracts touches `window`/`document`/`localStorage`; the only hit is the setting id string `'window.transparency'` (`packages/contracts/src/settings/keys.ts:367`).
- **TanStack Query:** neither reference TUI uses it. t1code's `packages/client-core` has no TanStack dependency; its TUI holds the snapshot in React `useState` fed by `transport.request(getSnapshot)` (`references/t1code/apps/tui/src/ui.tsx:4872-4910`), while its web app wraps the same transport in `queryOptions` (`references/t1code/apps/web/src/lib/serverReactQuery.ts`). opencode's TUI uses a Solid store (`references/opencode/packages/tui/src/context/sync.tsx:70`). For us, `@tanstack/query-core` 5.101.0 is already installed transitively and `QueryClient`/`queryOptions`/`QueryObserver` run without React, so the chat `queryOptions` modules can move as-is if the package depends on `@tanstack/query-core` instead of `@tanstack/react-query`. The chat read model itself does not use TanStack at all: it is a Zustand store fed by WebSocket subscriptions, which is exactly the shape a TUI wants.
- **Environments layer:** plan 077 is mostly landed in the working tree. The origin is a runtime value (`activeServerOrigin()`/`setActiveServerOrigin()`, `apps/web/src/lib/client.ts:20-26`) but is _seeded_ from `import.meta.env.VITE_SERVER_URL` at module scope (`client.ts:11`), and the environments store captures it at module scope too (`lib/environments/state/store.ts:31`). A TUI needs both seeds to become constructor arguments. The bigger blocker is server auth: `apps/server/src/auth.ts:75-76` rejects any request or WebSocket upgrade with **no `Origin` header** as `UNAUTHORIZED`, and Bun/Node `fetch`/`WebSocket` send none by default. A TUI must either send a synthetic allow-listed `Origin` (treaty `headers`, and `createSocket` for the RPC socket) or the server needs a non-browser principal. The comment at `auth.ts:97-104` says the token mode was deliberately deleted and real sessions are strategy milestone M4.
- Drift found: `apps/web/src/features/chat/state/server-connection-store.ts` (89 lines) and `apps/web/src/features/chat/state/server-restart-invalidation.ts` (41 lines) have **zero non-test consumers**. Plan 077 (`plans/077-environment-runtime-origin.md:173-181`) says they are deleted and folded into `lib/environments/state/store.ts`; the fold happened, the delete did not. Their tests still run against dead code.

## 2. What was read

- Every non-test module under `apps/web/src/lib/` (51 files), with a grep pass for `import`, `window.`, `document.`, `localStorage`, `navigator.`, `WebSocket`, `HTMLElement`, `import.meta.env`, `from 'react'`, `sonner`.
- `apps/web/src/features/chat/{transport,state,hooks,utils,providers}` and `features/chat-mode/{state,hooks,providers,utils}` — full listing, import scans, and full reads of the transport layer, the store entry points, the shell supervisor hook, command dispatch/sync, and the projection cache.
- `packages/contracts/package.json` + `src/index.ts` (450 lines of exports), `packages/observability/{package.json,src/*}`.
- `apps/server/src/client-contract.ts`, `apps/server/src/auth.ts`, `apps/server/src/orchestration/ws-rpc.ts` (auth path only), `apps/server/package.json` exports.
- `apps/web/src/main.tsx`, `apps/web/src/state/application-runtime.ts`, `apps/web/src/components/active-environment-application.tsx`, `apps/web/test/fixtures.ts`, `apps/web/test/env/dom.ts`.
- `docs/environments-and-remote-plan.md` (headings, transport section), `plans/077-environment-runtime-origin.md:1-245`, `plans/078-federated-environments.md` (headings), `docs/session-domain.md` (status).
- `references/t1code/packages/client-core/{package.json,src/index.ts,src/wsTransport.ts,src/nativeApi.ts,src/types.ts,src/scoped.ts,src/knownEnvironment.ts,src/appSettings.ts,src/ids.ts}`, every `@t3tools/client-core` import in `references/t1code/apps/tui/src` and `apps/web/src`, `apps/tui/src/ui.tsx:100-175,4855-4910`, `apps/tui/src/serverSupervisor.ts:1-175`, `apps/tui/src/log.ts`, `apps/tui/package.json`.
- `references/opencode/packages/sdk/js/{package.json,src/index.ts,src/v2/index.ts,src/v2/client.ts,src/v2/server.ts,src/error-interceptor.ts}`, `packages/tui/src/context/{sdk.tsx,sync.tsx,event.ts,local.tsx,data.tsx}`, `packages/tui/src/app.tsx:285-340`, `packages/opencode/src/cli/cmd/tui.ts:200-290`, `packages/tui/package.json`.
- `node_modules` checks: `zustand/esm/index.mjs` (re-exports `zustand/react`, so `import { create } from 'zustand'` pulls React; `zustand/vanilla` does not), `@tanstack/react-pacer/dist/debouncer/index.js` (imports React hooks, re-exports `@tanstack/pacer/debouncer`), `@tanstack/query-core` present at 5.101.0, `@elysia/eden` treaty (uses global `fetch` and `new WebSocket(...)`), `evlog/dist/http.mjs:25` (`sendBeacon` only behind `isBrowser()`).

## 3. `apps/web/src/lib/` — module by module

Legend: **pure** = imports nothing React/DOM and reads no browser global (or reads one that Bun also provides: `globalThis.crypto`, `performance.now`, `AbortSignal`, `DOMException`, `URL`); **needs-shim** = one or two call sites reach a Vite/browser facility that must become an injected port; **DOM-bound** = the module's purpose is the DOM/React and it stays in the web app.

| Module (lines)                                               | Class                   | Evidence                                                                                                                          | Note                                                                                                                                                                                                                                                                                                      |
| ------------------------------------------------------------ | ----------------------- | --------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `lib/client.ts` (43)                                         | needs-shim              | `import.meta.env.VITE_SERVER_URL` at module scope L11; `import type { App } from 'server/client-contract'` L3                     | Registry `environmentClientFor` L28-35 and `createEnvironmentClient` L14-18 are the reusable part. `getClient()` L37 is the ambient seam with 67 non-test call sites in 15 modules.                                                                                                                       |
| `lib/instance-id.ts` (23)                                    | pure                    | `globalThis.crypto?.randomUUID` L20 with fallback                                                                                 | Header `x-client-instance` L8; TUI should send it too.                                                                                                                                                                                                                                                    |
| `lib/client-logging.ts` (289)                                | needs-shim              | `import.meta.env` at L78, L82, L84, L172-173, L178, L181; `runtime: 'browser'` literal L166; `createHttpLogDrain` L66             | The redaction/sanitize core (L32-50, L189-231) is pure. `initializeClientLogging` needs `{ mode, dev, logLevel, observabilityEnabled, runtime }` injected; the TUI would likely use `@workspace/observability` `initializeObservabilityRuntime` (fs drain into `logs/`) instead of the HTTP ingest drain. |
| `lib/client-error-context.ts` (31)                           | pure                    | WeakMap annotate/read                                                                                                             |                                                                                                                                                                                                                                                                                                           |
| `lib/client-error-reporting.ts` (92)                         | pure                    | depends only on `client-logging` and contracts `errorStringField`                                                                 |                                                                                                                                                                                                                                                                                                           |
| `lib/client-error-taxonomy.ts` (231)                         | needs-shim              | `import { toast } from 'sonner'` L1, used in `reportError` L110-112; `DOMException` L171 (exists in Bun)                          | Split: `toClientError`/`clientErrorMessage` (L63-92) are pure; `reportError` (L94-113) = log + a `notify` port.                                                                                                                                                                                           |
| `lib/structured-errors.ts` (186)                             | pure                    | evlog `createError`/`defineErrorCatalog`                                                                                          | Catalog carries browser-only entries (`CLIPBOARD_UNAVAILABLE` L16, `SCREEN_STREAM_LOAD_FAILED` L88); harmless. `createRpcError` L147 parses Eden error envelopes.                                                                                                                                         |
| `lib/eden-events.ts` (69)                                    | pure                    |                                                                                                                                   | `unwrapEdenResponse` + `normalizeEdenDates` (Eden revives ISO strings to `Date`; a Bun treaty client does the same, so this stays needed).                                                                                                                                                                |
| `lib/server-sockets.ts` (137)                                | needs-shim (types only) | `WebSocketEventMap`, `EventListener`, `AddEventListenerOptions` DOM types L9-17; runtime is the Eden `subscribe()` socket         | Compiles under `@types/bun` too (Bun ships `WebSocket` types). Terminal (L31) and LSP (L44) sockets are ready for a TUI terminal pane.                                                                                                                                                                    |
| `lib/workspace-search-client.ts` (316)                       | pure                    | SSE via `parseEdenSseStream`                                                                                                      |                                                                                                                                                                                                                                                                                                           |
| `lib/file-server.ts` (652)                                   | pure                    | imports `@/features/workspace/utils/coalesced-log` (pure timers) L14                                                              | Every function takes `client: Client = getClient()`; portable once the default is dropped or supplied.                                                                                                                                                                                                    |
| `lib/file-system-types.ts` (69)                              | pure                    | re-exports contracts                                                                                                              |                                                                                                                                                                                                                                                                                                           |
| `lib/query-keys.ts` (63)                                     | pure                    |                                                                                                                                   |                                                                                                                                                                                                                                                                                                           |
| `lib/query-state.ts` (6)                                     | pure                    | `type Query` from `@tanstack/react-query`                                                                                         | swap to `@tanstack/query-core`.                                                                                                                                                                                                                                                                           |
| `lib/recent-folders-query.ts` (30)                           | pure                    | `queryOptions` from react-query                                                                                                   | swap import to query-core.                                                                                                                                                                                                                                                                                |
| `lib/file-snapshot-query-cache.ts` (156)                     | pure (IDE-mode)         | `globalThis.performance?.mark` L36-39 behind a trace flag                                                                         | Editor-only policy; not TUI.                                                                                                                                                                                                                                                                              |
| `lib/editor-visible-snapshot-cache.ts` (415)                 | needs-shim (IDE-mode)   | localStorage via `workspace-cache-storage`                                                                                        | Editor-only.                                                                                                                                                                                                                                                                                              |
| `lib/workspace-cache-storage.ts` (116)                       | needs-shim              | `localStorage` L40, L87, L98; `typeof localStorage` guard L105                                                                    | Becomes a `KeyValueStorage` port.                                                                                                                                                                                                                                                                         |
| `lib/lifecycle-flush.ts` (19)                                | DOM-bound               | `window.addEventListener('pagehide')` L4, `document.visibilityState` L8; guarded L2                                               | Port: `onFlush(cb)`; TUI maps to `SIGTERM`/exit.                                                                                                                                                                                                                                                          |
| `lib/load-state.ts` (7)                                      | pure                    |                                                                                                                                   |                                                                                                                                                                                                                                                                                                           |
| `lib/objects.ts` (23)                                        | pure                    |                                                                                                                                   |                                                                                                                                                                                                                                                                                                           |
| `lib/path-formatters.ts` (43)                                | pure                    |                                                                                                                                   | Comment L1-12 warns about the six non-mergeable `basename` variants.                                                                                                                                                                                                                                      |
| `lib/error-message.ts` (6)                                   | pure                    |                                                                                                                                   |                                                                                                                                                                                                                                                                                                           |
| `lib/stable-path-hash.ts` (18)                               | pure                    |                                                                                                                                   | **Load-bearing**: `ProjectId` = FNV-1a of the absolute root path (L1-8). A TUI must hash the identical string or it orphans every thread.                                                                                                                                                                 |
| `lib/intent-prefetch-options.ts` (1)                         | pure                    |                                                                                                                                   | UI constant only.                                                                                                                                                                                                                                                                                         |
| `lib/ui-mode.ts` (19)                                        | pure                    |                                                                                                                                   |                                                                                                                                                                                                                                                                                                           |
| `lib/wide-event-scope.ts` (139)                              | pure                    | `runtime: 'browser'` literal L51                                                                                                  | Parameterise runtime.                                                                                                                                                                                                                                                                                     |
| `lib/tree-model.ts` (388)                                    | pure                    |                                                                                                                                   |                                                                                                                                                                                                                                                                                                           |
| `lib/diagnostic.ts` (41)                                     | pure (IDE-mode)         | `@singapor/lsp-plugin/paths`                                                                                                      |                                                                                                                                                                                                                                                                                                           |
| `lib/file-icons.ts` (677) + `vscode-icon-symbols.ts` (347)   | pure but web-shaped     | imports `@workspace/tree` (a React package) for `getBuiltInFileIconColor` L1                                                      | Icon names/colours are for the web tree; a TUI would want nerd-font glyphs instead. Leave.                                                                                                                                                                                                                |
| `lib/file-icon-style.ts` (13)                                | React                   | `CSSProperties` L1                                                                                                                |                                                                                                                                                                                                                                                                                                           |
| `lib/clipboard.ts` (27)                                      | DOM-bound               | `navigator.clipboard` L10, L16; `toast` L1                                                                                        | Port: `copyText`.                                                                                                                                                                                                                                                                                         |
| `lib/default-nerd-font.ts` (153)                             | DOM-bound               | `globalThis.FontFace` L83, `document.documentElement` L146, `document.fonts` L152                                                 |                                                                                                                                                                                                                                                                                                           |
| `lib/react-error-reporting.ts` (119)                         | React                   | `ErrorInfo` L1                                                                                                                    |                                                                                                                                                                                                                                                                                                           |
| `lib/platform/*` (146)                                       | DOM-bound               | `window.platformBridge` `bridge.ts:22`, `document.documentElement` `backdrop.ts:23,54`, `navigator.userAgent` `backdrop.ts:74-76` | Desktop shell only.                                                                                                                                                                                                                                                                                       |
| `lib/focus/*` (871)                                          | DOM-bound               | `HTMLElement` throughout `state/service.ts:80,119,157,270`; `document.addEventListener('focusin')` `providers/provider.tsx:20`    |                                                                                                                                                                                                                                                                                                           |
| `lib/file-open-intent/*` (1,644)                             | IDE-mode                | `QueryClient` L1, `@singapor/core` L2-8, `globalThis.performance` `service.ts:1222,1419,1487`                                     | Editor prefetch pipeline; not TUI.                                                                                                                                                                                                                                                                        |
| `lib/environments/state/store.ts` (156)                      | needs-shim              | `create` from `zustand` L2 (pulls `zustand/react`); `const primaryOrigin = activeServerOrigin()` at module scope L31              | Logic is pure. Use `createStore` from `zustand/vanilla` in core and re-export a `useStore` hook in web. Seed must become a factory argument.                                                                                                                                                              |
| `lib/environments/state/query-clients.ts` (67)               | pure                    | `QueryClient` from react-query L1                                                                                                 | swap to query-core; the owner `WeakMap` (L16-17) + `clientForQueryClient` (L54) is the pattern every `queryFn` uses to reach the right Eden client.                                                                                                                                                       |
| `lib/environments/state/server-restart-invalidation.ts` (26) | pure                    |                                                                                                                                   |                                                                                                                                                                                                                                                                                                           |
| `lib/environments/state/activity.ts` (20)                    | pure                    | per-origin `AbortController`                                                                                                      |                                                                                                                                                                                                                                                                                                           |
| `lib/environments/utils/connection.ts` (59)                  | pure                    |                                                                                                                                   | `ServerConnectionState`, `connectionAfterHandshake` (generation bump on new `serverInstanceId`) L40-52.                                                                                                                                                                                                   |
| `lib/environments/utils/structured-errors.ts` (40)           | pure                    |                                                                                                                                   |                                                                                                                                                                                                                                                                                                           |

Rough totals for `lib/`: pure 4,630 lines (58%), needs-shim 1,340 (17%), DOM/React/IDE-bound 2,025 (25%).

## 4. Chat feature non-component layers

### 4.1 `features/chat/transport/` (997 lines) — portable with one injection

| Module                                                                                        | Class      | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `chat-transport.ts` (33)                                                                      | pure       | Types only; this is the interface a TUI consumes.                                                                                                                                                                                                                                                                                                                                                                        |
| `create-chat-transport.ts` (40)                                                               | pure       | Composes rpc client + subscription cache + earlier-page loader per origin.                                                                                                                                                                                                                                                                                                                                               |
| `orchestration-rpc-client.ts` (791)                                                           | needs-shim | `new WebSocket(url)` L786 behind `options.createSocket` L53/L311; `WebSocket.OPEN` L368; `MessageEvent`/`CloseEvent` types L397, L491, L755; `performance.now()` L234, L790; writes to `useEnvironmentsStore` L225, L254, L404 (handshake + slow-request accounting). Bun has all of these globals; the `createSocket` hook is where a TUI passes `new WebSocket(url, { headers: { origin, 'x-client-instance': … } })`. |
| `orchestration-http-snapshots.ts` (90)                                                        | pure       | `AbortSignal.timeout`/`any` (Bun supports).                                                                                                                                                                                                                                                                                                                                                                              |
| `orchestration-sequence.ts` (28), `orchestration-streams.ts` (4), `structured-errors.ts` (11) | pure       |                                                                                                                                                                                                                                                                                                                                                                                                                          |

The protocol it speaks is `packages/contracts/src/orchestration-ws.ts` (`ORCHESTRATION_WS_PROTOCOL_VERSION = 4`, L32; frames `connected`/`response`/`subscription.next`/`subscription.error`/`subscription.complete`/`pong`, handled at `orchestration-rpc-client.ts:400-443`). Snapshots go over HTTP on purpose (`orchestration-http-snapshots.ts:18-26`).

### 4.2 `features/chat/state/` (4,541 lines)

| Module                                                                                                  | Class      | Evidence                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `chat-projection-writers.ts` (1,582)                                                                    | **pure**   | Imports contracts + cache constants + store types only. The read-model reducer; the single most valuable thing to share.                                                                                                                                                                                                                                                                         |
| `chat-projection-selectors.ts` (421)                                                                    | pure       |                                                                                                                                                                                                                                                                                                                                                                                                  |
| `chat-projection-store.ts` (323)                                                                        | needs-shim | `create` from `zustand` L24; `Debouncer` from `@tanstack/react-pacer/debouncer` L23 (re-export of `@tanstack/pacer/debouncer`); `window.addEventListener('beforeunload')` L308-311, guarded. Actions L159-166: `applyOrchestrationEvent(s)`, `applyShellStreamItem`, `applyThreadStreamItem`, `prependThreadDetailPage`, `resetChatProjection`, `syncShellSnapshot`, `syncThreadDetailSnapshot`. |
| `chat-projection-cache.ts` (278)                                                                        | needs-shim | `localStorage` L59, L90, L154; `canUseLocalStorage` L277. Paint-before-connect cache; a TUI would back it with a file under XDG state or skip it.                                                                                                                                                                                                                                                |
| `thread-detail-subscriptions.ts` (447)                                                                  | pure       | Timers injectable via options L86-87 (`scheduleTimeout`, `clearScheduledTimeout`, `now`).                                                                                                                                                                                                                                                                                                        |
| `thread-earlier-pages.ts` (95), `thread-earlier-page-store.ts` (56), `thread-detail-sync-store.ts` (70) | pure       |                                                                                                                                                                                                                                                                                                                                                                                                  |
| `active-transports.ts` (26)                                                                             | pure       |                                                                                                                                                                                                                                                                                                                                                                                                  |
| `chat-cache-constants.ts` (21)                                                                          | pure       |                                                                                                                                                                                                                                                                                                                                                                                                  |
| `chat-optimistic-store.ts` (189)                                                                        | pure       | zustand + pacer.                                                                                                                                                                                                                                                                                                                                                                                 |
| `chat-input-draft-store.ts` (396)                                                                       | needs-shim | `beforeunload` L392-395 guarded; `draft-storage.ts` `localStorage` L61, L76, L87.                                                                                                                                                                                                                                                                                                                |
| `prompt-stash-store.ts` (145)                                                                           | needs-shim | `localStorage` L55.                                                                                                                                                                                                                                                                                                                                                                              |
| `thread-diff-scope-store.ts` (74)                                                                       | needs-shim | via `utils/thread-diff-scope-storage.ts` `localStorage` L59-82.                                                                                                                                                                                                                                                                                                                                  |
| `chat-changed-files-expansion-store.ts` (90)                                                            | needs-shim | via `utils/changed-files-expansion-storage.ts` `localStorage` L47-70.                                                                                                                                                                                                                                                                                                                            |
| `composer-inbox-store.ts` (85), `coarse-clock-store.ts` (64), `chat-work-log-expansion-store.ts` (32)   | pure       | `setInterval` only.                                                                                                                                                                                                                                                                                                                                                                              |
| `markdown-highlight-cache.ts` (17)                                                                      | web        | `@streamdown/code` type.                                                                                                                                                                                                                                                                                                                                                                         |
| `server-connection-store.ts` (89), `server-restart-invalidation.ts` (41)                                | **dead**   | Zero non-test consumers (`grep -rn useServerConnectionStore apps/web/src` returns only tests). Superseded by `lib/environments/state/store.ts` + `lib/environments/state/server-restart-invalidation.ts`.                                                                                                                                                                                        |

### 4.3 `features/chat/utils/` (10,570 lines)

**Pure domain, portable as-is** (~8,000 lines): `activity-presentation.ts` (358), `activity-visibility.ts`, `agent-markdown.ts`, `byte-bounded-lru.ts`, `changed-files-presentation.ts`, `command-builders.ts` (598; imports only `features/workspace/utils/path.ts` and `lib/stable-path-hash.ts`, both pure), `command-dispatch.ts` (117; the one door for every command, `performance.now()` L62), `command-sync.ts` (113; replay + snapshot after dispatch), `composer-command-search.ts`, `context-usage.ts`, `elapsed-ms.ts`, `formatters.ts`, `input-attachment-limits.ts`, `input-logic.ts` (465; trigger detection over a plain string), `markdown-fence.ts`, `markdown-file-links.ts` (280), `markdown-file-link-chips.ts`, `markdown-external-links.ts`, `markdown-list-indentation.ts` (129; `document` at L108 is a parsed markdown AST param, not the DOM), `message-metadata.ts`, `message-text.ts`, `model-effort.ts` (247), `model-picker-badges.ts`, `model-picker-search.ts`, `model-preferences.ts`, `pending-approvals.ts` (140), `pending-user-input.ts` (215), `pipeline-logging.ts` (166; depends on `lib/client-logging` + `wide-event-scope`), `project-qualifiers.ts`, `proposed-plan.ts` (183), `provider-auth.ts` (270), `provider-brand-marks.ts`, `provider-model-options.ts` (188), `resolve-model-selection.ts`, `runtime-state.ts` (292), `stream-reconnect.ts` (34; shared backoff ladder), `terminal-context.ts` (184; `<terminal_context>` block serialization), `thread-busy.ts`, `thread-status.ts`, `timeline-items.ts` (778), `timeline-minimap.ts` (210), `timeline-scroll-anchoring.ts` (367; pure geometry, web-specific but no DOM), `turn-diff-tree.ts`, `user-message-collapse.ts`, `work-log.ts` (403), `composer-drop.ts` (structural `{ getData, types }` type, no DOM).

**Needs-shim**: `attachment-image.ts` (`activeServerOrigin()` L3); `checkpoint-diff-query.ts` (`getClient()` + `queryOptions`; type imports from `features/git/utils/{diff-document,types}` which are pure); `composer-skills.ts`, `project-entry-query.ts`, `provider-auth-query.ts`, `provider-query.ts` (`queryOptions` from react-query + `getClient()` defaults); `changed-files-expansion-storage.ts`, `draft-storage.ts`, `thread-diff-scope-storage.ts` (`localStorage`).

**DOM/web-bound, stays in web**: `image-compression.ts` (324; `createImageBitmap` L302, `OffscreenCanvas` L279-281, `document.createElement('canvas')` L291 — comment at L7 says "Browser APIs only"), `input-attachments.ts` (151; `DataTransfer` L21-27, `FileReader` L141), `input-editor-actions.ts` (396; Lexical), `markdown-clipboard.ts` (295; `document.createElement` L35), `markdown-fragment-links.ts` (76; `querySelectorAll` L56-69), `markdown-highlight.ts` + `streamdown-editor-theme.ts` (shiki/streamdown), `link-menu.ts` + `message-menu.ts` (import `@/features/menus/utils/model` which types items with `@phosphor-icons/react` `Icon` and `@/keymap/types` `PlatformCommandId` — `menus/utils/model.ts:1-3`).

### 4.4 `features/chat/hooks/` (1,248) and `providers/` (842)

All React by definition, but three hooks carry React-free supervisors worth lifting:

- `hooks/use-chat-shell-subscription.ts:86-155` — `superviseShellSubscription` + `runShellStream` are a pure async loop over `ChatTransport` and the projection store. The only browser coupling is `isBrowserOnline()` L163-167 (`navigator.onLine`) and `createWakeSource()` L174-205 (`window` `online`/`focus`, `document.visibilitychange`, `window.setTimeout` L214). Extract a `WakeSource`/`online` port and the supervisor moves.
- `hooks/use-workspace-chat-project.ts:51-72` — `createWorkspaceProject` (dispatch `createWorkspaceProjectCommand`) is pure over the transport.
- `hooks/use-provider-sign-in.ts` (150) — `useMutation`/`useQuery` over `provider-auth-query.ts`; the polling logic could be a query-core observer.

Providers: `pending-requests-provider.tsx` (135) derives from `derivePendingApprovals`/`derivePendingUserInputs` (pure); `plan-follow-up-provider.tsx` (298) and `composer-modes-provider.tsx` (111) are thin over `dispatchChatCommand`/`command-builders`. `transport-provider.tsx:23-37` shows the intended per-origin lifecycle: `createChatTransport(origin)` in an effect keyed on `originForQueryClient(useQueryClient())`, released on cleanup. `session-context.ts:8-25` (`ChatModeSession`: `activeSession`, `transport`, `project`, `rootPath`, `selectSession`, `startDraft`, `addProject`, `openProject`, `retryProject`) is the domain API a TUI screen would need to reproduce.

### 4.5 `features/chat-mode/` non-component layers

- `state/` (960): all Zustand. Pure: `project-delete-request-store`, `project-rename-request-store`, `rail-order-store`, `session-delete-request-store`, `session-isolation-store`, `session-multi-select-store`, `session-rail-store`, `session-search-store`, `session-selection-store`, `thread`… `session-read-store.ts` has `beforeunload` L54-57 (guarded) and `localStorage` via `utils/session-read-storage.ts` L24-62. `rail-order-commands.ts` (182) and `session-commands.ts` (176) are command orchestration over the transport + projection store; `session-commands.ts:17` imports `features/workspace/state/active-project` (web workspace store) — the one cross-feature coupling to cut.
- `utils/` (1,339): pure except `rail-collapse-storage.ts` / `session-read-storage.ts` (`localStorage`), `project-menu.ts` / `session-menu.ts` (menus model with phosphor `Icon` type), `session-search-query.ts` (`queryOptions` + `getClient()`). `session-rail-model.ts` (315), `active-session.ts`, `session-order.ts`, `session-unread.ts`, `rail-reorder.ts`, `project-order.ts`, `project-scripts.ts` are exactly the sidebar derivations a TUI rail needs.
- `hooks/` (591) and `providers/` (230): React; `use-session-actions.ts:2` uses `sonner`, `use-project-menu.ts:9` uses `lib/clipboard`, `use-rail-drag-sensors.ts` is dnd-kit; `session-controller.tsx` composes editor workspace state and three dialogs.

## 5. `packages/contracts` and `packages/observability`

- `packages/contracts` (`package.json`: `"exports": { ".": "./src/index.ts" }`, deps `minimatch`, `valibot`; 36 non-test modules). `src/index.ts` exports, in order: tree entries (L1-12), `FileResult`, order keys (L14-20), git DTOs (L21-45), watch events, terminal message parsers + bounds (L47-56), workspace search types/preview/matcher/fuzzy rank (L57-87), error category/fields/`isRecord` (L88-90), workspace-edit protocol (L91-107), chat ids + schemas (L108-127), chat model + attachment limits (L128-163), orchestration runtime enums (L164-189), provider DTOs (L190-216), orchestration commands (L217-244), events (L245-250), snapshots + page size (L251-279), WS protocol (L280-312), health descriptor (L313), log dashboard (L314-329), LSP protocol constants (L330-340), settings schemas/registry/resolve/mutations/wire (L341-443), composer tokens (L444-450). No DOM, no node builtins outside tests. This is already the shared DTO layer and needs no change.
- `packages/observability` exports `.` (config + env + env-file + runtime), `./env` (28 lines, pure), `./env-file` (node:fs). `runtime.ts` uses `node:fs`/`node:path` and evlog fs/posthog drains (`runtime.ts:1-7`, `initializeObservabilityRuntime` L44-77 with a `source` plugin). The web imports only `./env` (`client-logging.ts:4`). For a Bun TUI, `initializeObservabilityRuntime({ source: 'tui' })` writing JSONL into `logs/` is the natural choice and matches the CLAUDE.md log conventions (`source` = `be`/`client`/`keyboard`, add `tui`).
- The server's public contract type is `apps/server/src/client-contract.ts` (`export type { App }`), exposed as `server/client-contract` (`apps/server/package.json` exports, types-only). `apps/web/package.json` depends on `"server": "workspace:*"` for that one type. A `client-core` package would carry the same types-only dependency.

## 6. Precedents

### 6.1 t1code `packages/client-core`

- `references/t1code/packages/client-core/package.json`: name `@t3tools/client-core`, deps `@t3tools/contracts`, `@t3tools/shared`, `effect`; **no TanStack, no React**. Subpath exports for `ids`, `contextWindow`, `nativeApi`, `pendingUserInput`, `proposedPlan`, `sessionLogic`, `types`, `wsTransport`.
- `src/index.ts` re-exports 18 modules: `appSettings` (settings model + `APP_SETTINGS_STORAGE_KEY`), `advertisedEndpoint`, `contextWindow`, `ids` (`newThreadId` etc. over `crypto.randomUUID`), `gitQuickAction`, `knownEnvironment` (`{ id, label, source, environmentId?, target: { httpBaseUrl, wsBaseUrl } }` — an origin registry model, `knownEnvironment.ts:3-16`), `modelPickerSearch`, `nativeApi` (`createTransportNativeApi({ transport, dialogs?, shell?, contextMenu? })` turns `transport.request/subscribe` into a typed `NativeApi`; host-only capabilities are injected, `nativeApi.ts:20-35`), `pendingUserInput`, `proposedPlan`, `scoped` (environment-scoped project/thread refs, `scoped.ts:9-31`), `sidebarStatus`, `sidebarSort`, `sessionLogic` (`derivePendingApprovals` L246, `derivePendingUserInputs` L351, `deriveActivePlanState` L400, `deriveWorkLogEntries` L521, `deriveTimelineEntries` L1047, `derivePhase` L1088 — the same family as our `pending-approvals.ts`, `pending-user-input.ts`, `work-log.ts`, `timeline-items.ts`, `thread-status.ts`), `slashCommands`, `sourceControlDiscoveryState`, `types`, `wsTransport` (`WsTransport` with injectable `WebSocketCtor` and a `WebSocketLike` structural type L30-44, reconnect ladder L53, request/subscribe/replayLatest L105-172).
- Consumers. TUI: `apps/tui/src/ui.tsx:100-138` imports `createTransportNativeApi`, `WsTransport`, all `derive*`, ids, `PROVIDER_OPTIONS`, `SLASH_COMMAND_DEFINITIONS`; plus `composerSlashMenu.ts:5`, `prefs.ts:1`, `rendererTheme.ts:2`, `theme.ts:1`, `threadSessionState.ts:1`, `workEntryIcons.ts:1`. Web: `apps/web/src/wsTransport.ts:1-17` subclasses `WsTransport` and picks the URL from `window.desktopBridge?.getWsUrl()` / `VITE_WS_URL` / `window.location`; `wsNativeApi.ts:42-60` calls `createTransportNativeApi` with browser dialogs; `appSettings.ts`, `Sidebar.logic.ts`, `GitActionsControl.logic.ts`; and `pendingUserInput.ts`/`proposedPlan.ts`/`session-logic.ts`/`types.ts` are one-line re-export shims.
- Connection: `apps/tui/src/ui.tsx:4872-4878` — `new WsTransport({ url: server.wsUrl, onWarning })` then `createTransportNativeApi({ transport })`; the snapshot is pulled with `transport.request(getSnapshot, undefined, { timeoutMs: 5_000 })` into React `useState` (L4896-4910) via a coalesced refresh runner. Attach vs spawn: `serverSupervisor.ts:127-150` `resolveAttachedServerConnection` reads `T1CODE_TUI_ATTACH_ONLY`, `T1CODE_HOST`, `T1CODE_PORT`, `T1CODE_AUTH_TOKEN` and builds `ws://host:port/?token=…` (`buildServerWsUrl` L123); otherwise the TUI spawns its own server. Logging is a plain append-only file (`apps/tui/src/log.ts:21-30`).
- Query library: web uses `@tanstack/react-query` (16 files; `apps/web/src/lib/serverReactQuery.ts:9-18` wraps `nativeApi.server.getConfig()` in `queryOptions`); TUI has none (`apps/tui/package.json` deps: `@opentui/core`, `@opentui/react`, `effect`, `react`, `ws`).

### 6.2 opencode `packages/sdk` + `tui/src/context/{sdk,sync}.tsx`

- `packages/sdk/js/package.json`: `@opencode-ai/sdk`, exports `.`, `./client`, `./server`, `./v2`, `./v2/client`, `./v2/gen/client`, `./v2/server`, `./v2/types`; a hey-api generated client. `v2/client.ts:53-80` `createOpencodeClient({ baseUrl, fetch?, headers?, directory?, signal })`; `v2/server.ts:22-40` `createOpencodeServer` spawns `opencode serve`. `error-interceptor.ts:13-40` wraps non-2xx bodies into `Error` with `.cause`.
- `tui/src/context/sdk.tsx:11-151`: Solid context taking `{ url, directory?, fetch?, headers?, events? }`; builds the client (L23-31), an in-memory emitter (L35-46), 16ms event batching (L54-80), an SSE loop with exponential backoff (L82-117); `events` lets an in-process worker replace SSE (cli/cmd/tui.ts:240-256 uses `http://opencode.internal` + a worker `fetch`; external mode adds `ServerAuth.headers()` L237).
- `tui/src/context/sync.tsx:60-144`: one `createStore` holding the whole read model (providers, sessions, messages, parts, permissions, questions, …); `event.subscribe` applies deltas by event type (L176+), `bootstrap()` on `server.instance.disposed` (L178). `event.ts:9-36` is a tiny typed filter over the emitter. No TanStack; the Solid store is the cache and Solid signals are the subscription.

### 6.3 What the precedents agree on

1. The shared package is **transport + read-model derivations + ids/keys**, not a UI state manager. Both keep host capabilities (dialogs, clipboard, open-external, storage) as injected options.
2. The socket constructor and the base URL are injected, never read from `window`/`import.meta.env` inside the package.
3. Query caching is a per-front-end choice; neither TUI uses TanStack.

## 7. Proposed boundary: `packages/client-core`

Name it `@workspace/client-core` next to `contracts` and `observability` (root `package.json` `workspaces.packages` lists packages explicitly, L4-11; add it there). Entry rule from CLAUDE.md: barrel only at `src/index.ts`; consumers still import exact files through subpath exports like t1code does (`./transport`, `./chat`, `./environments`, `./errors`, `./logging`).

Dependencies: `@workspace/contracts`, `@workspace/observability` (`./env` only), `@elysia/eden`, `evlog`, `valibot`, `zustand` (import from `zustand/vanilla`), `@tanstack/query-core`, `@tanstack/pacer`; types-only `server` (`server/client-contract`). No `react`, `sonner`, `@workspace/ui`, `@workspace/tree`, `@singapor/*`, `lexical`, `@streamdown/*`.

### 7.1 Ports the package declares (host supplies)

```ts
type ClientCoreHost = {
  readonly origin: string                                   // replaces client.ts:11 seed
  readonly instanceId?: string                              // default: instance-id.ts
  readonly fetch?: typeof fetch                             // treaty `fetcher`
  readonly createSocket?: (url: string) => WebSocket        // orchestration-rpc-client.ts:53
  readonly headers?: () => Record<string, string>           // Origin + x-client-instance
  readonly storage: KeyValueStorage | null                  // replaces every localStorage site
  readonly notify?: (level: 'error'|'warn'|'info', title: string, description?: string) => void  // replaces sonner in client-error-taxonomy.ts:110
  readonly wake?: WakeSource                                // use-chat-shell-subscription.ts:174-205
  readonly isOnline?: () => boolean                         // navigator.onLine, L163-167
  readonly onFlush?: (flush: () => void) => () => void      // lifecycle-flush.ts
  readonly logging: { runtime: 'browser'|'tui'|'desktop'; mode: string; dev: boolean; minLevel?: LogLevel; drain?: ... }  // client-logging.ts:62-85, 170-182
}
```

`KeyValueStorage` = `{ getItem, setItem, removeItem }`; web passes `localStorage`, TUI passes a JSON file under `$XDG_STATE_HOME/platform/` or `null`.

### 7.2 What moves (by target folder)

- `client-core/transport/`: `lib/client.ts` (as `createEnvironmentClient(origin, host)` + `createClientRegistry()`; drop the module-scope seed; keep `setClient` as a registry method for the test seam), `lib/instance-id.ts`, `lib/eden-events.ts`, `lib/server-sockets.ts`, `lib/workspace-search-client.ts`, `lib/file-server.ts` (with `client` required, not defaulted), `lib/file-system-types.ts`, `lib/query-keys.ts`, `lib/recent-folders-query.ts`, `features/workspace/utils/coalesced-log.ts`.
- `client-core/errors/`: `lib/structured-errors.ts`, `lib/client-error-context.ts`, `lib/client-error-reporting.ts`, `lib/client-error-taxonomy.ts` minus `reportError`'s toast (goes through `host.notify`), `lib/error-message.ts`.
- `client-core/logging/`: `lib/client-logging.ts` (env values injected; `runtime` injected), `lib/wide-event-scope.ts`.
- `client-core/environments/`: `lib/environments/**` (store built with `zustand/vanilla` `createStore` from a `primaryOrigin` argument; `query-clients.ts` over `@tanstack/query-core`), `features/environments/utils/dev-origin.ts` (`parseDevOrigin`/`checkDevOrigin` are pure over the registry).
- `client-core/chat/transport/`: all of `features/chat/transport/`.
- `client-core/chat/state/`: `chat-projection-writers.ts`, `chat-projection-selectors.ts`, `chat-projection-store.ts` (vanilla store; `beforeunload` → `host.onFlush`), `chat-projection-cache.ts` (over `host.storage`), `chat-cache-constants.ts`, `thread-detail-subscriptions.ts`, `thread-detail-sync-store.ts`, `thread-earlier-pages.ts`, `thread-earlier-page-store.ts`, `active-transports.ts`, `chat-optimistic-store.ts`, `chat-input-draft-store.ts` + `utils/draft-storage.ts` (over `host.storage`), `prompt-stash-store.ts`, `composer-inbox-store.ts`, `coarse-clock-store.ts`, `thread-diff-scope-store.ts` + storage.
- `client-core/chat/supervisors/`: `superviseShellSubscription` lifted from `use-chat-shell-subscription.ts:86-155` (React hook stays in web and calls it with `host.wake`), `createWorkspaceProject` from `use-workspace-chat-project.ts:51-72`.
- `client-core/chat/commands/`: `command-builders.ts`, `command-dispatch.ts`, `command-sync.ts`, `pipeline-logging.ts`, `stream-reconnect.ts`, `elapsed-ms.ts`, `lib/stable-path-hash.ts`, `features/workspace/utils/path.ts`.
- `client-core/chat/derive/`: `activity-presentation`, `activity-visibility`, `work-log`, `timeline-items`, `timeline-minimap`, `message-metadata`, `message-text`, `pending-approvals`, `pending-user-input`, `proposed-plan`, `thread-busy`, `thread-status`, `runtime-state`, `context-usage`, `changed-files-presentation`, `turn-diff-tree`, `formatters`, `user-message-collapse`, `agent-markdown`, `markdown-fence`, `markdown-file-links`, `markdown-file-link-chips`, `markdown-external-links`, `markdown-list-indentation`, `terminal-context`, `input-logic`, `input-attachment-limits`, `composer-command-search`, `composer-skills` (query-core), `project-entry-query` (query-core), `project-qualifiers`, `provider-auth`, `provider-auth-query`, `provider-query`, `provider-model-options`, `provider-brand-marks`, `model-effort`, `model-picker-search`, `model-picker-badges`, `model-preferences`, `resolve-model-selection`, `checkpoint-diff-query` (query-core; the two `features/git/utils` type modules are pure and move with it or into `client-core/git/`), `byte-bounded-lru`, `attachment-image` (origin injected).
- `client-core/chat-mode/`: every `chat-mode/state/*` store and `chat-mode/utils/*` derivation except the menu models; `session-commands.ts:17` must stop importing `features/workspace/state/active-project` (pass the active project id in).

### 7.3 What stays in `apps/web`

`lib/focus/**`, `lib/file-open-intent/**`, `lib/platform/**`, `lib/default-nerd-font.ts`, `lib/clipboard.ts`, `lib/react-error-reporting.ts`, `lib/file-icons.ts` + `vscode-icon-symbols.ts` + `file-icon-style.ts`, `lib/lifecycle-flush.ts` (becomes the web `onFlush` adapter), `lib/editor-visible-snapshot-cache.ts`, `lib/file-snapshot-query-cache.ts`, `lib/diagnostic.ts`, `lib/tree-model.ts` (only the web tree consumes it — but it is pure; move later if the TUI grows a file tree), `lib/ui-mode.ts`, `lib/intent-prefetch-options.ts`, `lib/load-state.ts`, `lib/query-state.ts`; all chat/chat-mode `components/`, `hooks/`, `providers/`; `chat/utils/{image-compression,input-attachments,input-editor-actions,markdown-clipboard,markdown-fragment-links,markdown-highlight,streamdown-editor-theme,link-menu,message-menu,timeline-scroll-anchoring}.ts`; `chat-mode/utils/{project-menu,session-menu}.ts`; `state/application-runtime.ts` (web composition: editor runtime + query client per origin, `application-runtime.ts:25-32`).

Web-side adapters become one file each: `createWebHost()` (origin from `import.meta.env`, `localStorage`, `sonner`, `window` wake source, `lifecycle-flush`) and thin `useStore(coreStore)` hooks over `zustand`'s `useStore` for the vanilla stores.

### 7.4 What a TUI gets on day one

`createEnvironmentClient(origin, host)` → `createChatTransport(origin)` → `superviseShellSubscription` into `chatProjectionStore` → `selectChatProjects`/`selectChatSidebarThreads`/`sessionRailModel` for the rail, `selectChatThreadById` + `chatTimelineItems`/`chatWorkLogEntries`/`derivePendingApprovals` for the transcript, `createThreadTurnStartCommand` + `dispatchChatCommand` + `scheduleThreadProjectionSyncAfterDispatch` for sending, `connectTerminalSocket` for a terminal pane, `providerListQueryOptions` (query-core) for the model picker. That is the whole agent-view root from `docs/product-vision.md:26-28`.

## 8. The TanStack Query question

- **t1code**: `packages/client-core` has no TanStack dependency; `apps/tui` has none either (`apps/tui/package.json`); the TUI keeps the snapshot in React state refreshed via `transport.request(...)` (`ui.tsx:4896-4910`); `apps/web` layers `@tanstack/react-query` on top in app code (`apps/web/src/lib/serverReactQuery.ts`). Answer: **the shared core is query-library-agnostic; TanStack is a web-app choice.**
- **opencode**: no TanStack anywhere in the TUI (`packages/tui/package.json` deps are opentui/solid/effect/remeda); Solid `createStore` is the cache.
- **Ours**: the chat read model is not a TanStack cache. It is `chat-projection-store.ts` (Zustand) fed by `shellStream`/`threadDetailStream` subscriptions, so the TUI's main surface never needs TanStack. TanStack appears only in the request/response side: 11 modules (`grep queryOptions|@tanstack/react-query` under chat utils, chat-mode utils, lib): `composer-skills.ts`, `project-entry-query.ts`, `provider-auth-query.ts`, `provider-query.ts`, `session-search-query.ts`, `lib/query-state.ts`, `lib/recent-folders-query.ts`, `lib/file-snapshot-query-cache.ts`, `lib/file-open-intent/state/service.ts`, `lib/environments/state/server-restart-invalidation.ts`, `lib/environments/state/query-clients.ts`. Every one of them imports `QueryClient`/`queryOptions`/`Query` — all exported by `@tanstack/query-core` 5.101.0 (present in `node_modules/@tanstack/query-core`). `queryFn: ({ client }) => …clientForQueryClient(client)` (`provider-query.ts:19`) is the pattern that binds a query to its origin's Eden client without React.
- Recommendation: `client-core` depends on `@tanstack/query-core`; it exports `queryOptions` builders and `queryClientFor(origin)`. The web keeps `@tanstack/react-query` (`useQuery` over the same options; `QueryClientProvider` at `active-environment-application.tsx:23`). The TUI either (a) uses `QueryObserver` from query-core with `useSyncExternalStore` (OpenTUI+React) or a Solid signal (OpenTUI+Solid), or (b) ignores query-core for the few one-shot reads and calls the `fetch*` functions directly. Both work because the builders and the fetchers are separate exports.

## 9. Environments layer status (what the TUI must connect through)

Landed in the working tree (plan 077 phases 1-3, `plans/077-environment-runtime-origin.md:134-245`):

- `apps/web/src/lib/client.ts`: `activeServerOrigin()` L20, `setActiveServerOrigin()` L24, `environmentClientFor(origin)` L28 (Map by origin), `createEnvironmentClient(origin)` L14 (treaty with `x-client-instance` header L16). Seed: `import.meta.env.VITE_SERVER_URL ?? 'http://localhost:3001'` L11. Desktop supplies it via env at build/launch (`apps/desktop/src/bun/index.ts:126`).
- `lib/environments/state/store.ts`: `EnvironmentsState { activeOrigin, entries, connectionByOrigin }` L14-18; actions `activate` L38 (calls `setActiveServerOrigin` first), `addDevEnvironment` L51, `recordHandshake` L60 (identity-drift check L129-146, generation bump via `connectionAfterHandshake`), `recordDescriptor` L76 (from `/health`), slow-request accounting L87-101. Seed `primaryOrigin = activeServerOrigin()` at module scope L31.
- `lib/environments/state/query-clients.ts`: one `QueryClient` per origin L19-37 (`gcTime` 5m, `retry` 1, `staleTime` 10s), owner map L16-17, `clientForQueryClient`/`originForQueryClient` L54-60.
- `lib/environments/state/activity.ts`: per-origin `AbortSignal` that `server-sockets.ts:35,50` default to, so switching origins closes terminal/LSP sockets.
- `lib/environments/state/server-restart-invalidation.ts`: invalidates that origin's QueryClient on a generation bump L9-25.
- `features/environments/utils/dev-origin.ts`: `parseDevOrigin` accepts loopback `http://` with a port only L8-20; `checkDevOrigin` reads `/health`, parses `healthDescriptorSchema`, records the descriptor L22-30. `components/dev-origin-dialog.tsx` is the dev-only switch plan 078 deletes.
- `state/application-runtime.ts:41-54`: the switch sequence (clear command binding, suspend activity, reset LSP pool, suspend editor, `cancelQueries`, close chat transports, resume activity, `activate`).
- Chat transport per origin: `features/chat/transport/create-chat-transport.ts:9-40`, provider at `features/chat/providers/transport-provider.tsx:23-37`, registry `state/active-transports.ts:8-26`.

Gaps for a runtime-origin TUI:

1. **Module-scope seeds** (`client.ts:11`, `store.ts:31`) must become factory inputs. Everything downstream already takes `origin` explicitly.
2. **Server auth is browser-origin based.** `apps/server/src/auth.ts:75-76`: no `Origin` header → `UNAUTHORIZED`; not in `allowedOrigins` → `FORBIDDEN_ORIGIN`; defaults are `localhost:3000/4173/5173` L22-29. The WS upgrade is checked the same way via `socket.data.headers.origin` (`auth.ts:67-69`, `originFromWebSocketData` L87-93) and closes with 1008 on failure (`orchestration/ws-rpc.ts:97-103`), which the RPC client maps to `ORCHESTRATION_WS_UNAUTHORIZED` (`orchestration-rpc-client.ts:756-764`) and the supervisor treats as `blocked` (`stream-reconnect.ts:20-25`). A Bun TUI has to send `Origin` on both `fetch` (treaty `headers` option, already a function at `client.ts:16`) and the RPC socket (`createSocket` option → `new WebSocket(url, { headers })`, which Bun supports). The alternative — a `local-process` principal or the token mode the comment at `auth.ts:97-104` says was deleted — is a server change; plan 078's SSH-forward model (`docs/environments-and-remote-plan.md:152-166`) assumes an origin allowlist, so the synthetic-Origin approach is the one that needs no server work. Either way this is a decision the TUI plan must record.
3. **Identity/handshake** already works for a TUI: the `connected` frame carries `environmentId`, `serverInstanceId`, `protocolVersion` (`orchestration-rpc-client.ts:400-417`), and `/health` returns `healthDescriptorSchema` (`packages/contracts/src/index.ts:313`).
4. **Persistence scoping** is not done (plan 078 §"Scoped persistence"); the TUI's file-backed `KeyValueStorage` should be keyed by `environmentId` from the start rather than inheriting the web's unscoped keys (`workspace-cache-storage.ts:5-7` is at version 19 and drops on mismatch, no migration).
5. **Dead duplicates** to delete: `features/chat/state/server-connection-store.ts`, `features/chat/state/server-restart-invalidation.ts` and their tests.

## 10. Risks

- `zustand`'s root import re-exports `zustand/react` (`node_modules/zustand/esm/index.mjs`), so every store that does `import { create } from 'zustand'` (13 in chat/state, 12 in chat-mode/state, 1 in lib/environments) drags React into the package unless rewritten to `createStore` from `zustand/vanilla`. Mechanical but touches every store file and every `useXStore(selector)` call site in components (they become `useStore(xStore, selector)`).
- `@tanstack/react-pacer/debouncer` (`index.js`) imports React hooks; the four stores using `Debouncer` must import from `@tanstack/pacer/debouncer` (already installed at 0.21.1 as a transitive dep; needs to be a direct dep of the new package).
- `getClient()` ambient default is used in 67 non-test places across 15 modules (`grep -rn "getClient()"`). Moving `file-server.ts` and the query builders with `client: Client = getClient()` defaults keeps a hidden global in the shared package; dropping the default forces the web to thread `clientForQueryClient(queryClient)` everywhere. The test seam (`apps/web/test/fixtures.ts:31-39`, `test/env/dom.ts:21-26`) depends on `setClient` and must move with the registry.
- `import.meta.env` inside a workspace package would be inlined by Vite for the web but undefined under `bun` for the TUI; the package must not contain any (`client.ts:11`, `client-logging.ts:78-181`).
- `ProjectId` derivation (`stable-path-hash.ts:1-8` + `command-builders.ts` `workspaceProjectId`) is a join key to server rows. If the TUI resolves paths differently (symlinks, trailing slash, `~`), it silently creates a second project. `features/workspace/utils/path.ts:20-22` normalises only trailing slashes on purpose.
- Eden `Date` revival (`eden-events.ts:13-18`) applies to a Bun treaty client as well; every contract-typed read must keep `normalizeDates: true`.
- The chat projection cache writes on every store change with a throttle (`chat-projection-store.ts:296-323`) and flushes on `beforeunload`; a TUI backing store must be cheap to write or the throttle constant needs to become host-tunable.
- `server-sockets.ts` uses DOM event types; under a Bun-only tsconfig (`@types/bun`) the names resolve, but a package tsconfig that includes neither `lib.dom` nor `@types/bun` fails to compile. Pin the package tsconfig `lib` to `["ESNext"]` + `types: ["bun"]` or define structural `WebSocketLike` types the way t1code does (`wsTransport.ts:30-44`).
- Line counts in §3/§4 are approximate classifications from import scans, not a build; the definitive check is a `tsgo --noEmit` of the extracted package with `lib: ["ESNext"]` and no `dom`.

## 11. Open questions

1. Synthetic `Origin` header from the TUI (no server change) or a non-browser principal on the server (`auth.ts:97-104` says token mode was deleted deliberately)? Which one does plan 078's SSH-forward story prefer?
2. Does the TUI own `@tanstack/query-core` or call the `fetch*` functions directly? The answer decides whether `queryClientFor`/`clientForQueryClient` go into the package or stay web-side.
3. OpenTUI + React (t1code) or OpenTUI + Solid (opencode)? With React the Zustand stores can stay as `create()`-built hooks and the vanilla rewrite is optional; with Solid the vanilla rewrite is mandatory.
4. Where does the TUI persist per-viewer state (drafts, prompt stash, read stamps, rail collapse) — a JSON file under XDG state, or nothing? This decides whether `KeyValueStorage` is a required or optional port.
5. Should the TUI log through `/_log/ingest` like the web (`client-logging.ts:150-156`) or through `@workspace/observability` `initializeObservabilityRuntime` with `source: 'tui'` into `logs/`? The latter matches the JSONL conventions in CLAUDE.md and needs no server round-trip.
6. Menu models (`features/menus/utils/model.ts`) type items with phosphor `Icon`. Do we split the model into an icon-free core (id, label, command, disabled) so `session-menu.ts`/`project-menu.ts`/`message-menu.ts` can move, or does the TUI re-derive menus?
7. Delete `chat/state/server-connection-store.ts` + `server-restart-invalidation.ts` now (dead), or leave for the 077 close-out pass?

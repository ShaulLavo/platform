# Native Mac Client — Plan of Plans

**Direction (settled 2026-08-28):** a native Swift macOS client as a second frontend. The web app stays for cross-platform. The Bun server stays the brain (fs, git, LSP proxy, PTYs, settings); the native app is a client of the same contracts. **Editor-first is the strategy and the gate:** until the from-scratch native editor matches or beats the web editor's measured standard, there is no app. Shell and platform FE grow around a proven editor, not before it.

**Reference architecture (settled 2026-08-28): the ghostty model.** Own every pixel of the hot surface; be ruthlessly native everywhere else. The editor surface is one custom owner-drawn `NSView` (CoreText + augmented line tree; Metal later only if the bench demands it) that still speaks the OS protocols — `NSTextInputClient` for IME, `NSAccessibility` — the way ghostty's terminal surface does. Around it: AppKit skeleton (window, splits, responder chain — CodeEdit tried SwiftUI at the window root and documentedly reverted), SwiftUI leaves for panels/settings/inspectors, native menus and glass. Never `NSTextView`, never TextKit for the editor. The terminal pane later embeds libghostty — the same model, applied twice.

**Port-first doctrine (settled 2026-08-28):** wherever the web editor (`../Editor`) already solved a problem, the native plan starts by porting that design **1:1** — data structures, algorithms, invariants, and its tests as the spec — then diverges deliberately: cleaner code, fixing known mistakes, and Swift-appropriate idioms (value types, `Sendable` snapshots, ARC-aware allocation) are encouraged; _novel design_ is reserved for problems the web editor never had (CoreText typesetting, `NSTextInputClient`, `NSAccessibility`, AppKit integration). Every plan doc names what it ports vs what it designs fresh. First instance: the piece-table treap (see `native-editor-internals-research.md` directive 1).

**Gate definition:** core editing (typing, scrolling, selection, undo) + tree-sitter highlighting + LSP diagnostics/completion via the server proxy, at or above the web editor's numbers on the same machine. Feature parity (minimap, diff, decode animation, scope lines…) accretes after the gate, not before.

## Step 0 — toolchain (done)

Done 2026-08-28: **Xcode 26.6 (Swift 6.3.3, macOS 26.5 SDK)** is installed and selected, and `swift build`/`swift test`/`swift run -c release EditorBench` all pass on Apple M1 / macOS 26.4. Xcode 27 is in beta for macOS 27 "Golden Gate" (~Sept 2026). The one-time selection was:

```bash
sudo xcode-select -s /Applications/Xcode.app/Contents/Developer && xcodebuild -runFirstLaunch
```

`apps/mac` is scaffolded (SwiftPM: `EditorCore` lib, `EditorBench` + `MacApp` executables, swift-testing tests) and is not part of the bun/turbo workspace — it has no `package.json` on purpose.

**Scaffolding approach (validated by research, Aug 2026):** pure SwiftPM is the state of the art for a solo CLI-first dev-only Mac app — no `.xcodeproj` in git, `swift build`/`run`/`test` all CLI-native, SourceKit-LSP works in any editor. `swift run MacApp` shows a real window without a bundle; when a real `.app` bundle is eventually wanted, [swift-bundler](https://github.com/stackotter/swift-bundler) (actively maintained) or a 15-line copy script does it, and unsigned `xcodebuild` works with `CODE_SIGNING_ALLOWED=NO`. XcodeGen (still maintained) only if entitlements/Instruments schemes ever force a project file; Tuist is overkill for one app.

## The plans

Each is its own doc, written in this order. A plan is done when its exit criterion is met, not when prose exists.

| #   | Doc                            | Purpose                                                                                       | Key questions                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Exit criterion                                                                                                                 | Status                                                                                                                                                                                                                                                                                                                                             |
| --- | ------------------------------ | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `native-bench-harness.md`      | Port the web editor's perf discipline before any editor code. The bar is measured, not vibes. | **Answered.** Recon confirmed: the 5.8 ms typing / p95 6 ms highlight figures are recorded nowhere in `../Editor` — they are dated session records (June 12 and Aug 22 2026) and the doc tags them tier 3 rather than quoting them as settled. The only checked-in table (`../Editor/docs/architecture/phase-0/performance-baseline.md`) was **re-run on this machine**: piece-table insertion has silently regressed 0.0048 → 0.0098 ms/insertion, virtualization 45.3 → 57.2 ms, and the minimap bench no longer runs at all. Native methodology settled: keystroke-to-**commit** via `os_signpost` (`dev.platform.editor`/`Latency`, verified end to end through `xctrace record --template Logging` → `analyze_trace.py --list-signposts`), commit-to-photon from the Animation Hitches lane. **Correction: this machine is 60 Hz, not ProMotion** — gate on the 8.33 ms 120 Hz budget, verify elsewhere. Lesson zero measured: `String` by `Character` is 46x the `memchr` floor on a 9 MiB scan (the `Document` placeholder, 73x, is kept as the control).                                                                                                                                                                                                                                                                                                                                                                                                                     | `EditorBench` produces the baseline table for the web editor and an empty native column, reproducible on this machine.         | **done** — `docs/native-bench-harness.md`; `swift run -c release EditorBench`. Blocker recorded for plan 2: the Playwright typing/scroll harness cannot seed a workspace (`bench-workspace.mjs` still writes `workspace-state.v14` flat keys; the app is on v19 per-project slices), so `typing.*`/`scroll.*` have no same-machine web column yet. |
| 2   | `native-editor-core-design.md` | The buffer + layout + render architecture.                                                    | Rope vs line array (web editor precedent counts). Rendering — research verdict (Aug 2026): TextKit 2 is still incomplete for editor-class work; CodeEdit abandoned it for from-scratch **CoreText + NSView viewport-lazy layout** (CodeEditTextView, "million-line files in milliseconds"); STTextView is the maintained TextKit 2 alternative; no Swift editor uses Metal — CoreText + custom layout is the practical ceiling. Spike that first; Metal stays a deliberate later decision. Input/IME handling. Study CodeEditTextView's layout model before designing ours.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | A spike hits sub-2 ms keystroke-to-paint on a 10 MiB file, measured by plan 1's harness, and the stack choice is written down. | research done — see `native-editor-internals-research.md` (verdict: custom CoreText, TextKit 2 rejected with evidence)                                                                                                                                                                                                                             |
| 3   | `native-type-safety.md`        | Typed boundary to the Bun server.                                                             | **Correction (recon 2026-08-28): the repo has zero TypeBox — all runtime schemas are Valibot** (87 files), consumed via Elysia's Standard Schema support. REST mechanism still settled: `@elysia/openapi` with `fromTypes()` (TS-type-driven) plus `mapJsonSchema: { valibot: toJsonSchema }` from `@valibot/to-json-schema` for the schema'd routes. ~89 HTTP endpoints, of which **7 are SSE streams** (need hand-built `URLSession` streaming, not OpenAPI). Caveats: monorepo needs `projectRoot`+`tsconfigPath`, explicit types may need `Prettify`, production wants a pre-generated `.d.ts`. swift-openapi-generator is mature (1.13.0; OpenAPI 3.0/3.1) — CLI generator with checked-in code in its own SPM target, not the build plugin. Discriminated unions are the rough edge — shape contracts with discriminators. **WS surfaces**: `/orchestration/rpc` is fully Valibot-schema'd (`kind`/`method`/`type` discriminators — emittable to JSON Schema → quicktype/Codable); `/terminal`, `watch-events` (SSE), search, and LSP-protocol are **plain TS unions with hand parsers — not emittable**; either Valibot-ify them (small) or hand-write Codable (also small: terminal is 7 message types). `/lsp` WS is raw JSON-RPC passthrough — a Swift LSP client speaks LSP, no codegen. **Auth finding**: no tokens — an exact `Origin`-header allowlist; URLSession sends no Origin by default, so the native client must set one (or extend `SERVER_ALLOWED_ORIGINS`). | One typed REST call and one typed WS event flow server→Swift with codegen, no hand-written mirror types.                       | not started                                                                                                                                                                                                                                                                                                                                        |
| 4   | `native-tree-sitter-lsp.md`    | Language intelligence.                                                                        | SwiftTreeSitter (ChimeHQ) confirmed maintained (v0.10.0 Mar 2026, commits Aug 2026) with **Neon** as the recommended highlighting layer on top — CodeEdit uses the same pair. Port-first: mirror the web editor's `editor-tree-sitter` design (measured: worker-boundary p95 6 ms, incremental scheduling) — background snapshot parsing replaces the worker boundary. Grammar loading/distribution. LSP over the existing server proxy WS (connection pooling is server-side already — the client is thin). Semantic tokens later.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Viewport highlighting + live diagnostics in the spike editor.                                                                  | not started                                                                                                                                                                                                                                                                                                                                        |
| 5   | `native-shell-platform.md`     | The app around the editor. **Gated on plans 1–4 passing.**                                    | 2026 consensus: **SwiftUI shell, AppKit editor surface** via `NSViewRepresentable` — that hybrid is what shipping Mac tools use. macOS 26 glass APIs exist on both sides (`NSGlassEffectView`/`NSGlassEffectContainerView`, SwiftUI `.glassEffect()`) and delete the CSS material stack's whole problem class; known wart: `NSGlassEffectView` hosting SwiftUI has rendering issues on 26.2, keep fallbacks. Resizable-panel workbench (tiling was removed in `21d30b57` — do not resurrect it here). Terminal: libghostty embeds natively.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Written only after the editor gate passes.                                                                                     | gated                                                                                                                                                                                                                                                                                                                                              |
| 6   | `native-dev-workflow.md`       | Keep sessions productive.                                                                     | CI (macOS runner? none for now — zero users). Whether `EditorCore` stays in this repo or moves to a sibling repo like `../Editor` did. Formatting (`swift-format`?), lint.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Skill + repo docs answer "how do I build/run/test/bench" without spelunking.                                                   | partially (skill exists)                                                                                                                                                                                                                                                                                                                           |

## Open decisions (talk about these)

- **App identity**: name and bundle id for `MacApp` when it becomes a real `.app` bundle.
- ~~**Deployment target**~~ decided: macOS 26 only (`.macOS(.v26)` in `Package.swift`) — personal tool, machine runs 26, unlocks the glass APIs; research confirms this is the sane floor for a dev-only app.
- **Editor repo home**: in-repo `apps/mac/Sources/EditorCore` vs sibling repo (the web editor lives in `../Editor`). In-repo until it hurts; extraction is cheap early.
- **Server OpenAPI wiring**: add the export now or when the Swift client first consumes it (plan 3 decides the mechanism first).
- ~~**Swift language mode**~~ decided: Swift 6.2 approachable concurrency is Apple's default for new app projects — adopted on `MacApp` (`.defaultIsolation(MainActor.self)` + upcoming features). `EditorCore` stays nonisolated: a text buffer must not be MainActor-pinned; practitioners flag background-work code as the place MainActor-default bites.
